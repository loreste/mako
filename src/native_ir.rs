//! Backend-neutral native IR.
//!
//! Heap ownership is resolved here, before machine-code selection: string
//! allocation, clone, move, and drop operations are explicit instructions that
//! every backend must preserve.

use crate::ast::{
    BinOp, Block as AstBlock, Expr, FnDef, Item, Pattern, Program, Stmt, TypeExpr, UnaryOp,
};
use std::cell::RefCell;
use std::collections::{HashMap, HashSet};
use std::fmt;

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum Type {
    I1,
    I32,
    I64,
    F64,
    Str,
    /// Owned `[]int` slice represented by a `(data, len, cap)` runtime value.
    IntSlice,
    /// Owned `[]float` / `[]float64` slice (pointer header ABI, like `[]int`).
    FloatSlice,
    /// Owned `[]byte` (uint8) slice — pointer header ABI.
    ByteSlice,
    /// Owned `[]bool` slice — pointer header ABI (packed as i8 elements).
    BoolSlice,
    /// Owned `[]string` slice; elements require recursive clone/drop.
    StrSlice,
    /// `chan[int]` — opaque pointer to runtime channel.
    ChanI,
    /// `chan[string]` — same mailbox ABI; elements are owned string pointers.
    ChanS,
    /// Arena region handle (opaque pointer).
    Arena,
    /// Structured concurrency nursery / crew handle (opaque pointer).
    Nursery,
    /// Threaded kick job handle (`MakoTask*`).
    Task,
    /// Opaque runtime handle (TlsConn, SqlDB, StrBuilder, …) — pointer-sized.
    Opaque,
    /// `[]Struct` — owned pointer-array of heap struct pointers.
    StructSlice(u32),
    /// `map[int]int` — owned pointer to open-addressing table.
    MapII,
    /// `map[string]int` — owned pointer; keys are owned string headers.
    MapSI,
    /// `map[string]string` — owned pointer; keys and values are string headers.
    MapSS,
    /// `map[int]float` — owned pointer; float values.
    MapIF,
    /// `map[float]int` — owned pointer; float keys.
    MapFI,
    /// `ShareInt` — RC cell pointer (`mako_native_share_*`). Clone bumps the
    /// refcount; drop frees the cell at zero. Multiple handles may alias.
    ShareInt,
    /// First-class function pointer (C ABI: i64 address of a function).
    /// Seed: `fn(int) -> int` and similar scalar signatures only.
    FnPtr,
    /// `chan[float]` — same mailbox as `ChanI`; payload is f64 bit pattern.
    ChanF,
    /// `chan[T]` for pointer-sized / heap payloads (structs, slices, …).
    /// Same mailbox ABI as `ChanI` at the machine level (intptr elements).
    /// `MapValKind` records the element type so recv can retype the i64 payload
    /// (e.g. `Struct(id)` for `chan[Point]`).
    ChanP(MapValKind),
    /// `map[int]V` for pointer-sized values (runtime MapII). Kind of V is carried.
    MapIPtr(MapValKind),
    /// `map[string]V` for pointer-sized values (runtime MapSI).
    MapSPtr(MapValKind),
    /// Owned array of pointer-sized heap values (`[][]int`, maps_values result).
    PtrSlice(MapValKind),
    /// A user struct with scalar fields, indexed into `Module::structs`. The
    /// value is an owned heap block of one 8-byte slot per field (value
    /// semantics: bindings copy, so both source and destination stay live).
    Struct(u32),
}

/// Value kind stored in pointer maps (`MapIPtr` / `MapSPtr`) and as elements of
/// `PtrSlice` (`[][]T`, `[]Option[T]`, …). Kept `Copy` with one nesting level.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum MapValKind {
    IntSlice,
    StrSlice,
    FloatSlice,
    ByteSlice,
    BoolSlice,
    /// Scalar float stored as i64 bit pattern in int-value maps.
    Float,
    /// Owned string pointer in int-value maps (`map[float]string`, …).
    OwnedStr,
    /// User struct or tuple stored by pointer (map[K]Point, map[K](int,int)).
    Struct(u32),
    /// `[]Struct` / `[]Point` value.
    StructSlice(u32),
    /// `[][]int` — pointer-array of int-slice headers.
    NestedIntSlice,
    /// `[][]string`
    NestedStrSlice,
    /// `[][]float`
    NestedFloatSlice,
    /// `[][]byte` / `[][]bool`
    NestedByteSlice,
    NestedBoolSlice,
    /// `[]Option[T]` / `[]Result[…]` / `[]Struct` as pointer-array of heap structs.
    NestedStruct(u32),
    /// `[]chan[…]` / other pointer-array of pointer-sized handles.
    NestedOther,
    /// `[]chan[int]` / `[]chan[string]` / `[]chan[float]` / `[]chan[T]`.
    NestedChanI,
    NestedChanS,
    NestedChanF,
    NestedChanPStruct(u32),
    NestedChanPOther,
    /// `[][]chan[int]` / `[][]chan[string]` / …
    NestedNestedChanI,
    NestedNestedChanS,
    NestedNestedChanF,
    /// `[][]chan[Point]` — preserves struct payload id through two nest levels.
    NestedNestedChanPStruct(u32),
    NestedNestedChanPOther,
    /// Map headers stored in pointer arrays (`[]map[K]V`).
    MapII,
    MapSI,
    MapSS,
    MapIF,
    MapFI,
    /// `map[int]V` pointer-value maps (V encoded as MapValKind flattened tags).
    MapIPtrIntSlice,
    MapIPtrStrSlice,
    MapIPtrStruct(u32),
    MapIPtrOther,
    /// `map[int|float|bool]float` as a nestable map value (float stored as i64 bits).
    MapIPtrFloat,
    /// Nested `map[…]map[…]float` (one extra map layer of MapIPtrFloat).
    MapIPtrNested,
    MapSPtrIntSlice,
    MapSPtrStrSlice,
    MapSPtrStruct(u32),
    MapSPtrOther,
    /// map[string][]map[string]int (and peers) — value is a map-slice.
    MapSPtrNestedMapSI,
    MapSPtrNestedMapII,
    MapSPtrNestedMapSS,
    MapIPtrNestedMapSI,
    MapIPtrNestedMapII,
    /// map[string]map[string][]int — inner is MapSPtr(IntSlice).
    MapSPtrMapSPtrIntSlice,
    /// map[string]map[string][]string
    MapSPtrMapSPtrStrSlice,
    /// map[string]map[int][]int
    MapSPtrMapIPtrIntSlice,
    /// map[string]map[int][]string
    MapSPtrMapIPtrStrSlice,
    /// map[int]map[string][]int
    MapIPtrMapSPtrIntSlice,
    /// map[int]map[int][]int
    MapIPtrMapIPtrIntSlice,
    /// map[int]map[int][]string
    MapIPtrMapIPtrStrSlice,
    /// map[string]map[int][][]int (and peers with NestedIntSlice inner).
    MapSPtrMapIPtrNestedIntSlice,
    MapIPtrMapIPtrNestedIntSlice,
    /// `[]map[…]` nested one level (map values that are map-slices).
    NestedMapII,
    NestedMapSI,
    NestedMapSS,
    NestedMapIF,
    NestedMapFI,
    /// Channel handles stored as map values / pointer-array elements.
    ChanI,
    ChanS,
    ChanF,
    /// `chan[Struct]` / other ChanP element kinds (pointer-sized mailbox).
    ChanPStruct(u32),
    ChanPOther,
    /// Other pointer-sized (Result, Option, Opaque, …) — exposed as I64.
    Other,
    /// `map[Struct]int` / `map[Struct]string` / `map[Struct]float` — key is a
    /// heap struct (content equality); value is the scalar named here.
    StructKeyInt(u32),
    StructKeyStr(u32),
    StructKeyFloat(u32),
    /// `map[StructK]StructV` — both key and value are heap structs.
    StructKeyToStruct(u32, u32),
    /// `map[Struct][]int` / `[]string` / `[]float` / `[]Struct` — content key,
    /// pointer-array value (must clone/copy via content-key helpers).
    StructKeyIntSlice(u32),
    StructKeyStrSlice(u32),
    StructKeyFloatSlice(u32),
    StructKeyStructSlice(u32, u32),
    /// `map[Struct]V` for other pointer-sized values (chan, map, nested, …).
    /// Value is stored as i64; get retypes via expression context / from_type.
    StructKeyPtr(u32),
}

impl MapValKind {
    fn from_type(ty: Type) -> Self {
        match ty {
            Type::IntSlice => MapValKind::IntSlice,
            Type::StrSlice => MapValKind::StrSlice,
            Type::FloatSlice => MapValKind::FloatSlice,
            Type::ByteSlice => MapValKind::ByteSlice,
            Type::BoolSlice => MapValKind::BoolSlice,
            Type::F64 => MapValKind::Float,
            Type::Str => MapValKind::OwnedStr,
            Type::Struct(id) => MapValKind::Struct(id),
            Type::StructSlice(id) => MapValKind::StructSlice(id),
            Type::MapII => MapValKind::MapII,
            Type::MapSI => MapValKind::MapSI,
            Type::MapSS => MapValKind::MapSS,
            Type::MapIF => MapValKind::MapIF,
            Type::MapFI => MapValKind::MapFI,
            Type::MapIPtr(vk) => match vk {
                MapValKind::IntSlice => MapValKind::MapIPtrIntSlice,
                MapValKind::StrSlice => MapValKind::MapIPtrStrSlice,
                MapValKind::Struct(id) => MapValKind::MapIPtrStruct(id),
                // MapIPtr(Float) as a map value → MapIPtrFloat.
                MapValKind::Float => MapValKind::MapIPtrFloat,
                // MapIPtr(MapIPtrFloat) as a map value → MapIPtrNested (depth-3).
                MapValKind::MapIPtrFloat => MapValKind::MapIPtrNested,
                MapValKind::MapIPtrNested => MapValKind::MapIPtrNested,
                // map[int]map[string][]T / map[int]map[int][]T
                MapValKind::MapSPtrIntSlice => MapValKind::MapIPtrMapSPtrIntSlice,
                MapValKind::MapIPtrIntSlice => MapValKind::MapIPtrMapIPtrIntSlice,
                MapValKind::MapIPtrStrSlice => MapValKind::MapIPtrMapIPtrStrSlice,
                // map[int][][]int as a map value
                MapValKind::NestedIntSlice => MapValKind::MapIPtrMapIPtrNestedIntSlice,
                MapValKind::MapSPtrMapIPtrNestedIntSlice
                | MapValKind::MapIPtrMapIPtrNestedIntSlice => {
                    MapValKind::MapIPtrMapIPtrNestedIntSlice
                }
                MapValKind::MapSPtrMapSPtrIntSlice
                | MapValKind::MapSPtrMapIPtrIntSlice
                | MapValKind::MapIPtrMapSPtrIntSlice
                | MapValKind::MapIPtrMapIPtrIntSlice => MapValKind::MapIPtrMapIPtrIntSlice,
                // Nested map values: map[int]map[…] (scalar-value inner maps).
                MapValKind::MapSI
                | MapValKind::NestedMapSI
                | MapValKind::MapSPtrNestedMapSI
                | MapValKind::MapIPtrNestedMapSI => MapValKind::MapIPtrNestedMapSI,
                MapValKind::MapII
                | MapValKind::NestedMapII
                | MapValKind::MapSPtrNestedMapII
                | MapValKind::MapIPtrNestedMapII => MapValKind::MapIPtrNestedMapII,
                MapValKind::MapSS
                | MapValKind::NestedMapSS
                | MapValKind::MapSPtrNestedMapSS => MapValKind::MapIPtrOther,
                MapValKind::MapIF | MapValKind::NestedMapIF => MapValKind::MapIPtrNestedMapII,
                MapValKind::MapFI | MapValKind::NestedMapFI => MapValKind::MapIPtrNestedMapII,
                _ => MapValKind::MapIPtrOther,
            },
            Type::MapSPtr(vk) => match vk {
                MapValKind::IntSlice => MapValKind::MapSPtrIntSlice,
                MapValKind::StrSlice => MapValKind::MapSPtrStrSlice,
                MapValKind::Struct(id) => MapValKind::MapSPtrStruct(id),
                // map[string]map[string][]T / map[string]map[int][]T
                MapValKind::MapSPtrIntSlice => MapValKind::MapSPtrMapSPtrIntSlice,
                MapValKind::MapSPtrStrSlice => MapValKind::MapSPtrMapSPtrStrSlice,
                MapValKind::MapIPtrIntSlice => MapValKind::MapSPtrMapIPtrIntSlice,
                MapValKind::MapIPtrStrSlice => MapValKind::MapSPtrMapIPtrStrSlice,
                // map[string]map[int][][]int — value kind NestedIntSlice under MapIPtr
                MapValKind::NestedIntSlice => MapValKind::MapSPtrMapIPtrNestedIntSlice,
                MapValKind::MapIPtrMapIPtrNestedIntSlice
                | MapValKind::MapSPtrMapIPtrNestedIntSlice => {
                    MapValKind::MapSPtrMapIPtrNestedIntSlice
                }
                MapValKind::MapSPtrMapSPtrIntSlice
                | MapValKind::MapSPtrMapIPtrIntSlice
                | MapValKind::MapIPtrMapSPtrIntSlice
                | MapValKind::MapIPtrMapIPtrIntSlice => MapValKind::MapSPtrMapSPtrIntSlice,
                // Nested map values: map[string]map[…] (scalar-value inner maps).
                MapValKind::MapSI
                | MapValKind::NestedMapSI
                | MapValKind::MapSPtrNestedMapSI
                | MapValKind::MapIPtrNestedMapSI => MapValKind::MapSPtrNestedMapSI,
                MapValKind::MapII
                | MapValKind::NestedMapII
                | MapValKind::MapSPtrNestedMapII
                | MapValKind::MapIPtrNestedMapII => MapValKind::MapSPtrNestedMapII,
                MapValKind::MapSS
                | MapValKind::NestedMapSS
                | MapValKind::MapSPtrNestedMapSS => MapValKind::MapSPtrNestedMapSS,
                _ => MapValKind::MapSPtrOther,
            },
            Type::PtrSlice(inner) => match inner {
                MapValKind::IntSlice => MapValKind::NestedIntSlice,
                MapValKind::StrSlice => MapValKind::NestedStrSlice,
                MapValKind::FloatSlice => MapValKind::NestedFloatSlice,
                MapValKind::ByteSlice => MapValKind::NestedByteSlice,
                MapValKind::BoolSlice => MapValKind::NestedBoolSlice,
                MapValKind::Struct(id) | MapValKind::StructSlice(id) => {
                    MapValKind::NestedStruct(id)
                }
                MapValKind::MapII => MapValKind::NestedMapII,
                MapValKind::MapSI => MapValKind::NestedMapSI,
                MapValKind::MapSS => MapValKind::NestedMapSS,
                MapValKind::MapIF => MapValKind::NestedMapIF,
                MapValKind::MapFI => MapValKind::NestedMapFI,
                MapValKind::ChanI => MapValKind::NestedChanI,
                MapValKind::ChanS => MapValKind::NestedChanS,
                MapValKind::ChanF => MapValKind::NestedChanF,
                MapValKind::ChanPStruct(id) => MapValKind::NestedChanPStruct(id),
                MapValKind::ChanPOther => MapValKind::NestedChanPOther,
                MapValKind::NestedChanI => MapValKind::NestedNestedChanI,
                MapValKind::NestedChanS => MapValKind::NestedNestedChanS,
                MapValKind::NestedChanF => MapValKind::NestedNestedChanF,
                MapValKind::NestedChanPStruct(id) => MapValKind::NestedNestedChanPStruct(id),
                MapValKind::NestedChanPOther => MapValKind::NestedNestedChanPOther,
                _ => MapValKind::NestedOther,
            },
            Type::ChanI => MapValKind::ChanI,
            Type::ChanS => MapValKind::ChanS,
            Type::ChanF => MapValKind::ChanF,
            Type::ChanP(vk) => match vk {
                MapValKind::Struct(id) => MapValKind::ChanPStruct(id),
                _ => MapValKind::ChanPOther,
            },
            _ => MapValKind::Other,
        }
    }
    fn to_type(self) -> Type {
        match self {
            MapValKind::IntSlice => Type::IntSlice,
            MapValKind::StrSlice => Type::StrSlice,
            MapValKind::FloatSlice => Type::FloatSlice,
            MapValKind::ByteSlice => Type::ByteSlice,
            MapValKind::BoolSlice => Type::BoolSlice,
            MapValKind::Float => Type::F64,
            MapValKind::OwnedStr => Type::Str,
            MapValKind::Struct(id) => Type::Struct(id),
            MapValKind::StructSlice(id) => Type::StructSlice(id),
            MapValKind::NestedIntSlice => Type::PtrSlice(MapValKind::IntSlice),
            MapValKind::NestedStrSlice => Type::PtrSlice(MapValKind::StrSlice),
            MapValKind::NestedFloatSlice => Type::PtrSlice(MapValKind::FloatSlice),
            MapValKind::NestedByteSlice => Type::PtrSlice(MapValKind::ByteSlice),
            MapValKind::NestedBoolSlice => Type::PtrSlice(MapValKind::BoolSlice),
            MapValKind::NestedStruct(id) => Type::PtrSlice(MapValKind::Struct(id)),
            MapValKind::NestedOther => Type::PtrSlice(MapValKind::Other),
            MapValKind::NestedChanI => Type::PtrSlice(MapValKind::ChanI),
            MapValKind::NestedChanS => Type::PtrSlice(MapValKind::ChanS),
            MapValKind::NestedChanF => Type::PtrSlice(MapValKind::ChanF),
            MapValKind::NestedChanPStruct(id) => {
                Type::PtrSlice(MapValKind::ChanPStruct(id))
            }
            MapValKind::NestedChanPOther => Type::PtrSlice(MapValKind::ChanPOther),
            MapValKind::NestedNestedChanI => Type::PtrSlice(MapValKind::NestedChanI),
            MapValKind::NestedNestedChanS => Type::PtrSlice(MapValKind::NestedChanS),
            MapValKind::NestedNestedChanF => Type::PtrSlice(MapValKind::NestedChanF),
            MapValKind::NestedNestedChanPStruct(id) => {
                Type::PtrSlice(MapValKind::NestedChanPStruct(id))
            }
            MapValKind::NestedNestedChanPOther => {
                Type::PtrSlice(MapValKind::NestedChanPOther)
            }
            MapValKind::MapII => Type::MapII,
            MapValKind::MapSI => Type::MapSI,
            MapValKind::MapSS => Type::MapSS,
            MapValKind::MapIF => Type::MapIF,
            MapValKind::MapFI => Type::MapFI,
            MapValKind::MapIPtrIntSlice => Type::MapIPtr(MapValKind::IntSlice),
            MapValKind::MapIPtrStrSlice => Type::MapIPtr(MapValKind::StrSlice),
            MapValKind::MapIPtrStruct(id) => Type::MapIPtr(MapValKind::Struct(id)),
            MapValKind::MapIPtrOther => Type::MapIPtr(MapValKind::Other),
            MapValKind::MapIPtrFloat => Type::MapIPtr(MapValKind::Float),
            MapValKind::MapIPtrNested => Type::MapIPtr(MapValKind::MapIPtrFloat),
            MapValKind::MapSPtrIntSlice => Type::MapSPtr(MapValKind::IntSlice),
            MapValKind::MapSPtrStrSlice => Type::MapSPtr(MapValKind::StrSlice),
            MapValKind::MapSPtrStruct(id) => Type::MapSPtr(MapValKind::Struct(id)),
            MapValKind::MapSPtrOther => Type::MapSPtr(MapValKind::Other),
            // Nested-map-in-map: value is the inner map handle (not a ptr-slice).
            MapValKind::MapSPtrNestedMapSI => Type::MapSPtr(MapValKind::MapSI),
            MapValKind::MapSPtrNestedMapII => Type::MapSPtr(MapValKind::MapII),
            MapValKind::MapSPtrNestedMapSS => Type::MapSPtr(MapValKind::MapSS),
            MapValKind::MapIPtrNestedMapSI => Type::MapIPtr(MapValKind::MapSI),
            MapValKind::MapIPtrNestedMapII => Type::MapIPtr(MapValKind::MapII),
            // Outer get returns the *inner map* type (not re-wrapped as MapSPtr).
            MapValKind::MapSPtrMapSPtrIntSlice => Type::MapSPtr(MapValKind::IntSlice),
            MapValKind::MapSPtrMapSPtrStrSlice => Type::MapSPtr(MapValKind::StrSlice),
            MapValKind::MapSPtrMapIPtrIntSlice => Type::MapIPtr(MapValKind::IntSlice),
            MapValKind::MapSPtrMapIPtrStrSlice => Type::MapIPtr(MapValKind::StrSlice),
            MapValKind::MapIPtrMapSPtrIntSlice => Type::MapSPtr(MapValKind::IntSlice),
            MapValKind::MapIPtrMapIPtrIntSlice => Type::MapIPtr(MapValKind::IntSlice),
            MapValKind::MapIPtrMapIPtrStrSlice => Type::MapIPtr(MapValKind::StrSlice),
            MapValKind::MapSPtrMapIPtrNestedIntSlice => {
                Type::MapIPtr(MapValKind::NestedIntSlice)
            }
            MapValKind::MapIPtrMapIPtrNestedIntSlice => {
                Type::MapIPtr(MapValKind::NestedIntSlice)
            }
            // NestedMap* on PtrSlice means []map[…] element kind.
            MapValKind::NestedMapII => Type::PtrSlice(MapValKind::MapII),
            MapValKind::NestedMapSI => Type::PtrSlice(MapValKind::MapSI),
            MapValKind::NestedMapSS => Type::PtrSlice(MapValKind::MapSS),
            MapValKind::NestedMapIF => Type::PtrSlice(MapValKind::MapIF),
            MapValKind::NestedMapFI => Type::PtrSlice(MapValKind::MapFI),
            MapValKind::ChanI => Type::ChanI,
            MapValKind::ChanS => Type::ChanS,
            MapValKind::ChanF => Type::ChanF,
            MapValKind::ChanPStruct(id) => Type::ChanP(MapValKind::Struct(id)),
            MapValKind::ChanPOther => Type::ChanP(MapValKind::Other),
            MapValKind::Other => Type::I64,
            // NestedChan* already handled above; StructKey* below.
            // Struct-key maps: value type (key recovered separately in range).
            MapValKind::StructKeyInt(_) => Type::I64,
            MapValKind::StructKeyStr(_) => Type::Str,
            MapValKind::StructKeyFloat(_) => Type::F64,
            MapValKind::StructKeyToStruct(_, vid) => Type::Struct(vid),
            MapValKind::StructKeyIntSlice(_) => Type::IntSlice,
            MapValKind::StructKeyStrSlice(_) => Type::StrSlice,
            MapValKind::StructKeyFloatSlice(_) => Type::FloatSlice,
            MapValKind::StructKeyStructSlice(_, eid) => Type::StructSlice(eid),
            // Pointer-sized payload; callers retype (chan/map/…) after get.
            MapValKind::StructKeyPtr(_) => Type::I64,
        }
    }

    /// When this kind encodes a struct map key, return that struct id.
    fn struct_key_id(self) -> Option<u32> {
        match self {
            MapValKind::StructKeyInt(id)
            | MapValKind::StructKeyStr(id)
            | MapValKind::StructKeyFloat(id)
            | MapValKind::StructKeyIntSlice(id)
            | MapValKind::StructKeyStrSlice(id)
            | MapValKind::StructKeyFloatSlice(id)
            | MapValKind::StructKeyPtr(id) => Some(id),
            MapValKind::StructKeyToStruct(kid, _) | MapValKind::StructKeyStructSlice(kid, _) => {
                Some(kid)
            }
            _ => None,
        }
    }
}

impl Type {
    /// Heap-owned value types: they carry an owning pointer that must be
    /// cloned/moved/dropped explicitly by the ownership machinery.
    fn is_heap(self) -> bool {
        matches!(
            self,
            Type::Str
                | Type::IntSlice
                | Type::FloatSlice
                | Type::ByteSlice
                | Type::BoolSlice
                | Type::StrSlice
                | Type::MapII
                | Type::MapSI
                | Type::MapSS
                | Type::MapIF
                | Type::MapFI
                | Type::MapIPtr(_)
                | Type::MapSPtr(_)
                | Type::PtrSlice(_)
                | Type::ChanI
                | Type::ChanS
                | Type::ChanF
                | Type::ChanP(_)
                | Type::Arena
                | Type::Nursery
                | Type::Task
                | Type::Opaque
                | Type::StructSlice(_)
                | Type::ShareInt
                | Type::Struct(_)
        )
    }

    /// Shared pointer handles: not deep-cloned; free is explicit (or end-of-scope
    /// drop of a *sole* owner). Cloning them is a no-op identity, so force-owning
    /// a borrow would dual-own the same pointer and double-free.
    fn is_shared_handle(self) -> bool {
        matches!(
            self,
            Type::Opaque
                | Type::ChanI
                | Type::ChanS
                | Type::ChanF
                | Type::ChanP(_)
                | Type::Arena
                | Type::Nursery
                | Type::Task
                | Type::FnPtr
        )
    }

    /// Pointer-sized machine value (can ride in i64 slots / chan[int] mailbox).
    fn is_ptr_sized(self) -> bool {
        matches!(
            self,
            Type::I64
                | Type::I1
                | Type::F64
                | Type::FnPtr
                | Type::Opaque
                | Type::ShareInt
                | Type::ChanI
                | Type::ChanS
                | Type::ChanF
                | Type::ChanP(_)
                | Type::Task
                | Type::Arena
                | Type::Nursery
                | Type::Struct(_)
                | Type::IntSlice
                | Type::FloatSlice
                | Type::ByteSlice
                | Type::BoolSlice
                | Type::StrSlice
                | Type::StructSlice(_)
                | Type::Str
                | Type::MapII
                | Type::MapSI
                | Type::MapSS
                | Type::MapIF
                | Type::MapFI
                | Type::MapIPtr(_)
                | Type::MapSPtr(_)
                | Type::PtrSlice(_)
        )
    }
}

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub struct Value(pub u32);

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub struct BlockId(pub u32);

#[allow(dead_code)]
#[derive(Clone, Debug)]
pub enum Inst {
    ConstInt {
        out: Value,
        value: i64,
        ty: Type,
    },
    ConstFloat {
        out: Value,
        value: f64,
    },
    Alloca {
        out: Value,
        ty: Type,
    },
    Load {
        out: Value,
        ptr: Value,
        ty: Type,
    },
    Store {
        ptr: Value,
        value: Value,
    },
    Binary {
        out: Value,
        op: BinOp,
        left: Value,
        right: Value,
        ty: Type,
    },
    Unary {
        out: Value,
        op: UnaryOp,
        value: Value,
        ty: Type,
    },
    /// Numeric / bool convert: `from` is the source type of `value`.
    Cast {
        out: Value,
        value: Value,
        from: Type,
        to: Type,
    },
    Call {
        out: Option<Value>,
        function: String,
        args: Vec<Value>,
        ret: Option<Type>,
    },
    /// Address of a module function (for `kick` trampolines / spawn).
    FuncAddr {
        out: Value,
        function: String,
    },
    /// Indirect call through a function pointer (`fn_ptr(args…)`).
    CallIndirect {
        out: Option<Value>,
        callee: Value,
        args: Vec<Value>,
        /// Param types + optional return (for ABI).
        param_tys: Vec<Type>,
        ret: Option<Type>,
    },
    PrintInt {
        value: Value,
    },
    PrintBool {
        value: Value,
    },
    StringLiteral {
        out: Value,
        bytes: Vec<u8>,
    },
    StringClone {
        out: Value,
        value: Value,
    },
    StringConcat {
        out: Value,
        left: Value,
        right: Value,
    },
    StringEqual {
        out: Value,
        left: Value,
        right: Value,
        negated: bool,
    },
    PrintString {
        value: Value,
    },
    /// Byte length of a string value (`len(s)`).
    StringLen {
        out: Value,
        value: Value,
    },
    /// `format_int(n)` / int interpolation → owned heap string.
    IntToString {
        out: Value,
        value: Value,
    },
    /// Bool interpolation → owned heap string `"true"` / `"false"`.
    BoolToString {
        out: Value,
        value: Value,
    },
    /// Null/zero heap value (pointer ABI: null; string value ABI: `{NULL,0}`).
    /// Dropping it is always a safe no-op. Used to initialise loop binders so
    /// path-insensitive continue/break drops never free garbage.
    NullHeap {
        out: Value,
        ty: Type,
    },
    DropString {
        value: Value,
    },
    SliceLiteral {
        out: Value,
        elements: Vec<Value>,
    },
    SliceMake {
        out: Value,
        len: Value,
        cap: Option<Value>,
    },
    SliceLen {
        out: Value,
        slice: Value,
    },
    SliceIndex {
        out: Value,
        slice: Value,
        index: Value,
    },
    SliceStore {
        slice: Value,
        index: Value,
        value: Value,
    },
    SliceAppend {
        out: Value,
        slice: Value,
        value: Value,
    },
    SliceSlice {
        out: Value,
        slice: Value,
        low: Value,
        high: Value,
        max: Option<Value>,
    },
    SliceClone {
        out: Value,
        slice: Value,
    },
    DropSlice {
        value: Value,
    },
    StrSliceLiteral { out: Value, elements: Vec<Value> },
    StrSliceMake { out: Value, len: Value, cap: Option<Value> },
    StrSliceLen { out: Value, slice: Value },
    StrSliceIndex { out: Value, slice: Value, index: Value },
    StrSliceStore { slice: Value, index: Value, value: Value },
    StrSliceAppend { out: Value, slice: Value, value: Value },
    StrSliceSlice { out: Value, slice: Value, low: Value, high: Value, max: Option<Value> },
    StrSliceClone { out: Value, slice: Value },
    DropStrSlice { value: Value },
    /// Allocate a fresh owned struct block and store each scalar field into its
    /// slot (`fields[i]` → slot `i`).
    StructMake {
        out: Value,
        struct_id: u32,
        fields: Vec<Value>,
    },
    /// Load scalar field `index` from a struct block.
    StructField {
        out: Value,
        base: Value,
        struct_id: u32,
        index: u32,
        ty: Type,
    },
    /// Store a scalar field `index` into a struct block (mutation).
    StructFieldStore {
        base: Value,
        struct_id: u32,
        index: u32,
        value: Value,
    },
    /// Deep-copy a struct block (value-copy semantics for scalar fields).
    StructClone {
        out: Value,
        base: Value,
        struct_id: u32,
    },
    /// Free an owned struct block, recursively dropping owned fields first.
    DropStruct {
        value: Value,
        struct_id: u32,
    },
    /// Allocate a zero-initialised enum block, store the discriminant into slot
    /// 0, and store the variant's payload into its dedicated slots. Slots the
    /// active variant does not use stay zero (null for owned fields), which the
    /// null-safe recursive clone/drop handles.
    EnumMake {
        out: Value,
        enum_id: u32,
        tag: i64,
        slot_base: u32,
        payload: Vec<Value>,
    },
}

#[derive(Clone, Debug)]
pub enum Terminator {
    Jump(BlockId),
    Branch {
        condition: Value,
        then_block: BlockId,
        else_block: BlockId,
    },
    Return(Option<Value>),
}

#[derive(Clone, Debug)]
pub struct BasicBlock {
    pub instructions: Vec<Inst>,
    pub terminator: Option<Terminator>,
}

#[allow(dead_code)]
#[derive(Clone, Debug)]
pub struct Function {
    pub name: String,
    pub params: Vec<(String, Value, Type)>,
    pub ret: Option<Type>,
    pub blocks: Vec<BasicBlock>,
    pub entry: BlockId,
    pub next_value: u32,
}

/// Layout of a user struct: fields in declaration order. Every scalar field
/// occupies one 8-byte slot; slot `i` lives at byte offset `i * 8`.
#[derive(Clone, Debug)]
pub struct StructLayout {
    /// Struct name, retained for diagnostics.
    #[allow(dead_code)]
    pub name: String,
    pub fields: Vec<(String, Type)>,
    /// Per-field default expressions from `field: T = expr` (const-ish in v1).
    pub defaults: Vec<Option<Expr>>,
}

#[derive(Clone, Debug)]
pub struct Module {
    pub functions: Vec<Function>,
    pub structs: Vec<StructLayout>,
}

/// Registry of aggregate layouts. Named structs are resolved up front;
/// anonymous tuple shapes are interned on demand during lowering (hence the
/// interior mutability). Both share one `id` space and one `StructLayout` list,
/// so tuples reuse the entire struct heap-ownership machinery — a tuple is just
/// an anonymous struct whose fields are named `"0"`, `"1"`, … .
struct StructRegistry {
    by_name: HashMap<String, u32>,
    layouts: RefCell<Vec<StructLayout>>,
    tuple_shapes: RefCell<HashMap<Vec<Type>, u32>>,
    result_shapes: RefCell<HashMap<(Type, Type), u32>>,
    option_shapes: RefCell<HashMap<Type, u32>>,
    /// Variant name → one or more monomorphs (Result/Option Ok/Err/Some can
    /// share a name across layouts). Enums are heap blocks `[tag, p0, …]`.
    variants: RefCell<HashMap<String, Vec<VariantInfo>>>,
    /// Which layout ids are enums (as opposed to structs/tuples).
    enum_ids: RefCell<std::collections::HashSet<u32>>,
    /// Interface type names — values are `Type::Opaque` fat-pointer handles.
    interfaces: std::collections::HashSet<String>,
    /// Interface → method names.
    interface_methods: HashMap<String, Vec<String>>,
    /// Concrete struct name → numeric tag for dyn boxing.
    concrete_tags: HashMap<String, i64>,
}

#[derive(Clone, Copy)]
struct VariantInfo {
    enum_id: u32,
    tag: i64,
    arity: usize,
    /// First payload slot index for this variant. Each variant owns dedicated,
    /// non-overlapping slots, so every slot has a single fixed type and the
    /// flat recursive clone/drop is correct (inactive owned slots hold null and
    /// are cloned/dropped as no-ops).
    slot_base: usize,
}

impl StructRegistry {
    fn struct_id(&self, name: &str) -> Option<u32> {
        self.by_name.get(name).copied()
    }

    fn layout_name(&self, id: u32) -> String {
        self.layouts.borrow()[id as usize].name.clone()
    }

    fn field_count(&self, id: u32) -> usize {
        self.layouts.borrow()[id as usize].fields.len()
    }

    fn field_type(&self, id: u32, index: usize) -> Type {
        self.layouts.borrow()[id as usize].fields[index].1
    }

    fn field_name(&self, id: u32, index: usize) -> String {
        self.layouts.borrow()[id as usize].fields[index].0.clone()
    }

    fn field_types(&self, id: u32) -> Vec<Type> {
        self.layouts.borrow()[id as usize]
            .fields
            .iter()
            .map(|(_, t)| *t)
            .collect()
    }

    fn field(&self, id: u32, name: &str) -> Option<(u32, Type)> {
        self.layouts.borrow()[id as usize]
            .fields
            .iter()
            .position(|(n, _)| n == name)
            .map(|index| (index as u32, self.layouts.borrow()[id as usize].fields[index].1))
    }

    /// Find or create the anonymous struct layout for a tuple shape.
    fn intern_tuple(&self, fields: Vec<Type>) -> u32 {
        if let Some(&id) = self.tuple_shapes.borrow().get(&fields) {
            return id;
        }
        let mut layouts = self.layouts.borrow_mut();
        let id = layouts.len() as u32;
        let named = fields
            .iter()
            .enumerate()
            .map(|(i, ty)| (i.to_string(), *ty))
            .collect();
        layouts.push(StructLayout {
            name: format!("tuple.{id}"),
            fields: named,
            defaults: Vec::new(),
        });
        self.tuple_shapes.borrow_mut().insert(fields, id);
        id
    }

    /// `Result[Ok, Err]` as a non-overlapping enum layout + Ok/Err variants.
    fn intern_result(&self, ok: Type, err: Type) -> u32 {
        if let Some(&id) = self.result_shapes.borrow().get(&(ok, err)) {
            return id;
        }
        let mut layouts = self.layouts.borrow_mut();
        let id = layouts.len() as u32;
        layouts.push(StructLayout {
            name: format!("Result.{id}"),
            fields: vec![
                ("tag".into(), Type::I64),
                ("Ok_0".into(), ok),
                ("Err_0".into(), err),
            ],
            defaults: Vec::new(),
        });
        drop(layouts);
        self.enum_ids.borrow_mut().insert(id);
        self.result_shapes.borrow_mut().insert((ok, err), id);
        // Multiple Result monomorphs may share Ok/Err names; pick by payload
        // type (or scrutinee enum_id in match) at use sites.
        self.push_variant(
            "Ok",
            VariantInfo {
                enum_id: id,
                tag: 0,
                arity: 1,
                slot_base: 1,
            },
        );
        self.push_variant(
            "Err",
            VariantInfo {
                enum_id: id,
                tag: 1,
                arity: 1,
                slot_base: 2,
            },
        );
        id
    }

    /// `Option[T]` as tag + Some_0 payload; None is nullary tag 1.
    fn intern_option(&self, inner: Type) -> u32 {
        if let Some(&id) = self.option_shapes.borrow().get(&inner) {
            return id;
        }
        let mut layouts = self.layouts.borrow_mut();
        let id = layouts.len() as u32;
        layouts.push(StructLayout {
            name: format!("Option.{id}"),
            fields: vec![("tag".into(), Type::I64), ("Some_0".into(), inner)],
            defaults: Vec::new(),
        });
        drop(layouts);
        self.enum_ids.borrow_mut().insert(id);
        self.option_shapes.borrow_mut().insert(inner, id);
        self.push_variant(
            "Some",
            VariantInfo {
                enum_id: id,
                tag: 0,
                arity: 1,
                slot_base: 1,
            },
        );
        self.push_variant(
            "None",
            VariantInfo {
                enum_id: id,
                tag: 1,
                arity: 0,
                slot_base: 2,
            },
        );
        id
    }

    fn into_layouts(self) -> Vec<StructLayout> {
        self.layouts.into_inner()
    }

    fn push_variant(&self, name: &str, info: VariantInfo) {
        self.variants
            .borrow_mut()
            .entry(name.to_string())
            .or_default()
            .push(info);
    }

    /// First registered monomorph (legacy callers / single-variant enums).
    fn variant(&self, name: &str) -> Option<VariantInfo> {
        self.variants
            .borrow()
            .get(name)
            .and_then(|v| v.first().copied())
    }

    fn variants_named(&self, name: &str) -> Vec<VariantInfo> {
        self.variants
            .borrow()
            .get(name)
            .cloned()
            .unwrap_or_default()
    }

    /// Pick the monomorph whose payload slots match `payload_tys`.
    fn variant_matching(&self, name: &str, payload_tys: &[Type]) -> Option<VariantInfo> {
        let list = self.variants_named(name);
        let mut arity_match = None;
        for info in list {
            if info.arity != payload_tys.len() {
                continue;
            }
            if arity_match.is_none() {
                arity_match = Some(info);
            }
            let mut ok = true;
            for (i, pty) in payload_tys.iter().enumerate() {
                if self.field_type(info.enum_id, info.slot_base + i) != *pty {
                    ok = false;
                    break;
                }
            }
            if ok {
                return Some(info);
            }
        }
        // Construction without a prior signature (e.g. `Some(Point{…})` /
        // `Ok([1,2])` when only Option[int]/Result[int,string] exist): auto-intern
        // a matching monomorph BEFORE the arity-only fallback. Returning the sole
        // arity match first would force Struct/IntSlice payloads into I64 slots.
        match (name, payload_tys) {
            ("Some", [inner]) if is_field_type(*inner) => {
                let id = self.intern_option(*inner);
                return self.variant_in_enum("Some", id);
            }
            ("None", []) => {
                // Prefer existing Option monomorph; else intern Option[int].
                if let Some(info) = arity_match {
                    return Some(info);
                }
                let id = self.intern_option(Type::I64);
                return self.variant_in_enum("None", id);
            }
            ("Ok", [ok_ty]) if is_field_type(*ok_ty) => {
                // Default Err payload to string for untyped Ok(x).
                let id = self.intern_result(*ok_ty, Type::Str);
                return self.variant_in_enum("Ok", id);
            }
            ("Err", [err_ty]) if is_field_type(*err_ty) => {
                // Default Ok payload to int for untyped Err(x).
                let id = self.intern_result(Type::I64, *err_ty);
                return self.variant_in_enum("Err", id);
            }
            _ => {}
        }
        // Last resort: sole arity-compatible monomorph (unit variants, or when
        // auto-intern cannot apply). Only safe when payload types already matched
        // above (empty payload) or there is exactly one arity candidate.
        if payload_tys.is_empty() {
            return arity_match;
        }
        if self
            .variants_named(name)
            .iter()
            .filter(|i| i.arity == payload_tys.len())
            .count()
            == 1
        {
            // Types did not match any monomorph and auto-intern did not apply —
            // still refuse a wrong payload shape rather than coerce into I64.
            return None;
        }
        None
    }

    /// Variant of a specific enum layout (match arms, typed construction).
    fn variant_in_enum(&self, name: &str, enum_id: u32) -> Option<VariantInfo> {
        self.variants_named(name)
            .into_iter()
            .find(|info| info.enum_id == enum_id)
    }

    fn is_enum(&self, id: u32) -> bool {
        self.enum_ids.borrow().contains(&id)
    }
}

#[derive(Debug)]
pub struct IrError(String);

impl IrError {
    fn new(message: impl Into<String>) -> Self {
        Self(message.into())
    }
}

impl fmt::Display for IrError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        f.write_str(&self.0)
    }
}

impl std::error::Error for IrError {}

fn scalar_type(ty: &TypeExpr) -> Result<Type, IrError> {
    match ty {
        TypeExpr::Named(name)
            if name == "int"
                || name == "int64"
                || name == "int32"
                || name == "int16"
                || name == "int8"
                || name == "uint64"
                || name == "uint32"
                || name == "uint16"
                || name == "uint8"
                || name == "byte"
                || name == "rune" =>
        {
            Ok(Type::I64)
        }
        TypeExpr::Named(name) if name == "bool" => Ok(Type::I1),
        TypeExpr::Named(name) if name == "float" || name == "float64" => Ok(Type::F64),
        // string_view is a non-owning string view; native IR models it as Str
        // (immortal/static high-bit already covers literal views).
        TypeExpr::Named(name) if name == "string" || name == "string_view" => Ok(Type::Str),
        TypeExpr::Named(name) if name == "ShareInt" => Ok(Type::ShareInt),
        TypeExpr::Array(inner) if matches!(inner.as_ref(), TypeExpr::Named(name) if name == "int" || name == "int64") => {
            Ok(Type::IntSlice)
        }
        TypeExpr::Array(inner)
            if matches!(inner.as_ref(), TypeExpr::Named(name) if name == "float" || name == "float64") =>
        {
            Ok(Type::FloatSlice)
        }
        TypeExpr::Array(inner)
            if matches!(inner.as_ref(), TypeExpr::Named(name) if name == "byte" || name == "uint8") =>
        {
            Ok(Type::ByteSlice)
        }
        TypeExpr::Array(inner) if matches!(inner.as_ref(), TypeExpr::Named(name) if name == "bool") => {
            Ok(Type::BoolSlice)
        }
        TypeExpr::Array(inner) if matches!(inner.as_ref(), TypeExpr::Named(name) if name == "string") => {
            Ok(Type::StrSlice)
        }
        TypeExpr::Generic(name, args)
            if name == "chan"
                && args.len() == 1
                && matches!(&args[0], TypeExpr::Named(n) if n == "int" || n == "int64") =>
        {
            Ok(Type::ChanI)
        }
        TypeExpr::Generic(name, args)
            if name == "chan" && args.len() == 1 && matches!(&args[0], TypeExpr::Named(n) if n == "string") =>
        {
            Ok(Type::ChanS)
        }
        TypeExpr::Generic(name, args)
            if name == "chan"
                && args.len() == 1
                && matches!(&args[0], TypeExpr::Named(n) if n == "float" || n == "float64") =>
        {
            Ok(Type::ChanF)
        }
        TypeExpr::Generic(name, args) if name == "chan" && args.len() == 1 => {
            // Element type refined in resolve_type when StructRegistry is available.
            Ok(Type::ChanP(MapValKind::Other))
        }
        TypeExpr::Fn(params, ret) => {
            // First-class functions: seed accepts scalar/pointer ABI signatures.
            let _ = (params, ret);
            Ok(Type::FnPtr)
        }
        TypeExpr::Named(name)
            if matches!(
                name.as_str(),
                "TlsConn"
                    | "SqlDB"
                    | "StrBuilder"
                    | "HttpConn"
                    | "MailMsg"
                    | "GpuDevice"
                    | "CMap"
                    | "Http2Conn"
                    | "H3Server"
                    | "Model"
                    | "GpuBuf"
                    | "SmtpConn"
                    | "EvLoop"
                    | "Mutex"
                    | "RwMutex"
                    | "BTree"
                    | "HIndex"
                    | "BufReader"
                    | "BufWriter"
                    | "TlsServer"
                    | "WaitGroup"
                    | "Once"
                    | "Cond"
                    | "File"
                    | "Dir"
                    | "Regex"
                    | "Template"
                    | "Router"
                    | "Bloom"
                    | "Ring"
                    | "Pool"
                    | "SqlPool"
                    | "SqlRows"
                    | "SqlStmt"
                    | "SqlTx"
                    | "PgConn"
                    | "MysqlConn"
                    | "Redis"
                    | "Actor"
                    | "Context"
                    | "Cancel"
                    | "Ticker"
                    | "Timer"
                    | "Listener"
                    | "Conn"
                    | "UdpConn"
                    | "WsConn"
                    | "HttpRequest"
                    | "HttpParsed"
                    | "HttpResponse"
            ) =>
        {
            Ok(Type::Opaque)
        }
        // Uuid is a 16-byte value in the typechecker; native IR stores the
        // canonical string form (same as uuid_v4 / uuid_string bridges).
        TypeExpr::Named(name) if name == "Uuid" || name == "uuid" => Ok(Type::Str),
        // TitleCase unknown names (runtime handles) → opaque pointer.
        TypeExpr::Named(name)
            if name
                .chars()
                .next()
                .is_some_and(|c| c.is_uppercase())
                && !matches!(name.as_str(), "ShareInt") =>
        {
            Ok(Type::Opaque)
        }
        // List[T] aliases []T for the common int/string specializations.
        TypeExpr::Generic(name, args) if name == "List" && args.len() == 1 => {
            match args[0] {
                TypeExpr::Named(ref n) if n == "int" || n == "int64" => Ok(Type::IntSlice),
                TypeExpr::Named(ref n) if n == "string" => Ok(Type::StrSlice),
                TypeExpr::Named(ref n) if n == "float" || n == "float64" => Ok(Type::FloatSlice),
                TypeExpr::Named(ref n) if n == "byte" || n == "uint8" => Ok(Type::ByteSlice),
                TypeExpr::Named(ref n) if n == "bool" => Ok(Type::BoolSlice),
                _ => Err(IrError::new(format!(
                    "native IR: List[{}] is not supported yet",
                    args[0]
                ))),
            }
        }
        // Angle-bracket form List<int> arrives as Generic too.
        _ => Err(IrError::new(format!(
            "native IR: type `{ty}` is not in the scalar increment"
        ))),
    }
}

/// Types that may appear as struct/tuple fields or enum payloads. Nested
/// aggregates are allowed because every aggregate is a heap pointer (or an
/// owned string/slice header); recursive clone/drop walks the layout.
fn is_field_type(ty: Type) -> bool {
    matches!(
        ty,
        Type::I1
            | Type::I32
            | Type::I64
            | Type::F64
            | Type::Str
            | Type::IntSlice
            | Type::FloatSlice
            | Type::ByteSlice
            | Type::BoolSlice
            | Type::StrSlice
            | Type::MapII
            | Type::MapSI
            | Type::MapSS
            | Type::MapIF
            | Type::MapFI
            | Type::MapIPtr(_)
            | Type::MapSPtr(_)
            | Type::PtrSlice(_)
            | Type::ChanI
            | Type::ChanS
            | Type::ChanF
            | Type::ChanP(_)
            | Type::Arena
            | Type::Nursery
            | Type::Task
            | Type::Opaque
            | Type::FnPtr
            | Type::StructSlice(_)
            | Type::ShareInt
            | Type::Struct(_)
    )
}

/// Resolve a type expression, allowing user struct names in addition to the
/// scalar set. Struct names bind to their registry index.
fn resolve_type(ty: &TypeExpr, structs: &StructRegistry) -> Result<Type, IrError> {
    match ty {
        TypeExpr::Named(name) => {
            if let Some(&id) = structs.by_name.get(name) {
                return Ok(Type::Struct(id));
            }
            // Interface types are opaque fat-pointer handles in this increment.
            if structs.interfaces.contains(name) {
                return Ok(Type::Opaque);
            }
            // Generic type params that escaped monomorphization.
            if name.len() == 1 && name.chars().next().is_some_and(|c| c.is_uppercase()) {
                return Err(IrError::new(format!(
                    "native IR: type `{name}` is not in the scalar increment"
                )));
            }
            scalar_type(ty)
        }
        TypeExpr::Array(inner) => {
            // Prefer scalar slices; then []NamedStruct; then nested / bag slices.
            if let Ok(t) = scalar_type(ty) {
                return Ok(t);
            }
            if let TypeExpr::Named(name) = inner.as_ref() {
                if let Some(&id) = structs.by_name.get(name) {
                    return Ok(Type::StructSlice(id));
                }
            }
            // [][]T, []Option[T], []Result[…], []chan[T], []map[…] as pointer arrays.
            match resolve_type(inner, structs) {
                Ok(elem)
                    if elem.is_heap()
                        || matches!(
                            elem,
                            Type::Struct(_)
                                | Type::ChanI
                                | Type::ChanS
                                | Type::ChanF
                                | Type::ChanP(_)
                                | Type::Opaque
                                | Type::FnPtr
                        ) =>
                {
                    Ok(Type::PtrSlice(MapValKind::from_type(elem)))
                }
                _ => Err(IrError::new(format!(
                    "native IR: type `{ty}` is not in the scalar increment"
                ))),
            }
        }
        TypeExpr::Tuple(elements) => {
            let fields = elements
                .iter()
                .map(|element| {
                    let field_ty = resolve_type(element, structs)?;
                    if !is_field_type(field_ty) {
                        return Err(IrError::new(
                            "native IR: tuple element type is not supported in the current increment",
                        ));
                    }
                    Ok(field_ty)
                })
                .collect::<Result<Vec<_>, _>>()?;
            Ok(Type::Struct(structs.intern_tuple(fields)))
        }
        TypeExpr::Map(k, v) => match (k.as_ref(), v.as_ref()) {
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if (kn == "int" || kn == "int64") && (vn == "int" || vn == "int64") =>
            {
                Ok(Type::MapII)
            }
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if kn == "string" && (vn == "int" || vn == "int64") =>
            {
                Ok(Type::MapSI)
            }
            (TypeExpr::Named(kn), TypeExpr::Named(vn)) if kn == "string" && vn == "string" => {
                Ok(Type::MapSS)
            }
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if (kn == "int" || kn == "int64")
                    && (vn == "float" || vn == "float64") =>
            {
                Ok(Type::MapIF)
            }
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if (kn == "float" || kn == "float64")
                    && (vn == "int" || vn == "int64") =>
            {
                Ok(Type::MapFI)
            }
            // Pointer-value maps: map[int][]T, map[string]Struct, map[int]Result[…], …
            (TypeExpr::Named(kn), _) if kn == "int" || kn == "int64" => {
                let vt = resolve_type(v, structs)?;
                Ok(Type::MapIPtr(MapValKind::from_type(vt)))
            }
            (TypeExpr::Named(kn), _) if kn == "string" => {
                let vt = resolve_type(v, structs)?;
                Ok(Type::MapSPtr(MapValKind::from_type(vt)))
            }
            // map[bool]/map[float] for scalar keys → int-key map of pointers.
            (TypeExpr::Named(kn), _) if kn == "bool" || kn == "float" || kn == "float64" => {
                let vt = resolve_type(v, structs)?;
                Ok(Type::MapIPtr(MapValKind::from_type(vt)))
            }
            // map[Struct]int|string|float — content-keyed struct keys.
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if structs.by_name.contains_key(kn)
                    && (vn == "int" || vn == "int64" || vn == "string" || vn == "float" || vn == "float64") =>
            {
                let kid = structs.by_name[kn];
                let kind = if vn == "string" {
                    MapValKind::StructKeyStr(kid)
                } else if vn == "float" || vn == "float64" {
                    MapValKind::StructKeyFloat(kid)
                } else {
                    MapValKind::StructKeyInt(kid)
                };
                Ok(Type::MapIPtr(kind))
            }
            // map[StructK]StructV
            (TypeExpr::Named(kn), TypeExpr::Named(vn))
                if structs.by_name.contains_key(kn) && structs.by_name.contains_key(vn) =>
            {
                let kid = structs.by_name[kn];
                let vid = structs.by_name[vn];
                Ok(Type::MapIPtr(MapValKind::StructKeyToStruct(kid, vid)))
            }
            _ => {
                // map[Struct]V for other V → content-keyed for slice values so
                // maps_clone/copy re-bucket by content. Other pointer values keep
                // MapIPtr(value) and still content-key at set/get by key expr type.
                if let TypeExpr::Named(kn) = k.as_ref() {
                    if let Some(&kid) = structs.by_name.get(kn) {
                        if let Ok(vt) = resolve_type(v, structs) {
                            let kind = match vt {
                                Type::IntSlice => MapValKind::StructKeyIntSlice(kid),
                                Type::StrSlice => MapValKind::StructKeyStrSlice(kid),
                                Type::FloatSlice => MapValKind::StructKeyFloatSlice(kid),
                                Type::StructSlice(eid) => {
                                    MapValKind::StructKeyStructSlice(kid, eid)
                                }
                                Type::Struct(vid) => MapValKind::StructKeyToStruct(kid, vid),
                                Type::Str => MapValKind::StructKeyStr(kid),
                                Type::F64 => MapValKind::StructKeyFloat(kid),
                                Type::I64 => MapValKind::StructKeyInt(kid),
                                other => MapValKind::from_type(other),
                            };
                            return Ok(Type::MapIPtr(kind));
                        }
                    }
                }
                if let Ok(vt) = resolve_type(v, structs) {
                    Ok(Type::MapIPtr(MapValKind::from_type(vt)))
                } else {
                    Err(IrError::new(format!(
                        "native IR: map type map[{k}]{v} is not implemented yet"
                    )))
                }
            }
        },
        TypeExpr::Generic(name, args) if name == "Result" && args.len() == 2 => {
            let ok = resolve_type(&args[0], structs)?;
            let err = resolve_type(&args[1], structs)?;
            Ok(Type::Struct(structs.intern_result(ok, err)))
        }
        TypeExpr::Generic(name, args) if name == "Option" && args.len() == 1 => {
            let inner = resolve_type(&args[0], structs)?;
            Ok(Type::Struct(structs.intern_option(inner)))
        }
        // chan[int]/chan[string]/chan[float] specialized; other elements via MapValKind.
        // Do not call scalar_type first: it returns ChanP(Other) for any remaining
        // elem type and would skip struct/slice refinement.
        TypeExpr::Generic(name, args) if name == "chan" && args.len() == 1 => {
            match &args[0] {
                TypeExpr::Named(n) if n == "int" || n == "int64" => return Ok(Type::ChanI),
                TypeExpr::Named(n) if n == "string" => return Ok(Type::ChanS),
                TypeExpr::Named(n) if n == "float" || n == "float64" => return Ok(Type::ChanF),
                _ => {}
            }
            let elem = resolve_type(&args[0], structs)?;
            Ok(Type::ChanP(MapValKind::from_type(elem)))
        }
        // User monomorph: Pair[int] → layout registered as Pair__int (typecheck/parser).
        TypeExpr::Generic(name, args)
            if !matches!(
                name.as_str(),
                "chan" | "Result" | "Option" | "List" | "map" | "Map"
            ) =>
        {
            let tags: Vec<String> = args.iter().map(type_expr_mono_tag).collect();
            let mono = format!("{name}__{}", tags.join("__"));
            if let Some(&id) = structs.by_name.get(&mono) {
                return Ok(Type::Struct(id));
            }
            Err(IrError::new(format!(
                "native IR: type `{ty}` is not in the scalar increment"
            )))
        }
        _ => scalar_type(ty),
    }
}

/// Collect aggregate layouts before lowering any function. Two passes so a
/// struct/enum field may name another aggregate defined later (or mutually).
/// Clone/drop is recursive over the field types of each layout id.
fn build_structs(program: &Program) -> Result<StructRegistry, IrError> {
    let mut by_name = HashMap::new();
    let mut layouts: Vec<StructLayout> = Vec::new();
    let mut enum_ids = std::collections::HashSet::new();
    let mut interfaces = std::collections::HashSet::new();
    let mut interface_methods = HashMap::new();
    let mut concrete_tags = HashMap::new();
    let mut next_tag = 1i64;

    // Pass 1: reserve an id for every named aggregate so field resolution can
    // refer to types declared later in the file.
    for item in &program.items {
        match item {
            Item::Struct(def) => {
                // Generic templates are monomorphized by the typechecker into
                // concrete items (e.g. `Pair[T]` → `Pair__int`); skip templates.
                if !def.type_params.is_empty() {
                    continue;
                }
                let id = layouts.len() as u32;
                if by_name.insert(def.name.clone(), id).is_some() {
                    return Err(IrError::new(format!(
                        "native IR: duplicate struct `{}`",
                        def.name
                    )));
                }
                layouts.push(StructLayout {
                    name: def.name.clone(),
                    fields: Vec::new(),
                    defaults: Vec::new(),
                });
                concrete_tags.insert(def.name.clone(), next_tag);
                next_tag += 1;
            }
            Item::Enum(def) => {
                // Same as structs: monomorphs arrive as concrete enums.
                if !def.type_params.is_empty() {
                    continue;
                }
                let id = layouts.len() as u32;
                if by_name.insert(def.name.clone(), id).is_some() {
                    return Err(IrError::new(format!(
                        "native IR: duplicate type `{}`",
                        def.name
                    )));
                }
                layouts.push(StructLayout {
                    name: def.name.clone(),
                    fields: Vec::new(),
                    defaults: Vec::new(),
                });
                enum_ids.insert(id);
            }
            Item::Interface(iface) => {
                interfaces.insert(iface.name.clone());
                let methods: Vec<String> = iface.methods.iter().map(|(n, _, _)| n.clone()).collect();
                interface_methods.insert(iface.name.clone(), methods);
            }
            _ => {}
        }
    }

    let registry = StructRegistry {
        by_name,
        layouts: RefCell::new(layouts),
        tuple_shapes: RefCell::new(HashMap::new()),
        result_shapes: RefCell::new(HashMap::new()),
        option_shapes: RefCell::new(HashMap::new()),
        variants: RefCell::new(HashMap::new()),
        enum_ids: RefCell::new(enum_ids),
        interfaces,
        interface_methods,
        concrete_tags,
    };

    // Pass 2: fill field lists with fully resolved types (including nested
    // aggregates and []string). Enums keep non-overlapping payload slots so the
    // flat recursive clone/drop remains correct for inactive null slots.
    for item in &program.items {
        match item {
            Item::Struct(def) => {
                if !def.type_params.is_empty() {
                    continue;
                }
                let id = registry.by_name[&def.name];
                let mut fields = Vec::with_capacity(def.fields.len());
                let mut defaults = Vec::with_capacity(def.fields.len());
                for (fname, fty, default) in &def.fields {
                    let ty = resolve_type(fty, &registry).map_err(|_| {
                        IrError::new(format!(
                            "native IR: struct field `{}.{fname}` type is not supported",
                            def.name
                        ))
                    })?;
                    if !is_field_type(ty) {
                        return Err(IrError::new(format!(
                            "native IR: struct field `{}.{fname}` of type `{fty}` is not supported",
                            def.name
                        )));
                    }
                    fields.push((fname.clone(), ty));
                    defaults.push(default.clone());
                }
                let mut layouts = registry.layouts.borrow_mut();
                layouts[id as usize].fields = fields;
                layouts[id as usize].defaults = defaults;
            }
            Item::Enum(def) => {
                if !def.type_params.is_empty() {
                    continue;
                }
                let id = registry.by_name[&def.name];
                let mut fields = vec![("tag".to_string(), Type::I64)];
                let mut slot_bases = Vec::with_capacity(def.variants.len());
                for variant in &def.variants {
                    slot_bases.push(fields.len());
                    for (index, field) in variant.fields.iter().enumerate() {
                        let ty = resolve_type(field, &registry).map_err(|_| {
                            IrError::new(format!(
                                "native IR: enum `{}` variant `{}` has an unsupported payload",
                                def.name, variant.name
                            ))
                        })?;
                        // Scalars, owned string/slice, and nested aggregates.
                        if !is_field_type(ty) {
                            return Err(IrError::new(format!(
                                "native IR: enum `{}` variant `{}` payload type is not supported",
                                def.name, variant.name
                            )));
                        }
                        fields.push((format!("{}_{index}", variant.name), ty));
                    }
                }
                registry.layouts.borrow_mut()[id as usize].fields = fields;
                for (tag, variant) in def.variants.iter().enumerate() {
                    // User enums: bare names must stay unique (Result/Option Ok/Err
                    // are the only names allowed to multi-register).
                    // Generic monomorphs (Maybe__int / MyBox__string) share Just/Empty/Val/Nothing.
                    if registry.variants.borrow().contains_key(&variant.name)
                        && !matches!(
                            variant.name.as_str(),
                            "Ok" | "Err" | "Some" | "None" | "Val" | "Nothing" | "Just"
                                | "Left" | "Right" | "Empty" | "Full" | "Absent" | "Present"
                        )
                    {
                        return Err(IrError::new(format!(
                            "native IR: duplicate enum variant `{}`",
                            variant.name
                        )));
                    }
                    registry.push_variant(
                        &variant.name,
                        VariantInfo {
                            enum_id: id,
                            tag: tag as i64,
                            arity: variant.fields.len(),
                            slot_base: slot_bases[tag],
                        },
                    );
                }
            }
            _ => {}
        }
    }

    // Ensure Result[int,string] and Option[int] monomorphs exist when referenced
    // only from signatures/bodies (no explicit enum item).
    let _ = registry.intern_result(Type::I64, Type::Str);
    let _ = registry.intern_option(Type::I64);

    Ok(registry)
}

fn item_kind_name(item: &Item) -> &'static str {
    match item {
        Item::Fn(_) => "fn",
        Item::Struct(_) => "struct",
        Item::Enum(_) => "enum",
        Item::Actor(_) => "actor",
        Item::Interface(_) => "interface",
        Item::ExternC(_) => "extern C",
        Item::Const(_) => "const",
        Item::On(_) => "on",
        Item::Package { .. } => "package",
        Item::Import { .. } => "import",
    }
}

/// Infer a monomorph tag from a call argument expression (seed: int/string/float/bool/slice/struct).
fn mono_tag_for_expr(expr: &Expr) -> String {
    match expr {
        Expr::Int(_) => "int".into(),
        Expr::Float(_) => "float".into(),
        Expr::Bool(_) => "bool".into(),
        Expr::String(_) => "string".into(),
        Expr::Unary { expr, .. } => mono_tag_for_expr(expr),
        Expr::Binary { left, .. } => mono_tag_for_expr(left),
        Expr::Array(xs) => {
            if let Some(first) = xs.first() {
                match mono_tag_for_expr(first).as_str() {
                    "string" => "strslice".into(),
                    "float" => "floatslice".into(),
                    "bool" => "boolslice".into(),
                    "point" | "Point" => "pointslice".into(),
                    "triple" | "Triple" => "tripleslice".into(),
                    "quad" | "Quad" => "quadslice".into(),
                    // Named user structs: []Pt → Ptslice style via TitleCase path
                    other if other.chars().next().is_some_and(|c| c.is_uppercase()) => {
                        format!("{other}slice")
                    }
                    _ => "intslice".into(),
                }
            } else {
                "intslice".into()
            }
        }
        Expr::StructLit { name, .. } | Expr::StructLitPos { name, .. } => {
            // Use the concrete struct name so wrap_some(Pt{…}) → wrap_some__Pt
            // (not a collapse to Point/int).
            name.clone()
        }
        Expr::Call { callee, .. } => {
            if let Expr::Ident(n) = callee.as_ref() {
                if n == "int" || n == "int64" {
                    return "int".into();
                }
                if n == "float" {
                    return "float".into();
                }
                if n == "string" {
                    return "string".into();
                }
                if n == "make" {
                    return "map".into();
                }
            }
            "int".into()
        }
        Expr::Ident(n) if n == "true" || n == "false" => "bool".into(),
        _ => "int".into(),
    }
}

fn type_expr_for_tag(tag: &str) -> TypeExpr {
    match tag {
        "string" => TypeExpr::Named("string".into()),
        "float" => TypeExpr::Named("float".into()),
        "bool" => TypeExpr::Named("bool".into()),
        "intslice" => TypeExpr::Array(Box::new(TypeExpr::Named("int".into()))),
        "strslice" => TypeExpr::Array(Box::new(TypeExpr::Named("string".into()))),
        "floatslice" => TypeExpr::Array(Box::new(TypeExpr::Named("float".into()))),
        "boolslice" => TypeExpr::Array(Box::new(TypeExpr::Named("bool".into()))),
        "pointslice" => TypeExpr::Array(Box::new(TypeExpr::Named("Point".into()))),
        "tripleslice" => TypeExpr::Array(Box::new(TypeExpr::Named("Triple".into()))),
        "quadslice" => TypeExpr::Array(Box::new(TypeExpr::Named("Quad".into()))),
        "structslice" => TypeExpr::Array(Box::new(TypeExpr::Named("Point".into()))),
        "point" => TypeExpr::Named("Point".into()),
        "triple" => TypeExpr::Named("Triple".into()),
        "quad" => TypeExpr::Named("Quad".into()),
        "struct" => TypeExpr::Named("Point".into()),
        "map" => TypeExpr::Map(
            Box::new(TypeExpr::Named("string".into())),
            Box::new(TypeExpr::Named("int".into())),
        ),
        // []Pt mono tags end with "slice" after a TitleCase name.
        other if other.ends_with("slice")
            && other.len() > 5
            && other
                .chars()
                .next()
                .is_some_and(|c| c.is_uppercase()) =>
        {
            let base = &other[..other.len() - 5];
            TypeExpr::Array(Box::new(TypeExpr::Named(base.into())))
        }
        // TitleCase → user struct monomorph tag (Dog, Cat, Point, Pt, …).
        other if other.chars().next().is_some_and(|c| c.is_uppercase()) => {
            TypeExpr::Named(other.into())
        }
        _ => TypeExpr::Named("int".into()),
    }
}

fn type_expr_mono_tag(ty: &TypeExpr) -> String {
    match ty {
        TypeExpr::Named(n) => match n.as_str() {
            "int" | "int64" => "int".into(),
            "float" | "float64" => "float".into(),
            "bool" => "bool".into(),
            "string" => "string".into(),
            "byte" | "uint8" => "byte".into(),
            other => other.to_string(),
        },
        TypeExpr::Generic(name, args) => {
            let tags: Vec<String> = args.iter().map(type_expr_mono_tag).collect();
            format!("{name}__{}", tags.join("__"))
        }
        _ => "int".into(),
    }
}

fn subst_type_expr(ty: &TypeExpr, map: &HashMap<String, TypeExpr>) -> TypeExpr {
    match ty {
        TypeExpr::Named(n) => map.get(n).cloned().unwrap_or_else(|| ty.clone()),
        TypeExpr::Array(inner) => TypeExpr::Array(Box::new(subst_type_expr(inner, map))),
        TypeExpr::Generic(name, args) => {
            let args: Vec<TypeExpr> = args.iter().map(|a| subst_type_expr(a, map)).collect();
            // Concrete user monomorph: Pair[int] → Named("Pair__int") so layouts match
            // typechecker / parser mono names.
            let tags: Vec<String> = args.iter().map(type_expr_mono_tag).collect();
            if !tags.is_empty()
                && !matches!(
                    name.as_str(),
                    "chan" | "Result" | "Option" | "List" | "map" | "Map"
                )
            {
                return TypeExpr::Named(format!("{name}__{}", tags.join("__")));
            }
            TypeExpr::Generic(name.clone(), args)
        }
        TypeExpr::Tuple(elems) => {
            TypeExpr::Tuple(elems.iter().map(|a| subst_type_expr(a, map)).collect())
        }
        TypeExpr::Map(k, v) => TypeExpr::Map(
            Box::new(subst_type_expr(k, map)),
            Box::new(subst_type_expr(v, map)),
        ),
        other => other.clone(),
    }
}

fn subst_mono_struct_name(name: &str, map: &HashMap<String, TypeExpr>) -> String {
    // Pair__T with T→int → Pair__int (typechecker/parser mono naming).
    if let Some((base, tags)) = name.split_once("__") {
        let new_tags: Vec<String> = tags
            .split("__")
            .map(|tag| {
                map.get(tag)
                    .map(type_expr_mono_tag)
                    .unwrap_or_else(|| tag.to_string())
            })
            .collect();
        return format!("{base}__{}", new_tags.join("__"));
    }
    name.to_string()
}

fn subst_expr(expr: &Expr, map: &HashMap<String, TypeExpr>) -> Expr {
    match expr {
        Expr::StructLit {
            name,
            fields,
            update,
        } => Expr::StructLit {
            name: subst_mono_struct_name(name, map),
            fields: fields
                .iter()
                .map(|(n, e)| (n.clone(), subst_expr(e, map)))
                .collect(),
            update: update.as_ref().map(|u| Box::new(subst_expr(u, map))),
        },
        Expr::StructLitPos { name, values } => Expr::StructLitPos {
            name: subst_mono_struct_name(name, map),
            values: values.iter().map(|e| subst_expr(e, map)).collect(),
        },
        Expr::Call { callee, args } => Expr::Call {
            callee: Box::new(subst_expr(callee, map)),
            args: args.iter().map(|a| subst_expr(a, map)).collect(),
        },
        Expr::Binary { op, left, right } => Expr::Binary {
            op: *op,
            left: Box::new(subst_expr(left, map)),
            right: Box::new(subst_expr(right, map)),
        },
        Expr::Unary { op, expr: e } => Expr::Unary {
            op: *op,
            expr: Box::new(subst_expr(e, map)),
        },
        Expr::Field { base, field } => Expr::Field {
            base: Box::new(subst_expr(base, map)),
            field: field.clone(),
        },
        Expr::Index { base, index } => Expr::Index {
            base: Box::new(subst_expr(base, map)),
            index: Box::new(subst_expr(index, map)),
        },
        Expr::Array(xs) => Expr::Array(xs.iter().map(|e| subst_expr(e, map)).collect()),
        Expr::Tuple(xs) => Expr::Tuple(xs.iter().map(|e| subst_expr(e, map)).collect()),
        Expr::Block(b) => Expr::Block(subst_block(b, map)),
        Expr::Match {
            scrutinee,
            arms,
        } => Expr::Match {
            scrutinee: Box::new(subst_expr(scrutinee, map)),
            arms: arms
                .iter()
                .map(|a| crate::ast::MatchArm {
                    pattern: a.pattern.clone(),
                    body: subst_expr(&a.body, map),
                    guard: a.guard.as_ref().map(|g| subst_expr(g, map)),
                })
                .collect(),
        },
        Expr::Lambda { params, body } => Expr::Lambda {
            params: params.clone(),
            body: Box::new(subst_expr(body, map)),
        },
        other => other.clone(),
    }
}

fn subst_block(block: &AstBlock, map: &HashMap<String, TypeExpr>) -> AstBlock {
    AstBlock {
        stmts: block
            .stmts
            .iter()
            .map(|s| match s {
                Stmt::Let {
                    name,
                    mutable,
                    ownership,
                    ty,
                    init,
                } => Stmt::Let {
                    name: name.clone(),
                    mutable: *mutable,
                    ownership: ownership.clone(),
                    ty: ty.as_ref().map(|t| subst_type_expr(t, map)),
                    init: subst_expr(init, map),
                },
                Stmt::Return(Some(e)) => Stmt::Return(Some(subst_expr(e, map))),
                Stmt::Expr(e) => Stmt::Expr(subst_expr(e, map)),
                Stmt::Assign { name, value } => Stmt::Assign {
                    name: name.clone(),
                    value: subst_expr(value, map),
                },
                Stmt::If {
                    init,
                    cond,
                    then_block,
                    else_block,
                } => Stmt::If {
                    init: init.as_ref().map(|i| Box::new(subst_stmt(i, map))),
                    cond: subst_expr(cond, map),
                    then_block: subst_block(then_block, map),
                    else_block: else_block.as_ref().map(|b| subst_block(b, map)),
                },
                other => other.clone(),
            })
            .collect(),
    }
}

fn subst_stmt(s: &Stmt, map: &HashMap<String, TypeExpr>) -> Stmt {
    match s {
        Stmt::Expr(e) => Stmt::Expr(subst_expr(e, map)),
        Stmt::Assign { name, value } => Stmt::Assign {
            name: name.clone(),
            value: subst_expr(value, map),
        },
        Stmt::Let {
            name,
            mutable,
            ownership,
            ty,
            init,
        } => Stmt::Let {
            name: name.clone(),
            mutable: *mutable,
            ownership: ownership.clone(),
            ty: ty.as_ref().map(|t| subst_type_expr(t, map)),
            init: subst_expr(init, map),
        },
        other => other.clone(),
    }
}

/// Collect call sites to generic templates and emit specialized `name__tag` functions.
fn monomorphize_generics(
    program: &Program,
    templates: &HashMap<String, FnDef>,
    structs: &StructRegistry,
) -> Result<Vec<FnDef>, IrError> {
    if templates.is_empty() {
        return Ok(Vec::new());
    }
    let struct_names: Vec<String> = structs
        .by_name
        .keys()
        .filter(|s| {
            s.chars().next().is_some_and(|c| c.is_uppercase()) && !s.contains('.')
        })
        .cloned()
        .collect();
    // Free-function names already in the program (includes desugared `Type_method`
    // and still-packed `on Type { fn method }` items).
    let mut free_fn_names: std::collections::HashSet<String> = std::collections::HashSet::new();
    for it in &program.items {
        match it {
            Item::Fn(f) => {
                free_fn_names.insert(f.name.clone());
            }
            Item::On(on) => {
                for m in &on.methods {
                    free_fn_names.insert(format!("{}_{}", on.ty, m.name));
                    if let Some(ref iface) = on.iface {
                        free_fn_names.insert(format!("{iface}_{}_{}", on.ty, m.name));
                    }
                }
            }
            _ => {}
        }
    }
    // Interface → method names for bound filtering.
    let iface_methods = &structs.interface_methods;
    let mut needed: HashMap<String, Vec<String>> = HashMap::new(); // mono_name -> tags
    fn walk_expr(
        expr: &Expr,
        templates: &HashMap<String, FnDef>,
        needed: &mut HashMap<String, Vec<String>>,
        struct_names: &[String],
        free_fn_names: &std::collections::HashSet<String>,
        iface_methods: &HashMap<String, Vec<String>>,
    ) {
        match expr {
            Expr::Call { callee, args } => {
                if let Expr::Ident(name) = callee.as_ref() {
                    if let Some(tmpl) = templates.get(name) {
                        let tags: Vec<String> = tmpl
                            .type_params
                            .iter()
                            .map(|tp| {
                                // Find first param with this type param name.
                                let mut tag = "int".to_string();
                                for (i, p) in tmpl.params.iter().enumerate() {
                                    if matches!(&p.ty, TypeExpr::Named(n) if n == tp) {
                                        if let Some(a) = args.get(i) {
                                            tag = mono_tag_for_expr(a).to_string();
                                        }
                                        break;
                                    }
                                }
                                tag
                            })
                            .collect();
                        let has_ident_tp = tmpl.type_params.iter().any(|tp| {
                            tmpl.params.iter().enumerate().any(|(i, p)| {
                                matches!(&p.ty, TypeExpr::Named(n) if n == tp)
                                    && matches!(args.get(i), Some(Expr::Ident(_)))
                            })
                        });
                        let bounded = !tmpl.type_bounds.is_empty();
                        // Primary tags: skip scalar tags for bounded generics
                        // (Ident → "int" would create a broken method body).
                        if !(bounded && tags.iter().all(|t| {
                            matches!(
                                t.as_str(),
                                "int" | "string" | "float" | "bool" | "map"
                                    | "intslice" | "strslice" | "point" | "pointslice"
                            )
                        })) {
                            let mono = format!("{name}__{}", tags.join("__"));
                            needed.entry(mono).or_insert(tags.clone());
                        }
                        // Ident args have unknown static tags — also demand a
                        // small set of common monomorphs so wrap_ok(m) / either
                        // work when m is a local map/slice/struct.
                        if has_ident_tp && tmpl.type_params.len() == 1 {
                            // Bounded generics (T: Describable) only monomorphize
                            // over user structs — mass int/string monomorphs would
                            // fail method dispatch inside the body.
                            if !bounded {
                                for extra in [
                                    "int", "string", "float", "bool", "intslice",
                                    "strslice", "map", "point", "pointslice",
                                ] {
                                    let m = format!("{name}__{extra}");
                                    needed
                                        .entry(m)
                                        .or_insert_with(|| vec![extra.to_string()]);
                                }
                            }
                            // Required methods for this bound (union across bounds).
                            let required_methods: Vec<String> = tmpl
                                .type_bounds
                                .values()
                                .flat_map(|iface| {
                                    iface_methods
                                        .get(iface)
                                        .into_iter()
                                        .flatten()
                                        .cloned()
                                })
                                .collect();
                            // User structs (Dog, Point, …) as type-arg monomorphs.
                            // For bounded generics, only emit monomorphs for types
                            // that implement the bound methods (have Type_method).
                            for sname in struct_names {
                                if sname.contains('.') || sname.contains("__") {
                                    continue;
                                }
                                if bounded {
                                    let ok = required_methods.iter().all(|m| {
                                        free_fn_names.contains(&format!("{sname}_{m}"))
                                    });
                                    if !ok {
                                        continue;
                                    }
                                }
                                let m = format!("{name}__{sname}");
                                needed
                                    .entry(m)
                                    .or_insert_with(|| vec![sname.clone()]);
                            }
                        }
                    }
                }
                for a in args {
                    walk_expr(a, templates, needed, struct_names, free_fn_names, iface_methods);
                }
                walk_expr(callee, templates, needed, struct_names, free_fn_names, iface_methods);
            }
            Expr::Method { receiver, args, .. } => {
                walk_expr(receiver, templates, needed, struct_names, free_fn_names, iface_methods);
                for a in args {
                    walk_expr(a, templates, needed, struct_names, free_fn_names, iface_methods);
                }
            }
            Expr::Binary { left, right, .. } => {
                walk_expr(left, templates, needed, struct_names, free_fn_names, iface_methods);
                walk_expr(right, templates, needed, struct_names, free_fn_names, iface_methods);
            }
            Expr::Unary { expr, .. }
            | Expr::Field { base: expr, .. }
            | Expr::Try(expr)
            | Expr::Join(expr)
            | Expr::Kick { expr, .. } => walk_expr(expr, templates, needed, struct_names, free_fn_names, iface_methods),
            Expr::Index { base, index } => {
                walk_expr(base, templates, needed, struct_names, free_fn_names, iface_methods);
                walk_expr(index, templates, needed, struct_names, free_fn_names, iface_methods);
            }
            Expr::Array(xs) | Expr::Tuple(xs) => {
                for x in xs {
                    walk_expr(x, templates, needed, struct_names, free_fn_names, iface_methods);
                }
            }
            Expr::Block(b) => walk_block(b, templates, needed, struct_names, free_fn_names, iface_methods),
            Expr::Match { scrutinee, arms } => {
                walk_expr(scrutinee, templates, needed, struct_names, free_fn_names, iface_methods);
                for arm in arms {
                    walk_expr(&arm.body, templates, needed, struct_names, free_fn_names, iface_methods);
                }
            }
            Expr::Lambda { body, .. } => walk_expr(body, templates, needed, struct_names, free_fn_names, iface_methods),
            Expr::Fan {
                collection,
                mapper,
            } => {
                walk_expr(collection, templates, needed, struct_names, free_fn_names, iface_methods);
                walk_expr(mapper, templates, needed, struct_names, free_fn_names, iface_methods);
            }
            _ => {}
        }
    }
    fn walk_block(
        b: &AstBlock,
        templates: &HashMap<String, FnDef>,
        needed: &mut HashMap<String, Vec<String>>,
        struct_names: &[String],
        free_fn_names: &std::collections::HashSet<String>,
        iface_methods: &HashMap<String, Vec<String>>,
    ) {
        for s in &b.stmts {
            match s {
                Stmt::Let { init, .. } => walk_expr(
                    init, templates, needed, struct_names, free_fn_names, iface_methods,
                ),
                Stmt::Expr(e) | Stmt::Return(Some(e)) => {
                    walk_expr(e, templates, needed, struct_names, free_fn_names, iface_methods)
                }
                Stmt::If {
                    cond,
                    then_block,
                    else_block,
                    ..
                } => {
                    walk_expr(cond, templates, needed, struct_names, free_fn_names, iface_methods);
                    walk_block(then_block, templates, needed, struct_names, free_fn_names, iface_methods);
                    if let Some(eb) = else_block {
                        walk_block(eb, templates, needed, struct_names, free_fn_names, iface_methods);
                    }
                }
                Stmt::While { cond, body, .. } => {
                    walk_expr(cond, templates, needed, struct_names, free_fn_names, iface_methods);
                    walk_block(body, templates, needed, struct_names, free_fn_names, iface_methods);
                }
                _ => {}
            }
        }
    }
    for item in &program.items {
        if let Item::Fn(f) = item {
            if f.type_params.is_empty() {
                walk_block(
                    &f.body, templates, &mut needed, &struct_names, &free_fn_names, iface_methods,
                );
            }
        }
    }
    let mut out = Vec::new();
    let mut seen = std::collections::HashSet::new();
    for (mono_name, tags) in needed {
        if !seen.insert(mono_name.clone()) {
            continue;
        }
        let base = mono_name.split("__").next().unwrap_or(&mono_name);
        let Some(tmpl) = templates.get(base) else {
            continue;
        };
        let mut map = HashMap::new();
        for (tp, tag) in tmpl.type_params.iter().zip(tags.iter()) {
            map.insert(tp.clone(), type_expr_for_tag(tag));
        }
        let params = tmpl
            .params
            .iter()
            .map(|p| crate::ast::Param {
                name: p.name.clone(),
                ty: subst_type_expr(&p.ty, &map),
                mutable: p.mutable,
            })
            .collect();
        let ret = tmpl.ret.as_ref().map(|t| subst_type_expr(t, &map));
        out.push(FnDef {
            type_bounds: tmpl.type_bounds.clone(),
            name: mono_name,
            type_params: Vec::new(),
            params,
            ret,
            body: subst_block(&tmpl.body, &map),
            exported: tmpl.exported,
            is_const: tmpl.is_const,
            stability: tmpl.stability.clone(),
        });
    }
    Ok(out)
}

pub fn lower(program: &Program) -> Result<Module, IrError> {
    lower_with_tests(program, &[])
}

/// Lower a program, optionally synthesizing a Go-style test harness `main`
/// that runs the listed test function names (used by `mako test --backend native`).
pub fn lower_with_tests(program: &Program, test_fns: &[String]) -> Result<Module, IrError> {
    let structs = build_structs(program)?;
    let mut signatures = HashMap::new();
    let mut generic_templates: HashMap<String, FnDef> = HashMap::new();
    for item in &program.items {
        match item {
            Item::Fn(function) => {
                if !function.type_params.is_empty() {
                    generic_templates.insert(function.name.clone(), function.clone());
                    continue;
                }
                let params = function
                    .params
                    .iter()
                    .map(|param| resolve_type(&param.ty, &structs))
                    .collect::<Result<Vec<_>, _>>()?;
                let ret = function
                    .ret
                    .as_ref()
                    .map(|ty| resolve_type(ty, &structs))
                    .transpose()?;
                signatures.insert(function.name.clone(), (params, ret));
            }
            Item::ExternC(ext) => {
                // Register as a free C symbol (same calling convention as runtime).
                let params = ext
                    .params
                    .iter()
                    .map(|param| resolve_type(&param.ty, &structs))
                    .collect::<Result<Vec<_>, _>>()?;
                let ret = ext
                    .ret
                    .as_ref()
                    .map(|ty| resolve_type(ty, &structs))
                    .transpose()?;
                signatures.insert(ext.name.clone(), (params, ret));
            }
            // Struct/enum layouts already registered; package/import/const/
            // interface are frontend bookkeeping for this increment.
            Item::Struct(_)
            | Item::Enum(_)
            | Item::Package { .. }
            | Item::Import { .. }
            | Item::Const(_)
            | Item::On(_)
            | Item::Interface(_) => {}
            other => {
                return Err(IrError::new(format!(
                    "native IR: non-function top-level item ({})",
                    item_kind_name(other)
                )));
            }
        }
    }
    // Demand-driven generic monomorphization: scan for calls, synthesize
    // specialized FnDefs (identity[T] → identity__int / identity__string).
    let mono_fns = monomorphize_generics(program, &generic_templates, &structs)?;
    for f in &mono_fns {
        let params = f
            .params
            .iter()
            .map(|param| resolve_type(&param.ty, &structs))
            .collect::<Result<Vec<_>, _>>()?;
        let ret = f
            .ret
            .as_ref()
            .map(|ty| resolve_type(ty, &structs))
            .transpose()?;
        signatures.insert(f.name.clone(), (params, ret));
    }
    // Synthetic harness main for `mako test --backend native`: call each TestXxx().
    let harness_main: Option<FnDef> = if !signatures.contains_key("main") && !test_fns.is_empty() {
        let mut stmts = Vec::new();
        for t in test_fns {
            if !signatures.contains_key(t) {
                return Err(IrError::new(format!(
                    "native IR: test harness missing function `{t}`"
                )));
            }
            stmts.push(Stmt::Expr(Expr::Call {
                callee: Box::new(Expr::Ident(t.clone())),
                args: vec![],
            }));
        }
        stmts.push(Stmt::Return(Some(Expr::Int(0))));
        let main_fn = FnDef {
            type_bounds: HashMap::new(),
            name: "main".into(),
            type_params: Vec::new(),
            params: Vec::new(),
            ret: Some(TypeExpr::Named("int".into())),
            body: AstBlock { stmts },
            exported: false,
            is_const: false,
            stability: crate::ast::ApiStability::Unspecified,
        };
        signatures.insert("main".into(), (vec![], Some(Type::I64)));
        Some(main_fn)
    } else {
        None
    };
    if !signatures.contains_key("main") {
        return Err(IrError::new("native IR: program has no `main` function"));
    }

    // Comptime const ints/strings (and const-fn bodies used by initializers).
    let (const_ints, const_strs) = collect_consts(program);
    let mut lambda_id = 0u32;
    let mut lambda_stubs: Vec<FnDef> = Vec::new();

    let mut kick_stubs = Vec::new();
    let mut kick_id = 0u32;
    let mut functions = Vec::new();
    let mut all_fns: Vec<&FnDef> = program
        .items
        .iter()
        .filter_map(|i| match i {
            Item::Fn(f) if f.type_params.is_empty() => Some(f),
            _ => None,
        })
        .collect();
    let mut mono_by_name = std::collections::HashMap::new();
    for f in &mono_fns {
        mono_by_name.insert(f.name.clone(), f);
    }
    for f in mono_by_name.values() {
        all_fns.push(f);
    }
    if let Some(ref hm) = harness_main {
        all_fns.push(hm);
    }
    // Track `mut self` methods (desugared `on T { fn m(mut self) }` → `T_m`).
    let mut mut_self_fns = HashSet::new();
    for f in &all_fns {
        if let Some(p) = f.params.first() {
            if p.name == "self" && p.mutable {
                mut_self_fns.insert(f.name.clone());
            }
        }
    }
    for function in all_fns {
        {
            // rebind for existing body that expected Item::Fn
            let function = function;
            functions.push(
                FunctionLowerer::new(
                    function,
                    &signatures,
                    &mut_self_fns,
                    &structs,
                    &const_ints,
                    &const_strs,
                )?
                .with_kick_collector(&mut kick_stubs, &mut kick_id)
                .with_lambda_collector(&mut lambda_stubs, &mut lambda_id)
                .lower(function)?,
            );
        }
    }
    // Lambda stubs synthesized during lowering — lower them too.
    for lf in &lambda_stubs {
        signatures.insert(
            lf.name.clone(),
            (
                lf.params
                    .iter()
                    .map(|p| resolve_type(&p.ty, &structs))
                    .collect::<Result<Vec<_>, _>>()?,
                lf.ret
                    .as_ref()
                    .map(|t| resolve_type(t, &structs))
                    .transpose()?,
            ),
        );
    }
    for lf in &lambda_stubs {
        functions.push(
            FunctionLowerer::new(
                lf,
                &signatures,
                &mut_self_fns,
                &structs,
                &const_ints,
                &const_strs,
            )?
            .with_kick_collector(&mut kick_stubs, &mut kick_id)
            .lower(lf)?,
        );
    }
    functions.extend(kick_stubs);
    // Dedupe by name (monomorph + kick edge cases).
    {
        let mut seen = std::collections::HashSet::new();
        functions.retain(|f| seen.insert(f.name.clone()));
    }
    Ok(Module {
        functions,
        structs: structs.into_layouts(),
    })
}

/// Fold `const NAME = …` / `const fn` initializers into int and string tables.
fn collect_consts(program: &Program) -> (HashMap<String, i64>, HashMap<String, String>) {
    let mut const_fns: HashMap<String, FnDef> = HashMap::new();
    for item in &program.items {
        if let Item::Fn(f) = item {
            if f.is_const {
                const_fns.insert(f.name.clone(), f.clone());
            }
        }
    }
    let mut consts: HashMap<String, i64> = HashMap::new();
    let mut const_strs: HashMap<String, String> = HashMap::new();
    // Multiple passes so later consts can depend on earlier ones of either kind.
    for _ in 0..8 {
        let mut progress = false;
        for item in &program.items {
            if let Item::Const(c) = item {
                if consts.contains_key(&c.name) || const_strs.contains_key(&c.name) {
                    continue;
                }
                if let Ok(v) =
                    crate::types::fold_const_expr_with(&c.value, &consts, &const_strs, &const_fns)
                {
                    consts.insert(c.name.clone(), v);
                    progress = true;
                } else if let Ok(s) =
                    crate::types::fold_const_str_with(&c.value, &consts, &const_strs, &const_fns)
                {
                    const_strs.insert(c.name.clone(), s);
                    progress = true;
                }
            }
        }
        if !progress {
            break;
        }
    }
    (consts, const_strs)
}

struct FunctionLowerer<'a> {
    signatures: &'a HashMap<String, (Vec<Type>, Option<Type>)>,
    /// Free-function names whose first parameter is `mut self` (in-place
    /// receiver). Method calls pass the live struct pointer without clone.
    mut_self_fns: &'a HashSet<String>,
    structs: &'a StructRegistry,
    const_ints: &'a HashMap<String, i64>,
    const_strs: &'a HashMap<String, String>,
    function: Function,
    current: BlockId,
    locals: HashMap<String, (Value, Type)>,
    heap_owned: HashMap<String, bool>,
    /// Innermost-first stack of loop frames: label, break target, continue
    /// target, and the heap-ownership / locals snapshots at body entry so
    /// break/continue can drop body-scoped heap values.
    loops: Vec<LoopFrame>,
    /// `defer` bodies (LIFO). Run on every return and fallthrough exit, before
    /// owned locals are dropped so a defer can still observe them.
    defers: Vec<crate::ast::Block>,
    /// Optional collector for synthetic `__mako_kick_N` trampoline functions.
    kick_stubs: Option<&'a mut Vec<Function>>,
    kick_id: Option<&'a mut u32>,
    /// Synthetic named functions for `Expr::Lambda` (lowered after main pass).
    lambda_stubs: Option<&'a mut Vec<FnDef>>,
    lambda_id: Option<&'a mut u32>,
    /// Return type of the most recently lowered `kick` (joined value type).
    pending_kick_ret: Option<Type>,
    /// Crew name of the most recently lowered `kick`.
    pending_kick_crew: Option<String>,
    /// Local job name → joined value type (from kick).
    job_ret_tys: HashMap<String, Type>,
    /// Local job name → enclosing crew local (for nursery_note_err).
    job_crews: HashMap<String, String>,
    /// When true, trailing `return e` in a block expression yields `e` without
    /// emitting a function Return (inlined fan mappers: `fn(x) { return x*x }`).
    /// Match/if arm bodies leave this false so `return` exits the function.
    return_as_yield: bool,
    /// Outer `let mut` int captures redirected through a heap cell pointer (i64).
    /// Loads/stores of the name go through `mako_native_i64_cell_{load,store}`.
    mut_i64_cells: HashMap<String, Value>,
}

struct LoopFrame {
    label: Option<String>,
    break_target: BlockId,
    continue_target: BlockId,
    /// Heap ownership map at body entry (outer locals only after restore).
    owned_before: HashMap<String, bool>,
    /// Locals present at body entry.
    locals_before: HashMap<String, (Value, Type)>,
}

impl<'a> FunctionLowerer<'a> {
    fn new(
        source: &FnDef,
        signatures: &'a HashMap<String, (Vec<Type>, Option<Type>)>,
        mut_self_fns: &'a HashSet<String>,
        structs: &'a StructRegistry,
        const_ints: &'a HashMap<String, i64>,
        const_strs: &'a HashMap<String, String>,
    ) -> Result<Self, IrError> {
        let entry = BlockId(0);
        let ret = signatures[&source.name].1;
        let mut function = Function {
            name: source.name.clone(),
            params: Vec::new(),
            ret,
            blocks: vec![BasicBlock {
                instructions: Vec::new(),
                terminator: None,
            }],
            entry,
            next_value: 0,
        };
        let mut locals = HashMap::new();
        let mut heap_owned = HashMap::new();
        for (index, param) in source.params.iter().enumerate() {
            let ty = signatures[&source.name].0[index];
            let incoming = Value(function.next_value);
            function.next_value += 1;
            function.params.push((param.name.clone(), incoming, ty));
            let slot = Value(function.next_value);
            function.next_value += 1;
            function.blocks[0]
                .instructions
                .push(Inst::Alloca { out: slot, ty });
            function.blocks[0].instructions.push(Inst::Store {
                ptr: slot,
                value: incoming,
            });
            locals.insert(param.name.clone(), (slot, ty));
            if ty.is_heap() {
                // Heap parameters are borrows. The caller retains ownership
                // (for structs the caller passes an owned copy it drops).
                // `mut self` still borrows the caller's pointer — mutations
                // go through the shared heap block.
                heap_owned.insert(param.name.clone(), false);
            }
        }
        Ok(Self {
            signatures,
            mut_self_fns,
            structs,
            const_ints,
            const_strs,
            function,
            current: entry,
            locals,
            heap_owned,
            loops: Vec::new(),
            defers: Vec::new(),
            kick_stubs: None,
            kick_id: None,
            lambda_stubs: None,
            lambda_id: None,
            pending_kick_ret: None,
            pending_kick_crew: None,
            job_ret_tys: HashMap::new(),
            job_crews: HashMap::new(),
            return_as_yield: false,
            mut_i64_cells: HashMap::new(),
        })
    }

    fn with_lambda_collector(
        mut self,
        stubs: &'a mut Vec<FnDef>,
        id: &'a mut u32,
    ) -> Self {
        self.lambda_stubs = Some(stubs);
        self.lambda_id = Some(id);
        self
    }

    fn with_kick_collector(
        mut self,
        stubs: &'a mut Vec<Function>,
        id: &'a mut u32,
    ) -> Self {
        self.kick_stubs = Some(stubs);
        self.kick_id = Some(id);
        self
    }

    fn lower(mut self, source: &FnDef) -> Result<Function, IrError> {
        // Implicit return: a non-void function whose body ends with a bare
        // expression (and has no explicit `return`) treats that expression as
        // the return value — matching the C backend.
        let implicit = self.function.ret.is_some()
            && source.name != "main"
            && !source
                .body
                .stmts
                .iter()
                .any(|s| matches!(s, Stmt::Return(_)))
            && matches!(source.body.stmts.last(), Some(Stmt::Expr(_)));
        if implicit {
            let stmts = &source.body.stmts;
            let n = stmts.len().saturating_sub(1);
            for statement in &stmts[..n] {
                if self.block().terminator.is_some() {
                    break;
                }
                self.lower_stmt(statement)?;
            }
            if self.block().terminator.is_none() {
                if let Some(Stmt::Expr(expr)) = stmts.last() {
                    let (mut value, mut ty, mut owned) = self.lower_expr(expr)?;
                    let expected = self.function.ret;
                    // Allow bool ↔ int at returns (runtime often returns i64 for bool).
                    if expected == Some(Type::I1) && ty == Type::I64 {
                        let b = self.value();
                        self.emit(Inst::Cast {
                            out: b,
                            value,
                            from: Type::I64,
                            to: Type::I1,
                        });
                        value = b;
                        ty = Type::I1;
                    } else if expected == Some(Type::I64) && ty == Type::I1 {
                        let i = self.value();
                        self.emit(Inst::Cast {
                            out: i,
                            value,
                            from: Type::I1,
                            to: Type::I64,
                        });
                        value = i;
                        ty = Type::I64;
                    }
                    if expected != Some(ty) {
                        return Err(IrError::new(format!(
                            "native IR: implicit return type mismatch in `{}`",
                            source.name
                        )));
                    }
                    if ty.is_heap() {
                        owned = self.take_bare_string_local(expr, owned);
                        if !owned {
                            value = self.emit_clone(value, ty);
                        }
                    }
                    self.emit_defers()?;
                    self.drop_owned_locals();
                    self.terminate(Terminator::Return(Some(value)))?;
                }
            }
        } else {
            self.lower_block(&source.body)?;
        }
        if self.block().terminator.is_none() {
            if source.name == "main" {
                let zero = self.const_int(0, Type::I32);
                self.emit_defers()?;
                self.drop_owned_locals();
                self.terminate(Terminator::Return(Some(zero)))?;
                self.function.ret = Some(Type::I32);
            } else if self.function.ret.is_none() {
                self.emit_defers()?;
                self.drop_owned_locals();
                self.terminate(Terminator::Return(None))?;
            } else {
                return Err(IrError::new(format!(
                    "native IR: function `{}` can reach its end",
                    source.name
                )));
            }
        }
        Ok(self.function)
    }

    fn emit_defers(&mut self) -> Result<(), IrError> {
        // LIFO: last deferred runs first.
        let bodies: Vec<_> = self.defers.iter().rev().cloned().collect();
        for body in bodies {
            self.lower_block(&body)?;
            if self.block().terminator.is_some() {
                return Err(IrError::new(
                    "native IR: defer body must not return/break/continue",
                ));
            }
        }
        Ok(())
    }

    fn resolve_loop_frame(&self, label: Option<&str>) -> Result<&LoopFrame, IrError> {
        match label {
            None => self
                .loops
                .last()
                .ok_or_else(|| IrError::new("native IR: break/continue outside loop")),
            Some(lab) => self
                .loops
                .iter()
                .rev()
                .find(|frame| frame.label.as_deref() == Some(lab))
                .ok_or_else(|| {
                    IrError::new(format!("native IR: unknown loop label `{lab}`"))
                }),
        }
    }

    /// Free heap locals introduced inside the current loop body. Outer locals
    /// that were dropped mid-body (e.g. via `share_drop`) keep their updated
    /// ownership; body-only names are stripped from the symbol tables.
    fn drop_loop_body_scope(
        &mut self,
        _owned_before: &HashMap<String, bool>,
        locals_before: &HashMap<String, (Value, Type)>,
    ) {
        for (name, owned) in self.heap_owned.clone() {
            if !owned || locals_before.contains_key(&name) {
                continue;
            }
            if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                let v = self.value();
                self.emit(Inst::Load {
                    out: v,
                    ptr,
                    ty,
                });
                self.emit_drop(v, ty);
            }
        }
        self.locals
            .retain(|name, _| locals_before.contains_key(name));
        self.heap_owned
            .retain(|name, _| locals_before.contains_key(name));
    }

    fn value(&mut self) -> Value {
        let value = Value(self.function.next_value);
        self.function.next_value += 1;
        value
    }

    fn block(&self) -> &BasicBlock {
        &self.function.blocks[self.current.0 as usize]
    }

    fn block_mut(&mut self) -> &mut BasicBlock {
        &mut self.function.blocks[self.current.0 as usize]
    }

    fn new_block(&mut self) -> BlockId {
        let id = BlockId(self.function.blocks.len() as u32);
        self.function.blocks.push(BasicBlock {
            instructions: Vec::new(),
            terminator: None,
        });
        id
    }

    fn emit(&mut self, instruction: Inst) {
        self.block_mut().instructions.push(instruction);
    }

    fn terminate(&mut self, terminator: Terminator) -> Result<(), IrError> {
        if self.block().terminator.is_some() {
            return Err(IrError::new("native IR: block already terminated"));
        }
        self.block_mut().terminator = Some(terminator);
        Ok(())
    }

    fn const_int(&mut self, value: i64, ty: Type) -> Value {
        let out = self.value();
        self.emit(Inst::ConstInt { out, value, ty });
        out
    }

    fn lower_block(&mut self, block: &AstBlock) -> Result<(), IrError> {
        for statement in &block.stmts {
            if self.block().terminator.is_some() {
                break;
            }
            self.lower_stmt(statement)?;
        }
        Ok(())
    }

    fn lower_stmt(&mut self, statement: &Stmt) -> Result<(), IrError> {
        match statement {
            Stmt::Let { name, ty, init, .. } => {
                // `let b: []byte = [72, 105]` — int array literal annotated as
                // []byte (elements are i64 at parse time).
                let annotated = ty
                    .as_ref()
                    .map(|t| resolve_type(t, self.structs))
                    .transpose()?;
                let (mut value, inferred, mut owned) =
                    if annotated == Some(Type::ByteSlice) {
                        if let Expr::Array(items) = init {
                            self.lower_byte_array_literal(items)?
                        } else {
                            self.lower_expr(init)?
                        }
                    } else if let (Some(ann), Expr::Array(items)) = (annotated, init) {
                        // Empty `[]` / typed literals: use the annotation when
                        // elements cannot be inferred (`let xs: []int = []`).
                        self.lower_array_literal_with_type(items, ann)?
                    } else if let Some(Type::Struct(eid)) = annotated {
                        // Typed Option/Result/enum lets: `let o: Option[map[K]V] = None`
                        // and `let s: Option[T] = Some(x)` must pick the annotated monomorph.
                        if self.structs.is_enum(eid) {
                            self.lower_expr_for_expected(init, Type::Struct(eid))?
                        } else {
                            self.lower_expr(init)?
                        }
                    } else {
                        self.lower_expr(init)?
                    };
                match inferred {
                    // Map headers are pointer handles: keep borrows so nested
                    // mutation through retrieved maps is shared (Go-like).
                    Type::MapII
                    | Type::MapSI
                    | Type::MapSS
                    | Type::MapIF
                    | Type::MapFI
                    | Type::MapIPtr(_)
                    | Type::MapSPtr(_) => {
                        // Owned maps still transfer; borrowed maps stay shared.
                    }
                    Type::Str
                    | Type::ChanI
                    | Type::Arena
                    | Type::Nursery
                    | Type::Task
                    | Type::Opaque
                    | Type::ShareInt => {
                        owned = self.take_bare_string_local(init, owned);
                        // Borrowed heap sources must be cloned so the binding
                        // owns an independent handle — except shared pointer
                        // handles (Opaque/chan/…), where clone is identity and
                        // force-owning would double-free with the source.
                        if !owned && !inferred.is_shared_handle() {
                            value = self.emit_clone(value, inferred);
                            owned = true;
                        }
                    }
                    // Slices: keep borrows so `let t = s[a:b]` shares backing
                    // (Go-like; SAFE-003). Assign still force-clones views.
                    Type::IntSlice
                    | Type::FloatSlice
                    | Type::ByteSlice
                    | Type::BoolSlice
                    | Type::StrSlice
                    | Type::StructSlice(_)
                    | Type::PtrSlice(_) => {
                        owned = self.take_bare_string_local(init, owned);
                        // Only force-own non-view borrows that are not slice
                        // expressions (e.g. field load of a slice for a new
                        // binding that should own a copy). Slice expressions
                        // stay unowned views.
                        let is_slice_expr = matches!(
                            init,
                            Expr::Slice { .. } | Expr::Array(_)
                        );
                        if !owned && !is_slice_expr {
                            value = self.emit_clone(value, inferred);
                            owned = true;
                        }
                    }
                    Type::Struct(id) => {
                        // Value-copy semantics: a borrowed source must be cloned
                        // so the binding owns independent storage.
                        if !owned {
                            let clone = self.value();
                            self.emit(Inst::StructClone {
                                out: clone,
                                base: value,
                                struct_id: id,
                            });
                            value = clone;
                            owned = true;
                        }
                    }
                    _ => {}
                }
                let ty = annotated.unwrap_or(inferred);
                let (value, owned) = if ty != inferred {
                    // Struct → interface (Opaque) conversion.
                    if ty == Type::Opaque && matches!(inferred, Type::Struct(_)) {
                        let boxed = self.box_iface(value, inferred)?;
                        (boxed, true)
                    } else if ty == Type::Opaque && inferred == Type::I64 {
                        // Unit interface from int placeholder (`let w: Writer = 0`).
                        (value, false)
                    } else if ty == Type::Str && inferred == Type::I64 {
                        // Env-pack borrow: pack owns the string clone for the fat-fn lifetime.
                        (value, false)
                    } else if ty == Type::I1 && inferred == Type::I64 {
                        let b = self.value();
                        self.emit(Inst::Cast {
                            out: b,
                            value,
                            from: Type::I64,
                            to: Type::I1,
                        });
                        (b, false)
                    } else if ty == Type::F64 && inferred == Type::I64 {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![value],
                            ret: Some(Type::F64),
                        });
                        (f, false)
                    } else if matches!(ty, Type::Struct(_)) && inferred == Type::I64 {
                        // Env-pack borrow of cloned struct.
                        (value, false)
                    } else if ty == Type::ShareInt && inferred == Type::I64 {
                        (value, false)
                    } else if ty == Type::IntSlice
                        && matches!(inferred, Type::IntSlice)
                    {
                        (value, owned)
                    } else {
                        return Err(IrError::new("native IR: initializer type mismatch"));
                    }
                } else {
                    (value, owned)
                };
                let slot = self.value();
                self.emit(Inst::Alloca { out: slot, ty });
                self.emit(Inst::Store { ptr: slot, value });
                self.locals.insert(name.clone(), (slot, ty));
                if ty.is_heap() {
                    self.heap_owned.insert(name.clone(), owned);
                }
                // Track kick job return type / crew for typed join + note_err.
                if ty == Type::Task {
                    if let Some(ret) = self.pending_kick_ret.take() {
                        self.job_ret_tys.insert(name.clone(), ret);
                    }
                    if let Some(crew) = self.pending_kick_crew.take() {
                        self.job_crews.insert(name.clone(), crew);
                    }
                } else {
                    self.pending_kick_ret = None;
                    self.pending_kick_crew = None;
                }
            }
            Stmt::LetCommaOk {
                value,
                ok,
                mutable: _,
                base,
                index,
            } => {
                // Go-style `let v, ok = m[k]`
                let (mapv, mty, mo) = self.lower_expr(base)?;
                if mo {
                    return Err(IrError::new(
                        "native IR: comma-ok map base must be a local",
                    ));
                }
                let (key, kty, ko) = self.lower_expr(index)?;
                // Content-keyed struct maps must use map_struct_key_* — map_ii_*
                // compares key pointers by identity and always misses on temps.
                let struct_key_sid = match mty {
                    Type::MapIPtr(vk) => vk.struct_key_id().or_else(|| {
                        if let Type::Struct(sid) = kty {
                            if !self.structs.is_enum(sid) {
                                return Some(sid);
                            }
                        }
                        None
                    }),
                    _ => None,
                };
                let vty = match mty {
                    Type::MapII | Type::MapSI | Type::MapFI => Type::I64,
                    Type::MapSS => Type::Str,
                    Type::MapIF => Type::F64,
                    Type::MapIPtr(vk) | Type::MapSPtr(vk) => vk.to_type(),
                    _ => {
                        return Err(IrError::new(
                            "native IR: comma-ok lookup requires a map",
                        ))
                    }
                };
                let (has_fn, get_fn) = match mty {
                    Type::MapII => (
                        "mako_native_map_ii_has_ptr",
                        "mako_native_map_ii_get_ptr",
                    ),
                    Type::MapSI => (
                        "mako_native_map_si_has_ptr",
                        "mako_native_map_si_get_ptr",
                    ),
                    Type::MapSS => (
                        "mako_native_map_ss_has_ptr",
                        "mako_native_map_ss_get_ptr",
                    ),
                    Type::MapIF => (
                        "mako_native_map_if_has_ptr",
                        "mako_native_map_if_get_ptr",
                    ),
                    Type::MapFI => (
                        "mako_native_map_fi_has_ptr",
                        "mako_native_map_fi_get_ptr",
                    ),
                    Type::MapIPtr(_) if struct_key_sid.is_some() => (
                        "mako_native_map_struct_key_has_ptr",
                        "mako_native_map_struct_key_get_ptr",
                    ),
                    Type::MapIPtr(_) => (
                        "mako_native_map_ii_has_ptr",
                        "mako_native_map_ii_get_ptr",
                    ),
                    Type::MapSPtr(_) => (
                        "mako_native_map_si_has_ptr",
                        "mako_native_map_si_get_ptr",
                    ),
                    _ => unreachable!(),
                };
                // Normalize int-key maps (enum → tag, bool/float casts).
                // Struct content keys keep the struct pointer (passed with meta).
                let key = if struct_key_sid.is_some() {
                    key
                } else if matches!(mty, Type::MapII | Type::MapIPtr(_) | Type::MapIF)
                    && (kty == Type::I64
                        || kty == Type::I1
                        || kty == Type::F64
                        || matches!(kty, Type::Struct(_)))
                {
                    self.map_i_key(key, kty, ko)?
                } else {
                    key
                };
                let struct_meta = struct_key_sid.map(|sid| self.struct_key_meta(sid));
                let raw_ok = self.value();
                if let Some((nf, sm, nm, nfp, nsp)) = struct_meta {
                    self.emit(Inst::Call {
                        out: Some(raw_ok),
                        function: has_fn.into(),
                        args: vec![mapv, key, nf, sm, nm, nfp, nsp],
                        ret: Some(Type::I64),
                    });
                } else {
                    self.emit(Inst::Call {
                        out: Some(raw_ok),
                        function: has_fn.into(),
                        args: vec![mapv, key],
                        ret: Some(Type::I64),
                    });
                }
                let ok_b = self.value();
                self.emit(Inst::Cast {
                    out: ok_b,
                    value: raw_ok,
                    from: Type::I64,
                    to: Type::I1,
                });
                let ok_slot = self.value();
                self.emit(Inst::Alloca {
                    out: ok_slot,
                    ty: Type::I1,
                });
                self.emit(Inst::Store {
                    ptr: ok_slot,
                    value: ok_b,
                });
                self.locals.insert(ok.clone(), (ok_slot, Type::I1));

                let v_slot = self.value();
                self.emit(Inst::Alloca {
                    out: v_slot,
                    ty: vty,
                });
                // Present → get (owned for heap values); missing → zero/empty.
                // Get only on the present path so a missing-key empty string is
                // not allocated twice / double-dropped with the else default.
                let then_id = self.new_block();
                let else_id = self.new_block();
                let merge_id = self.new_block();
                self.terminate(Terminator::Branch {
                    condition: ok_b,
                    then_block: then_id,
                    else_block: else_id,
                })?;
                self.current = then_id;
                {
                    let mut raw_v = self.value();
                    // Bridge get returns i64 bits/pointer, Str for MapSS, F64 for MapIF.
                    let get_ret = match mty {
                        Type::MapSS => Type::Str,
                        Type::MapIF => Type::F64,
                        _ => Type::I64,
                    };
                    if let Some((nf, sm, nm, nfp, nsp)) = struct_meta {
                        self.emit(Inst::Call {
                            out: Some(raw_v),
                            function: get_fn.into(),
                            args: vec![mapv, key, nf, sm, nm, nfp, nsp],
                            ret: Some(get_ret),
                        });
                    } else {
                        self.emit(Inst::Call {
                            out: Some(raw_v),
                            function: get_fn.into(),
                            args: vec![mapv, key],
                            ret: Some(get_ret),
                        });
                    }
                    // Float-in-int-maps: bit pattern → f64.
                    if vty == Type::F64 && get_ret == Type::I64 {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![raw_v],
                            ret: Some(Type::F64),
                        });
                        raw_v = f;
                    }
                    // Pointer maps return a borrow of map storage — clone so the
                    // binding owns an independent value (avoids double-free with
                    // the map). MapSS get already returns an owned string clone.
                    // Shared handles (chan/opaque/…) must stay borrowed: clone is
                    // identity and force-owning would chan_drop while the map
                    // still holds the same pointer.
                    // StructKeyStr stores the string pointer as i64 — treat like
                    // MapSPtr string values (clone for ownership of the binding).
                    let ptr_map = matches!(mty, Type::MapIPtr(_) | Type::MapSPtr(_))
                        || (struct_key_sid.is_some() && vty.is_heap());
                    if (ptr_map || vty == Type::Str && struct_key_sid.is_some())
                        && vty.is_heap()
                        && !vty.is_shared_handle()
                    {
                        raw_v = self.emit_clone(raw_v, vty);
                    }
                    self.emit(Inst::Store {
                        ptr: v_slot,
                        value: raw_v,
                    });
                }
                self.terminate(Terminator::Jump(merge_id))?;
                self.current = else_id;
                if vty == Type::Str {
                    // StringLiteral is a static non-owned view — clone so the
                    // binding owns a heap header (DropString must not free static).
                    let lit = self.value();
                    self.emit(Inst::StringLiteral {
                        out: lit,
                        bytes: vec![],
                    });
                    let empty = self.emit_clone(lit, Type::Str);
                    self.emit(Inst::Store {
                        ptr: v_slot,
                        value: empty,
                    });
                } else if vty.is_heap() && !vty.is_shared_handle() {
                    let null_ptr = self.const_int(0, Type::I64);
                    let empty = self.null_to_empty_heap(null_ptr, vty)?;
                    self.emit(Inst::Store {
                        ptr: v_slot,
                        value: empty,
                    });
                } else if vty == Type::F64 {
                    let z = self.value();
                    self.emit(Inst::ConstFloat { out: z, value: 0.0 });
                    self.emit(Inst::Store {
                        ptr: v_slot,
                        value: z,
                    });
                } else {
                    // Scalars and shared handles (null channel on miss): zero.
                    let z = self.const_int(0, Type::I64);
                    self.emit(Inst::Store {
                        ptr: v_slot,
                        value: z,
                    });
                }
                self.terminate(Terminator::Jump(merge_id))?;
                self.current = merge_id;
                // Drop string keys after both arms (not used past this point).
                if ko && kty == Type::Str {
                    self.emit(Inst::DropString { value: key });
                }
                // Owned temporary struct keys (literals) free after lookup.
                if ko && struct_key_sid.is_some() {
                    if let Type::Struct(sid) = kty {
                        self.emit_drop(key, Type::Struct(sid));
                    }
                }
                self.locals.insert(value.clone(), (v_slot, vty));
                if vty.is_heap() && !vty.is_shared_handle() {
                    // Own cloned heap values; shared handles stay borrowed.
                    self.heap_owned.insert(value.clone(), true);
                }
            }
            Stmt::LetMulti { names, init, .. } => {
                // `let a, b = f()` — bind each tuple field to a fresh local.
                let (value, ty, owned) = self.lower_expr(init)?;
                let Type::Struct(id) = ty else {
                    return Err(IrError::new(
                        "native IR: multi-binding requires a tuple result",
                    ));
                };
                if self.structs.field_count(id) != names.len() {
                    return Err(IrError::new("native IR: multi-binding arity mismatch"));
                }
                for (i, name) in names.iter().enumerate() {
                    let field_ty = self.structs.field_type(id, i);
                    let field_value = self.value();
                    self.emit(Inst::StructField {
                        out: field_value,
                        base: value,
                        struct_id: id,
                        index: i as u32,
                        ty: field_ty,
                    });
                    // Clone owned payloads so the binding is independent of the
                    // tuple drop. Shared handles (chan/opaque/…) are not deep
                    // cloned and must not be marked owned — the map/tuple still
                    // owns the handle.
                    let shared_handle = matches!(
                        field_ty,
                        Type::ChanI
                            | Type::ChanS
                            | Type::ChanF
                            | Type::ChanP(_)
                            | Type::Opaque
                            | Type::Arena
                            | Type::Nursery
                            | Type::Task
                            | Type::FnPtr
                    );
                    let bound = if field_ty.is_heap() && !shared_handle {
                        self.emit_clone(field_value, field_ty)
                    } else {
                        field_value
                    };
                    let slot = self.value();
                    self.emit(Inst::Alloca { out: slot, ty: field_ty });
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: bound,
                    });
                    self.locals.insert(name.clone(), (slot, field_ty));
                    if field_ty.is_heap() && !shared_handle {
                        self.heap_owned.insert(name.clone(), true);
                    }
                }
                // Drop the tuple: an owned temp frees now (recursively dropping
                // its owned fields); a borrowed tuple stays with its owner.
                if owned {
                    self.emit(Inst::DropStruct {
                        value,
                        struct_id: id,
                    });
                }
            }
            Stmt::Assign { name, value: rhs } => {
                let (slot, expected) =
                    self.locals.get(name).copied().ok_or_else(|| {
                        IrError::new(format!("native IR: unknown local `{name}`"))
                    })?;
                let (mut value, actual, mut owned) = self.lower_expr(rhs)?;
                // Sequential mut capture cell: write through the shared i64 cell.
                if let Some(&cell) = self.mut_i64_cells.get(name) {
                    if actual != Type::I64 || owned {
                        return Err(IrError::new(
                            "native IR: mut capture cell expects int assignment",
                        ));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_i64_cell_store".into(),
                        args: vec![cell, value],
                        ret: None,
                    });
                    // Keep stack local in sync for non-cell readers of the same slot.
                    self.emit(Inst::Store {
                        ptr: slot,
                        value,
                    });
                    return Ok(());
                }
                match actual {
                    // Map headers share like Go maps (see Let binding).
                    Type::MapII
                    | Type::MapSI
                    | Type::MapSS
                    | Type::MapIF
                    | Type::MapFI
                    | Type::MapIPtr(_)
                    | Type::MapSPtr(_) => {}
                    Type::Str
                    | Type::IntSlice
                    | Type::FloatSlice
                    | Type::ByteSlice
                    | Type::BoolSlice
                    | Type::StrSlice
                    | Type::ChanI
                    | Type::Arena
                    | Type::Nursery
                    | Type::Task
                    | Type::Opaque
                    | Type::StructSlice(_)
                    | Type::ShareInt => {
                        owned = self.take_bare_string_local(rhs, owned);
                        // Shared handles: clone is identity — do not force-own.
                        if !owned && !actual.is_shared_handle() {
                            value = self.emit_clone(value, actual);
                            owned = true;
                        }
                    }
                    Type::Struct(id) => {
                        if !owned {
                            let clone = self.value();
                            self.emit(Inst::StructClone {
                                out: clone,
                                base: value,
                                struct_id: id,
                            });
                            value = clone;
                            owned = true;
                        }
                    }
                    _ => {}
                }
                if actual != expected {
                    return Err(IrError::new("native IR: assignment type mismatch"));
                }
                // Move-through-call: `xs = f(xs, …)` / `xs = append(xs, v)`.
                // The callee (or `append`) may realloc/free the old buffer and
                // return a new header; dropping the pre-call slot would
                // double-free. Applies to any call whose first arg is the same
                // local (not only the builtin `append`).
                let consumes_old_slice = expected.is_heap()
                    && matches!(
                        rhs,
                        Expr::Call { args, .. }
                            if matches!(
                                args.first(),
                                Some(Expr::Ident(source)) if source == name
                            )
                    );
                if expected.is_heap()
                    && self.heap_owned.get(name) == Some(&true)
                    && !consumes_old_slice
                {
                    let old = self.value();
                    self.emit(Inst::Load {
                        out: old,
                        ptr: slot,
                        ty: expected,
                    });
                    self.emit_drop(old, expected);
                }
                self.emit(Inst::Store { ptr: slot, value });
                if expected.is_heap() {
                    self.heap_owned.insert(name.clone(), owned);
                }
            }
            Stmt::IndexAssign { base, index, value } => {
                let (slice, ty, owned) = self.lower_expr(base)?;
                // Owned temps are allowed for slice/map *headers* that alias
                // shared backing (`s[1:3][0] = x`, `matrix[0][1] = x`). The
                // store mutates the shared data; we drop the temporary header
                // afterward. Reject owned only for non-container scalars.
                let drop_base_after = owned;
                if owned
                    && !matches!(
                        ty,
                        Type::IntSlice
                            | Type::FloatSlice
                            | Type::ByteSlice
                            | Type::BoolSlice
                            | Type::StrSlice
                            | Type::StructSlice(_)
                            | Type::PtrSlice(_)
                            | Type::MapII
                            | Type::MapSI
                            | Type::MapSS
                            | Type::MapIF
                            | Type::MapFI
                            | Type::MapIPtr(_)
                            | Type::MapSPtr(_)
                    )
                {
                    return Err(IrError::new(
                        "native IR: index assignment requires a borrowed base local",
                    ));
                }
                let (idx, it, io) = self.lower_expr(index)?;
                let (val, vt, vo) = self.lower_expr(value)?;
                match ty {
                    Type::IntSlice => {
                        if it != Type::I64 || io || vt != Type::I64 || vo {
                            return Err(IrError::new("native IR: []int index assignment types"));
                        }
                        self.emit(Inst::SliceStore {
                            slice,
                            index: idx,
                            value: val,
                        });
                    }
                    Type::FloatSlice => {
                        if it != Type::I64 || io || vt != Type::F64 || vo {
                            return Err(IrError::new(
                                "native IR: []float index assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_float_slice_set_ptr".into(),
                            args: vec![slice, idx, val],
                            ret: None,
                        });
                    }
                    Type::ByteSlice => {
                        if it != Type::I64 || io || vt != Type::I64 || vo {
                            return Err(IrError::new(
                                "native IR: []byte index assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_byte_slice_set_ptr".into(),
                            args: vec![slice, idx, val],
                            ret: None,
                        });
                    }
                    Type::BoolSlice => {
                        if it != Type::I64 || io || (vt != Type::I1 && vt != Type::I64) || vo {
                            return Err(IrError::new(
                                "native IR: []bool index assignment types",
                            ));
                        }
                        let bval = if vt == Type::I1 {
                            let c = self.value();
                            self.emit(Inst::Cast {
                                out: c,
                                value: val,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            c
                        } else {
                            val
                        };
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_bool_slice_set_ptr".into(),
                            args: vec![slice, idx, bval],
                            ret: None,
                        });
                    }
                    Type::StructSlice(sid) => {
                        if it != Type::I64 || io || vt != Type::Struct(sid) {
                            return Err(IrError::new(
                                "native IR: []struct index assignment types",
                            ));
                        }
                        let owned_elem = if vo {
                            val
                        } else {
                            self.emit_clone(val, Type::Struct(sid))
                        };
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_ptr_slice_set".into(),
                            args: vec![slice, idx, owned_elem],
                            ret: None,
                        });
                    }
                    Type::PtrSlice(vk) => {
                        let ety = vk.to_type();
                        if it != Type::I64 || io || vt != ety {
                            return Err(IrError::new(
                                "native IR: ptr-slice index assignment types",
                            ));
                        }
                        let owned_elem = if vo {
                            val
                        } else if ety.is_heap() {
                            self.emit_clone(val, ety)
                        } else {
                            val
                        };
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_ptr_slice_set".into(),
                            args: vec![slice, idx, owned_elem],
                            ret: None,
                        });
                    }
                    Type::MapII => {
                        // Also used for map[int]bool (bool stored as 0/1 i64).
                        if it != Type::I64 || io || vo {
                            return Err(IrError::new(
                                "native IR: map[int]int assignment types",
                            ));
                        }
                        let mut v = val;
                        if vt == Type::I1 {
                            let c = self.value();
                            self.emit(Inst::Cast {
                                out: c,
                                value: val,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            v = c;
                        } else if vt != Type::I64 {
                            return Err(IrError::new(
                                "native IR: map[int]int assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_ii_set_ptr".into(),
                            args: vec![slice, idx, v],
                            ret: None,
                        });
                    }
                    Type::MapSI | Type::MapSPtr(_) => {
                        // MapSPtr: values are pointer-sized (slice/struct/…); store as i64.
                        let val_i64 = if matches!(ty, Type::MapSPtr(_)) {
                            if !vt.is_ptr_sized() {
                                return Err(IrError::new(
                                    "native IR: map[string]V assignment value unsupported",
                                ));
                            }
                            if it != Type::Str {
                                return Err(IrError::new(
                                    "native IR: map[string]V key must be string",
                                ));
                            }
                            let mut v = val;
                            if vt == Type::F64 {
                                let bits = self.value();
                                self.emit(Inst::Call {
                                    out: Some(bits),
                                    function: "mako_native_f64_to_bits".into(),
                                    args: vec![v],
                                    ret: Some(Type::I64),
                                });
                                v = bits;
                            } else if vt == Type::I1 {
                                let c = self.value();
                                self.emit(Inst::Cast {
                                    out: c,
                                    value: v,
                                    from: Type::I1,
                                    to: Type::I64,
                                });
                                v = c;
                            } else if vo {
                                if let Expr::Ident(name) = value {
                                    self.heap_owned.insert(name.clone(), false);
                                }
                            } else if vt.is_heap() {
                                v = self.emit_clone(v, vt);
                            }
                            v
                        } else {
                            // map[string]int or map[string]bool (0/1).
                            if it != Type::Str || vo {
                                return Err(IrError::new(
                                    "native IR: map[string]int assignment types",
                                ));
                            }
                            if vt == Type::I1 {
                                let c = self.value();
                                self.emit(Inst::Cast {
                                    out: c,
                                    value: val,
                                    from: Type::I1,
                                    to: Type::I64,
                                });
                                c
                            } else if vt == Type::I64 {
                                val
                            } else {
                                return Err(IrError::new(
                                    "native IR: map[string]int assignment types",
                                ));
                            }
                        };
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_si_set_ptr".into(),
                            args: vec![slice, idx, val_i64],
                            ret: None,
                        });
                        if io {
                            self.emit(Inst::DropString { value: idx });
                        }
                    }
                    Type::MapIPtr(vk) => {
                        // Keys: int/bool/float/struct (content) / enum (tag).
                        if !vt.is_ptr_sized() && !matches!(vt, Type::I64 | Type::I1 | Type::F64) {
                            return Err(IrError::new(
                                "native IR: map[int]V assignment value unsupported",
                            ));
                        }
                        let mut v = val;
                        if vt == Type::F64 {
                            let bits = self.value();
                            self.emit(Inst::Call {
                                out: Some(bits),
                                function: "mako_native_f64_to_bits".into(),
                                args: vec![v],
                                ret: Some(Type::I64),
                            });
                            v = bits;
                        } else if vt == Type::I1 {
                            let c = self.value();
                            self.emit(Inst::Cast {
                                out: c,
                                value: v,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            v = c;
                        } else if vo {
                            if let Expr::Ident(name) = value {
                                self.heap_owned.insert(name.clone(), false);
                            }
                        } else if vt.is_heap() {
                            v = self.emit_clone(v, vt);
                        }
                        // Struct keys (including map[Point]Label): content hash+eq.
                        let struct_key_id = vk.struct_key_id().or_else(|| {
                            if let Type::Struct(sid) = it {
                                if !self.structs.is_enum(sid) {
                                    return Some(sid);
                                }
                            }
                            None
                        });
                        if let Some(sid) = struct_key_id {
                            if it != Type::Struct(sid) {
                                return Err(IrError::new(
                                    "native IR: map[Struct] key type mismatch",
                                ));
                            }
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_struct_key_set_ptr".into(),
                                args: vec![slice, idx, nf, sm, nm, nfp, nsp, v],
                                ret: None,
                            });
                            if io {
                                self.emit_drop(idx, Type::Struct(sid));
                            }
                        } else {
                            let key = self.map_i_key(idx, it, io)?;
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_ii_set_ptr".into(),
                                args: vec![slice, key, v],
                                ret: None,
                            });
                        }
                    }
                    Type::MapSS => {
                        if it != Type::Str || vt != Type::Str {
                            return Err(IrError::new(
                                "native IR: map[string]string assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_ss_set_ptr".into(),
                            args: vec![slice, idx, val],
                            ret: None,
                        });
                        if io {
                            self.emit(Inst::DropString { value: idx });
                        }
                        if vo {
                            self.emit(Inst::DropString { value: val });
                        }
                    }
                    Type::MapIF => {
                        if it != Type::I64 || io || vt != Type::F64 || vo {
                            return Err(IrError::new(
                                "native IR: map[int]float assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_if_set_ptr".into(),
                            args: vec![slice, idx, val],
                            ret: None,
                        });
                    }
                    Type::MapFI => {
                        if it != Type::F64 || io || vt != Type::I64 || vo {
                            return Err(IrError::new(
                                "native IR: map[float]int assignment types",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_fi_set_ptr".into(),
                            args: vec![slice, idx, val],
                            ret: None,
                        });
                    }
                    Type::StrSlice => {
                        if it != Type::I64 || io || vt != Type::Str {
                            return Err(IrError::new(
                                "native IR: []string index assignment types",
                            ));
                        }
                        // StrSliceStore clones the value; drop owned temps after.
                        self.emit(Inst::StrSliceStore {
                            slice,
                            index: idx,
                            value: val,
                        });
                        if vo {
                            self.emit(Inst::DropString { value: val });
                        }
                    }
                    Type::MapSPtr(_) => {
                        if it != Type::Str {
                            return Err(IrError::new(
                                "native IR: map[string]V assignment key must be string",
                            ));
                        }
                        if !vt.is_ptr_sized() {
                            return Err(IrError::new(
                                "native IR: map[string]V assignment value unsupported",
                            ));
                        }
                        let mut v = val;
                        if vt == Type::F64 {
                            let bits = self.value();
                            self.emit(Inst::Call {
                                out: Some(bits),
                                function: "mako_native_f64_to_bits".into(),
                                args: vec![v],
                                ret: Some(Type::I64),
                            });
                            v = bits;
                        } else if vo {
                            if let Expr::Ident(name) = value {
                                self.heap_owned.insert(name.clone(), false);
                            }
                        } else if vt.is_heap() {
                            v = self.emit_clone(v, vt);
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_map_si_set_ptr".into(),
                            args: vec![slice, idx, v],
                            ret: None,
                        });
                        if io {
                            self.emit(Inst::DropString { value: idx });
                        }
                    }
                    _ => {
                        return Err(IrError::new(
                            "native IR: index assignment on unsupported base",
                        ))
                    }
                }
                if drop_base_after {
                    // Temporary header only (shared backing already mutated).
                    self.emit_drop(slice, ty);
                }
            }
            Stmt::FieldAssign { base, field, value } => {
                let (base_ptr, base_ty, base_owned) = self.lower_expr(base)?;
                let Type::Struct(id) = base_ty else {
                    return Err(IrError::new("native IR: field assignment on non-struct"));
                };
                let (index, field_ty) = self
                    .structs
                    .field(id, field)
                    .ok_or_else(|| IrError::new(format!("native IR: unknown field `{field}`")))?;
                let (val, vt, mut vo) = self.lower_expr(value)?;
                if vt != field_ty {
                    return Err(IrError::new("native IR: field assignment type mismatch"));
                }
                if field_ty.is_shared_handle() {
                    vo = self.take_bare_string_local(value, vo);
                }
                // Overwriting an owned field: own the new value *before* dropping
                // the old one so `self.xs = self.xs[a:b]` clones the view while
                // the base buffer is still live (otherwise UAF).
                // Exception: `b.xs = append(b.xs, v)` where append may free the
                // old buffer on realloc.
                let consumes_old_field = field_ty.is_heap()
                    && matches!(
                        value,
                        Expr::Call { callee, args }
                            if matches!(callee.as_ref(), Expr::Ident(n) if n == "append")
                                && matches!(
                                    args.first(),
                                    Some(Expr::Field {
                                        base: src_base,
                                        field: src_field,
                                    }) if src_field == field
                                        && Self::field_base_same_local(base, src_base)
                                )
                    );
                let stored = self.own_field_value(val, field_ty, vo)?;
                if field_ty.is_heap() && !consumes_old_field {
                    let old = self.value();
                    self.emit(Inst::StructField {
                        out: old,
                        base: base_ptr,
                        struct_id: id,
                        index,
                        ty: field_ty,
                    });
                    self.emit_drop(old, field_ty);
                }
                self.emit(Inst::StructFieldStore {
                    base: base_ptr,
                    struct_id: id,
                    index,
                    value: stored,
                });
                // A field assignment targets an lvalue (a live local), never an
                // owned temporary; nothing to drop.
                debug_assert!(!base_owned);
            }
            Stmt::Expr(expr) => {
                let (value, ty, owned) = self.lower_expr(expr)?;
                if owned {
                    self.emit_drop(value, ty);
                }
            }
            Stmt::Return(value) => {
                let value = match value {
                    Some(expr) => {
                        let (mut value, mut ty, mut owned) = self.lower_expr(expr)?;
                        if ty.is_heap() {
                            // The function is ending, so moving a bare owned
                            // local out is safe (it is never used afterwards);
                            // a borrow is cloned so the caller receives an owner.
                            owned = self.take_bare_string_local(expr, owned);
                            if !owned {
                                value = self.emit_clone(value, ty);
                            }
                        }
                        // bool ↔ int at returns (actor_send etc. return i64).
                        match (self.function.ret, ty) {
                            (Some(Type::I1), Type::I64) => {
                                let b = self.value();
                                self.emit(Inst::Cast {
                                    out: b,
                                    value,
                                    from: Type::I64,
                                    to: Type::I1,
                                });
                                value = b;
                                ty = Type::I1;
                            }
                            (Some(Type::I64), Type::I1) => {
                                let i = self.value();
                                self.emit(Inst::Cast {
                                    out: i,
                                    value,
                                    from: Type::I1,
                                    to: Type::I64,
                                });
                                value = i;
                                ty = Type::I64;
                            }
                            _ => {}
                        }
                        let _ = ty;
                        Some(value)
                    }
                    None => None,
                };
                self.emit_defers()?;
                self.drop_owned_locals();
                self.terminate(Terminator::Return(value))?;
            }
            Stmt::Defer { body } => {
                self.defers.push(body.clone());
            }
            Stmt::If {
                init,
                cond,
                then_block,
                else_block,
            } => {
                // Optional Go-style init is scoped to the whole if/else.
                let saved_locals = self.locals.clone();
                let saved_owned = self.heap_owned.clone();
                if let Some(init) = init {
                    self.lower_stmt(init)?;
                }
                // Constant-true / constant-false: lower the live arm in place.
                // Used by switch-with-init desugar (`if true { let v=…; if … }`)
                // so all-returning cases do not leave an empty fallthrough merge.
                match cond {
                    Expr::Bool(true) if else_block.is_none() => {
                        let owned_before = self.heap_owned.clone();
                        let locals_before = self.locals.clone();
                        self.lower_block(then_block)?;
                        if self.block().terminator.is_none() {
                            self.drop_loop_body_scope(&owned_before, &locals_before);
                        }
                        // Drop init-scoped owned locals, restore pre-init bindings
                        // only if we fall through (returned arms already cleaned).
                        if self.block().terminator.is_none() {
                            for (name, owned) in self.heap_owned.clone() {
                                if owned && !saved_owned.contains_key(&name) {
                                    if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                                        let v = self.value();
                                        self.emit(Inst::Load {
                                            out: v,
                                            ptr,
                                            ty,
                                        });
                                        self.emit_drop(v, ty);
                                    }
                                }
                            }
                            self.locals = saved_locals;
                            self.heap_owned = saved_owned;
                        }
                        return Ok(());
                    }
                    Expr::Bool(false) => {
                        if let Some(block) = else_block {
                            let owned_before = self.heap_owned.clone();
                            let locals_before = self.locals.clone();
                            self.lower_block(block)?;
                            if self.block().terminator.is_none() {
                                self.drop_loop_body_scope(&owned_before, &locals_before);
                            }
                        }
                        if self.block().terminator.is_none() {
                            for (name, owned) in self.heap_owned.clone() {
                                if owned && !saved_owned.contains_key(&name) {
                                    if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                                        let v = self.value();
                                        self.emit(Inst::Load {
                                            out: v,
                                            ptr,
                                            ty,
                                        });
                                        self.emit_drop(v, ty);
                                    }
                                }
                            }
                            self.locals = saved_locals;
                            self.heap_owned = saved_owned;
                        }
                        return Ok(());
                    }
                    _ => {}
                }
                let owned_before = self.heap_owned.clone();
                let locals_before = self.locals.clone();
                let (condition, ty, condition_owned) = self.lower_expr(cond)?;
                if condition_owned {
                    return Err(IrError::new("native IR: owned string condition"));
                }
                let condition = match ty {
                    Type::I1 => condition,
                    Type::I64 => {
                        // Truthy non-zero int (common for runtime bools returning i64).
                        let z = self.const_int(0, Type::I64);
                        let cmp = self.value();
                        self.emit(Inst::Binary {
                            out: cmp,
                            op: BinOp::Ne,
                            left: condition,
                            right: z,
                            ty: Type::I64,
                        });
                        cmp
                    }
                    _ => {
                        return Err(IrError::new("native IR: condition must be bool"));
                    }
                };
                let then_id = self.new_block();
                let else_id = self.new_block();
                let merge_id = self.new_block();
                self.terminate(Terminator::Branch {
                    condition,
                    then_block: then_id,
                    else_block: else_id,
                })?;
                self.current = then_id;
                self.lower_block(then_block)?;
                // Free arm-local heap values (share let / temps) before join.
                if self.block().terminator.is_none() {
                    self.drop_loop_body_scope(&owned_before, &locals_before);
                    self.terminate(Terminator::Jump(merge_id))?;
                }
                // Fallthrough to merge = Jump(merge) (or none before we add it).
                let then_falls = matches!(
                    self.block().terminator,
                    Some(Terminator::Jump(t)) if t == merge_id
                ) || self.block().terminator.is_none();
                self.current = else_id;
                self.heap_owned = owned_before.clone();
                self.locals = locals_before.clone();
                if let Some(block) = else_block {
                    self.lower_block(block)?;
                }
                if self.block().terminator.is_none() {
                    self.drop_loop_body_scope(&owned_before, &locals_before);
                    self.terminate(Terminator::Jump(merge_id))?;
                }
                let else_falls = matches!(
                    self.block().terminator,
                    Some(Terminator::Jump(t)) if t == merge_id
                ) || self.block().terminator.is_none();
                // Join: outer ownership may change if an arm explicitly drops;
                // body-only names are already stripped. Prefer then-path map
                // when both arms terminated differently — path-insensitive.
                self.locals
                    .retain(|name, _| locals_before.contains_key(name));
                self.heap_owned
                    .retain(|name, _| locals_before.contains_key(name));
                self.current = merge_id;
                // Both arms exit (return/break/continue): merge is unreachable.
                // Terminate it so the function doesn't look like it falls off the end.
                if !then_falls && !else_falls {
                    if let Some(ret_ty) = self.function.ret {
                        let z = if ret_ty.is_heap() || matches!(ret_ty, Type::Struct(_)) {
                            let out = self.value();
                            self.emit(Inst::NullHeap { out, ty: ret_ty });
                            out
                        } else if ret_ty == Type::F64 {
                            let out = self.value();
                            self.emit(Inst::ConstFloat { out, value: 0.0 });
                            out
                        } else {
                            self.const_int(0, ret_ty)
                        };
                        self.terminate(Terminator::Return(Some(z)))?;
                    } else {
                        self.terminate(Terminator::Return(None))?;
                    }
                }
                // Drop init-scoped owned locals, restore pre-init bindings.
                for (name, owned) in self.heap_owned.clone() {
                    if owned && !saved_owned.contains_key(&name) {
                        if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                            let v = self.value();
                            self.emit(Inst::Load {
                                out: v,
                                ptr,
                                ty,
                            });
                            self.emit_drop(v, ty);
                        }
                    }
                }
                self.locals = saved_locals;
                self.heap_owned = saved_owned;
            }
            Stmt::While { label, cond, body } => {
                let header = self.new_block();
                let body_id = self.new_block();
                let done = self.new_block();
                self.terminate(Terminator::Jump(header))?;
                self.current = header;
                let owned_before = self.heap_owned.clone();
                let locals_before = self.locals.clone();
                let (condition, ty, condition_owned) = self.lower_expr(cond)?;
                if condition_owned {
                    return Err(IrError::new("native IR: owned string loop condition"));
                }
                if ty != Type::I1 {
                    return Err(IrError::new("native IR: condition must be bool"));
                }
                self.terminate(Terminator::Branch {
                    condition,
                    then_block: body_id,
                    else_block: done,
                })?;
                self.current = body_id;
                self.loops.push(LoopFrame {
                    label: label.clone(),
                    break_target: done,
                    continue_target: header,
                    owned_before: owned_before.clone(),
                    locals_before: locals_before.clone(),
                });
                self.lower_block(body)?;
                self.loops.pop();
                // Body fallthrough → next iteration: free body-scoped heap
                // locals (e.g. `share let` inside the loop) then jump header.
                if self.block().terminator.is_none() {
                    self.drop_loop_body_scope(&owned_before, &locals_before);
                    self.terminate(Terminator::Jump(header))?;
                }
                // Join after the loop: ownership may have changed on a break
                // path (explicit share_drop); keep the post-body map but strip
                // any body-only names that a terminated path left behind.
                self.locals
                    .retain(|name, _| locals_before.contains_key(name));
                self.heap_owned
                    .retain(|name, _| locals_before.contains_key(name));
                self.current = done;
            }
            Stmt::For {
                label,
                binders,
                is_range,
                iter,
                body,
            } => {
                self.lower_for(label.clone(), binders, *is_range, iter, body)?;
            }
            Stmt::CFor {
                label,
                init,
                cond,
                post,
                body,
            } => {
                self.lower_cfor(label.clone(), init, cond, post, body)?;
            }
            Stmt::Break(label) => {
                let frame = self.resolve_loop_frame(label.as_deref())?;
                let break_target = frame.break_target;
                let owned_before = frame.owned_before.clone();
                let locals_before = frame.locals_before.clone();
                self.drop_loop_body_scope(&owned_before, &locals_before);
                self.terminate(Terminator::Jump(break_target))?;
            }
            Stmt::Continue(label) => {
                let frame = self.resolve_loop_frame(label.as_deref())?;
                let cont_target = frame.continue_target;
                let owned_before = frame.owned_before.clone();
                let locals_before = frame.locals_before.clone();
                self.drop_loop_body_scope(&owned_before, &locals_before);
                self.terminate(Terminator::Jump(cont_target))?;
            }
            Stmt::Unsafe { body } => {
                self.lower_block(body)?;
            }
            Stmt::Arena { name, body } => {
                let arena = self.value();
                self.emit(Inst::Call {
                    out: Some(arena),
                    function: "mako_native_arena_new".into(),
                    args: vec![],
                    ret: Some(Type::Arena),
                });
                let slot = self.value();
                self.emit(Inst::Alloca {
                    out: slot,
                    ty: Type::Arena,
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: arena,
                });
                self.locals.insert(name.clone(), (slot, Type::Arena));
                self.heap_owned.insert(name.clone(), true);
                self.lower_block(body)?;
                if self.heap_owned.get(name).copied().unwrap_or(false) {
                    let v = self.value();
                    self.emit(Inst::Load {
                        out: v,
                        ptr: slot,
                        ty: Type::Arena,
                    });
                    self.emit_drop(v, Type::Arena);
                    self.heap_owned.insert(name.clone(), false);
                }
                self.locals.remove(name);
            }
            Stmt::Crew { name, body } => {
                let nursery = self.value();
                self.emit(Inst::Call {
                    out: Some(nursery),
                    function: "mako_native_nursery_new".into(),
                    args: vec![],
                    ret: Some(Type::Nursery),
                });
                let slot = self.value();
                self.emit(Inst::Alloca {
                    out: slot,
                    ty: Type::Nursery,
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: nursery,
                });
                self.locals.insert(name.clone(), (slot, Type::Nursery));
                self.heap_owned.insert(name.clone(), true);
                self.lower_block(body)?;
                if self.heap_owned.get(name).copied().unwrap_or(false) {
                    let v = self.value();
                    self.emit(Inst::Load {
                        out: v,
                        ptr: slot,
                        ty: Type::Nursery,
                    });
                    self.emit_drop(v, Type::Nursery);
                    self.heap_owned.insert(name.clone(), false);
                }
                self.locals.remove(name);
            }
            Stmt::Select {
                timeout_ms,
                arms,
                default_arm,
            } => {
                self.lower_select(timeout_ms, arms, default_arm.as_ref())?;
            }
            _ => return Err(IrError::new("native IR: statement not implemented yet")),
        }
        Ok(())
    }

    /// `select timeout ms { ch => {…} default => {…} }` for any `chan[T]`.
    /// All channel kinds share the same runtime header pointer ABI.
    fn lower_select(
        &mut self,
        timeout_ms: &Expr,
        arms: &[(String, crate::ast::Block)],
        default_arm: Option<&crate::ast::Block>,
    ) -> Result<(), IrError> {
        if arms.is_empty() || arms.len() > 16 {
            return Err(IrError::new(
                "native IR: select supports 1..16 channel arms",
            ));
        }
        let (ms, mty, mo) = self.lower_expr(timeout_ms)?;
        if mty != Type::I64 || mo {
            return Err(IrError::new("native IR: select timeout must be int"));
        }
        let mut ch_vals = Vec::with_capacity(arms.len());
        for (ch_name, _) in arms {
            let (slot, ty) = self.locals.get(ch_name).copied().ok_or_else(|| {
                IrError::new(format!("native IR: select unknown channel `{ch_name}`"))
            })?;
            if !matches!(
                ty,
                Type::ChanI | Type::ChanS | Type::ChanF | Type::ChanP(_)
            ) {
                return Err(IrError::new(
                    "native IR: select arm must be a channel",
                ));
            }
            let v = self.value();
            self.emit(Inst::Load {
                out: v,
                ptr: slot,
                ty,
            });
            ch_vals.push(v);
        }
        let which = self.value();
        // General path: pack channel pointers and call selectn.
        let n = self.const_int(ch_vals.len() as i64, Type::I64);
        let pack = self.value();
        self.emit(Inst::Call {
            out: Some(pack),
            function: "mako_native_pack_new".into(),
            args: vec![n],
            ret: Some(Type::I64),
        });
        for (i, ch) in ch_vals.iter().enumerate() {
            let idx = self.const_int(i as i64, Type::I64);
            self.emit(Inst::Call {
                out: None,
                function: "mako_native_pack_set".into(),
                args: vec![pack, idx, *ch],
                ret: None,
            });
        }
        self.emit(Inst::Call {
            out: Some(which),
            function: "mako_native_chan_selectn".into(),
            args: vec![pack, n, ms],
            ret: Some(Type::I64),
        });
        self.emit(Inst::Call {
            out: None,
            function: "mako_native_pack_free".into(),
            args: vec![pack],
            ret: None,
        });

        // Chain: if which == i → arm i else …
        let done = self.new_block();
        let mut next_check = self.current;
        for (i, (_, body)) in arms.iter().enumerate() {
            self.current = next_check;
            let idx = self.const_int(i as i64, Type::I64);
            let is_i = self.value();
            self.emit(Inst::Binary {
                out: is_i,
                op: BinOp::Eq,
                left: which,
                right: idx,
                ty: Type::I64,
            });
            let arm_bb = self.new_block();
            let else_bb = self.new_block();
            self.terminate(Terminator::Branch {
                condition: is_i,
                then_block: arm_bb,
                else_block: else_bb,
            })?;
            self.current = arm_bb;
            let owned_before = self.heap_owned.clone();
            let locals_before = self.locals.clone();
            self.lower_block(body)?;
            if self.block().terminator.is_none() {
                self.drop_loop_body_scope(&owned_before, &locals_before);
                self.terminate(Terminator::Jump(done))?;
            }
            next_check = else_bb;
        }
        // Default / timeout (which == -1)
        self.current = next_check;
        if let Some(body) = default_arm {
            let owned_before = self.heap_owned.clone();
            let locals_before = self.locals.clone();
            self.lower_block(body)?;
            if self.block().terminator.is_none() {
                self.drop_loop_body_scope(&owned_before, &locals_before);
                self.terminate(Terminator::Jump(done))?;
            }
        } else if self.block().terminator.is_none() {
            self.terminate(Terminator::Jump(done))?;
        }
        self.current = done;
        Ok(())
    }

    /// Sequential `fan(xs, |x| body)` or `fan(xs, named_fn)` for slices.
    fn lower_fan(
        &mut self,
        collection: &Expr,
        mapper: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        // Named function mapper: wrap as synthetic lambda `x => named(x)`.
        let (pname, body_owned): (String, Expr) = match mapper {
            Expr::Lambda { params, body } => {
                if params.len() != 1 {
                    return Err(IrError::new("native IR: fan lambda arity"));
                }
                (params[0].clone(), *body.clone())
            }
            Expr::Ident(fname) if self.signatures.contains_key(fname) => {
                let pname = "__fan_x".to_string();
                let body = Expr::Call {
                    callee: Box::new(Expr::Ident(fname.clone())),
                    args: vec![Expr::Ident(pname.clone())],
                };
                (pname, body)
            }
            _ => {
                return Err(IrError::new(
                    "native IR: fan currently requires a lambda or named function mapper",
                ));
            }
        };
        let body = &body_owned;
        let (src, sty, so) = self.lower_expr(collection)?;
        let (elem_ty, out_ty) = match sty {
            Type::IntSlice => (Type::I64, Type::IntSlice),
            Type::FloatSlice => (Type::F64, Type::FloatSlice),
            Type::StrSlice => (Type::Str, Type::StrSlice),
            Type::StructSlice(id) => (Type::Struct(id), Type::StructSlice(id)),
            _ => {
                return Err(IrError::new(
                    "native IR: fan supports []int, []float, []string, and []Struct",
                ));
            }
        };
        let n = self.value();
        match sty {
            Type::IntSlice => self.emit(Inst::SliceLen {
                out: n,
                slice: src,
            }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: Some(n),
                function: "mako_native_float_slice_len_ptr".into(),
                args: vec![src],
                ret: Some(Type::I64),
            }),
            Type::StrSlice => self.emit(Inst::Call {
                out: Some(n),
                function: "mako_native_string_slice_len_ptr".into(),
                args: vec![src],
                ret: Some(Type::I64),
            }),
            Type::StructSlice(_) => self.emit(Inst::Call {
                out: Some(n),
                function: "mako_native_ptr_slice_len".into(),
                args: vec![src],
                ret: Some(Type::I64),
            }),
            _ => unreachable!(),
        }
        let out = self.value();
        match sty {
            Type::IntSlice => self.emit(Inst::SliceMake {
                out,
                len: n,
                cap: Some(n),
            }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_float_slice_make_ptr".into(),
                args: vec![n, n],
                ret: Some(Type::FloatSlice),
            }),
            Type::StrSlice => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_string_slice_make_ptr".into(),
                args: vec![n, n],
                ret: Some(Type::StrSlice),
            }),
            Type::StructSlice(_) => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_ptr_slice_make".into(),
                args: vec![n, n],
                ret: Some(out_ty),
            }),
            _ => unreachable!(),
        }
        // for i in 0..n
        let counter = self.value();
        self.emit(Inst::Alloca {
            out: counter,
            ty: Type::I64,
        });
        let zero = self.const_int(0, Type::I64);
        self.emit(Inst::Store {
            ptr: counter,
            value: zero,
        });
        let header = self.new_block();
        let body_bb = self.new_block();
        let latch = self.new_block();
        let done = self.new_block();
        self.terminate(Terminator::Jump(header))?;
        self.current = header;
        let iv = self.value();
        self.emit(Inst::Load {
            out: iv,
            ptr: counter,
            ty: Type::I64,
        });
        let cmp = self.value();
        self.emit(Inst::Binary {
            out: cmp,
            op: BinOp::Lt,
            left: iv,
            right: n,
            ty: Type::I64,
        });
        self.terminate(Terminator::Branch {
            condition: cmp,
            then_block: body_bb,
            else_block: done,
        })?;
        self.current = body_bb;
        let mut x = self.value();
        match sty {
            Type::IntSlice => self.emit(Inst::SliceIndex {
                out: x,
                slice: src,
                index: iv,
            }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: Some(x),
                function: "mako_native_float_slice_get_ptr".into(),
                args: vec![src, iv],
                ret: Some(Type::F64),
            }),
            Type::StrSlice => self.emit(Inst::Call {
                out: Some(x),
                function: "mako_native_string_slice_get_ptr".into(),
                args: vec![src, iv],
                ret: Some(Type::Str),
            }),
            Type::StructSlice(_) => self.emit(Inst::Call {
                out: Some(x),
                function: "mako_native_ptr_slice_get".into(),
                args: vec![src, iv],
                ret: Some(elem_ty),
            }),
            _ => unreachable!(),
        }
        // Borrowed element for map: clone heap so lambda can own/move.
        if matches!(elem_ty, Type::Str | Type::Struct(_)) {
            x = self.emit_clone(x, elem_ty);
        }
        let xslot = self.value();
        self.emit(Inst::Alloca {
            out: xslot,
            ty: elem_ty,
        });
        self.emit(Inst::Store {
            ptr: xslot,
            value: x,
        });
        let saved_locals = self.locals.clone();
        let saved_owned = self.heap_owned.clone();
        self.locals.insert(pname.clone(), (xslot, elem_ty));
        if matches!(elem_ty, Type::Str | Type::Struct(_)) {
            self.heap_owned.insert(pname.clone(), true);
        }
        // Fan inlines the mapper: `return e` inside `fn(x) { return e }` is the
        // mapped value, not a function exit.
        let prev_yield = self.return_as_yield;
        self.return_as_yield = true;
        let (mut y, yt, yo) = self.lower_expr(body)?;
        self.return_as_yield = prev_yield;
        if yt != elem_ty {
            return Err(IrError::new("native IR: fan mapper result type mismatch"));
        }
        if matches!(elem_ty, Type::Str | Type::Struct(_)) && !yo {
            y = self.emit_clone(y, elem_ty);
        }
        match sty {
            Type::IntSlice => self.emit(Inst::SliceStore {
                slice: out,
                index: iv,
                value: y,
            }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: None,
                function: "mako_native_float_slice_set_ptr".into(),
                args: vec![out, iv, y],
                ret: None,
            }),
            Type::StrSlice => self.emit(Inst::Call {
                out: None,
                function: "mako_native_string_slice_set_ptr".into(),
                args: vec![out, iv, y],
                ret: None,
            }),
            Type::StructSlice(_) => self.emit(Inst::Call {
                out: None,
                function: "mako_native_ptr_slice_set".into(),
                args: vec![out, iv, y],
                ret: None,
            }),
            _ => unreachable!(),
        }
        self.locals = saved_locals;
        self.heap_owned = saved_owned;
        self.terminate(Terminator::Jump(latch))?;
        self.current = latch;
        let one = self.const_int(1, Type::I64);
        let next = self.value();
        self.emit(Inst::Binary {
            out: next,
            op: BinOp::Add,
            left: iv,
            right: one,
            ty: Type::I64,
        });
        self.emit(Inst::Store {
            ptr: counter,
            value: next,
        });
        self.terminate(Terminator::Jump(header))?;
        self.current = done;
        if so {
            self.emit_drop(src, sty);
        }
        let _ = yo;
        Ok((out, out_ty, true))
    }

    /// Lower `|x| expr` / `fn(x) { … }` to a synthetic function + FuncAddr.
    /// `param_tys` is optional expected ABI (from call-site `fn(T) -> U` arg type).
    fn lower_lambda(
        &mut self,
        params: &[String],
        body: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        self.lower_lambda_with_types(params, body, &[])
    }

    /// True if `param` is used in a string context (`len(param)`, concat, …).
    fn param_used_as_string(body: &Expr, param: &str) -> bool {
        fn walk(e: &Expr, param: &str) -> bool {
            match e {
                Expr::Call { callee, args } => {
                    if let Expr::Ident(f) = callee.as_ref() {
                        if (f == "len" || f == "str_len")
                            && args.len() == 1
                            && matches!(&args[0], Expr::Ident(n) if n == param)
                        {
                            return true;
                        }
                    }
                    walk(callee, param) || args.iter().any(|a| walk(a, param))
                }
                Expr::Binary {
                    op: BinOp::Add,
                    left,
                    right,
                } => {
                    // string concat only if one side is string lit / len — weak; skip
                    walk(left, param) || walk(right, param)
                }
                Expr::Binary { left, right, .. } => walk(left, param) || walk(right, param),
                Expr::Unary { expr, .. }
                | Expr::Field { base: expr, .. }
                | Expr::Try(expr)
                | Expr::Join(expr) => walk(expr, param),
                Expr::Method { receiver, args, .. } => {
                    walk(receiver, param) || args.iter().any(|a| walk(a, param))
                }
                Expr::Index { base, index } => walk(base, param) || walk(index, param),
                Expr::Array(xs) | Expr::Tuple(xs) => xs.iter().any(|x| walk(x, param)),
                Expr::Block(b) => b.stmts.iter().any(|s| match s {
                    Stmt::Expr(e) | Stmt::Return(Some(e)) => walk(e, param),
                    Stmt::Let { init, .. } | Stmt::Assign { value: init, .. } => walk(init, param),
                    _ => false,
                }),
                Expr::Lambda { body, .. } => walk(body, param),
                _ => false,
            }
        }
        walk(body, param)
    }

    /// True if `param` is used in integer arithmetic / comparisons.
    fn param_used_as_int(body: &Expr, param: &str) -> bool {
        fn is_param(e: &Expr, param: &str) -> bool {
            matches!(e, Expr::Ident(n) if n == param)
        }
        fn walk(e: &Expr, param: &str) -> bool {
            match e {
                Expr::Binary {
                    op:
                        BinOp::Add
                        | BinOp::Sub
                        | BinOp::Mul
                        | BinOp::Div
                        | BinOp::Mod
                        | BinOp::Lt
                        | BinOp::Le
                        | BinOp::Gt
                        | BinOp::Ge
                        | BinOp::BitAnd
                        | BinOp::BitOr
                        | BinOp::BitXor
                        | BinOp::Shl
                        | BinOp::Shr,
                    left,
                    right,
                } => {
                    is_param(left, param)
                        || is_param(right, param)
                        || walk(left, param)
                        || walk(right, param)
                }
                Expr::Binary { left, right, .. } => walk(left, param) || walk(right, param),
                Expr::Call { callee, args } => {
                    walk(callee, param) || args.iter().any(|a| walk(a, param))
                }
                Expr::Unary { expr, .. }
                | Expr::Field { base: expr, .. }
                | Expr::Try(expr)
                | Expr::Join(expr) => walk(expr, param),
                Expr::Method { receiver, args, .. } => {
                    walk(receiver, param) || args.iter().any(|a| walk(a, param))
                }
                Expr::Index { base, index } => walk(base, param) || walk(index, param),
                Expr::Array(xs) | Expr::Tuple(xs) => xs.iter().any(|x| walk(x, param)),
                Expr::Block(b) => b.stmts.iter().any(|s| match s {
                    Stmt::Expr(e) | Stmt::Return(Some(e)) => walk(e, param),
                    Stmt::Let { init, .. } | Stmt::Assign { value: init, .. } => walk(init, param),
                    _ => false,
                }),
                Expr::Lambda { body, .. } => walk(body, param),
                _ => false,
            }
        }
        walk(body, param)
    }

    /// Collect free locals referenced by a lambda body (excluding params).
    fn collect_lambda_free_locals(
        &self,
        body: &Expr,
        params: &[String],
    ) -> Vec<(String, Type)> {
        fn walk(e: &Expr, out: &mut Vec<String>) {
            match e {
                Expr::Ident(n) => out.push(n.clone()),
                Expr::Call { callee, args } => {
                    walk(callee, out);
                    for a in args {
                        walk(a, out);
                    }
                }
                Expr::Binary { left, right, .. } => {
                    walk(left, out);
                    walk(right, out);
                }
                Expr::Unary { expr, .. }
                | Expr::Field { base: expr, .. }
                | Expr::Try(expr)
                | Expr::Join(expr) => walk(expr, out),
                Expr::Index { base, index } => {
                    walk(base, out);
                    walk(index, out);
                }
                Expr::Slice {
                    base,
                    low,
                    high,
                    max,
                } => {
                    walk(base, out);
                    if let Some(e) = low {
                        walk(e, out);
                    }
                    if let Some(e) = high {
                        walk(e, out);
                    }
                    if let Some(e) = max {
                        walk(e, out);
                    }
                }
                Expr::Method { receiver, args, .. } => {
                    walk(receiver, out);
                    for a in args {
                        walk(a, out);
                    }
                }
                Expr::Array(xs) | Expr::Tuple(xs) => {
                    for x in xs {
                        walk(x, out);
                    }
                }
                Expr::IfExpr {
                    cond,
                    then_block,
                    else_block,
                    ..
                } => {
                    walk(cond, out);
                    walk_block(then_block, out);
                    walk_block(else_block, out);
                }
                Expr::Block(b) => walk_block(b, out),
                Expr::Lambda { body, .. } => walk(body, out),
                Expr::Match { scrutinee, arms } => {
                    walk(scrutinee, out);
                    for a in arms {
                        walk(&a.body, out);
                    }
                }
                Expr::StructLit { fields, update, .. } => {
                    for (_, v) in fields {
                        walk(v, out);
                    }
                    if let Some(u) = update {
                        walk(u, out);
                    }
                }
                Expr::Kick { expr, .. } => walk(expr, out),
                Expr::Fan {
                    collection,
                    mapper,
                } => {
                    walk(collection, out);
                    walk(mapper, out);
                }
                _ => {}
            }
        }
        fn walk_block(b: &AstBlock, out: &mut Vec<String>) {
            for s in &b.stmts {
                match s {
                    Stmt::Expr(e) | Stmt::Return(Some(e)) => walk(e, out),
                    Stmt::Let { init, .. } => walk(init, out),
                    Stmt::Assign { name, value } => {
                        out.push(name.clone());
                        walk(value, out);
                    }
                    Stmt::If {
                        cond,
                        then_block,
                        else_block,
                        ..
                    } => {
                        walk(cond, out);
                        walk_block(then_block, out);
                        if let Some(eb) = else_block {
                            walk_block(eb, out);
                        }
                    }
                    Stmt::For { body, .. } | Stmt::While { body, .. } => walk_block(body, out),
                    _ => {}
                }
            }
        }
        let mut idents = Vec::new();
        walk(body, &mut idents);
        let mut out = Vec::new();
        for n in idents {
            if n == "_" || params.iter().any(|p| p == &n) {
                continue;
            }
            if let Some((_, ty)) = self.locals.get(&n) {
                // POD / pointer-sized captures by value (seed).
                if matches!(
                    *ty,
                    Type::I64
                        | Type::I1
                        | Type::I32
                        | Type::F64
                        | Type::Str
                        | Type::ShareInt
                        | Type::Opaque
                        | Type::FnPtr
                        | Type::Struct(_)
                ) && !out.iter().any(|(a, _)| a == &n)
                {
                    out.push((n, *ty));
                }
            }
        }
        out
    }

    /// Outer locals assigned inside a lambda body (for mut capture cells).
    fn collect_lambda_assigned_locals(body: &Expr, params: &[String]) -> HashSet<String> {
        let mut out = HashSet::new();
        fn walk_block(b: &AstBlock, params: &[String], out: &mut HashSet<String>) {
            for s in &b.stmts {
                match s {
                    Stmt::Assign { name, value } => {
                        if !params.iter().any(|p| p == name) {
                            out.insert(name.clone());
                        }
                        walk_expr(value, params, out);
                    }
                    Stmt::Expr(e) | Stmt::Return(Some(e)) | Stmt::Let { init: e, .. } => {
                        walk_expr(e, params, out);
                    }
                    Stmt::If {
                        cond,
                        then_block,
                        else_block,
                        ..
                    } => {
                        walk_expr(cond, params, out);
                        walk_block(then_block, params, out);
                        if let Some(eb) = else_block {
                            walk_block(eb, params, out);
                        }
                    }
                    Stmt::For { body, .. } | Stmt::While { body, .. } => {
                        walk_block(body, params, out);
                    }
                    _ => {}
                }
            }
        }
        fn walk_expr(e: &Expr, params: &[String], out: &mut HashSet<String>) {
            match e {
                Expr::Block(b) => walk_block(b, params, out),
                Expr::IfExpr {
                    cond,
                    then_block,
                    else_block,
                    ..
                } => {
                    walk_expr(cond, params, out);
                    walk_block(then_block, params, out);
                    walk_block(else_block, params, out);
                }
                Expr::Call { callee, args } => {
                    walk_expr(callee, params, out);
                    for a in args {
                        walk_expr(a, params, out);
                    }
                }
                Expr::Binary { left, right, .. } => {
                    walk_expr(left, params, out);
                    walk_expr(right, params, out);
                }
                Expr::Unary { expr, .. } | Expr::Field { base: expr, .. } => {
                    walk_expr(expr, params, out);
                }
                _ => {}
            }
        }
        walk_expr(body, params, &mut out);
        out
    }

    fn lower_lambda_with_types(
        &mut self,
        params: &[String],
        body: &Expr,
        expected_param_tys: &[Type],
    ) -> Result<(Value, Type, bool), IrError> {
        let captures = self.collect_lambda_free_locals(body, params);
        let assigned = Self::collect_lambda_assigned_locals(body, params);
        // Mut capture cells only for int-like outer locals (match C heap cells).
        let mut_caps: HashSet<String> = captures
            .iter()
            .filter(|(n, ty)| {
                assigned.contains(n) && matches!(ty, Type::I64 | Type::I32 | Type::I1)
            })
            .map(|(n, _)| n.clone())
            .collect();
        let stubs = self
            .lambda_stubs
            .as_deref_mut()
            .ok_or_else(|| IrError::new("native IR: lambda outside collector"))?;
        let lid = self
            .lambda_id
            .as_deref_mut()
            .ok_or_else(|| IrError::new("native IR: lambda id missing"))?;
        let name = format!("__mako_lambda_{lid}");
        *lid += 1;
        // Infer param types: prefer call-site expected types; else heuristic
        // (string if `len(param)` appears, else int).
        let body_s = format!("{body:?}");
        let ty_expr_for = |t: Type| -> TypeExpr {
            match t {
                Type::Str => TypeExpr::Named("string".into()),
                Type::F64 => TypeExpr::Named("float".into()),
                Type::I1 => TypeExpr::Named("bool".into()),
                _ => TypeExpr::Named("int".into()),
            }
        };
        // Captures become leading parameters (env unpacked by trampoline ABI:
        // closure helper is (env, formals…) with env pack of capture i64s.
        // Stub body starts by binding capture names from env pack slots.
        let mut fn_params: Vec<crate::ast::Param> = Vec::new();
        let has_env = !captures.is_empty();
        if has_env {
            fn_params.push(crate::ast::Param {
                name: "__env".into(),
                ty: TypeExpr::Named("int".into()),
                mutable: false,
            });
        }
        for (i, p) in params.iter().enumerate() {
            let ty = if let Some(t) = expected_param_tys.get(i) {
                ty_expr_for(*t)
            } else if Self::param_used_as_string(body, p) {
                TypeExpr::Named("string".into())
            } else if Self::param_used_as_int(body, p) {
                TypeExpr::Named("int".into())
            } else {
                let strish = body_s.contains("len")
                    || body_s.contains("str_")
                    || body_s.contains("String");
                if strish && params.len() == 1 && captures.is_empty() {
                    TypeExpr::Named("string".into())
                } else if strish && params.len() >= 2 && i + 1 == params.len() {
                    TypeExpr::Named("string".into())
                } else {
                    TypeExpr::Named("int".into())
                }
            };
            fn_params.push(crate::ast::Param {
                name: p.clone(),
                ty,
                mutable: false,
            });
        }
        let mut body_stmts: Vec<Stmt> = Vec::new();
        if has_env {
            // let cap_i = pack_get(__env, i)  — use synthetic Call to runtime.
            // Mut int captures store a cell pointer; bind the cell then load/store via helpers.
            for (i, (cname, cty)) in captures.iter().enumerate() {
                let is_mut_cell = mut_caps.contains(cname);
                let ty_ann = if is_mut_cell {
                    TypeExpr::Named("int".into())
                } else {
                    match cty {
                        Type::Str => TypeExpr::Named("string".into()),
                        Type::I1 => TypeExpr::Named("bool".into()),
                        Type::F64 => TypeExpr::Named("float".into()),
                        Type::Struct(id) => TypeExpr::Named(self.structs.layout_name(*id)),
                        Type::ShareInt => TypeExpr::Named("ShareInt".into()),
                        _ => TypeExpr::Named("int".into()),
                    }
                };
                if is_mut_cell {
                    let cell_name = format!("__cell_{cname}");
                    body_stmts.push(Stmt::Let {
                        name: cell_name.clone(),
                        mutable: false,
                        ownership: crate::ast::Ownership::None,
                        ty: Some(TypeExpr::Named("int".into())),
                        init: Expr::Call {
                            callee: Box::new(Expr::Ident("__mako_env_get".into())),
                            args: vec![
                                Expr::Ident("__env".into()),
                                Expr::Int(i as i64),
                            ],
                        },
                    });
                    // Mutable local mirrors the cell; rewritten assigns go through store.
                    body_stmts.push(Stmt::Let {
                        name: cname.clone(),
                        mutable: true,
                        ownership: crate::ast::Ownership::None,
                        ty: Some(ty_ann),
                        init: Expr::Call {
                            callee: Box::new(Expr::Ident("__mako_cell_load".into())),
                            args: vec![Expr::Ident(cell_name)],
                        },
                    });
                } else {
                    body_stmts.push(Stmt::Let {
                        name: cname.clone(),
                        mutable: false,
                        ownership: crate::ast::Ownership::None,
                        ty: Some(ty_ann),
                        init: Expr::Call {
                            callee: Box::new(Expr::Ident("__mako_env_get".into())),
                            args: vec![
                                Expr::Ident("__env".into()),
                                Expr::Int(i as i64),
                            ],
                        },
                    });
                }
            }
        }
        // Rewrite assigns to mut-cell locals into store+update.
        let raw_body: Vec<Stmt> = match body {
            Expr::Block(b) => b.stmts.clone(),
            other => vec![Stmt::Return(Some(other.clone()))],
        };
        for s in raw_body {
            if let Stmt::Assign { name, value } = &s {
                if mut_caps.contains(name) {
                    let cell_name = format!("__cell_{name}");
                    // cell_store(cell, value); name = cell_load(cell) — single eval of value.
                    body_stmts.push(Stmt::Expr(Expr::Call {
                        callee: Box::new(Expr::Ident("__mako_cell_store".into())),
                        args: vec![Expr::Ident(cell_name.clone()), value.clone()],
                    }));
                    body_stmts.push(Stmt::Assign {
                        name: name.clone(),
                        value: Expr::Call {
                            callee: Box::new(Expr::Ident("__mako_cell_load".into())),
                            args: vec![Expr::Ident(cell_name)],
                        },
                    });
                    continue;
                }
            }
            body_stmts.push(s);
        }
        let body_block = AstBlock { stmts: body_stmts };
        stubs.push(FnDef {
            type_bounds: HashMap::new(),
            name: name.clone(),
            type_params: Vec::new(),
            params: fn_params,
            ret: Some(TypeExpr::Named("int".into())),
            body: body_block,
            exported: false,
            is_const: false,
            stability: crate::ast::ApiStability::Unspecified,
        });
        // Code address of stub.
        let code = self.value();
        self.emit(Inst::FuncAddr {
            out: code,
            function: name,
        });
        // Pack capture values into env (or 0).
        let env = if has_env {
            let n = self.const_int(captures.len() as i64, Type::I64);
            let pack = self.value();
            self.emit(Inst::Call {
                out: Some(pack),
                function: "mako_native_pack_new".into(),
                args: vec![n],
                ret: Some(Type::I64),
            });
            for (i, (cname, cty)) in captures.iter().enumerate() {
                let (slot, _) = self.locals.get(cname).copied().ok_or_else(|| {
                    IrError::new(format!("native IR: capture `{cname}` not in locals"))
                })?;
                let mut v = self.value();
                self.emit(Inst::Load {
                    out: v,
                    ptr: slot,
                    ty: *cty,
                });
                // Widen to i64 slot.
                if *cty == Type::I1 {
                    let w = self.value();
                    self.emit(Inst::Cast {
                        out: w,
                        value: v,
                        from: Type::I1,
                        to: Type::I64,
                    });
                    v = w;
                } else if *cty == Type::F64 {
                    let bits = self.value();
                    self.emit(Inst::Call {
                        out: Some(bits),
                        function: "mako_native_f64_to_bits".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    v = bits;
                } else if *cty == Type::Str {
                    // Clone string into env so capture owns independent data.
                    v = self.emit_clone(v, Type::Str);
                } else if let Type::Struct(sid) = *cty {
                    // Clone struct so capture owns independent storage.
                    let clone = self.value();
                    self.emit(Inst::StructClone {
                        out: clone,
                        base: v,
                        struct_id: sid,
                    });
                    v = clone;
                }
                // Mut int capture: allocate a shared cell and redirect outer loads/stores.
                if mut_caps.contains(cname) {
                    let cell = self.value();
                    self.emit(Inst::Call {
                        out: Some(cell),
                        function: "mako_native_i64_cell_new".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    self.mut_i64_cells.insert(cname.clone(), cell);
                    v = cell;
                }
                let idx = self.const_int(i as i64, Type::I64);
                self.emit(Inst::Call {
                    out: None,
                    function: "mako_native_pack_set".into(),
                    args: vec![pack, idx, v],
                    ret: None,
                });
            }
            pack
        } else {
            self.const_int(0, Type::I64)
        };
        // Box as fat pointer (code + env).
        let out = self.value();
        self.emit(Inst::Call {
            out: Some(out),
            function: "mako_native_fn_box".into(),
            args: vec![code, env],
            ret: Some(Type::FnPtr),
        });
        Ok((out, Type::FnPtr, true))
    }

    /// Call through a function pointer expression.
    fn lower_indirect_call(
        &mut self,
        callee: &Expr,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        let (fp, ft, fo) = self.lower_expr(callee)?;
        if ft != Type::FnPtr {
            return Err(IrError::new("native IR: indirect call expects fn pointer"));
        }
        let result = self.lower_indirect_call_fp(fp, args)?;
        // Drop temporary fat-fn after call (env pack is intentionally retained
        // for the call; free the MakoFn box only — env pack is leaked for now).
        if fo {
            self.emit(Inst::Call {
                out: None,
                function: "mako_native_fn_drop".into(),
                args: vec![fp],
                ret: None,
            });
        }
        Ok(result)
    }

    fn lower_indirect_call_fp(
        &mut self,
        fp: Value,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        let mut lowered = Vec::new();
        let mut temps = Vec::new();
        for arg in args {
            let (v, t, o) = self.lower_expr(arg)?;
            // Seed ABI: pass i64 / pointer-sized values.
            let (v, t) = if t == Type::I1 {
                let c = self.value();
                self.emit(Inst::Cast {
                    out: c,
                    value: v,
                    from: Type::I1,
                    to: Type::I64,
                });
                (c, Type::I64)
            } else if t == Type::Str || t.is_heap() {
                (v, Type::I64)
            } else {
                (v, Type::I64)
            };
            if o {
                temps.push((v, t));
            }
            lowered.push(v);
            let _ = t;
        }
        // Fat-pointer call: runtime helper unpacks env if present.
        let out = self.value();
        let (fname, mut call_args) = match lowered.len() {
            0 => ("mako_native_fn_call0", vec![fp]),
            1 => ("mako_native_fn_call1", vec![fp, lowered[0]]),
            2 => (
                "mako_native_fn_call2",
                vec![fp, lowered[0], lowered[1]],
            ),
            3 => (
                "mako_native_fn_call3",
                vec![fp, lowered[0], lowered[1], lowered[2]],
            ),
            _ => {
                return Err(IrError::new(
                    "native IR: indirect call arity > 3 not supported yet",
                ));
            }
        };
        let _ = &mut call_args;
        self.emit(Inst::Call {
            out: Some(out),
            function: fname.into(),
            args: call_args,
            ret: Some(Type::I64),
        });
        for (v, t) in temps {
            let _ = (v, t);
        }
        Ok((out, Type::I64, false))
    }

    /// `crew.kick(f(args…))` — spawn a trampoline on the nursery.
    fn lower_kick(
        &mut self,
        crew: &str,
        expr: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let Expr::Call { callee, args } = expr else {
            // Non-call kick: sequential evaluate (int only).
            let (v, ty, owned) = self.lower_expr(expr)?;
            if ty != Type::I64 || owned {
                return Err(IrError::new(
                    "native IR: kick of non-call currently supports int only",
                ));
            }
            let out = self.value();
            self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_task_from_i64".into(),
                args: vec![v],
                ret: Some(Type::Task),
            });
            return Ok((out, Type::Task, true));
        };
        let Expr::Ident(fname) = callee.as_ref() else {
            return Err(IrError::new("native IR: kick requires a direct call"));
        };
        let (params, ret) = self
            .signatures
            .get(fname)
            .cloned()
            .ok_or_else(|| IrError::new(format!("native IR: kick unknown function `{fname}`")))?;
        // Allow int, bool, float (bits), void→0, and pointer-sized returns (as i64).
        let ret_ok = matches!(
            ret,
            Some(Type::I64)
                | Some(Type::I1)
                | Some(Type::F64)
                | Some(Type::Opaque)
                | Some(Type::FnPtr)
                | Some(Type::Struct(_))
                | Some(Type::Str)
                | Some(Type::IntSlice)
                | None
        );
        if !ret_ok {
            return Err(IrError::new(
                "native IR: kick currently requires int/pointer-returning functions",
            ));
        }
        if params.len() != args.len() {
            return Err(IrError::new("native IR: kick arity mismatch"));
        }
        // All params must be pointer-sized (i64 or heap pointers).
        for p in &params {
            if !matches!(
                p,
                Type::I64
                    | Type::I1
                    | Type::F64
                    | Type::ChanI
                    | Type::ChanS
                    | Type::ChanF
                    | Type::ChanP(_)
                    | Type::ShareInt
                    | Type::MapII
                    | Type::MapSI
                    | Type::MapSS
                    | Type::MapIF
                    | Type::MapFI
                    | Type::MapIPtr(_)
                    | Type::MapSPtr(_)
                    | Type::Str
                    | Type::IntSlice
                    | Type::FloatSlice
                    | Type::ByteSlice
                    | Type::BoolSlice
                    | Type::StrSlice
                    | Type::StructSlice(_)
                    | Type::PtrSlice(_)
                    | Type::Opaque
                    | Type::Struct(_)
                    | Type::FnPtr
                    | Type::Task
                    | Type::Nursery
            ) {
                return Err(IrError::new(
                    "native IR: kick argument type not supported for spawn pack",
                ));
            }
        }

        let nursery = if crew == "__detached__" {
            // Process-scoped detach: null nursery → mako_detach_spawn path.
            self.const_int(0, Type::I64)
        } else {
            let (slot, ty) = self.locals.get(crew).copied().ok_or_else(|| {
                IrError::new(format!("native IR: kick unknown crew `{crew}`"))
            })?;
            if ty != Type::Nursery {
                return Err(IrError::new("native IR: kick receiver must be a crew"));
            }
            let v = self.value();
            self.emit(Inst::Load {
                out: v,
                ptr: slot,
                ty: Type::Nursery,
            });
            v
        };

        // Build trampoline: __mako_kick_N(pack) → call fname(pack[0]…)
        let stub_name = {
            let stubs = self
                .kick_stubs
                .as_deref_mut()
                .ok_or_else(|| IrError::new("native IR: kick outside collector"))?;
            let kid = self
                .kick_id
                .as_deref_mut()
                .ok_or_else(|| IrError::new("native IR: kick id missing"))?;
            let name = format!("__mako_kick_{kid}");
            *kid += 1;
            stubs.push(Self::build_kick_stub(&name, fname, &params)?);
            name
        };

        // Pack arguments (intptr_t array).
        let n = self.const_int(args.len() as i64, Type::I64);
        let pack = self.value();
        self.emit(Inst::Call {
            out: Some(pack),
            function: "mako_native_pack_new".into(),
            args: vec![n],
            ret: Some(Type::I64), // opaque pointer as i64/ptr
        });
        for (i, arg) in args.iter().enumerate() {
            let (mut v, ty, owned) = self.lower_expr(arg)?;
            if ty != params[i]
                && !(params[i] == Type::I64 && ty == Type::I1)
            {
                // allow I1→I64 for bool params stored as i64
                if !(params[i] == Type::I64 && matches!(ty, Type::I64 | Type::I1)) {
                    return Err(IrError::new("native IR: kick arg type mismatch"));
                }
            }
            // Pack ABI is always i64 slots — widen bool / bitcast float.
            if ty == Type::I1 {
                let w = self.value();
                self.emit(Inst::Cast {
                    out: w,
                    value: v,
                    from: Type::I1,
                    to: Type::I64,
                });
                v = w;
            } else if ty == Type::F64 {
                let bits = self.value();
                self.emit(Inst::Call {
                    out: Some(bits),
                    function: "mako_native_f64_to_bits".into(),
                    args: vec![v],
                    ret: Some(Type::I64),
                });
                v = bits;
            }
            // By-value fn params: move fat-fn out of the local so the source is bare.
            if ty == Type::FnPtr || params[i] == Type::FnPtr {
                self.move_fnptr_local(arg);
            }
            let idx = self.const_int(i as i64, Type::I64);
            // Borrowed heap args: pass the pointer; callee does not free.
            self.emit(Inst::Call {
                out: None,
                function: "mako_native_pack_set".into(),
                args: vec![pack, idx, v],
                ret: None,
            });
            if owned && ty.is_heap() {
                // Pack holds a borrow of the pointer for the join lifetime.
                // Heap temps (struct lits, etc.) are stored in a stack local so
                // the pointer remains valid until join; drop after spawn via
                // the local's ownership at crew end (or leak into pack for POD
                // structs that the trampoline clones).
                if !matches!(arg, Expr::Ident(_)) {
                    let slot = self.value();
                    self.emit(Inst::Alloca {
                        out: slot,
                        ty,
                    });
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: v,
                    });
                    let loaded = self.value();
                    self.emit(Inst::Load {
                        out: loaded,
                        ptr: slot,
                        ty,
                    });
                    // Re-pack the stable local pointer (overwrite pack slot).
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_pack_set".into(),
                        args: vec![pack, idx, loaded],
                        ret: None,
                    });
                    // Keep slot alive: register as owned local with synthetic name.
                    let tmp_name = format!("__kick_tmp_{}", slot.0);
                    self.locals.insert(tmp_name.clone(), (slot, ty));
                    self.heap_owned.insert(tmp_name, true);
                    v = loaded;
                }
            }
        }

        let faddr = self.value();
        self.emit(Inst::FuncAddr {
            out: faddr,
            function: stub_name,
        });
        let task = self.value();
        let spawn_fn = if crew == "__detached__" {
            "mako_native_detach_spawn_i64"
        } else {
            "mako_native_spawn_i64"
        };
        self.emit(Inst::Call {
            out: Some(task),
            function: spawn_fn.into(),
            args: if crew == "__detached__" {
                vec![faddr, pack]
            } else {
                vec![nursery, faddr, pack]
            },
            ret: Some(Type::Task),
        });
        // Joined type: void→I64(0), else the function return (Struct/Result as i64 ptr).
        let joined = ret.unwrap_or(Type::I64);
        self.pending_kick_ret = Some(joined);
        self.pending_kick_crew = Some(crew.to_string());
        // Also stamp onto a side table keyed by nothing yet — Let binding records
        // the local name. For bare `kick(...).join()` chains, ret is still pending.
        Ok((task, Type::Task, true))
    }

    /// Synthetic trampoline: `fn __mako_kick_N(pack) -> int` loads args and calls target.
    fn build_kick_stub(
        stub_name: &str,
        target: &str,
        params: &[Type],
    ) -> Result<Function, IrError> {
        let entry = BlockId(0);
        let mut function = Function {
            name: stub_name.to_string(),
            params: Vec::new(),
            ret: Some(Type::I64),
            blocks: vec![BasicBlock {
                instructions: Vec::new(),
                terminator: None,
            }],
            entry,
            next_value: 0,
        };
        let pack_param = Value(function.next_value);
        function.next_value += 1;
        function
            .params
            .push(("pack".into(), pack_param, Type::I64));

        let mut next = || {
            let v = Value(function.next_value);
            function.next_value += 1;
            v
        };
        let mut emit = |inst: Inst| {
            function.blocks[0].instructions.push(inst);
        };

        let mut call_args = Vec::with_capacity(params.len());
        for (i, pty) in params.iter().enumerate() {
            let idx = next();
            emit(Inst::ConstInt {
                out: idx,
                value: i as i64,
                ty: Type::I64,
            });
            let raw = next();
            emit(Inst::Call {
                out: Some(raw),
                function: "mako_native_pack_get".into(),
                args: vec![pack_param, idx],
                ret: Some(Type::I64),
            });
            // Pack slots are always i64; narrow bool and bitcast float for the callee.
            let arg = match *pty {
                Type::I1 => {
                    let b = next();
                    emit(Inst::Cast {
                        out: b,
                        value: raw,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    b
                }
                Type::F64 => {
                    let f = next();
                    emit(Inst::Call {
                        out: Some(f),
                        function: "mako_native_bits_to_f64".into(),
                        args: vec![raw],
                        ret: Some(Type::F64),
                    });
                    f
                }
                _ => raw,
            };
            call_args.push(arg);
        }
        let ret_v = next();
        emit(Inst::Call {
            out: Some(ret_v),
            function: target.to_string(),
            args: call_args,
            ret: Some(Type::I64),
        });
        function.blocks[0].terminator = Some(Terminator::Return(Some(ret_v)));
        Ok(function)
    }

    /// C-style `for init; cond; post { body }`. `continue` runs `post`.
    fn lower_cfor(
        &mut self,
        label: Option<String>,
        init: &Stmt,
        cond: &Expr,
        post: &Stmt,
        body: &crate::ast::Block,
    ) -> Result<(), IrError> {
        let saved_locals = self.locals.clone();
        let saved_owned = self.heap_owned.clone();
        self.lower_stmt(init)?;
        let header = self.new_block();
        let body_id = self.new_block();
        let latch = self.new_block();
        let done = self.new_block();
        self.terminate(Terminator::Jump(header))?;
        self.current = header;
        let owned_before = self.heap_owned.clone();
        let (condition, ty, condition_owned) = self.lower_expr(cond)?;
        if condition_owned {
            return Err(IrError::new("native IR: owned string c-for condition"));
        }
        if ty != Type::I1 {
            return Err(IrError::new("native IR: c-for condition must be bool"));
        }
        self.terminate(Terminator::Branch {
            condition,
            then_block: body_id,
            else_block: done,
        })?;
        self.current = body_id;
        let locals_before = self.locals.clone();
        self.loops.push(LoopFrame {
            label,
            break_target: done,
            continue_target: latch,
            owned_before: owned_before.clone(),
            locals_before: locals_before.clone(),
        });
        self.lower_block(body)?;
        self.loops.pop();
        if self.block().terminator.is_none() {
            self.drop_loop_body_scope(&owned_before, &locals_before);
            self.terminate(Terminator::Jump(latch))?;
        }
        self.current = latch;
        self.lower_stmt(post)?;
        if self.block().terminator.is_none() {
            self.terminate(Terminator::Jump(header))?;
        }
        self.current = done;
        // Drop init-scoped owned locals.
        for (name, owned) in self.heap_owned.clone() {
            if owned && !saved_owned.contains_key(&name) {
                if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                    let v = self.value();
                    self.emit(Inst::Load {
                        out: v,
                        ptr,
                        ty,
                    });
                    self.emit_drop(v, ty);
                }
            }
        }
        self.locals = saved_locals;
        self.heap_owned = saved_owned;
        Ok(())
    }

    /// `for v in range ch` — receive until channel closes.
    fn lower_for_chan(
        &mut self,
        label: Option<String>,
        binder: &str,
        ch: Value,
        body: &crate::ast::Block,
    ) -> Result<(), IrError> {
        let out_slot = self.value();
        self.emit(Inst::Alloca {
            out: out_slot,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let pack = self.value();
        self.emit(Inst::Call {
            out: Some(pack),
            function: "mako_native_pack_new".into(),
            args: vec![one],
            ret: Some(Type::I64),
        });
        let header = self.new_block();
        let body_id = self.new_block();
        let done = self.new_block();
        self.terminate(Terminator::Jump(header))?;

        // header: try recv into pack[0]
        self.current = header;
        let ok = self.value();
        self.emit(Inst::Call {
            out: Some(ok),
            function: "mako_native_chan_recv_ok".into(),
            args: vec![ch, pack],
            ret: Some(Type::I64),
        });
        // pack[0] holds value when ok
        let z = self.const_int(0, Type::I64);
        let val = self.value();
        self.emit(Inst::Call {
            out: Some(val),
            function: "mako_native_pack_get".into(),
            args: vec![pack, z],
            ret: Some(Type::I64),
        });
        self.emit(Inst::Store {
            ptr: out_slot,
            value: val,
        });
        let is_ok = self.value();
        self.emit(Inst::Cast {
            out: is_ok,
            value: ok,
            from: Type::I64,
            to: Type::I1,
        });
        self.terminate(Terminator::Branch {
            condition: is_ok,
            then_block: body_id,
            else_block: done,
        })?;

        self.current = body_id;
        let owned_before = self.heap_owned.clone();
        let locals_before = self.locals.clone();
        if binder != "_" {
            self.locals
                .insert(binder.to_string(), (out_slot, Type::I64));
        }
        self.loops.push(LoopFrame {
            label: label.clone(),
            break_target: done,
            continue_target: header,
            owned_before: owned_before.clone(),
            locals_before: locals_before.clone(),
        });
        self.lower_block(body)?;
        self.loops.pop();
        if self.block().terminator.is_none() {
            self.drop_loop_body_scope(&owned_before, &locals_before);
            self.terminate(Terminator::Jump(header))?;
        }
        self.current = done;
        self.emit(Inst::Call {
            out: None,
            function: "mako_native_pack_free".into(),
            args: vec![pack],
            ret: None,
        });
        self.heap_owned = owned_before;
        self.locals = locals_before;
        Ok(())
    }

    /// Struct iterator protocol: `for v in it` calling `Type_next` (mut self preferred).
    fn lower_for_protocol(
        &mut self,
        label: Option<String>,
        binders: &[String],
        body: &crate::ast::Block,
        iter_val: Value,
        iter_ty: Type,
        iter_owned: bool,
        type_name: &str,
        next_fn: &str,
    ) -> Result<(), IrError> {
        if binders.len() > 1 {
            return Err(IrError::new(
                "native IR: protocol for supports at most one value binder",
            ));
        }
        let (params, ret) = self
            .signatures
            .get(next_fn)
            .cloned()
            .ok_or_else(|| IrError::new(format!("native IR: missing `{next_fn}`")))?;
        if params.is_empty() {
            return Err(IrError::new(format!(
                "native IR: `{next_fn}` has no receiver"
            )));
        }
        let ret_ty = ret.ok_or_else(|| {
            IrError::new(format!("native IR: `{next_fn}` must return Option[T]"))
        })?;
        let Type::Struct(opt_id) = ret_ty else {
            return Err(IrError::new(format!(
                "native IR: `{next_fn}` must return Option[T]"
            )));
        };
        let some_info = self
            .structs
            .variant_in_enum("Some", opt_id)
            .ok_or_else(|| IrError::new("native IR: Option Some missing"))?;
        let none_tag = self
            .structs
            .variant_in_enum("None", opt_id)
            .map(|v| v.tag)
            .unwrap_or(1);
        let elem_ty = self
            .structs
            .field_type(opt_id, some_info.slot_base as usize);

        let header = self.new_block();
        let body_bb = self.new_block();
        let done = self.new_block();
        self.terminate(Terminator::Jump(header))?;
        self.current = header;

        let is_mut_self = self.mut_self_fns.contains(next_fn);
        let mut recv = iter_val;
        // By-value self clones so the protocol never mutates the outer binding.
        if !is_mut_self {
            recv = self.emit_clone(iter_val, iter_ty);
        }
        let opt_val = self.value();
        self.emit(Inst::Call {
            out: Some(opt_val),
            function: next_fn.to_string(),
            args: vec![recv],
            ret: Some(ret_ty),
        });
        if !is_mut_self {
            self.emit_drop(recv, iter_ty);
        }
        // Drop Option after reading (payload moved out for Some).
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: opt_val,
            struct_id: opt_id,
            index: 0,
            ty: Type::I64,
        });
        let none_c = self.const_int(none_tag, Type::I64);
        let is_none = self.value();
        self.emit(Inst::Binary {
            out: is_none,
            op: BinOp::Eq,
            left: tag,
            right: none_c,
            ty: Type::I64,
        });
        let none_bb = self.new_block();
        self.terminate(Terminator::Branch {
            condition: is_none,
            then_block: none_bb,
            else_block: body_bb,
        })?;

        // None: free the Option, then exit the loop.
        self.current = none_bb;
        self.emit_drop(opt_val, ret_ty);
        self.terminate(Terminator::Jump(done))?;

        self.current = body_bb;
        let payload = self.value();
        let payload_idx = some_info.slot_base as u32;
        self.emit(Inst::StructField {
            out: payload,
            base: opt_val,
            struct_id: opt_id,
            index: payload_idx,
            ty: elem_ty,
        });
        // Detach payload ownership from the Option block before drop.
        let null = self.const_int(0, Type::I64);
        self.emit(Inst::StructFieldStore {
            base: opt_val,
            struct_id: opt_id,
            index: payload_idx,
            value: null,
        });
        self.emit_drop(opt_val, ret_ty);

        let owned_before = self.heap_owned.clone();
        let locals_before = self.locals.clone();
        if let Some(b) = binders.first() {
            if b != "_" {
                let slot = self.value();
                self.emit(Inst::Alloca {
                    out: slot,
                    ty: elem_ty,
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: payload,
                });
                self.locals.insert(b.clone(), (slot, elem_ty));
                if elem_ty.is_heap() {
                    self.heap_owned.insert(b.clone(), true);
                }
            }
        }
        self.loops.push(LoopFrame {
            label: label.clone(),
            break_target: done,
            continue_target: header,
            owned_before: owned_before.clone(),
            locals_before: locals_before.clone(),
        });
        self.lower_block(body)?;
        self.loops.pop();
        if self.block().terminator.is_none() {
            self.drop_loop_body_scope(&owned_before, &locals_before);
            self.terminate(Terminator::Jump(header))?;
        }
        self.current = done;
        if iter_owned && iter_ty.is_heap() {
            self.emit_drop(iter_val, iter_ty);
        }
        let _ = type_name;
        Ok(())
    }

    /// `for i in n` / `for i in range n` (0..n) and `for i[, v] in range xs`
    /// over a borrowed slice/string/map. `continue` targets the increment
    /// latch; `break` exits. Owned temporary iterators are rejected.
    ///
    /// Binder modes (match C backend):
    /// - counted: one binder = index
    /// - slice + `range` + one binder = index; without `range` = value only
    /// - slice + two binders = index, value (`_` blanks allowed)
    /// - string + `range` = Go-like runes (index = byte offset)
    /// - map: two binders = key, value
    fn lower_for(
        &mut self,
        label: Option<String>,
        binders: &[String],
        is_range: bool,
        iter: &Expr,
        body: &crate::ast::Block,
    ) -> Result<(), IrError> {
        let (iter_val, iter_ty, iter_owned) = self.lower_expr(iter)?;
        // Struct iterator protocol (`on T { fn next(mut self) -> Option[U] }`).
        if let Type::Struct(id) = iter_ty {
            let type_name = self.structs.layout_name(id);
            let next_fn = format!("{type_name}_next");
            if self.signatures.contains_key(&next_fn) {
                return self.lower_for_protocol(
                    label,
                    binders,
                    body,
                    iter_val,
                    iter_ty,
                    iter_owned,
                    &type_name,
                    &next_fn,
                );
            }
            return Err(IrError::new(format!(
                "native IR: cannot iterate struct `{type_name}` — implement `on {type_name} {{ fn next(mut self) -> Option[T] }}`"
            )));
        }
        // Owned temps (e.g. `for x in range maps_values(m)`) are freeable after the
        // loop ends; keep the value and drop at done.
        let free_iter_after = iter_owned && iter_ty.is_heap();
        // Classify iterator: counted int, slice, or map (slot-walk).
        enum ForIter {
            Counted,
            Slice(Type),
            /// Go-like `for i, r in range s` — runes; counter is byte offset.
            StrRunes,
            /// Legacy `for b in s` — bytes.
            StrBytes,
            MapII,
            MapIPtr(MapValKind),
            MapSI,
            MapSPtr(MapValKind),
            MapSS,
            MapIF,
            MapFI,
        }
        let for_kind = match iter_ty {
            Type::I64 => {
                if binders.len() != 1 {
                    return Err(IrError::new(
                        "native IR: counted for requires a single binder",
                    ));
                }
                ForIter::Counted
            }
            Type::IntSlice
            | Type::FloatSlice
            | Type::ByteSlice
            | Type::BoolSlice
            | Type::StrSlice
            | Type::StructSlice(_)
            | Type::PtrSlice(_) => {
                if binders.len() > 2 {
                    return Err(IrError::new(
                        "native IR: slice for requires zero, one, or two binders",
                    ));
                }
                ForIter::Slice(iter_ty)
            }
            Type::ChanI => {
                // `for v in range ch` — receive until closed.
                if binders.len() != 1 {
                    return Err(IrError::new(
                        "native IR: channel range requires one value binder",
                    ));
                }
                // Handled specially below (not Counted/Slice).
                // Reuse Slice path with a synthetic marker via special case after.
                return self.lower_for_chan(label, &binders[0], iter_val, body);
            }
            Type::Str if is_range => {
                if binders.len() > 2 {
                    return Err(IrError::new(
                        "native IR: string range for requires zero..two binders",
                    ));
                }
                ForIter::StrRunes
            }
            Type::Str => {
                // Legacy: `for b in s` iterates bytes.
                if binders.len() > 2 {
                    return Err(IrError::new(
                        "native IR: string for requires zero..two binders",
                    ));
                }
                ForIter::StrBytes
            }
            Type::MapII => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapII
            }
            Type::MapIPtr(vk) => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapIPtr(vk)
            }
            Type::MapSI => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapSI
            }
            Type::MapSPtr(vk) => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapSPtr(vk)
            }
            Type::MapSS => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapSS
            }
            Type::MapIF => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapIF
            }
            Type::MapFI => {
                if binders.len() != 2 {
                    return Err(IrError::new(
                        "native IR: map range requires two binders (k, v)",
                    ));
                }
                ForIter::MapFI
            }
            _ => {
                return Err(IrError::new(
                    "native IR: for iterator must be int, string, slice, or map",
                ))
            }
        };

        let bound = match &for_kind {
            ForIter::Counted => iter_val,
            ForIter::Slice(sty) => {
                let len = self.value();
                match sty {
                    Type::IntSlice => self.emit(Inst::SliceLen {
                        out: len,
                        slice: iter_val,
                    }),
                    Type::FloatSlice => self.emit(Inst::Call {
                        out: Some(len),
                        function: "mako_native_float_slice_len_ptr".into(),
                        args: vec![iter_val],
                        ret: Some(Type::I64),
                    }),
                    Type::ByteSlice => self.emit(Inst::Call {
                        out: Some(len),
                        function: "mako_native_byte_slice_len_ptr".into(),
                        args: vec![iter_val],
                        ret: Some(Type::I64),
                    }),
                    Type::BoolSlice => self.emit(Inst::Call {
                        out: Some(len),
                        function: "mako_native_bool_slice_len_ptr".into(),
                        args: vec![iter_val],
                        ret: Some(Type::I64),
                    }),
                    Type::StructSlice(_) | Type::PtrSlice(_) => self.emit(Inst::Call {
                        out: Some(len),
                        function: "mako_native_ptr_slice_len".into(),
                        args: vec![iter_val],
                        ret: Some(Type::I64),
                    }),
                    _ => self.emit(Inst::StrSliceLen {
                        out: len,
                        slice: iter_val,
                    }),
                }
                len
            }
            ForIter::StrRunes | ForIter::StrBytes => {
                let len = self.value();
                self.emit(Inst::StringLen {
                    out: len,
                    value: iter_val,
                });
                len
            }
            ForIter::MapII | ForIter::MapIPtr(_) => {
                let cap = self.value();
                self.emit(Inst::Call {
                    out: Some(cap),
                    function: "mako_native_map_ii_cap_ptr".into(),
                    args: vec![iter_val],
                    ret: Some(Type::I64),
                });
                cap
            }
            ForIter::MapSI | ForIter::MapSPtr(_) => {
                let cap = self.value();
                self.emit(Inst::Call {
                    out: Some(cap),
                    function: "mako_native_map_si_cap_ptr".into(),
                    args: vec![iter_val],
                    ret: Some(Type::I64),
                });
                cap
            }
            ForIter::MapSS => {
                let cap = self.value();
                self.emit(Inst::Call {
                    out: Some(cap),
                    function: "mako_native_map_ss_cap_ptr".into(),
                    args: vec![iter_val],
                    ret: Some(Type::I64),
                });
                cap
            }
            ForIter::MapIF => {
                let cap = self.value();
                self.emit(Inst::Call {
                    out: Some(cap),
                    function: "mako_native_map_if_cap_ptr".into(),
                    args: vec![iter_val],
                    ret: Some(Type::I64),
                });
                cap
            }
            ForIter::MapFI => {
                let cap = self.value();
                self.emit(Inst::Call {
                    out: Some(cap),
                    function: "mako_native_map_fi_cap_ptr".into(),
                    args: vec![iter_val],
                    ret: Some(Type::I64),
                });
                cap
            }
        };

        // Counter local: first binder if present, else synthetic (blank / no-binder).
        let index_name = binders.first().map(|s| s.as_str()).unwrap_or("_");
        let counter_slot = self.value();
        self.emit(Inst::Alloca {
            out: counter_slot,
            ty: Type::I64,
        });
        let zero = self.const_int(0, Type::I64);
        self.emit(Inst::Store {
            ptr: counter_slot,
            value: zero,
        });
        // Rune-width stash for UTF-8 range (advance cursor by decoded width).
        let rune_width_slot = if matches!(for_kind, ForIter::StrRunes) {
            let s = self.value();
            self.emit(Inst::Alloca {
                out: s,
                ty: Type::I64,
            });
            let one = self.const_int(1, Type::I64);
            self.emit(Inst::Store {
                ptr: s,
                value: one,
            });
            Some(s)
        } else {
            None
        };
        // Value binder: present when we need a value (two binders, or one binder
        // on a slice without `range` = value-only, or string byte/rune value).
        // Value-only: legacy `for v in arr` / `for b in s` (no `range` keyword).
        // String+`range` with one binder is **index** (byte offset), like C.
        let value_only = matches!(
            (&for_kind, binders.len(), is_range),
            (ForIter::Slice(_), 1, false) | (ForIter::StrBytes, 1, _)
        );
        let need_value = binders.len() == 2 || value_only;
        let value_slot = if need_value {
            let elem_ty = match &for_kind {
                ForIter::Slice(Type::IntSlice | Type::ByteSlice | Type::BoolSlice) => Type::I64,
                ForIter::Slice(Type::FloatSlice) => Type::F64,
                ForIter::Slice(Type::StructSlice(sid)) => Type::Struct(*sid),
                ForIter::Slice(Type::PtrSlice(vk)) => vk.to_type(),
                ForIter::Slice(_) => Type::Str,
                ForIter::StrRunes | ForIter::StrBytes => Type::I64,
                ForIter::MapII | ForIter::MapSI | ForIter::MapFI => Type::I64,
                ForIter::MapIPtr(vk) | ForIter::MapSPtr(vk) => vk.to_type(),
                ForIter::MapSS => Type::Str,
                ForIter::MapIF => Type::F64,
                ForIter::Counted => {
                    return Err(IrError::new(
                        "native IR: value binder requires a slice/string/map iterator",
                    ));
                }
            };
            let key_ty = match &for_kind {
                ForIter::MapSI | ForIter::MapSPtr(_) | ForIter::MapSS => Some(Type::Str),
                _ => None,
            };
            let vname = if binders.len() == 2 {
                binders[1].clone()
            } else if value_only {
                binders[0].clone()
            } else {
                "_".into()
            };
            let kname = if binders.len() == 2 {
                binders[0].clone()
            } else {
                "_".into()
            };
            let slot = self.value();
            self.emit(Inst::Alloca {
                out: slot,
                ty: elem_ty,
            });
            if elem_ty.is_heap() {
                let null = self.value();
                self.emit(Inst::NullHeap {
                    out: null,
                    ty: elem_ty,
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: null,
                });
            }
            let key_slot = if let Some(kty) = key_ty {
                let ks = self.value();
                self.emit(Inst::Alloca {
                    out: ks,
                    ty: kty,
                });
                let null = self.value();
                self.emit(Inst::NullHeap {
                    out: null,
                    ty: kty,
                });
                self.emit(Inst::Store {
                    ptr: ks,
                    value: null,
                });
                Some(ks)
            } else {
                None
            };
            Some((kname, vname, slot, elem_ty, key_slot, value_only))
        } else {
            None
        };

        let header = self.new_block();
        let body_id = self.new_block();
        let latch = self.new_block();
        let done = self.new_block();
        self.terminate(Terminator::Jump(header))?;

        // header: if counter < bound → body else done
        self.current = header;
        let iv = self.value();
        self.emit(Inst::Load {
            out: iv,
            ptr: counter_slot,
            ty: Type::I64,
        });
        let cmp = self.value();
        self.emit(Inst::Binary {
            out: cmp,
            op: BinOp::Lt,
            left: iv,
            right: bound,
            ty: Type::I64,
        });
        self.terminate(Terminator::Branch {
            condition: cmp,
            then_block: body_id,
            else_block: done,
        })?;

        // body: for maps, skip empty slots; then drop previous heap binders
        // (null-safe), rebind, lower.
        self.current = body_id;
        let owned_before = self.heap_owned.clone();
        let locals_before = self.locals.clone();

        // Map: if this slot is empty, jump latch without running the body.
        let body_run = match &for_kind {
            ForIter::MapII
            | ForIter::MapIPtr(_)
            | ForIter::MapSI
            | ForIter::MapSPtr(_)
            | ForIter::MapSS
            | ForIter::MapIF
            | ForIter::MapFI => {
                let full_fn = match &for_kind {
                    ForIter::MapII | ForIter::MapIPtr(_) => "mako_native_map_ii_slot_full_ptr",
                    ForIter::MapSI | ForIter::MapSPtr(_) => "mako_native_map_si_slot_full_ptr",
                    ForIter::MapSS => "mako_native_map_ss_slot_full_ptr",
                    ForIter::MapIF => "mako_native_map_if_slot_full_ptr",
                    ForIter::MapFI => "mako_native_map_fi_slot_full_ptr",
                    _ => unreachable!(),
                };
                let full = self.value();
                self.emit(Inst::Call {
                    out: Some(full),
                    function: full_fn.into(),
                    args: vec![iter_val, iv],
                    ret: Some(Type::I64),
                });
                let is_full = self.value();
                self.emit(Inst::Cast {
                    out: is_full,
                    value: full,
                    from: Type::I64,
                    to: Type::I1,
                });
                let run = self.new_block();
                self.terminate(Terminator::Branch {
                    condition: is_full,
                    then_block: run,
                    else_block: latch,
                })?;
                self.current = run;
                run
            }
            _ => body_id,
        };
        let _ = body_run;

        // Index binder for slice/counted/string when not value-only.
        let bind_index = !matches!(
            for_kind,
            ForIter::MapII
                | ForIter::MapIPtr(_)
                | ForIter::MapSI
                | ForIter::MapSPtr(_)
                | ForIter::MapSS
                | ForIter::MapIF
                | ForIter::MapFI
        ) && !value_only
            && index_name != "_";
        if bind_index {
            self.locals
                .insert(index_name.to_string(), (counter_slot, Type::I64));
        }

        if let Some((ref kname, ref vname, vslot, elem_ty, key_slot, val_only)) = value_slot {
            // Drop previous heap key/value binders.
            if let Some(ks) = key_slot {
                let old = self.value();
                self.emit(Inst::Load {
                    out: old,
                    ptr: ks,
                    ty: Type::Str,
                });
                self.emit_drop(old, Type::Str);
            }
            if elem_ty.is_heap() && !elem_ty.is_shared_handle() {
                let old = self.value();
                self.emit(Inst::Load {
                    out: old,
                    ptr: vslot,
                    ty: elem_ty,
                });
                self.emit_drop(old, elem_ty);
            }

            match &for_kind {
                ForIter::Slice(slice_ty) => {
                    let elem = self.value();
                    match slice_ty {
                        Type::IntSlice => self.emit(Inst::SliceIndex {
                            out: elem,
                            slice: iter_val,
                            index: iv,
                        }),
                        Type::FloatSlice => self.emit(Inst::Call {
                            out: Some(elem),
                            function: "mako_native_float_slice_get_ptr".into(),
                            args: vec![iter_val, iv],
                            ret: Some(Type::F64),
                        }),
                        Type::ByteSlice => self.emit(Inst::Call {
                            out: Some(elem),
                            function: "mako_native_byte_slice_get_ptr".into(),
                            args: vec![iter_val, iv],
                            ret: Some(Type::I64),
                        }),
                        Type::BoolSlice => self.emit(Inst::Call {
                            out: Some(elem),
                            function: "mako_native_bool_slice_get_ptr".into(),
                            args: vec![iter_val, iv],
                            ret: Some(Type::I64),
                        }),
                        Type::StructSlice(sid) => {
                            self.emit(Inst::Call {
                                out: Some(elem),
                                function: "mako_native_ptr_slice_get".into(),
                                args: vec![iter_val, iv],
                                ret: Some(Type::Struct(*sid)),
                            });
                            // Clone so the binder owns an independent block; the
                            // slice retains its own element pointer.
                            let clone = self.emit_clone(elem, Type::Struct(*sid));
                            // overwrite elem with clone for store below
                            // (rebind local)
                            let _ = elem;
                            // Use clone as the value to store.
                            self.emit(Inst::Store {
                                ptr: vslot,
                                value: clone,
                            });
                            if vname != "_" {
                                self.locals.insert(vname.clone(), (vslot, elem_ty));
                                self.heap_owned.insert(vname.clone(), true);
                            } else {
                                self.emit_drop(clone, elem_ty);
                                let null = self.value();
                                self.emit(Inst::NullHeap {
                                    out: null,
                                    ty: elem_ty,
                                });
                                self.emit(Inst::Store {
                                    ptr: vslot,
                                    value: null,
                                });
                            }
                            // Skip common store path
                            let skip_common = true;
                            let _ = skip_common;
                        }
                        Type::PtrSlice(vk) => {
                            // Element is a heap pointer (e.g. []int from maps_values).
                            self.emit(Inst::Call {
                                out: Some(elem),
                                function: "mako_native_ptr_slice_get".into(),
                                args: vec![iter_val, iv],
                                ret: Some(Type::I64),
                            });
                            let ety = vk.to_type();
                            let clone = if ety.is_heap() && !ety.is_shared_handle() {
                                self.emit_clone(elem, ety)
                            } else {
                                elem
                            };
                            self.emit(Inst::Store {
                                ptr: vslot,
                                value: clone,
                            });
                            if vname != "_" {
                                self.locals.insert(vname.clone(), (vslot, ety));
                                if ety.is_heap() && !ety.is_shared_handle() {
                                    self.heap_owned.insert(vname.clone(), true);
                                }
                            } else if ety.is_heap() && !ety.is_shared_handle() {
                                self.emit_drop(clone, ety);
                            }
                        }
                        _ => self.emit(Inst::StrSliceIndex {
                            out: elem,
                            slice: iter_val,
                            index: iv,
                        }),
                    }
                    // Common store path for non-struct/ptr-slice elements
                    if !matches!(slice_ty, Type::StructSlice(_) | Type::PtrSlice(_)) {
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: elem,
                    });
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, elem_ty));
                        if elem_ty.is_heap() {
                            self.heap_owned.insert(vname.clone(), true);
                        }
                    } else if elem_ty.is_heap() {
                        self.emit_drop(elem, elem_ty);
                        let null = self.value();
                        self.emit(Inst::NullHeap {
                            out: null,
                            ty: elem_ty,
                        });
                        self.emit(Inst::Store {
                            ptr: vslot,
                            value: null,
                        });
                    }
                    } // end non-struct-slice common path
                    let _ = val_only;
                }
                ForIter::StrBytes => {
                    let elem = self.value();
                    self.emit(Inst::Call {
                        out: Some(elem),
                        function: "mako_native_str_get_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: elem,
                    });
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, Type::I64));
                    }
                }
                ForIter::StrRunes => {
                    // Decode one UTF-8 rune at byte offset iv; stash width for latch.
                    let rune = self.value();
                    self.emit(Inst::Call {
                        out: Some(rune),
                        function: "mako_native_utf8_decode_rune_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    let width = self.value();
                    self.emit(Inst::Call {
                        out: Some(width),
                        function: "mako_native_utf8_decode_width_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    if let Some(wslot) = rune_width_slot {
                        self.emit(Inst::Store {
                            ptr: wslot,
                            value: width,
                        });
                    }
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: rune,
                    });
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, Type::I64));
                    }
                }
                ForIter::MapII | ForIter::MapIPtr(_) => {
                    let key = self.value();
                    self.emit(Inst::Call {
                        out: Some(key),
                        function: "mako_native_map_ii_slot_key_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    let mut val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_map_ii_slot_val_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    if elem_ty == Type::F64 {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![val],
                            ret: Some(Type::F64),
                        });
                        val = f;
                    }
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: val,
                    });
                    // Struct-key maps store a heap pointer as the i64 key — retype.
                    let key_ty = match &for_kind {
                        ForIter::MapIPtr(vk) => vk
                            .struct_key_id()
                            .map(Type::Struct)
                            .unwrap_or(Type::I64),
                        _ => Type::I64,
                    };
                    let kslot = self.value();
                    self.emit(Inst::Alloca {
                        out: kslot,
                        ty: key_ty,
                    });
                    self.emit(Inst::Store {
                        ptr: kslot,
                        value: key,
                    });
                    if kname != "_" {
                        self.locals.insert(kname.clone(), (kslot, key_ty));
                        if matches!(key_ty, Type::Struct(_)) {
                            // Borrow of key still owned by the map — do not free.
                            self.heap_owned.insert(kname.clone(), false);
                        }
                    }
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, elem_ty));
                        // Shared handles (channels) are borrows of map storage.
                        if elem_ty.is_heap() && !elem_ty.is_shared_handle() {
                            self.heap_owned.insert(vname.clone(), true);
                        }
                    }
                }
                ForIter::MapSI | ForIter::MapSPtr(_) => {
                    let ks = key_slot.expect("MapSI has key slot");
                    let key = self.value();
                    self.emit(Inst::Call {
                        out: Some(key),
                        function: "mako_native_map_si_slot_key_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::Str),
                    });
                    let mut val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_map_si_slot_val_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    if elem_ty == Type::F64 {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![val],
                            ret: Some(Type::F64),
                        });
                        val = f;
                    }
                    self.emit(Inst::Store {
                        ptr: ks,
                        value: key,
                    });
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: val,
                    });
                    if kname != "_" {
                        self.locals.insert(kname.clone(), (ks, Type::Str));
                        self.heap_owned.insert(kname.clone(), true);
                    } else {
                        self.emit_drop(key, Type::Str);
                        let null = self.value();
                        self.emit(Inst::NullHeap {
                            out: null,
                            ty: Type::Str,
                        });
                        self.emit(Inst::Store {
                            ptr: ks,
                            value: null,
                        });
                    }
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, elem_ty));
                        if elem_ty.is_heap() && !elem_ty.is_shared_handle() {
                            self.heap_owned.insert(vname.clone(), true);
                        }
                    }
                }
                ForIter::MapSS => {
                    let ks = key_slot.expect("MapSS has key slot");
                    let key = self.value();
                    self.emit(Inst::Call {
                        out: Some(key),
                        function: "mako_native_map_ss_slot_key_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::Str),
                    });
                    let val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_map_ss_slot_val_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::Str),
                    });
                    self.emit(Inst::Store {
                        ptr: ks,
                        value: key,
                    });
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: val,
                    });
                    if kname != "_" {
                        self.locals.insert(kname.clone(), (ks, Type::Str));
                        self.heap_owned.insert(kname.clone(), true);
                    } else {
                        self.emit_drop(key, Type::Str);
                        let null = self.value();
                        self.emit(Inst::NullHeap {
                            out: null,
                            ty: Type::Str,
                        });
                        self.emit(Inst::Store {
                            ptr: ks,
                            value: null,
                        });
                    }
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, Type::Str));
                        self.heap_owned.insert(vname.clone(), true);
                    } else {
                        self.emit_drop(val, Type::Str);
                        let null = self.value();
                        self.emit(Inst::NullHeap {
                            out: null,
                            ty: Type::Str,
                        });
                        self.emit(Inst::Store {
                            ptr: vslot,
                            value: null,
                        });
                    }
                }
                ForIter::MapIF => {
                    let key = self.value();
                    self.emit(Inst::Call {
                        out: Some(key),
                        function: "mako_native_map_if_slot_key_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    let val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_map_if_slot_val_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::F64),
                    });
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: val,
                    });
                    let kslot = self.value();
                    self.emit(Inst::Alloca {
                        out: kslot,
                        ty: Type::I64,
                    });
                    self.emit(Inst::Store {
                        ptr: kslot,
                        value: key,
                    });
                    if kname != "_" {
                        self.locals.insert(kname.clone(), (kslot, Type::I64));
                    }
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, Type::F64));
                    }
                }
                ForIter::MapFI => {
                    let key = self.value();
                    self.emit(Inst::Call {
                        out: Some(key),
                        function: "mako_native_map_fi_slot_key_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::F64),
                    });
                    let val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_map_fi_slot_val_ptr".into(),
                        args: vec![iter_val, iv],
                        ret: Some(Type::I64),
                    });
                    self.emit(Inst::Store {
                        ptr: vslot,
                        value: val,
                    });
                    let kslot = self.value();
                    self.emit(Inst::Alloca {
                        out: kslot,
                        ty: Type::F64,
                    });
                    self.emit(Inst::Store {
                        ptr: kslot,
                        value: key,
                    });
                    if kname != "_" {
                        self.locals.insert(kname.clone(), (kslot, Type::F64));
                    }
                    if vname != "_" {
                        self.locals.insert(vname.clone(), (vslot, Type::I64));
                    }
                }
                ForIter::Counted => {}
            }
        }

        // Snapshot after binders are installed so the value binder is treated
        // as outer to the body (not freed on continue/fallthrough).
        let body_owned_before = self.heap_owned.clone();
        let body_locals_before = self.locals.clone();
        self.loops.push(LoopFrame {
            label,
            break_target: done,
            continue_target: latch,
            owned_before: body_owned_before.clone(),
            locals_before: body_locals_before.clone(),
        });
        self.lower_block(body)?;
        self.loops.pop();

        // Fallthrough → latch. Do NOT drop the value binder here: continue also
        // reaches the next body without this path, and the next body's entry
        // drop (or the done drop after break/exhaustion) frees it once. Body-
        // scoped heap locals (share let, temps) are dropped.
        if self.block().terminator.is_none() {
            self.drop_loop_body_scope(&body_owned_before, &body_locals_before);
            self.terminate(Terminator::Jump(latch))?;
        }

        // latch: counter += 1 (or rune width); jump header
        self.current = latch;
        let cur = self.value();
        self.emit(Inst::Load {
            out: cur,
            ptr: counter_slot,
            ty: Type::I64,
        });
        let step = if let Some(wslot) = rune_width_slot {
            let w = self.value();
            self.emit(Inst::Load {
                out: w,
                ptr: wslot,
                ty: Type::I64,
            });
            w
        } else {
            self.const_int(1, Type::I64)
        };
        let next = self.value();
        self.emit(Inst::Binary {
            out: next,
            op: BinOp::Add,
            left: cur,
            right: step,
            ty: Type::I64,
        });
        self.emit(Inst::Store {
            ptr: counter_slot,
            value: next,
        });
        self.terminate(Terminator::Jump(header))?;

        self.current = done;
        // Always free heap binders (null-safe): covers break, and the last live
        // element when the loop exits via the header→done edge.
        if let Some((ref kname, ref vname, vslot, elem_ty, key_slot, _)) = value_slot {
            if let Some(ks) = key_slot {
                let old = self.value();
                self.emit(Inst::Load {
                    out: old,
                    ptr: ks,
                    ty: Type::Str,
                });
                self.emit_drop(old, Type::Str);
                self.heap_owned.remove(kname);
                self.locals.remove(kname);
            }
            if elem_ty.is_heap() && !elem_ty.is_shared_handle() {
                let old = self.value();
                self.emit(Inst::Load {
                    out: old,
                    ptr: vslot,
                    ty: elem_ty,
                });
                self.emit_drop(old, elem_ty);
            }
            self.heap_owned.remove(vname);
            self.locals.remove(vname);
            self.heap_owned.remove(kname);
            self.locals.remove(kname);
        }
        if bind_index {
            self.locals.remove(index_name);
        }
        if free_iter_after {
            self.emit_drop(iter_val, iter_ty);
        }
        for (name, binding) in locals_before {
            self.locals.entry(name).or_insert(binding);
        }
        self.heap_owned = owned_before;
        Ok(())
    }

    fn lower_expr(&mut self, expr: &Expr) -> Result<(Value, Type, bool), IrError> {
        match expr {
            Expr::Int(value) => Ok((self.const_int(*value, Type::I64), Type::I64, false)),
            Expr::Bool(value) => Ok((self.const_int(i64::from(*value), Type::I1), Type::I1, false)),
            Expr::Float(value) => {
                let out = self.value();
                self.emit(Inst::ConstFloat { out, value: *value });
                Ok((out, Type::F64, false))
            }
            Expr::String(bytes) => {
                let out = self.value();
                self.emit(Inst::StringLiteral {
                    out,
                    bytes: bytes.as_bytes().to_vec(),
                });
                Ok((out, Type::Str, false))
            }
            Expr::Array(items) => {
                // Infer slice kind from the first element when present; bare `[]`
                // defaults to []int (typed lets override via lower_array_literal_with_type).
                if items.is_empty() {
                    return self.lower_array_literal_with_type(items, Type::IntSlice);
                }
                // Peek first element type without committing ownership drops: lower
                // everything inside lower_array_literal_with_type after a type probe.
                // Probe by matching common literal shapes to avoid double-eval.
                let slice_ty = match &items[0] {
                    Expr::Int(_) => Type::IntSlice,
                    Expr::Float(_) => Type::FloatSlice,
                    Expr::String(_) => Type::StrSlice,
                    Expr::Bool(_) => Type::BoolSlice,
                    Expr::StructLit { name, .. } | Expr::StructLitPos { name, .. } => {
                        if let Some(id) = self.structs.struct_id(name) {
                            Type::StructSlice(id)
                        } else {
                            return Err(IrError::new(
                                "native IR: unsupported array element type",
                            ));
                        }
                    }
                    // Nested slice literal: `[[1,2],[3]]` → [][]int
                    Expr::Array(inner) if !inner.is_empty() => match &inner[0] {
                        Expr::Int(_) => Type::PtrSlice(MapValKind::IntSlice),
                        Expr::Float(_) => Type::PtrSlice(MapValKind::FloatSlice),
                        Expr::String(_) => Type::PtrSlice(MapValKind::StrSlice),
                        Expr::Bool(_) => Type::PtrSlice(MapValKind::BoolSlice),
                        _ => {
                            let (v, ty, o) = self.lower_expr(&items[0])?;
                            if o {
                                self.emit_drop(v, ty);
                            }
                            Type::PtrSlice(MapValKind::from_type(ty))
                        }
                    },
                    _ => {
                        // Fallback: lower first once, clone type, then re-lower all
                        // (rare non-literal elements).
                        let (v, ty, o) = self.lower_expr(&items[0])?;
                        if o {
                            self.emit_drop(v, ty);
                        }
                        match ty {
                            Type::I64 => Type::IntSlice,
                            Type::F64 => Type::FloatSlice,
                            Type::Str => Type::StrSlice,
                            Type::I1 => Type::BoolSlice,
                            Type::Struct(id) => Type::StructSlice(id),
                            Type::IntSlice => Type::PtrSlice(MapValKind::IntSlice),
                            Type::StrSlice => Type::PtrSlice(MapValKind::StrSlice),
                            Type::FloatSlice => Type::PtrSlice(MapValKind::FloatSlice),
                            Type::ByteSlice => Type::PtrSlice(MapValKind::ByteSlice),
                            Type::BoolSlice => Type::PtrSlice(MapValKind::BoolSlice),
                            Type::PtrSlice(vk) => Type::PtrSlice(MapValKind::from_type(Type::PtrSlice(vk))),
                            other if other.is_heap() || matches!(other, Type::ChanI | Type::ChanS | Type::ChanF | Type::ChanP(_)) => {
                                Type::PtrSlice(MapValKind::from_type(other))
                            }
                            _ => {
                                return Err(IrError::new(
                                    "native IR: unsupported array element type",
                                ));
                            }
                        }
                    }
                };
                self.lower_array_literal_with_type(items, slice_ty)
            }
            Expr::Ident(name) => {
                // Comptime `const NAME = …` folded to an int/string literal.
                if !self.locals.contains_key(name) {
                    if let Some(&v) = self.const_ints.get(name) {
                        return Ok((self.const_int(v, Type::I64), Type::I64, false));
                    }
                    if let Some(s) = self.const_strs.get(name) {
                        let out = self.value();
                        self.emit(Inst::StringLiteral {
                            out,
                            bytes: s.as_bytes().to_vec(),
                        });
                        return Ok((out, Type::Str, false));
                    }
                    // First-class function value: bare function name → boxed fat fn.
                    if self.signatures.contains_key(name) {
                        let code = self.value();
                        self.emit(Inst::FuncAddr {
                            out: code,
                            function: name.clone(),
                        });
                        let z = self.const_int(0, Type::I64);
                        let out = self.value();
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_fn_box".into(),
                            args: vec![code, z],
                            ret: Some(Type::FnPtr),
                        });
                        return Ok((out, Type::FnPtr, true));
                    }
                }
                // A bare identifier that names a nullary enum variant (`Point`,
                // `None`) constructs that variant. Prefer a unique arity-0
                // monomorph; multi-Option None without type context uses first.
                if !self.locals.contains_key(name) {
                    let nullary: Vec<_> = self
                        .structs
                        .variants_named(name)
                        .into_iter()
                        .filter(|info| info.arity == 0)
                        .collect();
                    if let Some(info) = nullary.first().copied() {
                        return self.build_variant(info, &[]);
                    }
                }
                let (ptr, ty) =
                    self.locals.get(name).copied().ok_or_else(|| {
                        IrError::new(format!("native IR: unknown local `{name}`"))
                    })?;
                // Sequential mut capture cell: always read the live heap value.
                if let Some(&cell) = self.mut_i64_cells.get(name) {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_i64_cell_load".into(),
                        args: vec![cell],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                let out = self.value();
                self.emit(Inst::Load { out, ptr, ty });
                Ok((out, ty, false))
            }
            Expr::Binary { op, left, right } => {
                // Lazy `&&` / `||` / `and` / `or` — do not evaluate RHS when short-circuiting.
                if matches!(op, BinOp::And | BinOp::Or) {
                    return self.lower_short_circuit(*op, left, right);
                }
                let (mut left, mut left_ty, left_owned) = self.lower_expr(left)?;
                let (mut right, mut right_ty, right_owned) = self.lower_expr(right)?;
                // Allow I1/I64 mixing at binary ops (common for len + int).
                if left_ty == Type::I1 && right_ty == Type::I64 {
                    let c = self.value();
                    self.emit(Inst::Cast {
                        out: c,
                        value: left,
                        from: Type::I1,
                        to: Type::I64,
                    });
                    left = c;
                    left_ty = Type::I64;
                } else if left_ty == Type::I64 && right_ty == Type::I1 {
                    let c = self.value();
                    self.emit(Inst::Cast {
                        out: c,
                        value: right,
                        from: Type::I1,
                        to: Type::I64,
                    });
                    right = c;
                    right_ty = Type::I64;
                }
                if left_ty != right_ty {
                    return Err(IrError::new(format!(
                        "native IR: binary type mismatch ({left_ty:?} vs {right_ty:?})"
                    )));
                }
                if left_ty == Type::Str {
                    let out = self.value();
                    match op {
                        BinOp::Add => self.emit(Inst::StringConcat { out, left, right }),
                        BinOp::Eq | BinOp::Ne => self.emit(Inst::StringEqual {
                            out,
                            left,
                            right,
                            negated: *op == BinOp::Ne,
                        }),
                        _ => {
                            return Err(IrError::new("native IR: string operation not implemented"))
                        }
                    }
                    if left_owned {
                        self.emit(Inst::DropString { value: left });
                    }
                    if right_owned {
                        self.emit(Inst::DropString { value: right });
                    }
                    return Ok((
                        out,
                        if *op == BinOp::Add {
                            Type::Str
                        } else {
                            Type::I1
                        },
                        *op == BinOp::Add,
                    ));
                }
                // Structural == / != for structs and enums (field / tag compare).
                if let Type::Struct(sid) = left_ty {
                    if matches!(op, BinOp::Eq | BinOp::Ne) {
                        let eq = self.lower_struct_eq(left, right, sid)?;
                        let out = if *op == BinOp::Ne {
                            let n = self.value();
                            self.emit(Inst::Unary {
                                out: n,
                                op: UnaryOp::Not,
                                value: eq,
                                ty: Type::I1,
                            });
                            n
                        } else {
                            eq
                        };
                        if left_owned {
                            self.emit(Inst::DropStruct {
                                value: left,
                                struct_id: sid,
                            });
                        }
                        if right_owned {
                            self.emit(Inst::DropStruct {
                                value: right,
                                struct_id: sid,
                            });
                        }
                        return Ok((out, Type::I1, false));
                    }
                }
                let result_ty = match op {
                    BinOp::Eq | BinOp::Ne | BinOp::Lt | BinOp::Le | BinOp::Gt | BinOp::Ge => {
                        Type::I1
                    }
                    _ => left_ty,
                };
                let out = self.value();
                self.emit(Inst::Binary {
                    out,
                    op: *op,
                    left,
                    right,
                    ty: left_ty,
                });
                Ok((out, result_ty, false))
            }
            Expr::Index { base, index } => {
                let (slice, sty, owned) = self.lower_expr(base)?;
                let (idx, ity, iowned) = self.lower_expr(index)?;
                let out = self.value();
                match sty {
                    Type::Str => {
                        // Go-like: s[i] is the i-th byte (0..255).
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new("native IR: string index must be int"));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_str_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::I64),
                        });
                        if owned {
                            self.emit(Inst::DropString { value: slice });
                        }
                        Ok((out, Type::I64, false))
                    }
                    Type::IntSlice | Type::FloatSlice | Type::ByteSlice | Type::BoolSlice | Type::StrSlice => {
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new("native IR: slice index must be int"));
                        }
                        match sty {
                            Type::IntSlice => self.emit(Inst::SliceIndex {
                                out,
                                slice,
                                index: idx,
                            }),
                            Type::FloatSlice => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_float_slice_get_ptr".into(),
                                args: vec![slice, idx],
                                ret: Some(Type::F64),
                            }),
                            Type::ByteSlice => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_byte_slice_get_ptr".into(),
                                args: vec![slice, idx],
                                ret: Some(Type::I64),
                            }),
                            Type::BoolSlice => {
                                self.emit(Inst::Call {
                                    out: Some(out),
                                    function: "mako_native_bool_slice_get_ptr".into(),
                                    args: vec![slice, idx],
                                    ret: Some(Type::I64),
                                });
                                // Coerce 0/1 i64 → bool for []bool indexing.
                                let b = self.value();
                                self.emit(Inst::Cast {
                                    out: b,
                                    value: out,
                                    from: Type::I64,
                                    to: Type::I1,
                                });
                                if owned {
                                    self.emit_drop(slice, sty);
                                }
                                return Ok((b, Type::I1, false));
                            }
                            _ => self.emit(Inst::StrSliceIndex {
                                out,
                                slice,
                                index: idx,
                            }),
                        }
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        Ok((
                            out,
                            match sty {
                                Type::IntSlice | Type::ByteSlice => Type::I64,
                                Type::FloatSlice => Type::F64,
                                _ => Type::Str,
                            },
                            sty == Type::StrSlice,
                        ))
                    }
                    Type::StructSlice(sid) => {
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new("native IR: []struct index must be int"));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_ptr_slice_get".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::Struct(sid)),
                        });
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        // Borrowed element (slice owns the pointer).
                        Ok((out, Type::Struct(sid), false))
                    }
                    Type::PtrSlice(vk) => {
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new("native IR: ptr-slice index must be int"));
                        }
                        let ety = vk.to_type();
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_ptr_slice_get".into(),
                            args: vec![slice, idx],
                            ret: Some(ety),
                        });
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        // Borrowed element (outer slice owns the pointer).
                        Ok((out, ety, false))
                    }
                    Type::MapII => {
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new("native IR: map[int] key must be int"));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_ii_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::I64),
                        });
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        Ok((out, Type::I64, false))
                    }
                    Type::MapIPtr(vk) => {
                        let struct_key_id = vk.struct_key_id().or_else(|| {
                            if let Type::Struct(sid) = ity {
                                if !self.structs.is_enum(sid) {
                                    return Some(sid);
                                }
                            }
                            None
                        });
                        if let Some(sid) = struct_key_id {
                            if ity != Type::Struct(sid) {
                                return Err(IrError::new(
                                    "native IR: map[Struct] key type mismatch",
                                ));
                            }
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_map_struct_key_get_ptr".into(),
                                args: vec![slice, idx, nf, sm, nm, nfp, nsp],
                                ret: Some(Type::I64),
                            });
                            if iowned {
                                self.emit_drop(idx, Type::Struct(sid));
                            }
                        } else {
                            let key = self.map_i_key(idx, ity, iowned)?;
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_map_ii_get_ptr".into(),
                                args: vec![slice, key],
                                ret: Some(Type::I64),
                            });
                        }
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        let vty = vk.to_type();
                        if vty == Type::F64 {
                            // Float stored as i64 bit pattern.
                            let f = self.value();
                            self.emit(Inst::Call {
                                out: Some(f),
                                function: "mako_native_bits_to_f64".into(),
                                args: vec![out],
                                ret: Some(Type::F64),
                            });
                            return Ok((f, Type::F64, false));
                        }
                        let fixed = self.null_to_empty_heap(out, vty)?;
                        // Borrowed view into map storage (map still owns the value).
                        // Callers that need ownership must clone explicitly.
                        Ok((fixed, vty, false))
                    }
                    Type::MapSI => {
                        if ity != Type::Str {
                            return Err(IrError::new(
                                "native IR: map[string] key must be string",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_si_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::I64),
                        });
                        if iowned {
                            self.emit(Inst::DropString { value: idx });
                        }
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        Ok((out, Type::I64, false))
                    }
                    Type::MapSPtr(vk) => {
                        if ity != Type::Str {
                            return Err(IrError::new(
                                "native IR: map[string] key must be string",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_si_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::I64),
                        });
                        if iowned {
                            self.emit(Inst::DropString { value: idx });
                        }
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        let vty = vk.to_type();
                        if vty == Type::F64 {
                            let f = self.value();
                            self.emit(Inst::Call {
                                out: Some(f),
                                function: "mako_native_bits_to_f64".into(),
                                args: vec![out],
                                ret: Some(Type::F64),
                            });
                            return Ok((f, Type::F64, false));
                        }
                        // Missing key → empty slice/null instead of raw null pointer.
                        let fixed = self.null_to_empty_heap(out, vty)?;
                        // Borrowed view into map storage (map still owns the value).
                        // Callers that need ownership must clone explicitly.
                        Ok((fixed, vty, false))
                    }
                    Type::MapSS => {
                        if ity != Type::Str {
                            return Err(IrError::new(
                                "native IR: map[string]string key must be string",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_ss_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::Str),
                        });
                        if iowned {
                            self.emit(Inst::DropString { value: idx });
                        }
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        // get returns an owned clone of the value string.
                        Ok((out, Type::Str, true))
                    }
                    Type::MapIF => {
                        if ity != Type::I64 || iowned {
                            return Err(IrError::new(
                                "native IR: map[int]float key must be int",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_if_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::F64),
                        });
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        Ok((out, Type::F64, false))
                    }
                    Type::MapFI => {
                        if ity != Type::F64 || iowned {
                            return Err(IrError::new(
                                "native IR: map[float]int key must be float",
                            ));
                        }
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_fi_get_ptr".into(),
                            args: vec![slice, idx],
                            ret: Some(Type::I64),
                        });
                        if owned {
                            self.emit_drop(slice, sty);
                        }
                        Ok((out, Type::I64, false))
                    }
                    other => Err(IrError::new(format!(
                        "native IR: indexing requires slice or map (got {other:?})"
                    ))),
                }
            }
            Expr::Slice {
                base,
                low,
                high,
                max,
            } => {
                let (slice, sty, owned) = self.lower_expr(base)?;
                if !matches!(
                    sty,
                    Type::Str
                        | Type::IntSlice
                        | Type::FloatSlice
                        | Type::ByteSlice
                        | Type::BoolSlice
                        | Type::StrSlice
                        | Type::StructSlice(_)
                        | Type::PtrSlice(_)
                ) {
                    return Err(IrError::new("native IR: slicing requires string or slice"));
                }
                let make_bound =
                    |this: &mut Self, e: Option<&Expr>, default: i64| -> Result<Value, IrError> {
                        if let Some(e) = e {
                            let (v, t, o) = this.lower_expr(e)?;
                            if t != Type::I64 || o {
                                return Err(IrError::new("native IR: slice bound must be int"));
                            }
                            Ok(v)
                        } else {
                            Ok(this.const_int(default, Type::I64))
                        }
                    };
                let lowv = make_bound(self, low.as_deref(), 0)?;
                // For string byte-slices, omitted high means len(s); pass -1 and
                // let the runtime clamp (same as []int).
                let highv = make_bound(self, high.as_deref(), -1)?;
                let maxv = if let Some(m) = max {
                    Some(make_bound(self, Some(m), -1)?)
                } else {
                    None
                };
                let out = self.value();
                match sty {
                    Type::Str => {
                        // Go-like substring by byte offsets [low:high); owned copy.
                        let high_arg = if high.is_none() {
                            // high default: len(s)
                            let len = self.value();
                            self.emit(Inst::StringLen {
                                out: len,
                                value: slice,
                            });
                            len
                        } else {
                            highv
                        };
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_str_slice_ptr".into(),
                            args: vec![slice, lowv, high_arg],
                            ret: Some(Type::Str),
                        });
                        if owned {
                            self.emit(Inst::DropString { value: slice });
                        }
                        return Ok((out, Type::Str, true));
                    }
                    Type::IntSlice => self.emit(Inst::SliceSlice {
                        out,
                        slice,
                        low: lowv,
                        high: highv,
                        max: maxv,
                    }),
                    Type::FloatSlice => {
                        let max_arg = maxv.unwrap_or_else(|| self.const_int(-1, Type::I64));
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_float_slice_slice_ptr".into(),
                            args: vec![slice, lowv, highv, max_arg],
                            ret: Some(Type::FloatSlice),
                        });
                    }
                    Type::ByteSlice | Type::BoolSlice => {
                        let max_arg = maxv.unwrap_or_else(|| self.const_int(-1, Type::I64));
                        // []bool reuses the byte-slice header ABI.
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_byte_slice_slice_ptr".into(),
                            args: vec![slice, lowv, highv, max_arg],
                            ret: Some(sty),
                        });
                    }
                    Type::StructSlice(_) | Type::PtrSlice(_) => {
                        let max_arg = maxv.unwrap_or_else(|| self.const_int(-1, Type::I64));
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_ptr_slice_slice".into(),
                            args: vec![slice, lowv, highv, max_arg],
                            ret: Some(sty),
                        });
                    }
                    _ => self.emit(Inst::StrSliceSlice {
                        out,
                        slice,
                        low: lowv,
                        high: highv,
                        max: maxv,
                    }),
                }
                if owned {
                    let clone = self.value();
                    match sty {
                        Type::IntSlice => {
                            self.emit(Inst::SliceClone {
                                out: clone,
                                slice: out,
                            });
                            self.emit(Inst::DropSlice { value: slice });
                            self.emit(Inst::DropSlice { value: out });
                            return Ok((clone, Type::IntSlice, true));
                        }
                        Type::FloatSlice => {
                            self.emit(Inst::Call {
                                out: Some(clone),
                                function: "mako_native_float_slice_clone_ptr".into(),
                                args: vec![out],
                                ret: Some(Type::FloatSlice),
                            });
                            self.emit_drop(slice, sty);
                            self.emit_drop(out, sty);
                            return Ok((clone, Type::FloatSlice, true));
                        }
                        Type::ByteSlice | Type::BoolSlice => {
                            self.emit(Inst::Call {
                                out: Some(clone),
                                function: "mako_native_byte_slice_clone_ptr".into(),
                                args: vec![out],
                                ret: Some(sty),
                            });
                            self.emit_drop(slice, sty);
                            self.emit_drop(out, sty);
                            return Ok((clone, sty, true));
                        }
                        Type::StructSlice(_) | Type::PtrSlice(_) => {
                            self.emit(Inst::Call {
                                out: Some(clone),
                                function: "mako_native_ptr_slice_clone".into(),
                                args: vec![out],
                                ret: Some(sty),
                            });
                            self.emit_drop(slice, sty);
                            self.emit_drop(out, sty);
                            return Ok((clone, sty, true));
                        }
                        _ => {
                            self.emit(Inst::StrSliceClone {
                                out: clone,
                                slice: out,
                            });
                            self.emit(Inst::DropStrSlice { value: slice });
                            self.emit(Inst::DropStrSlice { value: out });
                            return Ok((clone, Type::StrSlice, true));
                        }
                    }
                }
                // Borrowed base → non-owning *view* (C sets owned=0). IR marks
                // it unowned so:
                //   let t = s[a:b]  // shared writes into s (Go-like)
                //   s = s[a:b]      // Assign force-clones before dropping s
                // Marking the view owned would free s's buffer under the view.
                Ok((out, sty, false))
            }
            Expr::Make { ty, len, cap } => {
                // Maps / struct slices / scalar slices
                if let Ok(made_ty) = resolve_type(ty, self.structs) {
                    if matches!(
                        made_ty,
                        Type::MapII
                            | Type::MapSI
                            | Type::MapSS
                            | Type::MapIF
                            | Type::MapFI
                            | Type::MapIPtr(_)
                            | Type::MapSPtr(_)
                    ) {
                        let hint = if let Some(h) = len {
                            let (v, t, o) = self.lower_expr(h)?;
                            if t != Type::I64 || o {
                                return Err(IrError::new("native IR: map make hint must int"));
                            }
                            v
                        } else {
                            self.const_int(0, Type::I64)
                        };
                        let out = self.value();
                        // Pointer-value maps share the int/string-key runtime tables.
                        let fname = match made_ty {
                            Type::MapII | Type::MapIPtr(_) => "mako_native_map_ii_make_ptr",
                            Type::MapSI | Type::MapSPtr(_) => "mako_native_map_si_make_ptr",
                            Type::MapSS => "mako_native_map_ss_make_ptr",
                            Type::MapIF => "mako_native_map_if_make_ptr",
                            Type::MapFI => "mako_native_map_fi_make_ptr",
                            _ => unreachable!(),
                        };
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: fname.into(),
                            args: vec![hint],
                            ret: Some(made_ty),
                        });
                        return Ok((out, made_ty, true));
                    }
                    if matches!(
                        made_ty,
                        Type::IntSlice
                            | Type::FloatSlice
                            | Type::ByteSlice
                            | Type::BoolSlice
                            | Type::StrSlice
                            | Type::StructSlice(_)
                            | Type::PtrSlice(_)
                    ) {
                        let len = len
                            .as_deref()
                            .ok_or_else(|| IrError::new("native IR: make requires length"))?;
                        let (lv, lt, lo) = self.lower_expr(len)?;
                        if lt != Type::I64 || lo {
                            return Err(IrError::new("native IR: make length must int"));
                        }
                        let cv = if let Some(c) = cap {
                            let (v, t, o) = self.lower_expr(c)?;
                            if t != Type::I64 || o {
                                return Err(IrError::new("native IR: make capacity must int"));
                            }
                            v
                        } else {
                            lv
                        };
                        let out = self.value();
                        match made_ty {
                            Type::IntSlice => self.emit(Inst::SliceMake {
                                out,
                                len: lv,
                                cap: Some(cv),
                            }),
                            Type::FloatSlice => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_float_slice_make_ptr".into(),
                                args: vec![lv, cv],
                                ret: Some(Type::FloatSlice),
                            }),
                            Type::ByteSlice => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_byte_slice_make_ptr".into(),
                                args: vec![lv, cv],
                                ret: Some(Type::ByteSlice),
                            }),
                            Type::BoolSlice => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_bool_slice_make_ptr".into(),
                                args: vec![lv, cv],
                                ret: Some(Type::BoolSlice),
                            }),
                            Type::StructSlice(_) | Type::PtrSlice(_) => self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_ptr_slice_make".into(),
                                args: vec![lv, cv],
                                ret: Some(made_ty),
                            }),
                            _ => self.emit(Inst::StrSliceMake {
                                out,
                                len: lv,
                                cap: Some(cv),
                            }),
                        }
                        return Ok((out, made_ty, true));
                    }
                    if made_ty == Type::ChanI
                        || made_ty == Type::ChanS
                        || made_ty == Type::ChanF
                        || matches!(made_ty, Type::ChanP(_))
                    {
                        let len = len
                            .as_deref()
                            .ok_or_else(|| IrError::new("native IR: make(chan) requires capacity"))?;
                        let (lv, lt, lo) = self.lower_expr(len)?;
                        if lt != Type::I64 || lo {
                            return Err(IrError::new("native IR: make(chan) capacity must int"));
                        }
                        let out = self.value();
                        // ChanP reuses the int mailbox (payload = bitcast pointer/i64).
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_chan_new".into(),
                            args: vec![lv],
                            ret: Some(made_ty),
                        });
                        return Ok((out, made_ty, true));
                    }
                    if matches!(made_ty, Type::MapIPtr(_) | Type::MapSPtr(_)) {
                        let hint = if let Some(h) = len {
                            let (v, t, o) = self.lower_expr(h)?;
                            if t != Type::I64 || o {
                                return Err(IrError::new("native IR: map make hint must int"));
                            }
                            v
                        } else {
                            self.const_int(0, Type::I64)
                        };
                        let out = self.value();
                        // Pointer-value maps reuse int-value map runtime (values as i64 ptrs).
                        let fname = if matches!(made_ty, Type::MapIPtr(_)) {
                            "mako_native_map_ii_make_ptr"
                        } else {
                            "mako_native_map_si_make_ptr"
                        };
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: fname.into(),
                            args: vec![hint],
                            ret: Some(made_ty),
                        });
                        return Ok((out, made_ty, true));
                    }
                }
                Err(IrError::new("native IR: unsupported make type"))
            }
            Expr::ChanOpen { elem, cap } => {
                let cty = resolve_type(
                    &TypeExpr::Generic("chan".into(), vec![elem.clone()]),
                    self.structs,
                )?;
                let (cv, ct, co) = self.lower_expr(cap)?;
                if ct != Type::I64 || co {
                    return Err(IrError::new("native IR: chan_open capacity must int"));
                }
                let out = self.value();
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_chan_new".into(),
                    args: vec![cv],
                    ret: Some(cty),
                });
                Ok((out, cty, true))
            }
            Expr::Lambda { params, body } => self.lower_lambda(params, body),
            Expr::Unary { op, expr } => {
                let (value, ty, owned) = self.lower_expr(expr)?;
                if owned {
                    return Err(IrError::new("native IR: unary operation on owned string"));
                }
                let out = self.value();
                self.emit(Inst::Unary {
                    out,
                    op: *op,
                    value,
                    ty,
                });
                Ok((out, ty, false))
            }
            Expr::Call { callee, args } => {
                // Indirect call through a first-class function pointer.
                if !matches!(callee.as_ref(), Expr::Ident(_)) {
                    return self.lower_indirect_call(callee, args);
                }
                let Expr::Ident(function) = callee.as_ref() else {
                    return Err(IrError::new("native IR: indirect call"));
                };
                // Local fn pointer: `f(x)` where `f: fn(int) -> int`.
                if let Some((slot, lty)) = self.locals.get(function).copied() {
                    if lty == Type::FnPtr {
                        let fp = self.value();
                        self.emit(Inst::Load {
                            out: fp,
                            ptr: slot,
                            ty: Type::FnPtr,
                        });
                        return self.lower_indirect_call_fp(fp, args);
                    }
                }
                // Enum variant construction with a payload: `Circle(5)`, `Ok(v)`.
                // Ok/Err/Some may have multiple monomorphs — pick by payload type.
                if !self.structs.variants_named(function).is_empty() {
                    return self.build_variant_by_name(function, args);
                }
                // `error("msg")` → Err("msg") for Result[int,string].
                if function == "error" && args.len() == 1 {
                    if !self.structs.variants_named("Err").is_empty() {
                        return self.build_variant_by_name("Err", args);
                    }
                }
                if function == "len" && args.len() == 1 {
                    let (s, t, o) = self.lower_expr(&args[0])?;
                    let out = self.value();
                    match t {
                        Type::IntSlice => self.emit(Inst::SliceLen { out, slice: s }),
                        Type::FloatSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_float_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::ByteSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_byte_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::BoolSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_bool_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::StructSlice(_) => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_ptr_slice_len".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::StrSlice => self.emit(Inst::StrSliceLen { out, slice: s }),
                        Type::Str => self.emit(Inst::StringLen { out, value: s }),
                        Type::MapII | Type::MapIPtr(_) => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_ii_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::MapSI | Type::MapSPtr(_) => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_si_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::MapSS => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_ss_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::MapIF => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_if_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::MapFI => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_map_fi_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::PtrSlice(_) => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_ptr_slice_len".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        other => {
                            return Err(IrError::new(format!(
                                "native IR: len expects string, slice, or map (got {other:?})"
                            )));
                        }
                    }
                    if o {
                        self.emit_drop(s, t);
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "cap" && args.len() == 1 {
                    let (s, t, o) = self.lower_expr(&args[0])?;
                    let out = self.value();
                    match t {
                        Type::IntSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_int_slice_cap_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::FloatSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_float_slice_cap_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::ByteSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_byte_slice_cap_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::BoolSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_bool_slice_cap_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        Type::StrSlice => self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_string_slice_cap_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        }),
                        _ => {
                            return Err(IrError::new(
                                "native IR: cap expects a slice type",
                            ));
                        }
                    }
                    if o {
                        self.emit_drop(s, t);
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "has" && args.len() == 2 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    let (k, kt, ko) = self.lower_expr(&args[1])?;
                    let raw = self.value();
                    match mt {
                        // Content-keyed maps: StructKey* value kinds, or plain
                        // non-enum struct keys. Payload enums on StructKey* maps
                        // must content-match (tag-only would make Blue(1)==Blue(99)).
                        // Enum keys on non-StructKey maps (e.g. map[Color]chan)
                        // still use tag via map_i_key.
                        Type::MapIPtr(vk)
                            if matches!(kt, Type::Struct(_)) && vk.struct_key_id().is_some() =>
                        {
                            let Type::Struct(sid) = kt else { unreachable!() };
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_struct_key_has_ptr".into(),
                                args: vec![m, k, nf, sm, nm, nfp, nsp],
                                ret: Some(Type::I64),
                            });
                            if ko {
                                self.emit_drop(k, Type::Struct(sid));
                            }
                        }
                        Type::MapIPtr(_) | Type::MapII
                            if matches!(kt, Type::Struct(sid) if !self.structs.is_enum(sid)) =>
                        {
                            let Type::Struct(sid) = kt else { unreachable!() };
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_struct_key_has_ptr".into(),
                                args: vec![m, k, nf, sm, nm, nfp, nsp],
                                ret: Some(Type::I64),
                            });
                            if ko {
                                self.emit_drop(k, Type::Struct(sid));
                            }
                        }
                        Type::MapII | Type::MapIPtr(_)
                            if kt == Type::I64
                                || kt == Type::I1
                                || kt == Type::F64
                                || matches!(kt, Type::Struct(_)) =>
                        {
                            let key = self.map_i_key(k, kt, ko)?;
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_ii_has_ptr".into(),
                                args: vec![m, key],
                                ret: Some(Type::I64),
                            });
                        }
                        Type::MapSI | Type::MapSPtr(_) if kt == Type::Str => {
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_si_has_ptr".into(),
                                args: vec![m, k],
                                ret: Some(Type::I64),
                            });
                            if ko {
                                self.emit(Inst::DropString { value: k });
                            }
                        }
                        Type::MapSS if kt == Type::Str => {
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_ss_has_ptr".into(),
                                args: vec![m, k],
                                ret: Some(Type::I64),
                            });
                            if ko {
                                self.emit(Inst::DropString { value: k });
                            }
                        }
                        Type::MapIF if kt == Type::I64 => {
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_if_has_ptr".into(),
                                args: vec![m, k],
                                ret: Some(Type::I64),
                            });
                        }
                        Type::MapFI if kt == Type::F64 => {
                            self.emit(Inst::Call {
                                out: Some(raw),
                                function: "mako_native_map_fi_has_ptr".into(),
                                args: vec![m, k],
                                ret: Some(Type::I64),
                            });
                        }
                        _ => {
                            return Err(IrError::new("native IR: has expects map + key"));
                        }
                    }
                    if mo {
                        self.emit_drop(m, mt);
                    }
                    let out = self.value();
                    self.emit(Inst::Cast {
                        out,
                        value: raw,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    return Ok((out, Type::I1, false));
                }
                if function == "delete" && args.len() == 2 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    if mo {
                        return Err(IrError::new(
                            "native IR: delete requires a borrowed map local",
                        ));
                    }
                    let (k, kt, ko) = self.lower_expr(&args[1])?;
                    match mt {
                        Type::MapIPtr(vk)
                            if matches!(kt, Type::Struct(_)) && vk.struct_key_id().is_some() =>
                        {
                            let Type::Struct(sid) = kt else { unreachable!() };
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_struct_key_delete_ptr".into(),
                                args: vec![m, k, nf, sm, nm, nfp, nsp],
                                ret: None,
                            });
                            if ko {
                                self.emit_drop(k, Type::Struct(sid));
                            }
                        }
                        Type::MapIPtr(_) | Type::MapII
                            if matches!(kt, Type::Struct(sid) if !self.structs.is_enum(sid)) =>
                        {
                            let Type::Struct(sid) = kt else { unreachable!() };
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_struct_key_delete_ptr".into(),
                                args: vec![m, k, nf, sm, nm, nfp, nsp],
                                ret: None,
                            });
                            if ko {
                                self.emit_drop(k, Type::Struct(sid));
                            }
                        }
                        Type::MapII | Type::MapIPtr(_)
                            if kt == Type::I64
                                || kt == Type::I1
                                || kt == Type::F64
                                || matches!(kt, Type::Struct(_)) =>
                        {
                            let key = self.map_i_key(k, kt, ko)?;
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_ii_delete_ptr".into(),
                                args: vec![m, key],
                                ret: None,
                            });
                        }
                        Type::MapSI | Type::MapSPtr(_) if kt == Type::Str => {
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_si_delete_ptr".into(),
                                args: vec![m, k],
                                ret: None,
                            });
                            if ko {
                                self.emit(Inst::DropString { value: k });
                            }
                        }
                        Type::MapSS if kt == Type::Str => {
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_ss_delete_ptr".into(),
                                args: vec![m, k],
                                ret: None,
                            });
                            if ko {
                                self.emit(Inst::DropString { value: k });
                            }
                        }
                        Type::MapIF if kt == Type::I64 => {
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_if_delete_ptr".into(),
                                args: vec![m, k],
                                ret: None,
                            });
                        }
                        Type::MapFI if kt == Type::F64 => {
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_map_fi_delete_ptr".into(),
                                args: vec![m, k],
                                ret: None,
                            });
                        }
                        _ => {
                            return Err(IrError::new("native IR: delete expects map + key"));
                        }
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if (function == "format_int"
                    || function == "format_int_dec"
                    || function == "int_to_string")
                    && args.len() == 1
                {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        return Err(IrError::new("native IR: format_int expects int"));
                    }
                    let out = self.value();
                    self.emit(Inst::IntToString { out, value: v });
                    return Ok((out, Type::Str, true));
                }
                // Go-like copy(dst, src) → count of elements copied.
                if function == "copy" && args.len() == 2 {
                    let (dst, dt, do_) = self.lower_expr(&args[0])?;
                    if do_ {
                        return Err(IrError::new(
                            "native IR: copy requires a borrowed destination local",
                        ));
                    }
                    let (src, st, so) = self.lower_expr(&args[1])?;
                    if dt != st {
                        return Err(IrError::new(
                            "native IR: copy destination and source types must match",
                        ));
                    }
                    let out = self.value();
                    let fname = match dt {
                        Type::IntSlice => "mako_native_int_slice_copy_ptr",
                        Type::FloatSlice => "mako_native_float_slice_copy_ptr",
                        Type::ByteSlice => "mako_native_byte_slice_copy_ptr",
                        Type::BoolSlice => "mako_native_bool_slice_copy_ptr",
                        _ => {
                            return Err(IrError::new(
                                "native IR: copy supports []int, []float, []byte, []bool",
                            ));
                        }
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![dst, src],
                        ret: Some(Type::I64),
                    });
                    if so {
                        self.emit_drop(src, st);
                    }
                    return Ok((out, Type::I64, false));
                }
                // Go-style integer-family casts. The native backend collapses
                // int/int64/int32/… to i64; these are no-ops (or truncate via
                // mask for the smaller signed widths when needed later).
                if matches!(
                    function.as_str(),
                    "int" | "int64" | "int32" | "int16" | "int8" | "uint64" | "uint32"
                        | "uint16" | "uint8" | "byte" | "rune"
                ) && args.len() == 1
                {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    match ty {
                        Type::I64 => {
                            if owned {
                                return Err(IrError::new(
                                    "native IR: integer cast of owned value",
                                ));
                            }
                            // Truncate narrow destinations so print/arith match
                            // the C backend's intN_t narrowing.
                            let out = match function.as_str() {
                                "int8" => {
                                    let m = self.const_int(0xff, Type::I64);
                                    let sign = self.const_int(0x80, Type::I64);
                                    let masked = self.value();
                                    self.emit(Inst::Binary {
                                        out: masked,
                                        op: BinOp::BitAnd,
                                        left: v,
                                        right: m,
                                        ty: Type::I64,
                                    });
                                    // Sign-extend from i8: (x ^ 0x80) - 0x80
                                    let xored = self.value();
                                    self.emit(Inst::Binary {
                                        out: xored,
                                        op: BinOp::BitXor,
                                        left: masked,
                                        right: sign,
                                        ty: Type::I64,
                                    });
                                    let se = self.value();
                                    self.emit(Inst::Binary {
                                        out: se,
                                        op: BinOp::Sub,
                                        left: xored,
                                        right: sign,
                                        ty: Type::I64,
                                    });
                                    se
                                }
                                "int32" => {
                                    let m = self.const_int(0xffff_ffff, Type::I64);
                                    let sign = self.const_int(0x8000_0000, Type::I64);
                                    let masked = self.value();
                                    self.emit(Inst::Binary {
                                        out: masked,
                                        op: BinOp::BitAnd,
                                        left: v,
                                        right: m,
                                        ty: Type::I64,
                                    });
                                    let xored = self.value();
                                    self.emit(Inst::Binary {
                                        out: xored,
                                        op: BinOp::BitXor,
                                        left: masked,
                                        right: sign,
                                        ty: Type::I64,
                                    });
                                    let se = self.value();
                                    self.emit(Inst::Binary {
                                        out: se,
                                        op: BinOp::Sub,
                                        left: xored,
                                        right: sign,
                                        ty: Type::I64,
                                    });
                                    se
                                }
                                "byte" | "uint8" => {
                                    let m = self.const_int(0xff, Type::I64);
                                    let out = self.value();
                                    self.emit(Inst::Binary {
                                        out,
                                        op: BinOp::BitAnd,
                                        left: v,
                                        right: m,
                                        ty: Type::I64,
                                    });
                                    out
                                }
                                _ => v, // int/int64/uint64/rune: full width
                            };
                            return Ok((out, Type::I64, false));
                        }
                        Type::F64 => {
                            let out = self.value();
                            self.emit(Inst::Cast {
                                out,
                                value: v,
                                from: Type::F64,
                                to: Type::I64,
                            });
                            return Ok((out, Type::I64, false));
                        }
                        _ => {
                            return Err(IrError::new(format!(
                                "native IR: `{function}` cast expects int/float"
                            )));
                        }
                    }
                }
                if (function == "float" || function == "float64") && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if owned {
                        return Err(IrError::new("native IR: float cast of owned value"));
                    }
                    if ty == Type::F64 {
                        return Ok((v, Type::F64, false));
                    }
                    if ty == Type::I64 {
                        let out = self.value();
                        self.emit(Inst::Cast {
                            out,
                            value: v,
                            from: Type::I64,
                            to: Type::F64,
                        });
                        return Ok((out, Type::F64, false));
                    }
                    return Err(IrError::new("native IR: float cast expects int/float"));
                }
                if function == "string" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    match ty {
                        Type::Str => return Ok((v, Type::Str, owned)),
                        Type::I64 => {
                            let out = self.value();
                            self.emit(Inst::IntToString { out, value: v });
                            return Ok((out, Type::Str, true));
                        }
                        Type::I1 => {
                            let out = self.value();
                            self.emit(Inst::BoolToString { out, value: v });
                            return Ok((out, Type::Str, true));
                        }
                        Type::ByteSlice => {
                            let out = self.value();
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_string_from_bytes_ptr".into(),
                                args: vec![v],
                                ret: Some(Type::Str),
                            });
                            if owned {
                                self.emit_drop(v, Type::ByteSlice);
                            }
                            return Ok((out, Type::Str, true));
                        }
                        _ => {
                            return Err(IrError::new(
                                "native IR: string() expects int, bool, string, or []byte",
                            ));
                        }
                    }
                }
                if function == "bytes" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: bytes() expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_bytes_from_string_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::ByteSlice),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::ByteSlice, true));
                }
                if function == "sort_ints" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::IntSlice {
                        return Err(IrError::new("native IR: sort_ints expects []int"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_sort_ints_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::IntSlice),
                    });
                    if owned {
                        self.emit_drop(v, Type::IntSlice);
                    }
                    return Ok((out, Type::IntSlice, true));
                }
                if function == "sort_strings" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::StrSlice {
                        return Err(IrError::new("native IR: sort_strings expects []string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_sort_strings_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::StrSlice),
                    });
                    if owned {
                        self.emit_drop(v, Type::StrSlice);
                    }
                    return Ok((out, Type::StrSlice, true));
                }
                if function == "rune_count" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: rune_count expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_rune_count_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "result_unwrap_or" && args.len() == 2 {
                    return self.lower_result_unwrap_or(&args[0], &args[1]);
                }
                if function == "dbg" && args.len() == 1 {
                    // Seed: identity (no file:line stderr yet).
                    return self.lower_expr(&args[0]);
                }
                if function == "black_box" && args.len() == 1 {
                    // Real optimizer barrier (asm volatile in native_bridge).
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        // Seed: int only (bench kernels).
                        return Ok((v, ty, owned));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_black_box_i64".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                if function == "dbg_str" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: dbg_str expects string"));
                    }
                    return Ok((v, Type::Str, owned));
                }
                if function == "assert_eq_str" && args.len() == 2 {
                    let (a, at, ao) = self.lower_expr(&args[0])?;
                    let (b, bt, bo) = self.lower_expr(&args[1])?;
                    if at != Type::Str || bt != Type::Str {
                        return Err(IrError::new("native IR: assert_eq_str expects strings"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_assert_eq_str_ptr".into(),
                        args: vec![a, b],
                        ret: None,
                    });
                    if ao {
                        self.emit_drop(a, Type::Str);
                    }
                    if bo {
                        self.emit_drop(b, Type::Str);
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "assert_eq" && args.len() == 2 {
                    let (mut a, mut at, ao) = self.lower_expr(&args[0])?;
                    let (mut b, mut bt, bo) = self.lower_expr(&args[1])?;
                    // Coerce bool → int for `assert_eq(bytes_is_view(x), 1)`.
                    if at == Type::I1 {
                        let c = self.value();
                        self.emit(Inst::Cast {
                            out: c,
                            value: a,
                            from: Type::I1,
                            to: Type::I64,
                        });
                        a = c;
                        at = Type::I64;
                    }
                    if bt == Type::I1 {
                        let c = self.value();
                        self.emit(Inst::Cast {
                            out: c,
                            value: b,
                            from: Type::I1,
                            to: Type::I64,
                        });
                        b = c;
                        bt = Type::I64;
                    }
                    if at != Type::I64 || bt != Type::I64 || ao || bo {
                        return Err(IrError::new("native IR: assert_eq expects ints"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_assert_eq".into(),
                        args: vec![a, b],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "str_eq" && args.len() == 2 {
                    let (a, at, ao) = self.lower_expr(&args[0])?;
                    let (b, bt, bo) = self.lower_expr(&args[1])?;
                    if at != Type::Str || bt != Type::Str {
                        return Err(IrError::new("native IR: str_eq expects strings"));
                    }
                    let out = self.value();
                    self.emit(Inst::StringEqual {
                        out,
                        left: a,
                        right: b,
                        negated: false,
                    });
                    if ao {
                        self.emit(Inst::DropString { value: a });
                    }
                    if bo {
                        self.emit(Inst::DropString { value: b });
                    }
                    return Ok((out, Type::I1, false));
                }
                if function == "str_contains" && args.len() == 2 {
                    let (a, at, ao) = self.lower_expr(&args[0])?;
                    let (b, bt, bo) = self.lower_expr(&args[1])?;
                    if at != Type::Str || bt != Type::Str {
                        return Err(IrError::new("native IR: str_contains expects strings"));
                    }
                    let raw = self.value();
                    self.emit(Inst::Call {
                        out: Some(raw),
                        function: "mako_native_str_contains_ptr".into(),
                        args: vec![a, b],
                        ret: Some(Type::I64),
                    });
                    let out = self.value();
                    self.emit(Inst::Cast {
                        out,
                        value: raw,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    if ao {
                        self.emit(Inst::DropString { value: a });
                    }
                    if bo {
                        self.emit(Inst::DropString { value: b });
                    }
                    return Ok((out, Type::I1, false));
                }
                if function == "buf_put" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::ByteSlice {
                        return Err(IrError::new("native IR: buf_put expects []byte"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_buf_put_ptr".into(),
                        args: vec![v],
                        ret: None,
                    });
                    // C frees the header; if it was a named local, mark unowned.
                    if owned {
                        if let Expr::Ident(name) = &args[0] {
                            self.heap_owned.insert(name.clone(), false);
                        }
                    } else if let Expr::Ident(name) = &args[0] {
                        self.heap_owned.insert(name.clone(), false);
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                // Handle free/close that release Opaque ownership (avoid double-free via opaque_drop).
                if matches!(
                    function.as_str(),
                    "btree_free"
                        | "pbtree_free"
                        | "lsm_free"
                        | "wal_close"
                        | "bloom_free"
                        | "pman_close"
                        | "mvcc_free"
                        | "multimap_free"
                        | "rollback_free"
                        | "store_free"
                        | "sst_free"
                        | "kv_cache_free"
                        | "gfx_window_close"
                        | "domain_reg_del"
                ) && args.len() == 1
                {
                    if let Some(result) = self.lower_runtime_builtin(function, args)? {
                        // Consume the handle: bare local no longer owns it.
                        if let Expr::Ident(name) = &args[0] {
                            self.heap_owned.insert(name.clone(), false);
                        }
                        // Field free: ownership lived in a bare local moved into the
                        // struct, or is solely via free — DropStruct skips Opaque.
                        return Ok(result);
                    }
                }
                if function == "assert" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    // Accept bool or int-ish truthiness (stdlib helpers often return i64 0/1).
                    let as_i64 = match ty {
                        Type::I1 if !owned => {
                            let w = self.value();
                            self.emit(Inst::Cast {
                                out: w,
                                value: v,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            w
                        }
                        Type::I64 if !owned => v,
                        Type::I32 if !owned => {
                            let w = self.value();
                            self.emit(Inst::Cast {
                                out: w,
                                value: v,
                                from: Type::I32,
                                to: Type::I64,
                            });
                            w
                        }
                        _ => {
                            return Err(IrError::new("native IR: assert expects bool"));
                        }
                    };
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_assert".into(),
                        args: vec![as_i64],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "reflect_value_of" && args.len() == 1 {
                    return self.lower_reflect_value_of(&args[0]);
                }
                if function == "wrap_err" && args.len() == 2 {
                    return self.lower_wrap_err(&args[0], &args[1]);
                }
                // Alias of wrap_err (same RT semantics).
                if function == "error_context" && args.len() == 2 {
                    return self.lower_wrap_err(&args[0], &args[1]);
                }
                if function == "error_join" && args.len() == 2 {
                    return self.lower_error_join(&args[0], &args[1]);
                }
                if function == "parse_bool" && args.len() == 1 {
                    return self.lower_parse_bool(&args[0]);
                }
                if function == "errorf" && args.len() == 2 {
                    return self.lower_errorf(&args[0], &args[1]);
                }
                if function == "error_tag" && args.len() == 2 {
                    return self.lower_error_tag(&args[0], &args[1]);
                }
                if function == "error_is" && args.len() == 2 {
                    return self.lower_error_is(&args[0], &args[1]);
                }
                if function == "error_string" && args.len() == 1 {
                    return self.lower_error_string(&args[0]);
                }
                if function == "error_unwrap" && args.len() == 1 {
                    return self.lower_error_peel(&args[0], /*root=*/ false);
                }
                if function == "error_root" && args.len() == 1 {
                    return self.lower_error_peel(&args[0], /*root=*/ true);
                }
                if function == "error_as_tag" && args.len() == 1 {
                    return self.lower_error_as_tag(&args[0]);
                }
                if function == "error_has_tag" && args.len() == 2 {
                    return self.lower_error_has_tag(&args[0], &args[1]);
                }
                if function == "arg_get" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        return Err(IrError::new("native IR: arg_get expects int"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_arg_get_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    return Ok((out, Type::Str, true));
                }
                if function == "args" && args.is_empty() {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_args_ptr".into(),
                        args: vec![],
                        ret: Some(Type::StrSlice),
                    });
                    return Ok((out, Type::StrSlice, true));
                }
                if function == "mkdir" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: mkdir expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_mkdir_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "file_exists" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: file_exists expects string"));
                    }
                    let raw = self.value();
                    self.emit(Inst::Call {
                        out: Some(raw),
                        function: "mako_native_file_exists_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    let out = self.value();
                    self.emit(Inst::Cast {
                        out,
                        value: raw,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I1, false));
                }
                if function == "remove_file" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: remove_file expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_remove_file_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "base64_encode" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: base64_encode expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_base64_encode_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::Str, true));
                }
                if function == "base64_decode" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: base64_decode expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_base64_decode_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::Str, true));
                }
                if function == "read_file" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: read_file expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_read_file_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::Str, true));
                }
                if function == "write_file" && args.len() == 2 {
                    let (p, pt, po) = self.lower_expr(&args[0])?;
                    let (c, ct, co) = self.lower_expr(&args[1])?;
                    if pt != Type::Str || ct != Type::Str {
                        return Err(IrError::new("native IR: write_file expects strings"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_write_file_ptr".into(),
                        args: vec![p, c],
                        ret: Some(Type::I64),
                    });
                    if po {
                        self.emit(Inst::DropString { value: p });
                    }
                    if co {
                        self.emit(Inst::DropString { value: c });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "env_get" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: env_get expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_env_get_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::Str, true));
                }
                if function == "env_set" && args.len() == 2 {
                    let (k, kt, ko) = self.lower_expr(&args[0])?;
                    let (v, vt, vo) = self.lower_expr(&args[1])?;
                    if kt != Type::Str || vt != Type::Str {
                        return Err(IrError::new("native IR: env_set expects strings"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_env_set_ptr".into(),
                        args: vec![k, v],
                        ret: Some(Type::I64),
                    });
                    if ko {
                        self.emit(Inst::DropString { value: k });
                    }
                    if vo {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "path_join" && args.len() == 2 {
                    let (a, at, ao) = self.lower_expr(&args[0])?;
                    let (b, bt, bo) = self.lower_expr(&args[1])?;
                    if at != Type::Str || bt != Type::Str {
                        return Err(IrError::new("native IR: path_join expects strings"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_path_join_ptr".into(),
                        args: vec![a, b],
                        ret: Some(Type::Str),
                    });
                    if ao {
                        self.emit(Inst::DropString { value: a });
                    }
                    if bo {
                        self.emit(Inst::DropString { value: b });
                    }
                    return Ok((out, Type::Str, true));
                }
                if function == "now_ms" && args.is_empty() {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_now_ms".into(),
                        args: vec![],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                if function == "now_ns" && args.is_empty() {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_now_ns".into(),
                        args: vec![],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                if function == "format_float" && args.len() == 2 {
                    let (v, vt, vo) = self.lower_expr(&args[0])?;
                    let (p, pt, po) = self.lower_expr(&args[1])?;
                    if vt != Type::F64 || pt != Type::I64 || vo || po {
                        return Err(IrError::new(
                            "native IR: format_float expects (float, int)",
                        ));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_format_float_ptr".into(),
                        args: vec![v, p],
                        ret: Some(Type::Str),
                    });
                    return Ok((out, Type::Str, true));
                }
                if function == "read_dir" && args.len() == 1 {
                    let (p, pt, po) = self.lower_expr(&args[0])?;
                    if pt != Type::Str {
                        return Err(IrError::new("native IR: read_dir expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_read_dir_ptr".into(),
                        args: vec![p],
                        ret: Some(Type::StrSlice),
                    });
                    if po {
                        self.emit(Inst::DropString { value: p });
                    }
                    return Ok((out, Type::StrSlice, true));
                }
                if function == "str_len" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: str_len expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_str_len_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: v });
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "share_int" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        return Err(IrError::new("native IR: share_int expects int"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_share_int".into(),
                        args: vec![v],
                        ret: Some(Type::ShareInt),
                    });
                    return Ok((out, Type::ShareInt, true));
                }
                if function == "share_clone" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::ShareInt {
                        return Err(IrError::new("native IR: share_clone expects ShareInt"));
                    }
                    // Clone bumps RC; the source handle stays live. Drop any
                    // owned temporary after the clone so it is not leaked.
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_share_clone_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::ShareInt),
                    });
                    if owned {
                        self.emit_drop(v, Type::ShareInt);
                    }
                    return Ok((out, Type::ShareInt, true));
                }
                if function == "share_get" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::ShareInt {
                        return Err(IrError::new("native IR: share_get expects ShareInt"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_share_get_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit_drop(v, Type::ShareInt);
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "share_set" && args.len() == 2 {
                    let (s, st, so) = self.lower_expr(&args[0])?;
                    let (v, vt, vo) = self.lower_expr(&args[1])?;
                    if st != Type::ShareInt || vt != Type::I64 || vo {
                        return Err(IrError::new("native IR: share_set expects (ShareInt, int)"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_share_set_ptr".into(),
                        args: vec![s, v],
                        ret: None,
                    });
                    if so {
                        self.emit_drop(s, Type::ShareInt);
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "share_drop" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::ShareInt {
                        return Err(IrError::new("native IR: share_drop expects ShareInt"));
                    }
                    // Explicit drop: consume the handle. If the arg is a bare
                    // local, clear its ownership so end-of-scope does not
                    // double-drop (mirrors C-backend NLL tracking).
                    if let Expr::Ident(name) = &args[0] {
                        if self.heap_owned.get(name).copied().unwrap_or(false) {
                            self.heap_owned.insert(name.clone(), false);
                        }
                    }
                    if owned || matches!(&args[0], Expr::Ident(_)) {
                        // For a local: load already reported owned=false, but
                        // we just transferred ownership above; always drop.
                        self.emit_drop(v, Type::ShareInt);
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "exit" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        return Err(IrError::new("native IR: exit expects int"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_exit".into(),
                        args: vec![v],
                        ret: None,
                    });
                    // Unreachable; keep the block open for the terminator.
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "argc" && args.is_empty() {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_argc".into(),
                        args: vec![],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                if function == "sleep_ms" && args.len() == 1 {
                    let (v, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::I64 || owned {
                        return Err(IrError::new("native IR: sleep_ms expects int"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_sleep_ms".into(),
                        args: vec![v],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "parse_int" && args.len() == 1 {
                    return self.lower_parse_int_base(&args[0], /*base_expr=*/ None, 10);
                }
                if function == "parse_int_hex" && args.len() == 1 {
                    return self.lower_parse_int_base(&args[0], None, 16);
                }
                if function == "parse_int_bin" && args.len() == 1 {
                    return self.lower_parse_int_base(&args[0], None, 2);
                }
                if function == "parse_int_oct" && args.len() == 1 {
                    return self.lower_parse_int_base(&args[0], None, 8);
                }
                if function == "parse_int_auto" && args.len() == 1 {
                    return self.lower_parse_int_base(&args[0], None, 0);
                }
                if function == "parse_int_base" && args.len() == 2 {
                    return self.lower_parse_int_base(&args[0], Some(&args[1]), 10);
                }
                // uuid_check(s) -> Result[int,string] (Ok(1) or Err(msg)).
                if function == "uuid_check" && args.len() == 1 {
                    let (s, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: uuid_check expects string"));
                    }
                    let ok_i = self.value();
                    self.emit(Inst::Call {
                        out: Some(ok_i),
                        function: "mako_native_uuid_parse_ok_ptr".into(),
                        args: vec![s],
                        ret: Some(Type::I64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: s });
                    }
                    let ok_info = self
                        .structs
                        .variant("Ok")
                        .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
                    let err_info = self
                        .structs
                        .variant("Err")
                        .ok_or_else(|| IrError::new("native IR: Err missing"))?;
                    let result_ty = Type::Struct(ok_info.enum_id);
                    let result_slot = self.value();
                    self.emit(Inst::Alloca {
                        out: result_slot,
                        ty: result_ty,
                    });
                    let ok_b = self.value();
                    self.emit(Inst::Cast {
                        out: ok_b,
                        value: ok_i,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    let then_b = self.new_block();
                    let else_b = self.new_block();
                    let merge = self.new_block();
                    self.terminate(Terminator::Branch {
                        condition: ok_b,
                        then_block: then_b,
                        else_block: else_b,
                    })?;
                    self.current = then_b;
                    let one = self.const_int(1, Type::I64);
                    let ok_out = self.value();
                    self.emit(Inst::EnumMake {
                        out: ok_out,
                        enum_id: ok_info.enum_id,
                        tag: ok_info.tag,
                        slot_base: ok_info.slot_base as u32,
                        payload: vec![one],
                    });
                    self.emit(Inst::Store {
                        ptr: result_slot,
                        value: ok_out,
                    });
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = else_b;
                    let msg_lit = self.value();
                    self.emit(Inst::StringLiteral {
                        out: msg_lit,
                        bytes: b"invalid UUID".to_vec(),
                    });
                    let msg = self.emit_clone(msg_lit, Type::Str);
                    let err_out = self.value();
                    self.emit(Inst::EnumMake {
                        out: err_out,
                        enum_id: err_info.enum_id,
                        tag: err_info.tag,
                        slot_base: err_info.slot_base as u32,
                        payload: vec![msg],
                    });
                    self.emit(Inst::Store {
                        ptr: result_slot,
                        value: err_out,
                    });
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = merge;
                    let loaded = self.value();
                    self.emit(Inst::Load {
                        out: loaded,
                        ptr: result_slot,
                        ty: result_ty,
                    });
                    return Ok((loaded, result_ty, true));
                }
                if function == "append" && args.len() == 2 {
                    let (s, t, _) = self.lower_expr(&args[0])?;
                    if !matches!(
                        t,
                        Type::IntSlice
                            | Type::FloatSlice
                            | Type::ByteSlice
                            | Type::BoolSlice
                            | Type::StrSlice
                            | Type::StructSlice(_)
                            | Type::PtrSlice(_)
                    ) {
                        return Err(IrError::new("native IR: append expects a slice"));
                    }
                    let expected_elem = match t {
                        Type::IntSlice | Type::ByteSlice => Type::I64,
                        Type::FloatSlice => Type::F64,
                        Type::BoolSlice => Type::I1,
                        Type::StrSlice => Type::Str,
                        Type::StructSlice(sid) => Type::Struct(sid),
                        Type::PtrSlice(vk) => vk.to_type(),
                        _ => Type::I64,
                    };
                    let (v, vt, o) = self.lower_expr_for_expected(&args[1], expected_elem)?;
                    let ptr_elem_ok = match t {
                        Type::PtrSlice(vk) => vk.to_type() == vt,
                        _ => false,
                    };
                    match (t, vt, o) {
                        (Type::IntSlice, Type::I64, false) => {}
                        (Type::ByteSlice, Type::I64, false) => {}
                        (Type::BoolSlice, Type::I1 | Type::I64, false) => {}
                        (Type::FloatSlice, Type::F64, false) => {}
                        (Type::StrSlice, Type::Str, _) => {}
                        (Type::StructSlice(sid), Type::Struct(eid), _) if sid == eid => {}
                        (Type::PtrSlice(_), _, _) if ptr_elem_ok => {}
                        _ => {
                            return Err(IrError::new(
                                "native IR: append element type mismatch",
                            ))
                        }
                    }
                    // `append` consumes its slice source — it grows the buffer in
                    // place or reallocs and frees the old one — so an owned source
                    // local must not be dropped again. (The Ident read reports
                    // `owned=false`; consult the local's real ownership instead.)
                    if let Expr::Ident(name) = &args[0] {
                        if self.heap_owned.get(name).copied().unwrap_or(false) {
                            self.heap_owned.insert(name.clone(), false);
                        }
                    }
                    let out = self.value();
                    match t {
                        Type::IntSlice => {
                            self.emit(Inst::SliceAppend {
                                out,
                                slice: s,
                                value: v,
                            });
                            return Ok((out, Type::IntSlice, true));
                        }
                        Type::FloatSlice => {
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_float_slice_append_ptr".into(),
                                args: vec![s, v],
                                ret: Some(Type::FloatSlice),
                            });
                            return Ok((out, Type::FloatSlice, true));
                        }
                        Type::BoolSlice => {
                            let bv = if vt == Type::I1 {
                                let c = self.value();
                                self.emit(Inst::Cast {
                                    out: c,
                                    value: v,
                                    from: Type::I1,
                                    to: Type::I64,
                                });
                                c
                            } else {
                                v
                            };
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_bool_slice_append_ptr".into(),
                                args: vec![s, bv],
                                ret: Some(Type::BoolSlice),
                            });
                            return Ok((out, Type::BoolSlice, true));
                        }
                        Type::ByteSlice => {
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_byte_slice_append_ptr".into(),
                                args: vec![s, v],
                                ret: Some(Type::ByteSlice),
                            });
                            return Ok((out, Type::ByteSlice, true));
                        }
                        Type::StructSlice(sid) => {
                            let owned_elem = if o {
                                v
                            } else {
                                self.emit_clone(v, Type::Struct(sid))
                            };
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_ptr_slice_append".into(),
                                args: vec![s, owned_elem],
                                ret: Some(Type::StructSlice(sid)),
                            });
                            return Ok((out, Type::StructSlice(sid), true));
                        }
                        Type::PtrSlice(vk) => {
                            let ety = vk.to_type();
                            let owned_elem = if o {
                                v
                            } else if ety.is_heap() {
                                self.emit_clone(v, ety)
                            } else {
                                v
                            };
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_ptr_slice_append".into(),
                                args: vec![s, owned_elem],
                                ret: Some(Type::PtrSlice(vk)),
                            });
                            return Ok((out, Type::PtrSlice(vk), true));
                        }
                        _ => {
                            self.emit(Inst::StrSliceAppend {
                                out,
                                slice: s,
                                value: v,
                            });
                            return Ok((out, Type::StrSlice, true));
                        }
                    }
                }
                if function == "print_float" && args.len() == 1 {
                    let (value, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::F64 || owned {
                        return Err(IrError::new("native IR: print_float expects float"));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_print_f64".into(),
                        args: vec![value],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "print_int" || function == "print_int64" || function == "print_int32" || function == "print_int8" || function == "print_uint64" {
                    if args.len() != 1 {
                        return Err(IrError::new("native IR: print_int arity"));
                    }
                    let (value, ty, owned) = self.lower_expr(&args[0])?;
                    let value = match ty {
                        Type::I64 => value,
                        Type::I1 => {
                            let casted = self.value();
                            self.emit(Inst::Cast {
                                out: casted,
                                value,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            casted
                        }
                        _ => {
                            return Err(IrError::new("native IR: print_int expects int"));
                        }
                    };
                    self.emit(Inst::PrintInt { value });
                    if owned {
                        self.emit(Inst::DropString { value });
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "print" {
                    if args.len() != 1 {
                        return Err(IrError::new("native IR: print arity"));
                    }
                    let (value, ty, owned) = self.lower_expr(&args[0])?;
                    if ty == Type::Str {
                        self.emit(Inst::PrintString { value });
                    } else if ty == Type::I1 {
                        self.emit(Inst::PrintBool { value });
                    } else if ty == Type::I64 {
                        self.emit(Inst::PrintInt { value });
                    } else if ty == Type::F64 {
                        if owned {
                            return Err(IrError::new("native IR: print float cannot be owned"));
                        }
                        self.emit(Inst::Call {
                            out: None,
                            function: "mako_native_print_f64".into(),
                            args: vec![value],
                            ret: None,
                        });
                    } else {
                        return Err(IrError::new("native IR: print type not implemented"));
                    }
                    if owned {
                        self.emit(Inst::DropString { value });
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "parse_float" && args.len() == 1 {
                    let (s, ty, owned) = self.lower_expr(&args[0])?;
                    if ty != Type::Str {
                        return Err(IrError::new("native IR: parse_float expects string"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_parse_float_ptr".into(),
                        args: vec![s],
                        ret: Some(Type::F64),
                    });
                    if owned {
                        self.emit(Inst::DropString { value: s });
                    }
                    return Ok((out, Type::F64, false));
                }
                if function == "maps_keys" && args.len() == 1 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    let out = self.value();
                    let (fname, ret_ty) = match mt {
                        Type::MapIPtr(vk) if vk.struct_key_id().is_some() => {
                            // Keys are owned struct pointers — return as []Struct.
                            let sid = vk.struct_key_id().unwrap();
                            (
                                "mako_native_maps_keys_ii_ptr",
                                Type::StructSlice(sid),
                            )
                        }
                        Type::MapII | Type::MapIPtr(_) => {
                            ("mako_native_maps_keys_ii_ptr", Type::IntSlice)
                        }
                        Type::MapIF => ("mako_native_maps_keys_if_ptr", Type::IntSlice),
                        Type::MapSI | Type::MapSPtr(_) => {
                            ("mako_native_maps_keys_si_ptr", Type::StrSlice)
                        }
                        Type::MapSS => ("mako_native_maps_keys_ss_ptr", Type::StrSlice),
                        Type::MapFI => ("mako_native_maps_keys_fi_ptr", Type::FloatSlice),
                        _ => {
                            return Err(IrError::new(
                                "native IR: maps_keys currently supports int/string/float-key maps",
                            ));
                        }
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![m],
                        ret: Some(ret_ty),
                    });
                    if mo {
                        self.emit_drop(m, mt);
                    }
                    return Ok((out, ret_ty, true));
                }
                if function == "maps_values" && args.len() == 1 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    let out = self.value();
                    let (fname, ret_ty) = match mt {
                        Type::MapII => ("mako_native_maps_values_ii_ptr", Type::IntSlice),
                        Type::MapIPtr(MapValKind::StructKeyInt(_)) => {
                            ("mako_native_maps_values_ii_ptr", Type::IntSlice)
                        }
                        Type::MapIPtr(MapValKind::StructKeyIntSlice(_)) => {
                            // Values are []int headers stored as pointers.
                            (
                                "mako_native_maps_values_ii_as_ptr_slice",
                                Type::PtrSlice(MapValKind::IntSlice),
                            )
                        }
                        Type::MapIPtr(MapValKind::StructKeyStr(_)) => {
                            // Values are string pointers stored as i64 → treat as []string via ptr slice.
                            (
                                "mako_native_maps_values_ii_as_ptr_slice",
                                Type::StrSlice,
                            )
                        }
                        Type::MapIPtr(MapValKind::StructKeyStrSlice(_)) => (
                            "mako_native_maps_values_ii_as_ptr_slice",
                            Type::PtrSlice(MapValKind::StrSlice),
                        ),
                        Type::MapIPtr(MapValKind::StructKeyFloat(_)) => {
                            (
                                "mako_native_maps_values_ii_as_float_ptr",
                                Type::FloatSlice,
                            )
                        }
                        Type::MapIPtr(MapValKind::StructKeyFloatSlice(_)) => (
                            "mako_native_maps_values_ii_as_ptr_slice",
                            Type::PtrSlice(MapValKind::FloatSlice),
                        ),
                        Type::MapIPtr(MapValKind::StructKeyToStruct(_, vid)) => (
                            "mako_native_maps_values_ii_as_ptr_slice",
                            Type::StructSlice(vid),
                        ),
                        Type::MapIPtr(MapValKind::StructKeyStructSlice(_, eid)) => (
                            "mako_native_maps_values_ii_as_ptr_slice",
                            Type::PtrSlice(MapValKind::Struct(eid)),
                        ),
                        Type::MapIPtr(vk) => (
                            "mako_native_maps_values_ii_as_ptr_slice",
                            Type::PtrSlice(vk),
                        ),
                        Type::MapSI => ("mako_native_maps_values_si_as_i64_ptr", Type::IntSlice),
                        Type::MapSPtr(MapValKind::Float) => (
                            "mako_native_maps_values_si_as_float_ptr",
                            Type::FloatSlice,
                        ),
                        Type::MapIPtr(MapValKind::Float) => (
                            "mako_native_maps_values_ii_as_float_ptr",
                            Type::FloatSlice,
                        ),
                        Type::MapSPtr(vk) => (
                            "mako_native_maps_values_si_ptr",
                            Type::PtrSlice(vk),
                        ),
                        Type::MapSS => ("mako_native_maps_values_ss_ptr", Type::StrSlice),
                        Type::MapIF => ("mako_native_maps_values_if_ptr", Type::FloatSlice),
                        Type::MapFI => ("mako_native_maps_values_fi_ptr", Type::IntSlice),
                        _ => {
                            return Err(IrError::new(
                                "native IR: maps_values currently supports int/string/float maps",
                            ));
                        }
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![m],
                        ret: Some(ret_ty),
                    });
                    if mo {
                        self.emit_drop(m, mt);
                    }
                    return Ok((out, ret_ty, true));
                }
                if function == "maps_clone" && args.len() == 1 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    let out = self.value();
                    // Content-keyed struct maps must re-bucket by content hash.
                    if let Type::MapIPtr(vk) = mt {
                        if let Some(sid) = vk.struct_key_id() {
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_map_struct_key_clone_ptr".into(),
                                args: vec![m, nf, sm, nm, nfp, nsp],
                                ret: Some(mt),
                            });
                            if mo {
                                self.emit_drop(m, mt);
                            }
                            return Ok((out, mt, true));
                        }
                    }
                    let fname = match mt {
                        Type::MapII | Type::MapIPtr(_) => "mako_native_map_ii_clone_ptr",
                        Type::MapSI | Type::MapSPtr(_) => "mako_native_map_si_clone_ptr",
                        Type::MapSS => "mako_native_map_ss_clone_ptr",
                        Type::MapIF => "mako_native_map_if_clone_ptr",
                        Type::MapFI => "mako_native_map_fi_clone_ptr",
                        _ => {
                            return Err(IrError::new("native IR: maps_clone unsupported map type"));
                        }
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![m],
                        ret: Some(mt),
                    });
                    if mo {
                        self.emit_drop(m, mt);
                    }
                    return Ok((out, mt, true));
                }
                if function == "maps_equal" && args.len() == 2 {
                    let (a, at, ao) = self.lower_expr(&args[0])?;
                    let (b, bt, bo) = self.lower_expr(&args[1])?;
                    if at != bt {
                        return Err(IrError::new("native IR: maps_equal type mismatch"));
                    }
                    let out = self.value();
                    // map[StructK]V — compare keys by content, values by kind.
                    if let Type::MapIPtr(vk) = at {
                        if let Some(sid) = vk.struct_key_id() {
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            let (val_kind, val_nf, val_sm) = match vk {
                                MapValKind::StructKeyStr(_) => (
                                    self.const_int(1, Type::I64),
                                    self.const_int(0, Type::I64),
                                    self.const_int(0, Type::I64),
                                ),
                                MapValKind::StructKeyToStruct(_, vid) => {
                                    let (vnf, vsm, _vnm, _vnfp, _vnsp) = self.struct_key_meta(vid);
                                    (self.const_int(2, Type::I64), vnf, vsm)
                                }
                                // Int / float-bits / other scalar values.
                                _ => (
                                    self.const_int(0, Type::I64),
                                    self.const_int(0, Type::I64),
                                    self.const_int(0, Type::I64),
                                ),
                            };
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_maps_equal_struct_key".into(),
                                args: vec![a, b, nf, sm, nm, nfp, nsp, val_kind, val_nf, val_sm],
                                ret: Some(Type::I64),
                            });
                            if ao {
                                self.emit_drop(a, at);
                            }
                            if bo {
                                self.emit_drop(b, bt);
                            }
                            return Ok((out, Type::I64, false));
                        }
                    }
                    // Pointer maps of structs: deep field equality (strings by
                    // content, scalars by value). Flat Str/I64/I1 fields only.
                    let deep_struct = match at {
                        Type::MapIPtr(MapValKind::Struct(sid))
                        | Type::MapSPtr(MapValKind::Struct(sid)) => Some(sid),
                        _ => None,
                    };
                    if let Some(sid) = deep_struct {
                        let nfields = self.structs.field_count(sid) as i64;
                        let mut str_mask: i64 = 0;
                        let mut ok_layout = true;
                        for i in 0..self.structs.field_count(sid) {
                            match self.structs.field_type(sid, i) {
                                Type::Str => str_mask |= 1 << i,
                                Type::I64 | Type::I1 | Type::I32 | Type::F64 => {}
                                _ => ok_layout = false,
                            }
                        }
                        if ok_layout {
                            let nf = self.const_int(nfields, Type::I64);
                            let sm = self.const_int(str_mask, Type::I64);
                            let fname = match at {
                                Type::MapIPtr(_) => "mako_native_maps_equal_ii_deep",
                                Type::MapSPtr(_) => "mako_native_maps_equal_si_deep",
                                _ => unreachable!(),
                            };
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: fname.into(),
                                args: vec![a, b, nf, sm],
                                ret: Some(Type::I64),
                            });
                            if ao {
                                self.emit_drop(a, at);
                            }
                            if bo {
                                self.emit_drop(b, bt);
                            }
                            return Ok((out, Type::I64, false));
                        }
                    }
                    let fname = match at {
                        Type::MapII | Type::MapIPtr(_) => "mako_native_maps_equal_ii",
                        Type::MapSI | Type::MapSPtr(_) => "mako_native_maps_equal_si",
                        Type::MapSS => "mako_native_maps_equal_ss",
                        Type::MapIF => "mako_native_maps_equal_if",
                        Type::MapFI => "mako_native_maps_equal_fi",
                        _ => {
                            return Err(IrError::new("native IR: maps_equal unsupported map type"));
                        }
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![a, b],
                        ret: Some(Type::I64),
                    });
                    if ao {
                        self.emit_drop(a, at);
                    }
                    if bo {
                        self.emit_drop(b, bt);
                    }
                    return Ok((out, Type::I64, false));
                }
                if function == "maps_clear" && args.len() == 1 {
                    let (m, mt, mo) = self.lower_expr(&args[0])?;
                    if mo {
                        return Err(IrError::new(
                            "native IR: maps_clear requires a borrowed map local",
                        ));
                    }
                    let fname = match mt {
                        Type::MapII | Type::MapIPtr(_) => "mako_native_maps_clear_ii",
                        Type::MapSI | Type::MapSPtr(_) => "mako_native_maps_clear_si",
                        Type::MapSS => "mako_native_maps_clear_ss",
                        Type::MapIF => "mako_native_maps_clear_if",
                        Type::MapFI => "mako_native_maps_clear_fi",
                        _ => {
                            return Err(IrError::new("native IR: maps_clear unsupported map type"));
                        }
                    };
                    self.emit(Inst::Call {
                        out: None,
                        function: fname.into(),
                        args: vec![m],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                if function == "maps_copy" && args.len() == 2 {
                    // maps_copy(dst, src)
                    let (dst, dt, d_owned) = self.lower_expr(&args[0])?;
                    let (src, st, s_owned) = self.lower_expr(&args[1])?;
                    if d_owned {
                        return Err(IrError::new(
                            "native IR: maps_copy dst must be a borrowed map local",
                        ));
                    }
                    if dt != st {
                        return Err(IrError::new("native IR: maps_copy type mismatch"));
                    }
                    if let Type::MapIPtr(vk) = dt {
                        if let Some(sid) = vk.struct_key_id() {
                            let (nf, sm, nm, nfp, nsp) = self.struct_key_meta(sid);
                            self.emit(Inst::Call {
                                out: None,
                                function: "mako_native_maps_copy_struct_key".into(),
                                args: vec![dst, src, nf, sm, nm, nfp, nsp],
                                ret: None,
                            });
                            if s_owned {
                                self.emit_drop(src, st);
                            }
                            return Ok((self.const_int(0, Type::I64), Type::I64, false));
                        }
                    }
                    let fname = match dt {
                        Type::MapII | Type::MapIPtr(_) => "mako_native_maps_copy_ii",
                        Type::MapSI | Type::MapSPtr(_) => "mako_native_maps_copy_si",
                        Type::MapSS => "mako_native_maps_copy_ss",
                        Type::MapIF => "mako_native_maps_copy_if",
                        Type::MapFI => "mako_native_maps_copy_fi",
                        _ => {
                            return Err(IrError::new("native IR: maps_copy unsupported map type"));
                        }
                    };
                    self.emit(Inst::Call {
                        out: None,
                        function: fname.into(),
                        args: vec![dst, src],
                        ret: None,
                    });
                    if s_owned {
                        self.emit_drop(src, st);
                    }
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                // Free-form channel timeouts: desugar to method form.
                if function == "chan_send_timeout" && args.len() == 3 {
                    return self.lower_method(&args[0], "send_timeout", &args[1..]);
                }
                if function == "chan_recv_timeout" && args.len() == 2 {
                    return self.lower_method(&args[0], "recv_timeout", &args[1..]);
                }
                // Lambda capture unpack: __mako_env_get(env, i) → pack_get.
                if function == "__mako_env_get" && args.len() == 2 {
                    let (env, et, eo) = self.lower_expr(&args[0])?;
                    let (idx, it, io) = self.lower_expr(&args[1])?;
                    if et != Type::I64 || it != Type::I64 || eo || io {
                        return Err(IrError::new("native IR: __mako_env_get expects int env,i"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_pack_get".into(),
                        args: vec![env, idx],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                // Mut capture cell helpers (heap i64 cell shared outer↔closure).
                if function == "__mako_cell_load" && args.len() == 1 {
                    let (cell, ct, co) = self.lower_expr(&args[0])?;
                    if ct != Type::I64 || co {
                        return Err(IrError::new("native IR: __mako_cell_load expects int cell"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_i64_cell_load".into(),
                        args: vec![cell],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                if function == "__mako_cell_store" && args.len() == 2 {
                    let (cell, ct, co) = self.lower_expr(&args[0])?;
                    let (v, vt, vo) = self.lower_expr(&args[1])?;
                    if ct != Type::I64 || vt != Type::I64 || co || vo {
                        return Err(IrError::new(
                            "native IR: __mako_cell_store expects int cell, int value",
                        ));
                    }
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_i64_cell_store".into(),
                        args: vec![cell, v],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                // Runtime bridge builtins (mako_rt via native_bridge.c).
                if let Some(result) = self.lower_runtime_builtin(function, args)? {
                    return Ok(result);
                }
                // Prefer monomorphized specialty (identity__int / either__string).
                // Lower args first, then pick the monomorph whose param types match.
                let mut lowered_raw: Vec<(Value, Type, bool)> = Vec::with_capacity(args.len());
                for argument in args {
                    lowered_raw.push(self.lower_expr(argument)?);
                }
                let actual_tys: Vec<Type> = lowered_raw.iter().map(|(_, t, _)| *t).collect();
                let mono_candidates: Vec<String> = {
                    let mut c = vec![function.to_string()];
                    let tags: Vec<String> = args.iter().map(mono_tag_for_expr).collect();
                    for t in &tags {
                        c.push(format!("{function}__{t}"));
                    }
                    if tags.len() >= 2 {
                        c.push(format!("{function}__{}__{}", tags[0], tags[1]));
                    }
                    if tags.len() >= 3 {
                        c.push(format!(
                            "{function}__{}__{}__{}",
                            tags[0], tags[1], tags[2]
                        ));
                    }
                    // Also consider every registered monomorph of this base.
                    let prefix = format!("{function}__");
                    for name in self.signatures.keys() {
                        if name.starts_with(&prefix) || name == function {
                            if !c.iter().any(|x| x == name) {
                                c.push(name.clone());
                            }
                        }
                    }
                    c
                };
                let call_ty_ok = |expected: &Type, actual: Type| -> bool {
                    if *expected == actual {
                        return true;
                    }
                    matches!(
                        (expected, actual),
                        (Type::I64, Type::I1)
                            | (Type::I1, Type::I64)
                            | (Type::Opaque, Type::Struct(_))
                            | (Type::Opaque, Type::I64)
                            | (Type::I64, Type::Opaque)
                    )
                };
                let (fn_name, params, ret) = mono_candidates
                    .iter()
                    .filter_map(|n| {
                        self.signatures
                            .get(n)
                            .cloned()
                            .map(|(p, r)| (n.clone(), p, r))
                    })
                    .find(|(_, params, _)| {
                        params.len() == actual_tys.len()
                            && params
                                .iter()
                                .zip(actual_tys.iter())
                                .all(|(e, a)| call_ty_ok(e, *a))
                    })
                    .or_else(|| {
                        // Fall back to first registered candidate (legacy).
                        mono_candidates.iter().find_map(|n| {
                            self.signatures
                                .get(n)
                                .cloned()
                                .map(|(p, r)| (n.clone(), p, r))
                        })
                    })
                    .ok_or_else(|| {
                        IrError::new(format!("native IR: unknown call `{function}`"))
                    })?;
                if params.len() != args.len() {
                    return Err(IrError::new("native IR: call arity mismatch"));
                }
                let mut lowered = Vec::with_capacity(args.len());
                let mut temporary_owned = Vec::new();
                for (arg_expr, ((value0, actual0, owned), expected)) in args
                    .iter()
                    .zip(lowered_raw.into_iter().zip(params))
                {
                    let mut value = value0;
                    let mut actual = actual0;
                    // Allow I1↔I64 at call boundaries for mono/int-bool flexibility.
                    if actual != expected && expected == Type::I64 && actual == Type::I1 {
                        let casted = self.value();
                        self.emit(Inst::Cast {
                            out: casted,
                            value,
                            from: Type::I1,
                            to: Type::I64,
                        });
                        value = casted;
                        actual = Type::I64;
                    } else if actual != expected && expected == Type::I1 && actual == Type::I64 {
                        let casted = self.value();
                        self.emit(Inst::Cast {
                            out: casted,
                            value,
                            from: Type::I64,
                            to: Type::I1,
                        });
                        value = casted;
                        actual = Type::I1;
                    } else if actual != expected {
                        // Interface param: box concrete struct into Opaque.
                        if expected == Type::Opaque && matches!(actual, Type::Struct(_)) {
                            value = self.box_iface(value, actual)?;
                            actual = Type::Opaque;
                        } else if expected == Type::Opaque && actual == Type::I64 {
                            // Unit iface from int placeholder.
                            actual = Type::Opaque;
                        } else if expected == Type::I64 && actual == Type::Opaque {
                            // Opaque handle passed where i64 fd/handle expected.
                            actual = Type::I64;
                        } else {
                            return Err(IrError::new(format!(
                                "native IR: call type mismatch ({actual:?} vs {expected:?})"
                            )));
                        }
                    }
                    if let Type::Struct(id) = actual {
                        // By-value argument: hand the callee an owned copy so a
                        // mutating callee cannot observe the caller's storage,
                        // and drop that copy once the call returns.
                        if !owned {
                            let clone = self.value();
                            self.emit(Inst::StructClone {
                                out: clone,
                                base: value,
                                struct_id: id,
                            });
                            value = clone;
                        }
                        temporary_owned.push((value, actual));
                        lowered.push(value);
                        continue;
                    }
                    let _ = arg_expr;
                    lowered.push(value);
                    if owned && !actual.is_shared_handle() {
                        // An owned string/slice temporary is handed to the callee
                        // as a borrow (a parameter is never freed by the callee)
                        // and dropped here after the call returns — no clone, so
                        // nothing leaks. Shared handles (Opaque/chan) escape or
                        // alias; post-call drop would double-free.
                        temporary_owned.push((value, actual));
                    }
                }
                let out = ret.map(|_| self.value());
                self.emit(Inst::Call {
                    out,
                    function: fn_name,
                    args: lowered,
                    ret,
                });
                for (value, ty) in temporary_owned {
                    self.emit_drop(value, ty);
                }
                match (out, ret) {
                    (Some(value), Some(ty)) => Ok((value, ty, ty.is_heap())),
                    _ => Ok((self.const_int(0, Type::I64), Type::I64, false)),
                }
            }
            Expr::Field { base, field } => {
                // Pack unit variants: `eng.Red`, `eng.Color.Red` (alias is not a local).
                if let Some(result) = self.try_lower_pack_field(base, field)? {
                    return Ok(result);
                }
                let structs = self.structs;
                let (base_ptr, base_ty, base_owned) = self.lower_expr(base)?;
                let Type::Struct(id) = base_ty else {
                    return Err(IrError::new("native IR: field access on non-struct"));
                };
                let (index, field_ty) = structs
                    .field(id, field)
                    .ok_or_else(|| IrError::new(format!("native IR: unknown field `{field}`")))?;
                let out = self.value();
                self.emit(Inst::StructField {
                    out,
                    base: base_ptr,
                    struct_id: id,
                    index,
                    ty: field_ty,
                });
                if base_owned {
                    // The base temporary is freed here (with its owned fields),
                    // so an owned field must be cloned out first; scalars are
                    // already value copies.
                    if field_ty.is_heap() {
                        let cloned = self.emit_clone(out, field_ty);
                        self.emit(Inst::DropStruct {
                            value: base_ptr,
                            struct_id: id,
                        });
                        return Ok((cloned, field_ty, true));
                    }
                    self.emit(Inst::DropStruct {
                        value: base_ptr,
                        struct_id: id,
                    });
                }
                Ok((out, field_ty, false))
            }
            Expr::StructLit {
                name,
                fields,
                update,
            } => {
                let structs = self.structs;
                let id = *structs.by_name.get(name).ok_or_else(|| {
                    IrError::new(format!("native IR: unknown struct `{name}`"))
                })?;
                let nfields = structs.field_count(id);
                let mut slots: Vec<Option<Value>> = vec![None; nfields];
                for (fname, fexpr) in fields {
                    let (index, field_ty) = structs.field(id, fname).ok_or_else(|| {
                        IrError::new(format!("native IR: unknown field `{fname}`"))
                    })?;
                    let (v, vt, mut vo) = self.lower_expr(fexpr)?;
                    if vt != field_ty {
                        return Err(IrError::new("native IR: struct field type mismatch"));
                    }
                    if slots[index as usize].is_some() {
                        return Err(IrError::new(format!("native IR: duplicate field `{fname}`")));
                    }
                    // Shared handles: move ownership out of a bare local so the
                    // struct is the sole freer (clone is identity for these).
                    if field_ty.is_shared_handle() {
                        vo = self.take_bare_string_local(fexpr, vo);
                    }
                    // The struct owns its fields: move an owned temporary in,
                    // clone a borrow, and reject an owned scalar (impossible).
                    let stored = self.own_field_value(v, field_ty, vo)?;
                    slots[index as usize] = Some(stored);
                }
                // Fill missing fields from struct field defaults (`x: int = 0`).
                {
                    let defaults = structs.layouts.borrow()[id as usize].defaults.clone();
                    for (i, slot) in slots.iter_mut().enumerate() {
                        if slot.is_some() {
                            continue;
                        }
                        if let Some(Some(def_expr)) = defaults.get(i) {
                            let field_ty = structs.field_type(id, i);
                            let (v, vt, vo) = self.lower_expr(def_expr)?;
                            if vt != field_ty {
                                return Err(IrError::new(
                                    "native IR: struct field default type mismatch",
                                ));
                            }
                            *slot = Some(self.own_field_value(v, field_ty, vo)?);
                        }
                    }
                }
                if let Some(base) = update {
                    let (base_ptr, base_ty, base_owned) = self.lower_expr(base)?;
                    if base_ty != Type::Struct(id) {
                        return Err(IrError::new("native IR: struct update base type mismatch"));
                    }
                    for i in 0..nfields {
                        if slots[i].is_none() {
                            let field_ty = structs.field_type(id, i);
                            let out = self.value();
                            self.emit(Inst::StructField {
                                out,
                                base: base_ptr,
                                struct_id: id,
                                index: i as u32,
                                ty: field_ty,
                            });
                            // An owned field carried over from the base must be
                            // cloned: the new struct and the base must not share
                            // (and later both free) the same heap value.
                            let value = if field_ty.is_heap() {
                                self.emit_clone(out, field_ty)
                            } else {
                                out
                            };
                            slots[i] = Some(value);
                        }
                    }
                    if base_owned {
                        self.emit(Inst::DropStruct {
                            value: base_ptr,
                            struct_id: id,
                        });
                    }
                }
                let mut field_values = Vec::with_capacity(nfields);
                for (i, slot) in slots.into_iter().enumerate() {
                    match slot {
                        Some(v) => field_values.push(v),
                        None => {
                            let fname = structs.field_name(id, i);
                            return Err(IrError::new(format!("native IR: missing field `{fname}`")));
                        }
                    }
                }
                let out = self.value();
                self.emit(Inst::StructMake {
                    out,
                    struct_id: id,
                    fields: field_values,
                });
                Ok((out, Type::Struct(id), true))
            }
            Expr::StructLitPos { name, values } => {
                let structs = self.structs;
                let id = *structs.by_name.get(name).ok_or_else(|| {
                    IrError::new(format!("native IR: unknown struct `{name}`"))
                })?;
                let field_types: Vec<Type> = structs.field_types(id);
                // Go-style: full positional `Point{1, 2}` or zero-value `Point{}`.
                if !values.is_empty() && values.len() != field_types.len() {
                    return Err(IrError::new("native IR: struct literal arity mismatch"));
                }
                let mut field_values = Vec::with_capacity(field_types.len());
                if values.is_empty() {
                    // Zero-value every field (0 / null / empty string).
                    for field_ty in &field_types {
                        let z = if field_ty.is_heap() || matches!(field_ty, Type::Struct(_)) {
                            let out = self.value();
                            self.emit(Inst::NullHeap {
                                out,
                                ty: *field_ty,
                            });
                            out
                        } else if *field_ty == Type::F64 {
                            let out = self.value();
                            self.emit(Inst::ConstFloat { out, value: 0.0 });
                            out
                        } else if *field_ty == Type::I1 {
                            self.const_int(0, Type::I1)
                        } else {
                            self.const_int(0, *field_ty)
                        };
                        field_values.push(z);
                    }
                } else {
                    for (v_expr, field_ty) in values.iter().zip(field_types.iter().copied()) {
                        let (v, vt, mut vo) = self.lower_expr(v_expr)?;
                        if vt != field_ty {
                            return Err(IrError::new(
                                "native IR: struct literal field type mismatch",
                            ));
                        }
                        if field_ty.is_shared_handle() {
                            vo = self.take_bare_string_local(v_expr, vo);
                        }
                        field_values.push(self.own_field_value(v, field_ty, vo)?);
                    }
                }
                let out = self.value();
                self.emit(Inst::StructMake {
                    out,
                    struct_id: id,
                    fields: field_values,
                });
                Ok((out, Type::Struct(id), true))
            }
            Expr::Tuple(elements) => {
                // A tuple is an anonymous positional struct. Intern its shape
                // and lower exactly like a positional struct literal.
                let structs = self.structs;
                let mut field_types = Vec::with_capacity(elements.len());
                let mut field_values = Vec::with_capacity(elements.len());
                for element in elements {
                    let (v, vt, mut vo) = self.lower_expr(element)?;
                    if !is_field_type(vt) {
                        return Err(IrError::new(
                            "native IR: tuple element type is not supported in the current increment",
                        ));
                    }
                    if vt.is_shared_handle() {
                        vo = self.take_bare_string_local(element, vo);
                    }
                    // The tuple owns its elements: move an owned temporary in,
                    // clone a borrow.
                    let stored = self.own_field_value(v, vt, vo)?;
                    field_types.push(vt);
                    field_values.push(stored);
                }
                let id = structs.intern_tuple(field_types);
                let out = self.value();
                self.emit(Inst::StructMake {
                    out,
                    struct_id: id,
                    fields: field_values,
                });
                Ok((out, Type::Struct(id), true))
            }
            Expr::Match { scrutinee, arms } => self.lower_match(scrutinee, arms),
            Expr::Try(inner) => self.lower_try(inner),
            Expr::StringInterp(parts) => self.lower_string_interp(parts),
            Expr::IfExpr {
                cond,
                then_block,
                else_block,
            } => self.lower_if_expr(cond, then_block, else_block),
            Expr::Block(block) => self.lower_block_expr(block),
            Expr::Method {
                receiver,
                method,
                args,
            } => self.lower_method(receiver, method, args),
            Expr::Convert { ty, args } => {
                if args.len() != 1 {
                    return Err(IrError::new("native IR: convert expects one argument"));
                }
                self.lower_convert(ty, &args[0])
            }
            Expr::Kick { crew, expr } => self.lower_kick(crew, expr),
            Expr::Fan { collection, mapper } => self.lower_fan(collection, mapper),
            Expr::Join(inner) => {
                // Desugared `j.join()` — same typing as method join (job_ret_tys).
                let (task, ty, _owned) = self.lower_expr(inner)?;
                if ty != Type::Task {
                    return Err(IrError::new("native IR: join expects a task"));
                }
                let joined_ty = match inner.as_ref() {
                    Expr::Ident(name) => self
                        .job_ret_tys
                        .get(name)
                        .copied()
                        .unwrap_or(Type::I64),
                    _ => Type::I64,
                };
                let out = self.value();
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_task_join_i64".into(),
                    args: vec![task],
                    ret: Some(Type::I64),
                });
                // Note Result Err into enclosing nursery for wait()/err_count.
                if let Type::Struct(eid) = joined_ty {
                    if self.structs.is_enum(eid) {
                        if let (Expr::Ident(jname), Some(err_info)) = (
                            inner.as_ref(),
                            self.structs.variant_in_enum("Err", eid),
                        ) {
                            if let Some(crew) = self.job_crews.get(jname).cloned() {
                                if let Some((nslot, Type::Nursery)) =
                                    self.locals.get(&crew).copied()
                                {
                                    let n = self.value();
                                    self.emit(Inst::Load {
                                        out: n,
                                        ptr: nslot,
                                        ty: Type::Nursery,
                                    });
                                    let tag = self.value();
                                    self.emit(Inst::StructField {
                                        out: tag,
                                        base: out,
                                        struct_id: eid,
                                        index: 0,
                                        ty: Type::I64,
                                    });
                                    let want = self.const_int(err_info.tag as i64, Type::I64);
                                    let is_err = self.value();
                                    self.emit(Inst::Binary {
                                        out: is_err,
                                        op: BinOp::Eq,
                                        left: tag,
                                        right: want,
                                        ty: Type::I64,
                                    });
                                    let then_b = self.new_block();
                                    let merge = self.new_block();
                                    self.terminate(Terminator::Branch {
                                        condition: is_err,
                                        then_block: then_b,
                                        else_block: merge,
                                    })?;
                                    self.current = then_b;
                                    let msg = self.value();
                                    self.emit(Inst::StructField {
                                        out: msg,
                                        base: out,
                                        struct_id: eid,
                                        index: err_info.slot_base as u32,
                                        ty: Type::Str,
                                    });
                                    self.emit(Inst::Call {
                                        out: None,
                                        function: "mako_native_nursery_note_err_ptr".into(),
                                        args: vec![n, msg],
                                        ret: None,
                                    });
                                    self.terminate(Terminator::Jump(merge))?;
                                    self.current = merge;
                                }
                            }
                        }
                    }
                }
                if let Expr::Ident(name) = inner.as_ref() {
                    self.heap_owned.insert(name.clone(), false);
                }
                let owned = joined_ty.is_heap() || matches!(joined_ty, Type::Struct(_));
                Ok((out, joined_ty, owned))
            }
        }
    }

    /// `if cond { … expr } else { … expr }` as a value. Merges through a stack
    /// slot (same strategy as match).
    fn lower_if_expr(
        &mut self,
        cond: &Expr,
        then_block: &crate::ast::Block,
        else_block: &crate::ast::Block,
    ) -> Result<(Value, Type, bool), IrError> {
        let (condition, cty, cowned) = self.lower_expr(cond)?;
        if cowned {
            return Err(IrError::new("native IR: owned string if-expression condition"));
        }
        let condition = match cty {
            Type::I1 => condition,
            Type::I64 => {
                // Truthy non-zero (bool captures reloaded as i64 from env pack).
                let z = self.const_int(0, Type::I64);
                let cmp = self.value();
                self.emit(Inst::Binary {
                    out: cmp,
                    op: BinOp::Ne,
                    left: condition,
                    right: z,
                    ty: Type::I64,
                });
                cmp
            }
            _ => {
                return Err(IrError::new("native IR: if-expression condition must be bool"));
            }
        };
        let result_slot = self.value();
        let result_block = self.current;
        let result_index = self.block().instructions.len();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: Type::I64,
        });
        let then_id = self.new_block();
        let else_id = self.new_block();
        let merge = self.new_block();
        self.terminate(Terminator::Branch {
            condition,
            then_block: then_id,
            else_block: else_id,
        })?;

        let owned_before = self.heap_owned.clone();
        let locals_before = self.locals.clone();

        self.current = then_id;
        let (tval, tty, towned) = self.lower_block_trailing(then_block)?;
        let tval = if tty.is_heap() && !towned {
            self.emit_clone(tval, tty)
        } else {
            tval
        };
        self.emit(Inst::Store {
            ptr: result_slot,
            value: tval,
        });
        // Drop arm-local owned values that aren't the result.
        self.drop_arm_locals(&locals_before)?;
        if self.block().terminator.is_none() {
            self.terminate(Terminator::Jump(merge))?;
        }

        self.current = else_id;
        self.locals = locals_before.clone();
        self.heap_owned = owned_before.clone();
        let (eval, ety, eowned) = self.lower_block_trailing(else_block)?;
        if ety != tty {
            return Err(IrError::new(
                "native IR: if-expression branches disagree on type",
            ));
        }
        let eval = if ety.is_heap() && !eowned {
            self.emit_clone(eval, ety)
        } else {
            eval
        };
        self.emit(Inst::Store {
            ptr: result_slot,
            value: eval,
        });
        self.drop_arm_locals(&locals_before)?;
        if self.block().terminator.is_none() {
            self.terminate(Terminator::Jump(merge))?;
        }

        self.locals = locals_before;
        self.heap_owned = owned_before;
        self.current = merge;
        if let Inst::Alloca { ty, .. } =
            &mut self.function.blocks[result_block.0 as usize].instructions[result_index]
        {
            *ty = tty;
        }
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: tty,
        });
        Ok((out, tty, tty.is_heap()))
    }

    /// `wrap_err(r, prefix)` — if Err, prefix the message with `prefix: `.
    fn lower_wrap_err(
        &mut self,
        result: &Expr,
        prefix: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (mut r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: wrap_err expects Result"));
        };
        if self.structs.field(id, "Err_0").is_none() {
            return Err(IrError::new("native IR: wrap_err expects Result"));
        }
        if !owned {
            r = self.emit_clone(r, rty);
        }
        let (p, pty, po) = self.lower_expr(prefix)?;
        if pty != Type::Str {
            return Err(IrError::new("native IR: wrap_err prefix expects string"));
        }
        let prefix_owned = if po {
            p
        } else {
            self.emit_clone(p, Type::Str)
        };

        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let zero = self.const_int(0, Type::I64);
        let is_ok = self.value();
        self.emit(Inst::Binary {
            out: is_ok,
            op: BinOp::Eq,
            left: tag,
            right: zero,
            ty: Type::I64,
        });

        let ok_block = self.new_block();
        let err_block = self.new_block();
        let merge = self.new_block();
        let result_slot = self.value();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: rty,
        });
        self.terminate(Terminator::Branch {
            condition: is_ok,
            then_block: ok_block,
            else_block: err_block,
        })?;

        // Ok: keep r, drop prefix.
        self.current = ok_block;
        self.emit(Inst::DropString { value: prefix_owned });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: r,
        });
        self.terminate(Terminator::Jump(merge))?;

        // Err: build Err(prefix + ": " + old_err), drop old r.
        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        let old_err = self.value();
        self.emit(Inst::StructField {
            out: old_err,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        // Clone err out before dropping the Result shell.
        let old_err_owned = self.emit_clone(old_err, Type::Str);
        self.emit(Inst::DropStruct {
            value: r,
            struct_id: id,
        });
        let sep = self.value();
        self.emit(Inst::StringLiteral {
            out: sep,
            bytes: b": ".to_vec(),
        });
        let mid = self.value();
        self.emit(Inst::StringConcat {
            out: mid,
            left: prefix_owned,
            right: sep,
        });
        self.emit(Inst::DropString { value: prefix_owned });
        // sep is a literal (non-owned) — no drop.
        let msg = self.value();
        self.emit(Inst::StringConcat {
            out: msg,
            left: mid,
            right: old_err_owned,
        });
        self.emit(Inst::DropString { value: mid });
        self.emit(Inst::DropString { value: old_err_owned });
        // Build Err(msg).
        let info = self
            .structs
            .variant("Err")
            .ok_or_else(|| IrError::new("native IR: Err variant missing"))?;
        let out_err = self.value();
        self.emit(Inst::EnumMake {
            out: out_err,
            enum_id: info.enum_id,
            tag: info.tag,
            slot_base: info.slot_base as u32,
            payload: vec![msg],
        });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: out_err,
        });
        self.terminate(Terminator::Jump(merge))?;

        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: rty,
        });
        Ok((out, rty, true))
    }

    /// `error_join(a, b)` — prefer first Err; both Err → combined message; both Ok → a.
    fn lower_error_join(
        &mut self,
        left: &Expr,
        right: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (mut a, aty, ao) = self.lower_expr(left)?;
        let Type::Struct(aid) = aty else {
            return Err(IrError::new("native IR: error_join expects Result"));
        };
        if self.structs.field(aid, "Err_0").is_none() {
            return Err(IrError::new("native IR: error_join expects Result"));
        }
        if !ao {
            a = self.emit_clone(a, aty);
        }
        let (mut b, bty, bo) = self.lower_expr(right)?;
        if bty != aty {
            return Err(IrError::new("native IR: error_join Result types must match"));
        }
        if !bo {
            b = self.emit_clone(b, bty);
        }

        let tag_a = self.value();
        self.emit(Inst::StructField {
            out: tag_a,
            base: a,
            struct_id: aid,
            index: 0,
            ty: Type::I64,
        });
        let tag_b = self.value();
        self.emit(Inst::StructField {
            out: tag_b,
            base: b,
            struct_id: aid,
            index: 0,
            ty: Type::I64,
        });
        let zero = self.const_int(0, Type::I64);
        let a_ok = self.value();
        self.emit(Inst::Binary {
            out: a_ok,
            op: BinOp::Eq,
            left: tag_a,
            right: zero,
            ty: Type::I64,
        });
        let b_ok = self.value();
        self.emit(Inst::Binary {
            out: b_ok,
            op: BinOp::Eq,
            left: tag_b,
            right: zero,
            ty: Type::I64,
        });

        // Nested branches: if a_ok then (if b_ok keep a else keep b) else (if b_ok keep a else join)
        let result_slot = self.value();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: aty,
        });
        let a_ok_block = self.new_block();
        let a_err_block = self.new_block();
        let merge = self.new_block();
        self.terminate(Terminator::Branch {
            condition: a_ok,
            then_block: a_ok_block,
            else_block: a_err_block,
        })?;

        // a Ok
        self.current = a_ok_block;
        {
            let both_ok = self.new_block();
            let b_err = self.new_block();
            self.terminate(Terminator::Branch {
                condition: b_ok,
                then_block: both_ok,
                else_block: b_err,
            })?;
            self.current = both_ok;
            self.emit(Inst::DropStruct {
                value: b,
                struct_id: aid,
            });
            self.emit(Inst::Store {
                ptr: result_slot,
                value: a,
            });
            self.terminate(Terminator::Jump(merge))?;
            self.current = b_err;
            self.emit(Inst::DropStruct {
                value: a,
                struct_id: aid,
            });
            self.emit(Inst::Store {
                ptr: result_slot,
                value: b,
            });
            self.terminate(Terminator::Jump(merge))?;
        }

        // a Err
        self.current = a_err_block;
        {
            let a_only = self.new_block();
            let both_err = self.new_block();
            self.terminate(Terminator::Branch {
                condition: b_ok,
                then_block: a_only,
                else_block: both_err,
            })?;
            self.current = a_only;
            self.emit(Inst::DropStruct {
                value: b,
                struct_id: aid,
            });
            self.emit(Inst::Store {
                ptr: result_slot,
                value: a,
            });
            self.terminate(Terminator::Jump(merge))?;

            self.current = both_err;
            let (err_idx, err_ty) = self
                .structs
                .field(aid, "Err_0")
                .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
            let ea = self.value();
            self.emit(Inst::StructField {
                out: ea,
                base: a,
                struct_id: aid,
                index: err_idx,
                ty: err_ty,
            });
            let eb = self.value();
            self.emit(Inst::StructField {
                out: eb,
                base: b,
                struct_id: aid,
                index: err_idx,
                ty: err_ty,
            });
            let ea_o = self.emit_clone(ea, Type::Str);
            let eb_o = self.emit_clone(eb, Type::Str);
            self.emit(Inst::DropStruct {
                value: a,
                struct_id: aid,
            });
            self.emit(Inst::DropStruct {
                value: b,
                struct_id: aid,
            });
            let sep = self.value();
            self.emit(Inst::StringLiteral {
                out: sep,
                bytes: b"; ".to_vec(),
            });
            let mid = self.value();
            self.emit(Inst::StringConcat {
                out: mid,
                left: ea_o,
                right: sep,
            });
            self.emit(Inst::DropString { value: ea_o });
            let msg = self.value();
            self.emit(Inst::StringConcat {
                out: msg,
                left: mid,
                right: eb_o,
            });
            self.emit(Inst::DropString { value: mid });
            self.emit(Inst::DropString { value: eb_o });
            let info = self
                .structs
                .variant_matching("Err", &[Type::Str])
                .or_else(|| self.structs.variant("Err"))
                .ok_or_else(|| IrError::new("native IR: Err missing"))?;
            let out_err = self.value();
            self.emit(Inst::EnumMake {
                out: out_err,
                enum_id: info.enum_id,
                tag: info.tag,
                slot_base: info.slot_base as u32,
                payload: vec![msg],
            });
            self.emit(Inst::Store {
                ptr: result_slot,
                value: out_err,
            });
            self.terminate(Terminator::Jump(merge))?;
        }

        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: aty,
        });
        Ok((out, aty, true))
    }

    /// `parse_bool(s)` → `Result[int, string]`.
    fn lower_parse_bool(&mut self, s_expr: &Expr) -> Result<(Value, Type, bool), IrError> {
        let (s, ty, owned) = self.lower_expr(s_expr)?;
        if ty != Type::Str {
            return Err(IrError::new("native IR: parse_bool expects string"));
        }
        let pack = self.value();
        let one = self.const_int(1, Type::I64);
        self.emit(Inst::Call {
            out: Some(pack),
            function: "mako_native_pack_new".into(),
            args: vec![one],
            ret: Some(Type::I64),
        });
        let ok_i = self.value();
        self.emit(Inst::Call {
            out: Some(ok_i),
            function: "mako_native_parse_bool_try_ptr".into(),
            args: vec![s, pack],
            ret: Some(Type::I64),
        });
        if owned {
            self.emit(Inst::DropString { value: s });
        }
        let z = self.const_int(0, Type::I64);
        let val = self.value();
        self.emit(Inst::Call {
            out: Some(val),
            function: "mako_native_pack_get".into(),
            args: vec![pack, z],
            ret: Some(Type::I64),
        });
        let ok_info = self
            .structs
            .variant_matching("Ok", &[Type::I64])
            .or_else(|| self.structs.variant("Ok"))
            .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
        let err_info = self
            .structs
            .variant_matching("Err", &[Type::Str])
            .or_else(|| self.structs.variant("Err"))
            .ok_or_else(|| IrError::new("native IR: Err missing"))?;
        let result_ty = Type::Struct(ok_info.enum_id);
        let result_slot = self.value();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: result_ty,
        });
        let ok_b = self.value();
        self.emit(Inst::Cast {
            out: ok_b,
            value: ok_i,
            from: Type::I64,
            to: Type::I1,
        });
        let then_b = self.new_block();
        let else_b = self.new_block();
        let merge = self.new_block();
        self.terminate(Terminator::Branch {
            condition: ok_b,
            then_block: then_b,
            else_block: else_b,
        })?;
        self.current = then_b;
        let ok_out = self.value();
        self.emit(Inst::EnumMake {
            out: ok_out,
            enum_id: ok_info.enum_id,
            tag: ok_info.tag,
            slot_base: ok_info.slot_base as u32,
            payload: vec![val],
        });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: ok_out,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = else_b;
        let msg_lit = self.value();
        self.emit(Inst::StringLiteral {
            out: msg_lit,
            bytes: b"parse_bool failed".to_vec(),
        });
        let msg = self.emit_clone(msg_lit, Type::Str);
        let err_out = self.value();
        self.emit(Inst::EnumMake {
            out: err_out,
            enum_id: err_info.enum_id,
            tag: err_info.tag,
            slot_base: err_info.slot_base as u32,
            payload: vec![msg],
        });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: err_out,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let loaded = self.value();
        self.emit(Inst::Load {
            out: loaded,
            ptr: result_slot,
            ty: result_ty,
        });
        self.emit(Inst::Call {
            out: None,
            function: "mako_native_pack_free".into(),
            args: vec![pack],
            ret: None,
        });
        Ok((loaded, result_ty, true))
    }

    /// `errorf(fmt, arg)` — Err with `%s` substitution (or `fmt: arg`).
    fn lower_errorf(
        &mut self,
        fmt: &Expr,
        arg: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        self.lower_tagged_err_msg(fmt, arg, "errorf")
    }

    /// `error_tag(tag, msg)` → `Err("tag: msg")` (enum-like string tags).
    fn lower_error_tag(
        &mut self,
        tag: &Expr,
        msg: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        self.lower_tagged_err_msg(tag, msg, "error_tag")
    }

    /// Build `Err(left + ": " + right)` for Result[int,string]-style monomorphs.
    fn lower_tagged_err_msg(
        &mut self,
        left: &Expr,
        right: &Expr,
        who: &str,
    ) -> Result<(Value, Type, bool), IrError> {
        let (f, ft, fo) = self.lower_expr(left)?;
        let (a, at, ao) = self.lower_expr(right)?;
        if ft != Type::Str || at != Type::Str {
            return Err(IrError::new(format!("native IR: {who} expects strings")));
        }
        // Seed: always `left + ": " + right` (matches mako_error_tag / simple errorf).
        let f_owned = if fo {
            f
        } else {
            self.emit_clone(f, Type::Str)
        };
        let a_owned = if ao {
            a
        } else {
            self.emit_clone(a, Type::Str)
        };
        let sep = self.value();
        self.emit(Inst::StringLiteral {
            out: sep,
            bytes: b": ".to_vec(),
        });
        let mid = self.value();
        self.emit(Inst::StringConcat {
            out: mid,
            left: f_owned,
            right: sep,
        });
        self.emit(Inst::DropString { value: f_owned });
        let msg = self.value();
        self.emit(Inst::StringConcat {
            out: msg,
            left: mid,
            right: a_owned,
        });
        self.emit(Inst::DropString { value: mid });
        self.emit(Inst::DropString { value: a_owned });
        // Prefer Result[int,string] monomorph (payload Str on Err).
        let info = self
            .structs
            .variant_matching("Err", &[Type::Str])
            .or_else(|| self.structs.variant("Err"))
            .ok_or_else(|| IrError::new("native IR: Err variant missing"))?;
        let out = self.value();
        self.emit(Inst::EnumMake {
            out,
            enum_id: info.enum_id,
            tag: info.tag,
            slot_base: info.slot_base as u32,
            payload: vec![msg],
        });
        Ok((out, Type::Struct(info.enum_id), true))
    }

    /// `error_is(r, needle)` — true if Err message contains needle.
    fn lower_error_is(
        &mut self,
        result: &Expr,
        needle: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: error_is expects Result"));
        };
        let (n, nt, no) = self.lower_expr(needle)?;
        if nt != Type::Str {
            return Err(IrError::new("native IR: error_is needle expects string"));
        }
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });
        let err_block = self.new_block();
        let ok_block = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::I1,
        });
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;

        self.current = ok_block;
        let f = self.const_int(0, Type::I1);
        self.emit(Inst::Store { ptr: slot, value: f });
        if no {
            self.emit(Inst::DropString { value: n });
        }
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;

        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        let err_s = self.value();
        self.emit(Inst::StructField {
            out: err_s,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        let raw = self.value();
        self.emit(Inst::Call {
            out: Some(raw),
            function: "mako_native_str_contains_ptr".into(),
            args: vec![err_s, n],
            ret: Some(Type::I64),
        });
        let contains = self.value();
        self.emit(Inst::Cast {
            out: contains,
            value: raw,
            from: Type::I64,
            to: Type::I1,
        });
        self.emit(Inst::Store {
            ptr: slot,
            value: contains,
        });
        if no {
            self.emit(Inst::DropString { value: n });
        }
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;

        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::I1,
        });
        Ok((out, Type::I1, false))
    }

    /// `error_unwrap` / `error_root` — peel `ctx: rest` layers on Err messages.
    fn lower_error_peel(
        &mut self,
        result: &Expr,
        root: bool,
    ) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: error_unwrap/root expects Result"));
        };
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });
        let err_block = self.new_block();
        let ok_block = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: rty,
        });
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;

        // Ok: clone / move result through.
        self.current = ok_block;
        let ok_val = if owned {
            r
        } else {
            self.emit_clone(r, rty)
        };
        self.emit(Inst::Store {
            ptr: slot,
            value: ok_val,
        });
        self.terminate(Terminator::Jump(merge))?;

        // Err: peel message, rebuild Err.
        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        if err_ty != Type::Str {
            return Err(IrError::new("native IR: error peel expects string Err"));
        }
        let err_s = self.value();
        self.emit(Inst::StructField {
            out: err_s,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        let peeled = self.value();
        let peel_fn = if root {
            "mako_native_error_peel_root_ptr"
        } else {
            "mako_native_error_peel_once_ptr"
        };
        self.emit(Inst::Call {
            out: Some(peeled),
            function: peel_fn.into(),
            args: vec![err_s],
            ret: Some(Type::Str),
        });
        // Build new Err with peeled message (owned).
        let info = self
            .structs
            .variant_matching("Err", &[Type::Str])
            .or_else(|| self.structs.variant("Err"))
            .ok_or_else(|| IrError::new("native IR: Err variant missing"))?;
        let new_err = self.value();
        self.emit(Inst::EnumMake {
            out: new_err,
            enum_id: info.enum_id,
            tag: info.tag,
            slot_base: info.slot_base as u32,
            payload: vec![peeled],
        });
        self.emit(Inst::Store {
            ptr: slot,
            value: new_err,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;

        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: rty,
        });
        Ok((out, rty, true))
    }

    /// `error_as_tag(r)` — prefix before first `": "` of Err, else `""`.
    fn lower_error_as_tag(&mut self, result: &Expr) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: error_as_tag expects Result"));
        };
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });
        let err_block = self.new_block();
        let ok_block = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::Str,
        });
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;
        self.current = ok_block;
        let empty = self.value();
        self.emit(Inst::StringLiteral {
            out: empty,
            bytes: Vec::new(),
        });
        self.emit(Inst::Store {
            ptr: slot,
            value: empty,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        let err_s = self.value();
        self.emit(Inst::StructField {
            out: err_s,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        let out_s = self.value();
        self.emit(Inst::Call {
            out: Some(out_s),
            function: "mako_native_error_as_tag_ptr".into(),
            args: vec![err_s],
            ret: Some(Type::Str),
        });
        self.emit(Inst::Store {
            ptr: slot,
            value: out_s,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::Str,
        });
        Ok((out, Type::Str, true))
    }

    /// `error_has_tag(r, tag)` — Err message starts with `tag + ": "`.
    fn lower_error_has_tag(
        &mut self,
        result: &Expr,
        tag_expr: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: error_has_tag expects Result"));
        };
        let (t, tty, to) = self.lower_expr(tag_expr)?;
        if tty != Type::Str {
            return Err(IrError::new("native IR: error_has_tag tag expects string"));
        }
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });
        let err_block = self.new_block();
        let ok_block = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::I1,
        });
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;
        self.current = ok_block;
        let f = self.const_int(0, Type::I1);
        self.emit(Inst::Store { ptr: slot, value: f });
        if to {
            self.emit(Inst::DropString { value: t });
        }
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        let err_s = self.value();
        self.emit(Inst::StructField {
            out: err_s,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        let raw = self.value();
        self.emit(Inst::Call {
            out: Some(raw),
            function: "mako_native_error_has_tag_ptr".into(),
            args: vec![err_s, t],
            ret: Some(Type::I64),
        });
        let b = self.value();
        self.emit(Inst::Cast {
            out: b,
            value: raw,
            from: Type::I64,
            to: Type::I1,
        });
        self.emit(Inst::Store { ptr: slot, value: b });
        if to {
            self.emit(Inst::DropString { value: t });
        }
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::I1,
        });
        Ok((out, Type::I1, false))
    }

    /// `parse_int*` / `parse_int_base` → `Result[int, string]`.
    /// When `base_expr` is `Some`, use that runtime base; else use `fixed_base`.
    fn lower_parse_int_base(
        &mut self,
        s_expr: &Expr,
        base_expr: Option<&Expr>,
        fixed_base: i64,
    ) -> Result<(Value, Type, bool), IrError> {
        let (s, ty, owned) = self.lower_expr(s_expr)?;
        if ty != Type::Str {
            return Err(IrError::new("native IR: parse_int expects string"));
        }
        let base_v = if let Some(be) = base_expr {
            let (b, bt, bo) = self.lower_expr(be)?;
            if bt != Type::I64 || bo {
                return Err(IrError::new("native IR: parse_int_base base must be int"));
            }
            b
        } else {
            self.const_int(fixed_base, Type::I64)
        };
        let pack = self.value();
        let one = self.const_int(1, Type::I64);
        self.emit(Inst::Call {
            out: Some(pack),
            function: "mako_native_pack_new".into(),
            args: vec![one],
            ret: Some(Type::I64),
        });
        let ok_i = self.value();
        self.emit(Inst::Call {
            out: Some(ok_i),
            function: "mako_native_parse_int_base_try_ptr".into(),
            args: vec![s, base_v, pack],
            ret: Some(Type::I64),
        });
        if owned {
            self.emit(Inst::DropString { value: s });
        }
        let z = self.const_int(0, Type::I64);
        let val = self.value();
        self.emit(Inst::Call {
            out: Some(val),
            function: "mako_native_pack_get".into(),
            args: vec![pack, z],
            ret: Some(Type::I64),
        });
        let ok_info = self
            .structs
            .variant_matching("Ok", &[Type::I64])
            .or_else(|| self.structs.variant("Ok"))
            .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
        let err_info = self
            .structs
            .variant_matching("Err", &[Type::Str])
            .or_else(|| self.structs.variant("Err"))
            .ok_or_else(|| IrError::new("native IR: Err missing"))?;
        let result_ty = Type::Struct(ok_info.enum_id);
        let result_slot = self.value();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: result_ty,
        });
        let ok_b = self.value();
        self.emit(Inst::Cast {
            out: ok_b,
            value: ok_i,
            from: Type::I64,
            to: Type::I1,
        });
        let then_b = self.new_block();
        let else_b = self.new_block();
        let merge = self.new_block();
        self.terminate(Terminator::Branch {
            condition: ok_b,
            then_block: then_b,
            else_block: else_b,
        })?;
        self.current = then_b;
        let ok_out = self.value();
        self.emit(Inst::EnumMake {
            out: ok_out,
            enum_id: ok_info.enum_id,
            tag: ok_info.tag,
            slot_base: ok_info.slot_base as u32,
            payload: vec![val],
        });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: ok_out,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = else_b;
        let msg_lit = self.value();
        self.emit(Inst::StringLiteral {
            out: msg_lit,
            bytes: b"parse_int failed".to_vec(),
        });
        let msg = self.emit_clone(msg_lit, Type::Str);
        let err_out = self.value();
        self.emit(Inst::EnumMake {
            out: err_out,
            enum_id: err_info.enum_id,
            tag: err_info.tag,
            slot_base: err_info.slot_base as u32,
            payload: vec![msg],
        });
        self.emit(Inst::Store {
            ptr: result_slot,
            value: err_out,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let loaded = self.value();
        self.emit(Inst::Load {
            out: loaded,
            ptr: result_slot,
            ty: result_ty,
        });
        self.emit(Inst::Call {
            out: None,
            function: "mako_native_pack_free".into(),
            args: vec![pack],
            ret: None,
        });
        Ok((loaded, result_ty, true))
    }

    /// `error_string(r)` — Err message, or empty string if Ok.
    fn lower_error_string(&mut self, result: &Expr) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new("native IR: error_string expects Result"));
        };
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });
        let err_block = self.new_block();
        let ok_block = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::Str,
        });
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;

        self.current = ok_block;
        let empty = self.value();
        self.emit(Inst::StringLiteral {
            out: empty,
            bytes: Vec::new(),
        });
        // Clone to owned heap so both arms yield owned strings.
        let empty_owned = self.emit_clone(empty, Type::Str);
        self.emit(Inst::Store {
            ptr: slot,
            value: empty_owned,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;

        self.current = err_block;
        let (err_idx, err_ty) = self
            .structs
            .field(id, "Err_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
        let err_s = self.value();
        self.emit(Inst::StructField {
            out: err_s,
            base: r,
            struct_id: id,
            index: err_idx,
            ty: err_ty,
        });
        let err_owned = self.emit_clone(err_s, Type::Str);
        self.emit(Inst::Store {
            ptr: slot,
            value: err_owned,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;

        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::Str,
        });
        Ok((out, Type::Str, true))
    }

    /// `expr?` — early-return Err/None; peel Ok/Some payload.
    /// Supports cross-monomorph rewrap when the function returns a different
    /// Result/Option layout (e.g. `Result[Pt,string]?` inside
    /// `-> Result[int,string]`), plus Option↔Result conversion (None→Err("None"),
    /// Err→None).
    fn lower_try(&mut self, inner: &Expr) -> Result<(Value, Type, bool), IrError> {
        let (mut val, ty, owned) = self.lower_expr(inner)?;
        let Type::Struct(id) = ty else {
            return Err(IrError::new("native IR: `?` expects Result or Option"));
        };
        if !self.structs.is_enum(id) {
            return Err(IrError::new("native IR: `?` expects Result or Option"));
        }
        let is_result = self.structs.field(id, "Ok_0").is_some();
        let is_option = self.structs.field(id, "Some_0").is_some();
        if !is_result && !is_option {
            return Err(IrError::new("native IR: `?` expects Result or Option"));
        }
        let Some(Type::Struct(ret_id)) = self.function.ret else {
            return Err(IrError::new(
                "native IR: `?` requires a Result/Option-returning function",
            ));
        };
        let ret_is_result = self.structs.field(ret_id, "Ok_0").is_some();
        let ret_is_option = self.structs.field(ret_id, "Some_0").is_some();
        if !ret_is_result && !ret_is_option {
            return Err(IrError::new(
                "native IR: `?` requires a Result/Option-returning function",
            ));
        }

        // Own the scrutinee for the branch so both paths can free or return it.
        if !owned {
            val = self.emit_clone(val, ty);
        }

        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: val,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let one = self.const_int(1, Type::I64);
        let is_err = self.value();
        self.emit(Inst::Binary {
            out: is_err,
            op: BinOp::Eq,
            left: tag,
            right: one,
            ty: Type::I64,
        });

        let err_block = self.new_block();
        let ok_block = self.new_block();
        self.terminate(Terminator::Branch {
            condition: is_err,
            then_block: err_block,
            else_block: ok_block,
        })?;

        // Err/None → early-return (same monomorph, rewrap, or cross-family).
        self.current = err_block;
        self.emit_defers()?;
        // Locals dropped after we materialize the return value so we still
        // own any payload extracted from `val`.
        if ret_id == id {
            self.drop_owned_locals();
            self.terminate(Terminator::Return(Some(val)))?;
        } else if is_result && ret_is_result {
            // Result[A,E]? in Result[B,E] fn: rewrap Err payload into ret monomorph.
            let (err_idx, err_ty) = self
                .structs
                .field(id, "Err_0")
                .ok_or_else(|| IrError::new("native IR: Result missing Err_0"))?;
            let (ret_err_idx, ret_err_ty) = self
                .structs
                .field(ret_id, "Err_0")
                .ok_or_else(|| IrError::new("native IR: return Result missing Err_0"))?;
            if err_ty != ret_err_ty {
                return Err(IrError::new(
                    "native IR: `?` error type must match the function return type",
                ));
            }
            let err_payload = self.value();
            self.emit(Inst::StructField {
                out: err_payload,
                base: val,
                struct_id: id,
                index: err_idx,
                ty: err_ty,
            });
            let owned_err = if err_ty.is_heap() {
                self.emit_clone(err_payload, err_ty)
            } else {
                err_payload
            };
            self.emit(Inst::DropStruct {
                value: val,
                struct_id: id,
            });
            let ret_err_info = self
                .structs
                .variant_in_enum("Err", ret_id)
                .ok_or_else(|| IrError::new("native IR: return Result missing Err"))?;
            let out_err = self.value();
            self.emit(Inst::EnumMake {
                out: out_err,
                enum_id: ret_id,
                tag: ret_err_info.tag,
                slot_base: ret_err_info.slot_base as u32,
                payload: vec![owned_err],
            });
            let _ = ret_err_idx;
            self.drop_owned_locals();
            self.terminate(Terminator::Return(Some(out_err)))?;
        } else if is_option && ret_is_option {
            // Option[A]? in Option[B] fn: return None of the return monomorph.
            self.emit(Inst::DropStruct {
                value: val,
                struct_id: id,
            });
            let none_info = self
                .structs
                .variant_in_enum("None", ret_id)
                .ok_or_else(|| IrError::new("native IR: return Option missing None"))?;
            let out_none = self.value();
            self.emit(Inst::EnumMake {
                out: out_none,
                enum_id: ret_id,
                tag: none_info.tag,
                slot_base: none_info.slot_base as u32,
                payload: vec![],
            });
            self.drop_owned_locals();
            self.terminate(Terminator::Return(Some(out_none)))?;
        } else if is_option && ret_is_result {
            // Option? in Result-returning fn: None → Err("None").
            self.emit(Inst::DropStruct {
                value: val,
                struct_id: id,
            });
            let msg = self.value();
            self.emit(Inst::StringLiteral {
                out: msg,
                bytes: b"None".to_vec(),
            });
            let err_info = self
                .structs
                .variant_in_enum("Err", ret_id)
                .ok_or_else(|| IrError::new("native IR: return Result missing Err"))?;
            let out_err = self.value();
            self.emit(Inst::EnumMake {
                out: out_err,
                enum_id: ret_id,
                tag: err_info.tag,
                slot_base: err_info.slot_base as u32,
                payload: vec![msg],
            });
            self.drop_owned_locals();
            self.terminate(Terminator::Return(Some(out_err)))?;
        } else if is_result && ret_is_option {
            // Result? in Option-returning fn: Err → None.
            self.emit(Inst::DropStruct {
                value: val,
                struct_id: id,
            });
            let none_info = self
                .structs
                .variant_in_enum("None", ret_id)
                .ok_or_else(|| IrError::new("native IR: return Option missing None"))?;
            let out_none = self.value();
            self.emit(Inst::EnumMake {
                out: out_none,
                enum_id: ret_id,
                tag: none_info.tag,
                slot_base: none_info.slot_base as u32,
                payload: vec![],
            });
            self.drop_owned_locals();
            self.terminate(Terminator::Return(Some(out_none)))?;
        } else {
            return Err(IrError::new(
                "native IR: `?` error type must match the function return type",
            ));
        }

        // Ok/Some → peel payload, drop the shell.
        self.current = ok_block;
        let (payload_index, payload_ty) = if is_result {
            self.structs
                .field(id, "Ok_0")
                .ok_or_else(|| IrError::new("native IR: Result missing Ok_0"))?
        } else {
            self.structs
                .field(id, "Some_0")
                .ok_or_else(|| IrError::new("native IR: Option missing Some_0"))?
        };
        let payload = self.value();
        self.emit(Inst::StructField {
            out: payload,
            base: val,
            struct_id: id,
            index: payload_index,
            ty: payload_ty,
        });
        // Take an owned copy of the payload, then free the enum (which would
        // otherwise free the same heap payload a second time if we moved).
        let owned_payload = if payload_ty.is_heap() {
            self.emit_clone(payload, payload_ty)
        } else {
            payload
        };
        self.emit(Inst::DropStruct {
            value: val,
            struct_id: id,
        });
        Ok((owned_payload, payload_ty, payload_ty.is_heap()))
    }

    /// Count POD reflect leaves (int/bool/str + nested struct fields) for capacity.
    fn count_reflect_pod_leaves(&self, sid: u32) -> i64 {
        let mut n = 0i64;
        for i in 0..self.structs.field_count(sid) {
            match self.structs.field_type(sid, i) {
                Type::I64 | Type::I32 | Type::I1 | Type::Str => n += 1,
                Type::Struct(inner) if !self.structs.is_enum(inner) => {
                    n += self.count_reflect_pod_leaves(inner);
                }
                _ => {}
            }
        }
        n
    }

    /// `reflect_value_of(pod_struct)` — flatten POD int leaves into a ReflectValue bag.
    fn lower_reflect_value_of(&mut self, arg: &Expr) -> Result<(Value, Type, bool), IrError> {
        let (base, ty, owned) = self.lower_expr(arg)?;
        let Type::Struct(sid) = ty else {
            // Soft: empty bag for non-struct values.
            let schema = self.value();
            self.emit(Inst::StringLiteral {
                out: schema,
                bytes: b"".to_vec(),
            });
            let z = self.const_int(0, Type::I64);
            let out = self.value();
            self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_reflect_value_new_n_ptr".into(),
                args: vec![schema, z],
                ret: Some(Type::Opaque),
            });
            if owned {
                self.emit_drop(base, ty);
            }
            return Ok((out, Type::Opaque, false));
        };
        let name = self.structs.layout_name(sid);
        let leaf_n = self.count_reflect_pod_leaves(sid);
        let schema = self.value();
        self.emit(Inst::StringLiteral {
            out: schema,
            bytes: name.into_bytes(),
        });
        let nconst = self.const_int(leaf_n, Type::I64);
        let bag = self.value();
        self.emit(Inst::Call {
            out: Some(bag),
            function: "mako_native_reflect_value_new_n_ptr".into(),
            args: vec![schema, nconst],
            ret: Some(Type::Opaque),
        });
        let mut leaf_idx: i64 = 0;
        self.emit_reflect_pod_fields(bag, base, sid, &mut leaf_idx)?;
        if owned {
            self.emit(Inst::DropStruct {
                value: base,
                struct_id: sid,
            });
        }
        Ok((bag, Type::Opaque, false))
    }

    /// Recursively set leaf POD fields (I64 / nested struct) as string values on a ReflectValue.
    fn emit_reflect_pod_fields(
        &mut self,
        bag: Value,
        base: Value,
        sid: u32,
        leaf_idx: &mut i64,
    ) -> Result<(), IrError> {
        let n = self.structs.field_count(sid);
        for i in 0..n {
            let fty = self.structs.field_type(sid, i);
            let fv = self.value();
            self.emit(Inst::StructField {
                out: fv,
                base,
                struct_id: sid,
                index: i as u32,
                ty: fty,
            });
            match fty {
                Type::I64 | Type::I32 | Type::I1 => {
                    let mut v = fv;
                    if fty != Type::I64 {
                        let c = self.value();
                        self.emit(Inst::Cast {
                            out: c,
                            value: fv,
                            from: fty,
                            to: Type::I64,
                        });
                        v = c;
                    }
                    let s = self.value();
                    self.emit(Inst::Call {
                        out: Some(s),
                        function: "mako_native_int_to_string_ptr".into(),
                        args: vec![v],
                        ret: Some(Type::Str),
                    });
                    let idx = self.const_int(*leaf_idx, Type::I64);
                    *leaf_idx += 1;
                    let _rc = self.value();
                    self.emit(Inst::Call {
                        out: Some(_rc),
                        function: "mako_native_reflect_value_set_at_ptr".into(),
                        args: vec![bag, idx, s],
                        ret: Some(Type::I64),
                    });
                    self.emit(Inst::DropString { value: s });
                }
                Type::Str => {
                    let idx = self.const_int(*leaf_idx, Type::I64);
                    *leaf_idx += 1;
                    let _rc = self.value();
                    self.emit(Inst::Call {
                        out: Some(_rc),
                        function: "mako_native_reflect_value_set_at_ptr".into(),
                        args: vec![bag, idx, fv],
                        ret: Some(Type::I64),
                    });
                }
                Type::Struct(inner) if !self.structs.is_enum(inner) => {
                    self.emit_reflect_pod_fields(bag, fv, inner, leaf_idx)?;
                }
                _ => {
                    // Skip non-POD leaves (maps/slices/chans) for this seed.
                }
            }
        }
        Ok(())
    }

    /// Block expression `{ stmts; trailing }` — value is the trailing expression.
    fn lower_block_expr(
        &mut self,
        block: &crate::ast::Block,
    ) -> Result<(Value, Type, bool), IrError> {
        let saved_locals = self.locals.clone();
        let saved_owned = self.heap_owned.clone();
        let result = self.lower_block_trailing(block)?;
        // Drop locals introduced in the block (except the returned heap value
        // which is owned by the caller of this expression).
        let (val, ty, owned) = result;
        let keep = if owned { Some(val) } else { None };
        for (name, is_owned) in self.heap_owned.clone() {
            if is_owned && !saved_owned.contains_key(&name) {
                if let Some((ptr, lty)) = self.locals.get(&name).copied() {
                    let v = self.value();
                    self.emit(Inst::Load {
                        out: v,
                        ptr,
                        ty: lty,
                    });
                    if keep != Some(v) {
                        // Don't drop if this local was moved into the result.
                        // Heuristic: if the result was a bare load of this local
                        // that transferred ownership, flag is already cleared.
                        self.emit_drop(v, lty);
                    }
                }
            }
        }
        self.locals = saved_locals;
        self.heap_owned = saved_owned;
        // Re-mark result ownership for the outer scope.
        Ok((val, ty, owned))
    }

    /// Lower a block, returning the trailing expression value (last `Stmt::Expr`).
    /// If the block ends with a non-value statement (assign / if without value),
    /// yield unit `0` so match arms used for side effects still type-check.
    fn lower_block_trailing(
        &mut self,
        block: &crate::ast::Block,
    ) -> Result<(Value, Type, bool), IrError> {
        let n = block.stmts.len();
        if n == 0 {
            // Empty block → unit.
            return Ok((self.const_int(0, Type::I64), Type::I64, false));
        }
        for (i, stmt) in block.stmts.iter().enumerate() {
            if i + 1 == n {
                match stmt {
                    Stmt::Expr(e) => return self.lower_expr(e),
                    Stmt::If {
                        init: None,
                        cond,
                        then_block,
                        else_block: Some(eb),
                    } => {
                        return self.lower_if_expr(cond, then_block, eb);
                    }
                    Stmt::Return(Some(e)) if self.return_as_yield => {
                        // Inlined fan mapper: `return e` yields the mapped value.
                        return self.lower_expr(e);
                    }
                    Stmt::Return(Some(_))
                    | Stmt::Return(None)
                    | Stmt::Break(_)
                    | Stmt::Continue(_) => {
                        // Match/if arm (and normal blocks): real function return /
                        // break so control does not fall through after the match.
                        // Invent a unit value (unreachable) for the arm expression type.
                        self.lower_stmt(stmt)?;
                        return Ok((self.const_int(0, Type::I64), Type::I64, false));
                    }
                    _ => {
                        // Side-effect-only trailing stmt → unit.
                        self.lower_stmt(stmt)?;
                        return Ok((self.const_int(0, Type::I64), Type::I64, false));
                    }
                }
            }
            self.lower_stmt(stmt)?;
            if self.block().terminator.is_some() {
                // Diverging middle (e.g. return in match arm): remaining stmts
                // are dead; invent a unit value for the expression type.
                return Ok((self.const_int(0, Type::I64), Type::I64, false));
            }
        }
        Ok((self.const_int(0, Type::I64), Type::I64, false))
    }

    fn drop_arm_locals(
        &mut self,
        keep: &HashMap<String, (Value, Type)>,
    ) -> Result<(), IrError> {
        let owned: Vec<_> = self
            .heap_owned
            .iter()
            .filter_map(|(n, o)| o.then_some(n.clone()))
            .collect();
        for name in owned {
            if keep.contains_key(&name) {
                continue;
            }
            if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                let v = self.value();
                self.emit(Inst::Load {
                    out: v,
                    ptr,
                    ty,
                });
                self.emit_drop(v, ty);
                self.heap_owned.insert(name, false);
            }
        }
        Ok(())
    }

    /// `recv.method(args…)` → `{Type}_{method}(recv, args…)` for structs/enums.
    fn lower_method(
        &mut self,
        receiver: &Expr,
        method: &str,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        // Built-in methods on strings/slices. Struct receivers fall through to
        // user methods (e.g. Go-style `v.len()` on `Vec2`).
        if method == "len" && args.is_empty() {
            // Peek type without consuming: only handle known builtins here.
            let probe = match receiver {
                Expr::Ident(n) => self.locals.get(n).map(|(_, t)| *t),
                _ => None,
            };
            let is_struct_recv = matches!(probe, Some(Type::Struct(_)));
            if !is_struct_recv {
                let (s, t, o) = self.lower_expr(receiver)?;
                let out = self.value();
                let handled = match t {
                    Type::IntSlice => {
                        self.emit(Inst::SliceLen { out, slice: s });
                        true
                    }
                    Type::StrSlice => {
                        self.emit(Inst::StrSliceLen { out, slice: s });
                        true
                    }
                    Type::FloatSlice => {
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_float_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        });
                        true
                    }
                    Type::ByteSlice => {
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_byte_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        });
                        true
                    }
                    Type::BoolSlice => {
                        self.emit(Inst::Call {
                            out: Some(out),
                            function: "mako_native_bool_slice_len_ptr".into(),
                            args: vec![s],
                            ret: Some(Type::I64),
                        });
                        true
                    }
                    Type::Str => {
                        self.emit(Inst::StringLen { out, value: s });
                        true
                    }
                    Type::Struct(_) => false,
                    Type::StructSlice(_) | Type::PtrSlice(_) | Type::MapII | Type::MapSI
                    | Type::MapSS | Type::MapIF | Type::MapFI | Type::MapIPtr(_)
                    | Type::MapSPtr(_) => {
                        // len(map)/len(slice) style via runtime helpers later; for
                        // now maps use free `len()` call, not method form.
                        false
                    }
                    _ => false,
                };
                if handled {
                    if o {
                        self.emit_drop(s, t);
                    }
                    return Ok((out, Type::I64, false));
                }
                // Struct / other: re-lower via method path below (we already
                // consumed the expression when not a local probe). Fall through
                // only when we still have the receiver unevaluated.
                if !matches!(t, Type::Struct(_)) {
                    return Err(IrError::new(
                        "native IR: .len() requires string or slice receiver",
                    ));
                }
                // Struct receiver was lowered — call user method Type_len.
                let Type::Struct(id) = t else { unreachable!() };
                let type_name = self.structs.layout_name(id);
                let preferred = format!("{type_name}_{method}");
                if let Some((params, ret)) = self.signatures.get(&preferred).cloned() {
                    let mut call_args = vec![s];
                    if params.len() != 1 {
                        return Err(IrError::new("native IR: method len arity mismatch"));
                    }
                    let out = ret.map(|_| self.value());
                    self.emit(Inst::Call {
                        out,
                        function: preferred,
                        args: call_args,
                        ret,
                    });
                    if o {
                        // borrow self — do not drop if shared
                    }
                    return Ok(match (out, ret) {
                        (Some(o), Some(r)) => (o, r, r.is_heap()),
                        _ => (self.const_int(0, Type::I64), Type::I64, false),
                    });
                }
            }
        }

        // Package-qualified call: `lib.add(...)` → `lib__add(...)` after import rewrite.
        // Must run before lowering the receiver Ident (package aliases are not locals).
        // Also: pack variants `eng.Green(7)` and type-path `eng.Color.Green(4)`.
        if let Some(result) = self.try_lower_pack_method(receiver, method, args)? {
            return Ok(result);
        }

        let (recv, rty, rowned) = self.lower_expr(receiver)?;
        // Task.join() — retype joined i64 bits as the kicked function return type.
        if rty == Type::Task && method == "join" && args.is_empty() {
            let joined_ty = match receiver {
                Expr::Ident(name) => self
                    .job_ret_tys
                    .get(name)
                    .copied()
                    .unwrap_or(Type::I64),
                _ => Type::I64,
            };
            let out = self.value();
            self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_task_join_i64".into(),
                args: vec![recv],
                ret: Some(Type::I64),
            });
            // Result-returning jobs: record Err into enclosing nursery (err_count).
            if let Type::Struct(eid) = joined_ty {
                if self.structs.is_enum(eid) {
                    if let (Expr::Ident(jname), Some(err_info)) = (
                        receiver,
                        self.structs.variant_in_enum("Err", eid),
                    ) {
                        if let Some(crew) = self.job_crews.get(jname).cloned() {
                            if let Some((nslot, Type::Nursery)) =
                                self.locals.get(&crew).copied()
                            {
                                let n = self.value();
                                self.emit(Inst::Load {
                                    out: n,
                                    ptr: nslot,
                                    ty: Type::Nursery,
                                });
                                let tag = self.value();
                                self.emit(Inst::StructField {
                                    out: tag,
                                    base: out,
                                    struct_id: eid,
                                    index: 0,
                                    ty: Type::I64,
                                });
                                let want = self.const_int(err_info.tag as i64, Type::I64);
                                let is_err = self.value();
                                self.emit(Inst::Binary {
                                    out: is_err,
                                    op: BinOp::Eq,
                                    left: tag,
                                    right: want,
                                    ty: Type::I64,
                                });
                                let then_b = self.new_block();
                                let merge = self.new_block();
                                self.terminate(Terminator::Branch {
                                    condition: is_err,
                                    then_block: then_b,
                                    else_block: merge,
                                })?;
                                self.current = then_b;
                                let msg = self.value();
                                self.emit(Inst::StructField {
                                    out: msg,
                                    base: out,
                                    struct_id: eid,
                                    index: err_info.slot_base as u32,
                                    ty: Type::Str,
                                });
                                self.emit(Inst::Call {
                                    out: None,
                                    function: "mako_native_nursery_note_err_ptr".into(),
                                    args: vec![n, msg],
                                    ret: None,
                                });
                                self.terminate(Terminator::Jump(merge))?;
                                self.current = merge;
                            }
                        }
                    }
                }
            }
            if let Expr::Ident(name) = receiver {
                self.heap_owned.insert(name.clone(), false);
            }
            let owned = joined_ty.is_heap() || matches!(joined_ty, Type::Struct(_));
            return Ok((out, joined_ty, owned));
        }
        // Task.join_deadline(dl) → Result[T,string] (timeout wraps as Err).
        if rty == Type::Task && method == "join_deadline" && args.len() == 1 {
            let (dl, dty, do_) = self.lower_expr(&args[0])?;
            if dty != Type::I64 || do_ {
                return Err(IrError::new("native IR: join_deadline expects deadline int"));
            }
            let joined_ty = match receiver {
                Expr::Ident(name) => self
                    .job_ret_tys
                    .get(name)
                    .copied()
                    .unwrap_or(Type::I64),
                _ => Type::I64,
            };
            // If already Result[*,string], return that type; else wrap I64 in Result.
            let (result_id, payload_is_result) = match joined_ty {
                Type::Struct(eid) if self.structs.is_enum(eid) => (eid, true),
                other => (self.structs.intern_result(other, Type::Str), false),
            };
            let result_ty = Type::Struct(result_id);
            let pack = self.value();
            let one = self.const_int(1, Type::I64);
            self.emit(Inst::Call {
                out: Some(pack),
                function: "mako_native_pack_new".into(),
                args: vec![one],
                ret: Some(Type::I64),
            });
            let ok_i = self.value();
            self.emit(Inst::Call {
                out: Some(ok_i),
                function: "mako_native_task_join_deadline_i64".into(),
                args: vec![recv, dl, pack],
                ret: Some(Type::I64),
            });
            let z = self.const_int(0, Type::I64);
            let val = self.value();
            self.emit(Inst::Call {
                out: Some(val),
                function: "mako_native_pack_get".into(),
                args: vec![pack, z],
                ret: Some(Type::I64),
            });
            let is_ok = self.value();
            self.emit(Inst::Binary {
                out: is_ok,
                op: BinOp::Ne,
                left: ok_i,
                right: z,
                ty: Type::I64,
            });
            let then_id = self.new_block();
            let else_id = self.new_block();
            let merge_id = self.new_block();
            let slot = self.value();
            self.emit(Inst::Alloca {
                out: slot,
                ty: result_ty,
            });
            self.terminate(Terminator::Branch {
                condition: is_ok,
                then_block: then_id,
                else_block: else_id,
            })?;
            self.current = then_id;
            if payload_is_result {
                // Joined bits already a Result pointer — store as-is.
                self.emit(Inst::Store {
                    ptr: slot,
                    value: val,
                });
            } else {
                let ok_info = self
                    .structs
                    .variant_in_enum("Ok", result_id)
                    .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
                let ok_v = self.value();
                self.emit(Inst::EnumMake {
                    out: ok_v,
                    enum_id: result_id,
                    tag: ok_info.tag,
                    slot_base: ok_info.slot_base as u32,
                    payload: vec![val],
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: ok_v,
                });
            }
            self.terminate(Terminator::Jump(merge_id))?;
            self.current = else_id;
            {
                let msg_lit = self.value();
                self.emit(Inst::StringLiteral {
                    out: msg_lit,
                    bytes: b"timeout".to_vec(),
                });
                let msg = self.emit_clone(msg_lit, Type::Str);
                let err_info = self
                    .structs
                    .variant_in_enum("Err", result_id)
                    .ok_or_else(|| IrError::new("native IR: Err missing"))?;
                let err_v = self.value();
                self.emit(Inst::EnumMake {
                    out: err_v,
                    enum_id: result_id,
                    tag: err_info.tag,
                    slot_base: err_info.slot_base as u32,
                    payload: vec![msg],
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: err_v,
                });
            }
            self.terminate(Terminator::Jump(merge_id))?;
            self.current = merge_id;
            let loaded = self.value();
            self.emit(Inst::Load {
                out: loaded,
                ptr: slot,
                ty: result_ty,
            });
            self.emit(Inst::Call {
                out: None,
                function: "mako_native_pack_free".into(),
                args: vec![pack],
                ret: None,
            });
            if let Expr::Ident(name) = receiver {
                self.heap_owned.insert(name.clone(), false);
            }
            return Ok((loaded, result_ty, true));
        }
        // Task.join_timeout(ms) → Result[int, string]
        if rty == Type::Task && method == "join_timeout" && args.len() == 1 {
            let (ms, mt, mo) = self.lower_expr(&args[0])?;
            if mt != Type::I64 || mo {
                return Err(IrError::new("native IR: join_timeout expects int ms"));
            }
            let result_id = self.structs.intern_result(Type::I64, Type::Str);
            let result_ty = Type::Struct(result_id);
            let pack = self.value();
            let one = self.const_int(1, Type::I64);
            self.emit(Inst::Call {
                out: Some(pack),
                function: "mako_native_pack_new".into(),
                args: vec![one],
                ret: Some(Type::I64),
            });
            let ok_i = self.value();
            self.emit(Inst::Call {
                out: Some(ok_i),
                function: "mako_native_task_join_timeout_i64".into(),
                args: vec![recv, ms, pack],
                ret: Some(Type::I64),
            });
            let z = self.const_int(0, Type::I64);
            let val = self.value();
            self.emit(Inst::Call {
                out: Some(val),
                function: "mako_native_pack_get".into(),
                args: vec![pack, z],
                ret: Some(Type::I64),
            });
            let is_ok = self.value();
            self.emit(Inst::Binary {
                out: is_ok,
                op: BinOp::Ne,
                left: ok_i,
                right: z,
                ty: Type::I64,
            });
            let then_id = self.new_block();
            let else_id = self.new_block();
            let merge_id = self.new_block();
            let slot = self.value();
            self.emit(Inst::Alloca {
                out: slot,
                ty: result_ty,
            });
            self.terminate(Terminator::Branch {
                condition: is_ok,
                then_block: then_id,
                else_block: else_id,
            })?;
            self.current = then_id;
            {
                let ok_info = self
                    .structs
                    .variant_in_enum("Ok", result_id)
                    .ok_or_else(|| IrError::new("native IR: Ok missing for Result[int,string]"))?;
                let ok_v = self.value();
                self.emit(Inst::EnumMake {
                    out: ok_v,
                    enum_id: result_id,
                    tag: ok_info.tag,
                    slot_base: ok_info.slot_base as u32,
                    payload: vec![val],
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: ok_v,
                });
            }
            self.terminate(Terminator::Jump(merge_id))?;
            self.current = else_id;
            {
                let msg_lit = self.value();
                self.emit(Inst::StringLiteral {
                    out: msg_lit,
                    bytes: b"timeout".to_vec(),
                });
                // Enum owns heap payloads; clone the literal view so DropStruct
                // does not free static/rodata (SIGABRT under malloc).
                let msg = self.emit_clone(msg_lit, Type::Str);
                let err_info = self
                    .structs
                    .variant_in_enum("Err", result_id)
                    .ok_or_else(|| IrError::new("native IR: Err missing for Result[int,string]"))?;
                let err_v = self.value();
                self.emit(Inst::EnumMake {
                    out: err_v,
                    enum_id: result_id,
                    tag: err_info.tag,
                    slot_base: err_info.slot_base as u32,
                    payload: vec![msg],
                });
                self.emit(Inst::Store {
                    ptr: slot,
                    value: err_v,
                });
            }
            self.terminate(Terminator::Jump(merge_id))?;
            self.current = merge_id;
            let loaded = self.value();
            self.emit(Inst::Load {
                out: loaded,
                ptr: slot,
                ty: result_ty,
            });
            if let Expr::Ident(name) = receiver {
                self.heap_owned.insert(name.clone(), false);
            }
            let _ = rowned;
            return Ok((loaded, result_ty, true));
        }
        // Crew/nursery methods
        if rty == Type::Nursery {
            match (method, args.len()) {
                ("cancel", 0) => {
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_nursery_cancel".into(),
                        args: vec![recv],
                        ret: None,
                    });
                    return Ok((self.const_int(0, Type::I64), Type::I64, false));
                }
                ("cancelled", 0) => {
                    let raw = self.value();
                    self.emit(Inst::Call {
                        out: Some(raw),
                        function: "mako_native_nursery_cancelled".into(),
                        args: vec![recv],
                        ret: Some(Type::I64),
                    });
                    let b = self.value();
                    self.emit(Inst::Cast {
                        out: b,
                        value: raw,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    return Ok((b, Type::I1, false));
                }
                ("drain", 0) | ("drain", 1) => {
                    let ms = if args.len() == 1 {
                        let (v, t, o) = self.lower_expr(&args[0])?;
                        if t != Type::I64 || o {
                            return Err(IrError::new("native IR: nursery.drain ms must int"));
                        }
                        v
                    } else {
                        self.const_int(-1, Type::I64)
                    };
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_nursery_drain".into(),
                        args: vec![recv, ms],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    return Ok((out, Type::I64, false));
                }
                ("kick", _) => {
                    return Err(IrError::new(
                        "native IR: use crew.kick(call) expression form",
                    ));
                }
                ("err_count", 0) => {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_nursery_err_count".into(),
                        args: vec![recv],
                        ret: Some(Type::I64),
                    });
                    return Ok((out, Type::I64, false));
                }
                ("first_err", 0) => {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_nursery_first_err".into(),
                        args: vec![recv],
                        ret: Some(Type::Str),
                    });
                    return Ok((out, Type::Str, true));
                }
                ("wait", 0) => {
                    // join_pending; Ok(err_count) or Err(first_err)
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_nursery_join_pending".into(),
                        args: vec![recv],
                        ret: None,
                    });
                    let ok_i = self.value();
                    self.emit(Inst::Call {
                        out: Some(ok_i),
                        function: "mako_native_nursery_ok".into(),
                        args: vec![recv],
                        ret: Some(Type::I64),
                    });
                    let result_id = self.structs.intern_result(Type::I64, Type::Str);
                    let result_ty = Type::Struct(result_id);
                    let z = self.const_int(0, Type::I64);
                    let is_ok = self.value();
                    self.emit(Inst::Binary {
                        out: is_ok,
                        op: BinOp::Ne,
                        left: ok_i,
                        right: z,
                        ty: Type::I64,
                    });
                    let then_b = self.new_block();
                    let else_b = self.new_block();
                    let merge = self.new_block();
                    let slot = self.value();
                    self.emit(Inst::Alloca {
                        out: slot,
                        ty: result_ty,
                    });
                    self.terminate(Terminator::Branch {
                        condition: is_ok,
                        then_block: then_b,
                        else_block: else_b,
                    })?;
                    self.current = then_b;
                    {
                        let cnt = self.value();
                        self.emit(Inst::Call {
                            out: Some(cnt),
                            function: "mako_native_nursery_err_count".into(),
                            args: vec![recv],
                            ret: Some(Type::I64),
                        });
                        let ok_info = self
                            .structs
                            .variant_in_enum("Ok", result_id)
                            .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
                        let ok_v = self.value();
                        self.emit(Inst::EnumMake {
                            out: ok_v,
                            enum_id: result_id,
                            tag: ok_info.tag,
                            slot_base: ok_info.slot_base as u32,
                            payload: vec![cnt],
                        });
                        self.emit(Inst::Store {
                            ptr: slot,
                            value: ok_v,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = else_b;
                    {
                        let msg = self.value();
                        self.emit(Inst::Call {
                            out: Some(msg),
                            function: "mako_native_nursery_first_err".into(),
                            args: vec![recv],
                            ret: Some(Type::Str),
                        });
                        let err_info = self
                            .structs
                            .variant_in_enum("Err", result_id)
                            .ok_or_else(|| IrError::new("native IR: Err missing"))?;
                        let err_v = self.value();
                        self.emit(Inst::EnumMake {
                            out: err_v,
                            enum_id: result_id,
                            tag: err_info.tag,
                            slot_base: err_info.slot_base as u32,
                            payload: vec![msg],
                        });
                        self.emit(Inst::Store {
                            ptr: slot,
                            value: err_v,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = merge;
                    let loaded = self.value();
                    self.emit(Inst::Load {
                        out: loaded,
                        ptr: slot,
                        ty: result_ty,
                    });
                    return Ok((loaded, result_ty, true));
                }
                _ => {
                    return Err(IrError::new(format!(
                        "native IR: unknown nursery method `{method}`"
                    )));
                }
            }
        }
        // Channel methods: ch.send(v) / ch.recv() / ch.close()
        if rty == Type::ChanI
            || rty == Type::ChanS
            || rty == Type::ChanF
            || matches!(rty, Type::ChanP(_))
        {
            match (method, args.len()) {
                ("send", 1) | ("try_send", 1) => {
                    let (mut v, vt, vo) = self.lower_expr(&args[0])?;
                    if rty == Type::ChanI {
                        if vt != Type::I64 || vo {
                            return Err(IrError::new("native IR: chan.send expects int"));
                        }
                    } else if rty == Type::ChanF {
                        if vt != Type::F64 || vo {
                            return Err(IrError::new("native IR: chan[float].send expects float"));
                        }
                        let bits = self.value();
                        self.emit(Inst::Call {
                            out: Some(bits),
                            function: "mako_native_f64_to_bits".into(),
                            args: vec![v],
                            ret: Some(Type::I64),
                        });
                        v = bits;
                    } else if matches!(rty, Type::ChanP(_)) {
                        // Pointer channel: bitcast heap/struct/bool to i64 payload.
                        if vt == Type::I1 {
                            let w = self.value();
                            self.emit(Inst::Cast {
                                out: w,
                                value: v,
                                from: Type::I1,
                                to: Type::I64,
                            });
                            v = w;
                        } else if !vt.is_ptr_sized() || vt == Type::F64 {
                            return Err(IrError::new("native IR: chan[T].send payload unsupported"));
                        } else {
                            // Move owned heap into the channel (caller must not drop).
                            if vo {
                                if let Expr::Ident(name) = &args[0] {
                                    self.heap_owned.insert(name.clone(), false);
                                }
                            } else if vt.is_heap() {
                                v = self.emit_clone(v, vt);
                            }
                        }
                    } else {
                        // String channel: pass owned string pointer as i64 payload.
                        if vt != Type::Str {
                            return Err(IrError::new("native IR: chan[string].send expects string"));
                        }
                        if !vo {
                            v = self.emit_clone(v, Type::Str);
                        } else if let Expr::Ident(name) = &args[0] {
                            // Move owned local into the channel — do not drop later.
                            self.heap_owned.insert(name.clone(), false);
                        }
                        // Channel owns the header after a successful send.
                    }
                    let out = self.value();
                    let fname = if method == "send" {
                        "mako_native_chan_send"
                    } else {
                        "mako_native_chan_try_send"
                    };
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: fname.into(),
                        args: vec![recv, v],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    return Ok((out, Type::I64, false));
                }
                ("recv", 0) => {
                    let raw = self.value();
                    self.emit(Inst::Call {
                        out: Some(raw),
                        function: "mako_native_chan_recv".into(),
                        args: vec![recv],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    if rty == Type::ChanS {
                        return Ok((raw, Type::Str, true));
                    }
                    if rty == Type::ChanF {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![raw],
                            ret: Some(Type::F64),
                        });
                        return Ok((f, Type::F64, false));
                    }
                    if let Type::ChanP(vk) = rty {
                        let ety = vk.to_type();
                        return Ok((raw, ety, ety.is_heap()));
                    }
                    return Ok((raw, Type::I64, false));
                }
                ("try_recv", 0) => {
                    // ch.try_recv() → Result[T,string] (Ok payload / Err "empty").
                    let elem_ty = match rty {
                        Type::ChanS => Type::Str,
                        Type::ChanF => Type::F64,
                        Type::ChanP(vk) => vk.to_type(),
                        _ => Type::I64,
                    };
                    let result_id = self.structs.intern_result(elem_ty, Type::Str);
                    let result_ty = Type::Struct(result_id);
                    let pack = self.value();
                    let one = self.const_int(1, Type::I64);
                    self.emit(Inst::Call {
                        out: Some(pack),
                        function: "mako_native_pack_new".into(),
                        args: vec![one],
                        ret: Some(Type::I64),
                    });
                    let rc = self.value();
                    self.emit(Inst::Call {
                        out: Some(rc),
                        function: "mako_native_chan_try_recv_out".into(),
                        args: vec![recv, pack],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    let z = self.const_int(0, Type::I64);
                    let val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_pack_get".into(),
                        args: vec![pack, z],
                        ret: Some(Type::I64),
                    });
                    // retype float bits if needed
                    let val = if elem_ty == Type::F64 {
                        let f = self.value();
                        self.emit(Inst::Call {
                            out: Some(f),
                            function: "mako_native_bits_to_f64".into(),
                            args: vec![val],
                            ret: Some(Type::F64),
                        });
                        f
                    } else {
                        val
                    };
                    let one_c = self.const_int(1, Type::I64);
                    let is_ok = self.value();
                    self.emit(Inst::Binary {
                        out: is_ok,
                        op: BinOp::Eq,
                        left: rc,
                        right: one_c,
                        ty: Type::I64,
                    });
                    let then_b = self.new_block();
                    let else_b = self.new_block();
                    let merge = self.new_block();
                    let result_slot = self.value();
                    self.emit(Inst::Alloca {
                        out: result_slot,
                        ty: result_ty,
                    });
                    self.terminate(Terminator::Branch {
                        condition: is_ok,
                        then_block: then_b,
                        else_block: else_b,
                    })?;
                    self.current = then_b;
                    {
                        let ok_info = self
                            .structs
                            .variant_in_enum("Ok", result_id)
                            .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
                        let ok_out = self.value();
                        self.emit(Inst::EnumMake {
                            out: ok_out,
                            enum_id: result_id,
                            tag: ok_info.tag,
                            slot_base: ok_info.slot_base as u32,
                            payload: vec![val],
                        });
                        self.emit(Inst::Store {
                            ptr: result_slot,
                            value: ok_out,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = else_b;
                    {
                        let msg_lit = self.value();
                        self.emit(Inst::StringLiteral {
                            out: msg_lit,
                            bytes: b"empty".to_vec(),
                        });
                        let msg = self.emit_clone(msg_lit, Type::Str);
                        let err_info = self
                            .structs
                            .variant_in_enum("Err", result_id)
                            .ok_or_else(|| IrError::new("native IR: Err missing"))?;
                        let err_out = self.value();
                        self.emit(Inst::EnumMake {
                            out: err_out,
                            enum_id: result_id,
                            tag: err_info.tag,
                            slot_base: err_info.slot_base as u32,
                            payload: vec![msg],
                        });
                        self.emit(Inst::Store {
                            ptr: result_slot,
                            value: err_out,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = merge;
                    let loaded = self.value();
                    self.emit(Inst::Load {
                        out: loaded,
                        ptr: result_slot,
                        ty: result_ty,
                    });
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_pack_free".into(),
                        args: vec![pack],
                        ret: None,
                    });
                    return Ok((loaded, result_ty, true));
                }
                ("recv_timeout", 1) => {
                    // ch.recv_timeout(ms) → Result[int,string] (Ok payload or Err timeout/closed).
                    let (ms, mst, mso) = self.lower_expr(&args[0])?;
                    if mst != Type::I64 || mso {
                        return Err(IrError::new("native IR: recv_timeout ms must be int"));
                    }
                    let pack = self.value();
                    let one = self.const_int(1, Type::I64);
                    self.emit(Inst::Call {
                        out: Some(pack),
                        function: "mako_native_pack_new".into(),
                        args: vec![one],
                        ret: Some(Type::I64),
                    });
                    let rc = self.value();
                    self.emit(Inst::Call {
                        out: Some(rc),
                        function: "mako_native_chan_recv_timeout".into(),
                        args: vec![recv, ms, pack],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    let z = self.const_int(0, Type::I64);
                    let val = self.value();
                    self.emit(Inst::Call {
                        out: Some(val),
                        function: "mako_native_pack_get".into(),
                        args: vec![pack, z],
                        ret: Some(Type::I64),
                    });
                    // rc==1 → Ok(val); else Err(msg)
                    let ok_info = self
                        .structs
                        .variant_matching("Ok", &[Type::I64])
                        .or_else(|| self.structs.variant("Ok"))
                        .ok_or_else(|| IrError::new("native IR: Ok missing"))?;
                    let err_info = self
                        .structs
                        .variant_matching("Err", &[Type::Str])
                        .or_else(|| self.structs.variant("Err"))
                        .ok_or_else(|| IrError::new("native IR: Err missing"))?;
                    let result_ty = Type::Struct(ok_info.enum_id);
                    let result_slot = self.value();
                    self.emit(Inst::Alloca {
                        out: result_slot,
                        ty: result_ty,
                    });
                    let one_c = self.const_int(1, Type::I64);
                    let is_ok = self.value();
                    self.emit(Inst::Binary {
                        out: is_ok,
                        op: BinOp::Eq,
                        left: rc,
                        right: one_c,
                        ty: Type::I64,
                    });
                    let then_b = self.new_block();
                    let else_b = self.new_block();
                    let merge = self.new_block();
                    self.terminate(Terminator::Branch {
                        condition: is_ok,
                        then_block: then_b,
                        else_block: else_b,
                    })?;
                    self.current = then_b;
                    let ok_out = self.value();
                    self.emit(Inst::EnumMake {
                        out: ok_out,
                        enum_id: ok_info.enum_id,
                        tag: ok_info.tag,
                        slot_base: ok_info.slot_base as u32,
                        payload: vec![val],
                    });
                    self.emit(Inst::Store {
                        ptr: result_slot,
                        value: ok_out,
                    });
                    self.terminate(Terminator::Jump(merge))?;
                    // rc == 0 → Err("timeout"); else (rc == -1) → Err("closed")
                    self.current = else_b;
                    let zc = self.const_int(0, Type::I64);
                    let is_timeout = self.value();
                    self.emit(Inst::Binary {
                        out: is_timeout,
                        op: BinOp::Eq,
                        left: rc,
                        right: zc,
                        ty: Type::I64,
                    });
                    let timeout_b = self.new_block();
                    let closed_b = self.new_block();
                    self.terminate(Terminator::Branch {
                        condition: is_timeout,
                        then_block: timeout_b,
                        else_block: closed_b,
                    })?;
                    self.current = timeout_b;
                    let timeout_lit = self.value();
                    self.emit(Inst::StringLiteral {
                        out: timeout_lit,
                        bytes: b"timeout".to_vec(),
                    });
                    let timeout_msg = self.emit_clone(timeout_lit, Type::Str);
                    let timeout_err = self.value();
                    self.emit(Inst::EnumMake {
                        out: timeout_err,
                        enum_id: err_info.enum_id,
                        tag: err_info.tag,
                        slot_base: err_info.slot_base as u32,
                        payload: vec![timeout_msg],
                    });
                    self.emit(Inst::Store {
                        ptr: result_slot,
                        value: timeout_err,
                    });
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = closed_b;
                    let closed_lit = self.value();
                    self.emit(Inst::StringLiteral {
                        out: closed_lit,
                        bytes: b"closed".to_vec(),
                    });
                    let closed_msg = self.emit_clone(closed_lit, Type::Str);
                    let closed_err = self.value();
                    self.emit(Inst::EnumMake {
                        out: closed_err,
                        enum_id: err_info.enum_id,
                        tag: err_info.tag,
                        slot_base: err_info.slot_base as u32,
                        payload: vec![closed_msg],
                    });
                    self.emit(Inst::Store {
                        ptr: result_slot,
                        value: closed_err,
                    });
                    self.terminate(Terminator::Jump(merge))?;
                    self.current = merge;
                    let loaded = self.value();
                    self.emit(Inst::Load {
                        out: loaded,
                        ptr: result_slot,
                        ty: result_ty,
                    });
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_pack_free".into(),
                        args: vec![pack],
                        ret: None,
                    });
                    return Ok((loaded, result_ty, true));
                }
                ("send_timeout", 2) => {
                    // ch.send_timeout(val, timeout_ms) — full channel may fail.
                    let (mut v, vt, vo) = self.lower_expr(&args[0])?;
                    let (ms, mst, mso) = self.lower_expr(&args[1])?;
                    if mst != Type::I64 || mso {
                        return Err(IrError::new("native IR: send_timeout ms must be int"));
                    }
                    if rty == Type::ChanI {
                        if vt != Type::I64 || vo {
                            return Err(IrError::new("native IR: send_timeout expects int"));
                        }
                    } else if rty == Type::ChanF {
                        if vt != Type::F64 || vo {
                            return Err(IrError::new("native IR: send_timeout expects float"));
                        }
                        let bits = self.value();
                        self.emit(Inst::Call {
                            out: Some(bits),
                            function: "mako_native_f64_to_bits".into(),
                            args: vec![v],
                            ret: Some(Type::I64),
                        });
                        v = bits;
                    } else if rty == Type::ChanS {
                        if vt != Type::Str {
                            return Err(IrError::new("native IR: send_timeout expects string"));
                        }
                        // Clone so failed timeout send does not consume caller's string.
                        if !vo {
                            v = self.emit_clone(v, Type::Str);
                        } else {
                            // Owned temp: clone so we keep a freeable copy on failure path
                            // via the channel (runtime may free on close only).
                            // For POD-like keep: always clone for string timeout.
                            let c = self.emit_clone(v, Type::Str);
                            self.emit_drop(v, Type::Str);
                            v = c;
                        }
                    } else if matches!(rty, Type::ChanP(_)) {
                        if !vt.is_ptr_sized() || vt == Type::F64 {
                            return Err(IrError::new("native IR: send_timeout payload unsupported"));
                        }
                        if !vo && vt.is_heap() {
                            v = self.emit_clone(v, vt);
                        }
                    } else {
                        return Err(IrError::new("native IR: send_timeout unsupported chan"));
                    }
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_chan_send_timeout".into(),
                        args: vec![recv, v, ms],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    return Ok((out, Type::I64, false));
                }
                ("close", 0) => {
                    let out = self.value();
                    self.emit(Inst::Call {
                        out: Some(out),
                        function: "mako_native_chan_close".into(),
                        args: vec![recv],
                        ret: Some(Type::I64),
                    });
                    if rowned {
                        self.emit_drop(recv, rty);
                    }
                    return Ok((out, Type::I64, false));
                }
                _ => {
                    return Err(IrError::new(format!(
                        "native IR: unknown channel method `{method}`"
                    )));
                }
            }
        }

        // Dyn / unit interface method on Opaque (or bare int placeholder).
        if rty == Type::Opaque || rty == Type::I64 {
            if let Some(result) = self.lower_iface_method(recv, rty, rowned, method, args)? {
                return Ok(result);
            }
            // Fall through to unit free-fn (Writer_write(msg) with no self).
            let suffix = format!("_{method}");
            if let Some((fn_name, (params, ret))) = self
                .signatures
                .iter()
                .find(|(name, (params, _))| {
                    name.ends_with(&suffix) && params.len() == args.len()
                })
                .map(|(n, sig)| (n.clone(), sig.clone()))
            {
                if rowned {
                    self.emit_drop(recv, rty);
                }
                let mut call_args = Vec::with_capacity(args.len());
                let mut temps = Vec::new();
                for (arg, expected) in args.iter().zip(params.iter()) {
                    let (mut v, t, o) = self.lower_expr(arg)?;
                    if t != *expected {
                        return Err(IrError::new(format!(
                            "native IR: interface method `{fn_name}` arg type mismatch"
                        )));
                    }
                    if t.is_heap() {
                        if !o {
                            v = self.emit_clone(v, t);
                        }
                        temps.push((v, t));
                    } else if o {
                        temps.push((v, t));
                    }
                    call_args.push(v);
                }
                let out = ret.map(|_| self.value());
                self.emit(Inst::Call {
                    out,
                    function: fn_name,
                    args: call_args,
                    ret,
                });
                for (v, t) in temps {
                    self.emit_drop(v, t);
                }
                return Ok(match (out, ret) {
                    (Some(o), Some(r)) => (o, r, r.is_heap()),
                    _ => (self.const_int(0, Type::I64), Type::I64, false),
                });
            }
            return Err(IrError::new(format!(
                "native IR: method `{method}` on non-aggregate receiver"
            )));
        }
        let Type::Struct(id) = rty else {
            return Err(IrError::new(format!(
                "native IR: method `{method}` on non-aggregate receiver"
            )));
        };
        let type_name = self.structs.layout_name(id);
        // Prefer Type_method; else any Free_method whose first param is this struct
        // (interface free functions like `Adder_add(self: Counter, …)`).
        let fn_name = {
            let preferred = format!("{type_name}_{method}");
            if self.signatures.contains_key(&preferred) {
                preferred
            } else {
                let suffix = format!("_{method}");
                self.signatures
                    .iter()
                    .find(|(name, (params, _))| {
                        name.ends_with(&suffix)
                            && params.first().copied() == Some(Type::Struct(id))
                    })
                    .map(|(name, _)| name.clone())
                    .ok_or_else(|| {
                        IrError::new(format!(
                            "native IR: unknown method `{type_name}.{method}` (expected `{preferred}`)"
                        ))
                    })?
            }
        };
        // Build call: first arg is receiver.
        // - `mut self`: pass the live pointer (no clone); caller keeps ownership.
        // - plain `self`: by-value — clone a borrow so callee mutations stay local.
        let mut call_args = Vec::with_capacity(args.len() + 1);
        let mut temps = Vec::new();
        let is_mut_self = self.mut_self_fns.contains(&fn_name);
        let mut recv_val = recv;
        if is_mut_self {
            // In-place: share the caller's heap block. Drop only if the receiver
            // was a temporary the caller no longer holds.
            if rowned {
                temps.push((recv_val, rty));
            }
        } else {
            if !rowned {
                recv_val = self.emit_clone(recv, rty);
            }
            temps.push((recv_val, rty));
        }
        call_args.push(recv_val);
        let (params, ret) = self.signatures[&fn_name].clone();
        if params.len() != args.len() + 1 {
            return Err(IrError::new(format!(
                "native IR: method `{fn_name}` arity mismatch"
            )));
        }
        for (arg, expected) in args.iter().zip(params.iter().skip(1)) {
            let (mut v, t, o) = self.lower_expr(arg)?;
            if t != *expected {
                return Err(IrError::new("native IR: method argument type mismatch"));
            }
            if let Type::Struct(_sid) = t {
                if !o {
                    v = self.emit_clone(v, t);
                }
                temps.push((v, t));
            } else if o {
                temps.push((v, t));
            }
            call_args.push(v);
        }
        let out = ret.map(|_| self.value());
        self.emit(Inst::Call {
            out,
            function: fn_name,
            args: call_args,
            ret,
        });
        for (v, t) in temps {
            self.emit_drop(v, t);
        }
        match (out, ret) {
            (Some(v), Some(t)) => Ok((v, t, t.is_heap())),
            _ => Ok((self.const_int(0, Type::I64), Type::I64, false)),
        }
    }

    fn lower_convert(
        &mut self,
        ty: &TypeExpr,
        expr: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let target = resolve_type(ty, self.structs)?;
        let (v, src, owned) = self.lower_expr(expr)?;
        if target == src {
            return Ok((v, src, owned));
        }
        // `[]byte("hi")` / `[]byte(s)` and `string(bytes)`.
        if target == Type::ByteSlice && src == Type::Str {
            let out = self.value();
            self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_bytes_from_string_ptr".into(),
                args: vec![v],
                ret: Some(Type::ByteSlice),
            });
            if owned {
                self.emit(Inst::DropString { value: v });
            }
            return Ok((out, Type::ByteSlice, true));
        }
        if target == Type::Str && src == Type::ByteSlice {
            let out = self.value();
            self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_string_from_bytes_ptr".into(),
                args: vec![v],
                ret: Some(Type::Str),
            });
            if owned {
                self.emit_drop(v, Type::ByteSlice);
            }
            return Ok((out, Type::Str, true));
        }
        // Supported casts: int↔float, int↔bool.
        match (src, target) {
            (Type::I64, Type::F64)
            | (Type::F64, Type::I64)
            | (Type::I64, Type::I1)
            | (Type::I1, Type::I64)
            | (Type::F64, Type::I1)
            | (Type::I1, Type::F64) => {
                let out = self.value();
                self.emit(Inst::Cast {
                    out,
                    value: v,
                    from: src,
                    to: target,
                });
                if owned {
                    self.emit_drop(v, src);
                }
                Ok((out, target, false))
            }
            _ => Err(IrError::new(format!(
                "native IR: convert from `{src:?}` to `{target:?}` is not implemented"
            ))),
        }
    }

    /// Lower `expr` preferring a variant monomorph of `expected` when it is an enum
    /// (so `None`/`Err` inside `[]Option[T]` / `[]Result[T,E]` hit the right layout).
    fn lower_expr_for_expected(
        &mut self,
        expr: &Expr,
        expected: Type,
    ) -> Result<(Value, Type, bool), IrError> {
        if let Type::Struct(eid) = expected {
            if self.structs.is_enum(eid) {
                // Nullary: `None`, unit tags.
                if let Expr::Ident(name) = expr {
                    if let Some(info) = self.structs.variant_in_enum(name, eid) {
                        if info.arity == 0 {
                            return self.build_variant(info, &[]);
                        }
                    }
                }
                // Payload: `Some(x)`, `Ok(x)`, `Err(x)`, `Color(…)` .
                if let Expr::Call { callee, args } = expr {
                    if let Expr::Ident(name) = callee.as_ref() {
                        if let Some(info) = self.structs.variant_in_enum(name, eid) {
                            if info.arity == args.len() {
                                return self.build_variant(info, args);
                            }
                        }
                    }
                }
            }
        }
        self.lower_expr(expr)
    }

    /// Build a slice literal of the given slice type (including empty `[]`).
    fn lower_array_literal_with_type(
        &mut self,
        items: &[Expr],
        slice_ty: Type,
    ) -> Result<(Value, Type, bool), IrError> {
        let expected_elem = match slice_ty {
            Type::IntSlice | Type::ByteSlice => Type::I64,
            Type::FloatSlice => Type::F64,
            Type::StrSlice => Type::Str,
            Type::BoolSlice => Type::I1,
            Type::StructSlice(id) => Type::Struct(id),
            Type::PtrSlice(vk) => vk.to_type(),
            other => {
                return Err(IrError::new(format!(
                    "native IR: cannot build array literal of type `{other:?}`"
                )));
            }
        };
        if items.is_empty() {
            let out = self.value();
            let z = self.const_int(0, Type::I64);
            match slice_ty {
                Type::IntSlice => self.emit(Inst::SliceMake {
                    out,
                    len: z,
                    cap: Some(z),
                }),
                Type::FloatSlice => self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_float_slice_make_ptr".into(),
                    args: vec![z, z],
                    ret: Some(Type::FloatSlice),
                }),
                Type::ByteSlice => self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_byte_slice_make_ptr".into(),
                    args: vec![z, z],
                    ret: Some(Type::ByteSlice),
                }),
                Type::BoolSlice => self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_bool_slice_make_ptr".into(),
                    args: vec![z, z],
                    ret: Some(Type::BoolSlice),
                }),
                Type::StrSlice => self.emit(Inst::StrSliceMake {
                    out,
                    len: z,
                    cap: Some(z),
                }),
                Type::StructSlice(_) | Type::PtrSlice(_) => self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_ptr_slice_make".into(),
                    args: vec![z, z],
                    ret: Some(slice_ty),
                }),
                _ => unreachable!(),
            }
            return Ok((out, slice_ty, true));
        }
        let mut values = Vec::with_capacity(items.len());
        for item in items {
            let (mut v, ty, owned) =
                self.lower_expr_for_expected(item, expected_elem)?;
            // Allow bool↔int and pointer-sized identity aliases (e.g. ChanI vs
            // ChanP(Other) in []chan[T] seeds, Opaque handles as I64 slots).
            let compatible = ty == expected_elem
                || (expected_elem == Type::I64 && ty == Type::I1)
                || (expected_elem == Type::I1 && ty == Type::I64)
                || (expected_elem == Type::I64
                    && ty.is_ptr_sized()
                    && ty != Type::F64
                    && !matches!(ty, Type::Struct(_)))
                || (ty == Type::I64
                    && expected_elem.is_ptr_sized()
                    && expected_elem != Type::F64
                    && !matches!(expected_elem, Type::Struct(_)))
                || matches!(
                    (expected_elem, ty),
                    (Type::ChanI, Type::ChanP(_))
                        | (Type::ChanP(_), Type::ChanI)
                        | (Type::ChanS, Type::ChanP(_))
                        | (Type::ChanP(_), Type::ChanS)
                        | (Type::ChanF, Type::ChanP(_))
                        | (Type::ChanP(_), Type::ChanF)
                        | (Type::Opaque, Type::I64)
                        | (Type::I64, Type::Opaque)
                );
            if !compatible {
                return Err(IrError::new(
                    "native IR: array literal element type mismatch",
                ));
            }
            if expected_elem == Type::I1 && ty == Type::I64 {
                let b = self.value();
                self.emit(Inst::Cast {
                    out: b,
                    value: v,
                    from: Type::I64,
                    to: Type::I1,
                });
                v = b;
            }
            // Scalars must not be owned temps; heap elements (slices/structs) are
            // moved into the array (caller transfers ownership).
            if owned && !ty.is_heap() && !matches!(ty, Type::Struct(_)) {
                return Err(IrError::new(
                    "native IR: array literal requires non-owned scalar elements",
                ));
            }
            // Move ownership of a bare local into the array (maps, slices, …).
            if owned && ty.is_heap() {
                if let Expr::Ident(name) = item {
                    self.heap_owned.insert(name.clone(), false);
                }
            } else if !owned && ty.is_heap() && !ty.is_shared_handle() {
                v = self.emit_clone(v, ty);
            }
            values.push(v);
        }
        let out = self.value();
        match slice_ty {
            Type::IntSlice => {
                self.emit(Inst::SliceLiteral {
                    out,
                    elements: values,
                });
                Ok((out, Type::IntSlice, true))
            }
            Type::FloatSlice => {
                let n = self.const_int(values.len() as i64, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_float_slice_make_ptr".into(),
                    args: vec![n, n],
                    ret: Some(Type::FloatSlice),
                });
                for (i, v) in values.into_iter().enumerate() {
                    let idx = self.const_int(i as i64, Type::I64);
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_float_slice_set_ptr".into(),
                        args: vec![out, idx, v],
                        ret: None,
                    });
                }
                Ok((out, Type::FloatSlice, true))
            }
            Type::ByteSlice => {
                // Reuse byte path via make + set
                let n = self.const_int(values.len() as i64, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_byte_slice_make_ptr".into(),
                    args: vec![n, n],
                    ret: Some(Type::ByteSlice),
                });
                for (i, v) in values.into_iter().enumerate() {
                    let idx = self.const_int(i as i64, Type::I64);
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_byte_slice_set_ptr".into(),
                        args: vec![out, idx, v],
                        ret: None,
                    });
                }
                Ok((out, Type::ByteSlice, true))
            }
            Type::BoolSlice => {
                let n = self.const_int(values.len() as i64, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_bool_slice_make_ptr".into(),
                    args: vec![n, n],
                    ret: Some(Type::BoolSlice),
                });
                for (i, v) in values.into_iter().enumerate() {
                    let idx = self.const_int(i as i64, Type::I64);
                    // C ABI is int64_t element; widen I1 → I64.
                    let mut b = v;
                    // Probe: const true/false are I1; cast always safe for small ints.
                    let w = self.value();
                    self.emit(Inst::Cast {
                        out: w,
                        value: b,
                        from: Type::I1,
                        to: Type::I64,
                    });
                    b = w;
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_bool_slice_set_ptr".into(),
                        args: vec![out, idx, b],
                        ret: None,
                    });
                }
                Ok((out, Type::BoolSlice, true))
            }
            Type::StrSlice => {
                self.emit(Inst::StrSliceLiteral {
                    out,
                    elements: values,
                });
                Ok((out, Type::StrSlice, true))
            }
            Type::StructSlice(sid) => {
                let n = self.const_int(values.len() as i64, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_ptr_slice_make".into(),
                    args: vec![n, n],
                    ret: Some(Type::StructSlice(sid)),
                });
                for (i, v) in values.into_iter().enumerate() {
                    let idx = self.const_int(i as i64, Type::I64);
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_ptr_slice_set".into(),
                        args: vec![out, idx, v],
                        ret: None,
                    });
                }
                Ok((out, Type::StructSlice(sid), true))
            }
            Type::PtrSlice(vk) => {
                let n = self.const_int(values.len() as i64, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_ptr_slice_make".into(),
                    args: vec![n, n],
                    ret: Some(Type::PtrSlice(vk)),
                });
                for (i, v) in values.into_iter().enumerate() {
                    let idx = self.const_int(i as i64, Type::I64);
                    self.emit(Inst::Call {
                        out: None,
                        function: "mako_native_ptr_slice_set".into(),
                        args: vec![out, idx, v],
                        ret: None,
                    });
                }
                Ok((out, Type::PtrSlice(vk), true))
            }
            _ => unreachable!(),
        }
    }

    fn lower_byte_array_literal(
        &mut self,
        items: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        let mut values = Vec::with_capacity(items.len());
        for item in items {
            let (v, ty, owned) = self.lower_expr(item)?;
            if ty != Type::I64 || owned {
                return Err(IrError::new(
                    "native IR: []byte literal requires int elements",
                ));
            }
            values.push(v);
        }
        let out = self.value();
        let n = self.const_int(values.len() as i64, Type::I64);
        self.emit(Inst::Call {
            out: Some(out),
            function: "mako_native_byte_slice_make_ptr".into(),
            args: vec![n, n],
            ret: Some(Type::ByteSlice),
        });
        for (i, v) in values.into_iter().enumerate() {
            let idx = self.const_int(i as i64, Type::I64);
            self.emit(Inst::Call {
                out: None,
                function: "mako_native_byte_slice_set_ptr".into(),
                args: vec![out, idx, v],
                ret: None,
            });
        }
        Ok((out, Type::ByteSlice, true))
    }

    /// `result_unwrap_or(r, default)` — Ok payload or default.
    fn lower_result_unwrap_or(
        &mut self,
        result: &Expr,
        default: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (r, rty, owned) = self.lower_expr(result)?;
        let Type::Struct(id) = rty else {
            return Err(IrError::new(
                "native IR: result_unwrap_or expects Result",
            ));
        };
        let (d, dty, do_) = self.lower_expr(default)?;
        if dty != Type::I64 || do_ {
            return Err(IrError::new(
                "native IR: result_unwrap_or default expects int",
            ));
        }
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: r,
            struct_id: id,
            index: 0,
            ty: Type::I64,
        });
        let zero = self.const_int(0, Type::I64);
        let is_ok = self.value();
        self.emit(Inst::Binary {
            out: is_ok,
            op: BinOp::Eq,
            left: tag,
            right: zero,
            ty: Type::I64,
        });
        let ok_b = self.new_block();
        let err_b = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::I64,
        });
        self.terminate(Terminator::Branch {
            condition: is_ok,
            then_block: ok_b,
            else_block: err_b,
        })?;
        self.current = ok_b;
        let (ok_idx, ok_ty) = self
            .structs
            .field(id, "Ok_0")
            .ok_or_else(|| IrError::new("native IR: Result missing Ok_0"))?;
        let payload = self.value();
        self.emit(Inst::StructField {
            out: payload,
            base: r,
            struct_id: id,
            index: ok_idx,
            ty: ok_ty,
        });
        self.emit(Inst::Store {
            ptr: slot,
            value: payload,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = err_b;
        self.emit(Inst::Store {
            ptr: slot,
            value: d,
        });
        if owned {
            self.emit(Inst::DropStruct {
                value: r,
                struct_id: id,
            });
        }
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::I64,
        });
        Ok((out, Type::I64, false))
    }

    /// `f"…{expr}…"` — fold to a left-associative chain of owned concatenations.
    /// Supported holes: `string`, `int`, `bool` (no format specs yet).
    fn lower_string_interp(
        &mut self,
        parts: &[crate::ast::InterpPart],
    ) -> Result<(Value, Type, bool), IrError> {
        use crate::ast::InterpPart;
        let mut acc: Option<(Value, bool)> = None;
        let mut drop_temps = Vec::new();
        for part in parts {
            let (piece, piece_owned) = match part {
                InterpPart::Lit(s) => {
                    let out = self.value();
                    self.emit(Inst::StringLiteral {
                        out,
                        bytes: s.as_bytes().to_vec(),
                    });
                    (out, false)
                }
                InterpPart::Expr(expr, spec) => {
                    let (v, ty, owned) = self.lower_expr(expr)?;
                    match (ty, spec.as_deref()) {
                        (Type::Str, None) => (v, owned),
                        (Type::Str, Some(sp)) => {
                            // Width pad for strings: `{s:10}` seed.
                            let spec_s = self.value();
                            self.emit(Inst::StringLiteral {
                                out: spec_s,
                                bytes: sp.as_bytes().to_vec(),
                            });
                            let out = self.value();
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_format_spec_str_ptr".into(),
                                args: vec![v, spec_s],
                                ret: Some(Type::Str),
                            });
                            if owned {
                                self.emit(Inst::DropString { value: v });
                            }
                            (out, true)
                        }
                        (Type::I64, None) => {
                            let out = self.value();
                            self.emit(Inst::IntToString { out, value: v });
                            (out, true)
                        }
                        (Type::I64, Some(sp)) => {
                            let spec_s = self.value();
                            self.emit(Inst::StringLiteral {
                                out: spec_s,
                                bytes: sp.as_bytes().to_vec(),
                            });
                            let out = self.value();
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_format_spec_int_ptr".into(),
                                args: vec![v, spec_s],
                                ret: Some(Type::Str),
                            });
                            (out, true)
                        }
                        (Type::I1, None) => {
                            let out = self.value();
                            self.emit(Inst::BoolToString { out, value: v });
                            (out, true)
                        }
                        (Type::F64, None) => {
                            let out = self.value();
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_float_to_string_ptr".into(),
                                args: vec![v],
                                ret: Some(Type::Str),
                            });
                            (out, true)
                        }
                        (Type::F64, Some(sp)) => {
                            let spec_s = self.value();
                            self.emit(Inst::StringLiteral {
                                out: spec_s,
                                bytes: sp.as_bytes().to_vec(),
                            });
                            let out = self.value();
                            self.emit(Inst::Call {
                                out: Some(out),
                                function: "mako_native_format_spec_float_ptr".into(),
                                args: vec![v, spec_s],
                                ret: Some(Type::Str),
                            });
                            (out, true)
                        }
                        _ => {
                            return Err(IrError::new(
                                "native IR: f-string hole must be string, int, bool, or float",
                            ))
                        }
                    }
                }
            };
            match acc {
                None => acc = Some((piece, piece_owned)),
                Some((left, left_owned)) => {
                    let out = self.value();
                    self.emit(Inst::StringConcat {
                        out,
                        left,
                        right: piece,
                    });
                    if left_owned {
                        drop_temps.push(left);
                    }
                    if piece_owned {
                        drop_temps.push(piece);
                    }
                    // Concat consumes copies of the bytes; drop owned operands
                    // after the concat instruction (backends free after copy).
                    for d in drop_temps.drain(..) {
                        self.emit(Inst::DropString { value: d });
                    }
                    acc = Some((out, true));
                }
            }
        }
        match acc {
            Some((v, owned)) => Ok((v, Type::Str, owned)),
            None => {
                let out = self.value();
                self.emit(Inst::StringLiteral {
                    out,
                    bytes: Vec::new(),
                });
                Ok((out, Type::Str, false))
            }
        }
    }

    /// Lower a `match`. Enum and scalar (int/bool) scrutinees are supported.
    /// Results merge through a stack slot (no block parameters). An owned enum
    /// scrutinee is dropped exactly once on the taken arm.
    fn lower_match(
        &mut self,
        scrutinee: &Expr,
        arms: &[crate::ast::MatchArm],
    ) -> Result<(Value, Type, bool), IrError> {
        if arms.is_empty() {
            return Err(IrError::new("native IR: empty match"));
        }
        let (sval, sty, sowned) = self.lower_expr(scrutinee)?;
        match sty {
            Type::Struct(id) if self.structs.is_enum(id) => {
                self.lower_enum_match(sval, id, sowned, arms)
            }
            Type::Struct(id) if !self.structs.is_enum(id) => {
                // Tuple / struct irrefutable destructure: `match t { (a, b) => … }`.
                self.lower_tuple_match(sval, id, sowned, arms)
            }
            Type::I64 | Type::I1 => self.lower_scalar_match(sval, sty, arms),
            Type::Str => self.lower_string_match(sval, sowned, arms),
            other => Err(IrError::new(format!(
                "native IR: match scrutinee must be an enum, int, bool, or tuple (got {:?})",
                other
            ))),
        }
    }

    /// `match s { "a" => …, _ => … }` — sequential string equality.
    fn lower_string_match(
        &mut self,
        sval: Value,
        sowned: bool,
        arms: &[crate::ast::MatchArm],
    ) -> Result<(Value, Type, bool), IrError> {
        let result_slot = self.value();
        let result_block = self.current;
        let result_index = self.block().instructions.len();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: Type::I64,
        });
        let mut result_ty: Option<Type> = None;
        let merge = self.new_block();
        let mut test_block = self.current;
        for (i, arm) in arms.iter().enumerate() {
            let is_last = i == arms.len() - 1;
            let arm_block = self.new_block();
            let next_block = if is_last { None } else { Some(self.new_block()) };
            self.current = test_block;
            match &arm.pattern {
                Pattern::Wildcard | Pattern::Ident(_) => {
                    self.terminate(Terminator::Jump(arm_block))?;
                }
                Pattern::Literal(Expr::String(lit)) => {
                    let want = self.value();
                    self.emit(Inst::StringLiteral {
                        out: want,
                        bytes: lit.as_bytes().to_vec(),
                    });
                    let eq = self.value();
                    self.emit(Inst::StringEqual {
                        out: eq,
                        left: sval,
                        right: want,
                        negated: false,
                    });
                    if is_last {
                        self.terminate(Terminator::Jump(arm_block))?;
                    } else {
                        self.terminate(Terminator::Branch {
                            condition: eq,
                            then_block: arm_block,
                            else_block: next_block.unwrap(),
                        })?;
                    }
                }
                Pattern::Or(parts) => {
                    // "a" | "b" => … : OR of string equals into arm.
                    let mut acc: Option<Value> = None;
                    for p in parts {
                        let Pattern::Literal(Expr::String(lit)) = p else {
                            return Err(IrError::new(
                                "native IR: string match OR requires string literals",
                            ));
                        };
                        let want = self.value();
                        self.emit(Inst::StringLiteral {
                            out: want,
                            bytes: lit.as_bytes().to_vec(),
                        });
                        let eq = self.value();
                        self.emit(Inst::StringEqual {
                            out: eq,
                            left: sval,
                            right: want,
                            negated: false,
                        });
                        acc = Some(match acc {
                            None => eq,
                            Some(prev) => {
                                let o = self.value();
                                self.emit(Inst::Binary {
                                    out: o,
                                    op: BinOp::Or,
                                    left: prev,
                                    right: eq,
                                    ty: Type::I1,
                                });
                                o
                            }
                        });
                    }
                    let cond = acc.ok_or_else(|| IrError::new("native IR: empty OR pattern"))?;
                    if is_last {
                        self.terminate(Terminator::Jump(arm_block))?;
                    } else {
                        self.terminate(Terminator::Branch {
                            condition: cond,
                            then_block: arm_block,
                            else_block: next_block.unwrap(),
                        })?;
                    }
                }
                _ => {
                    return Err(IrError::new(
                        "native IR: string match arm must be string or wildcard",
                    ));
                }
            }
            self.current = arm_block;
            let (mut body_value, body_ty, body_owned) = self.lower_expr(&arm.body)?;
            if self.block().terminator.is_none() {
                if body_ty.is_heap() && !body_owned {
                    body_value = self.emit_clone(body_value, body_ty);
                }
                match result_ty {
                    None => result_ty = Some(body_ty),
                    Some(t) if t != body_ty => {
                        return Err(IrError::new(
                            "native IR: match arms disagree on result type",
                        ));
                    }
                    _ => {}
                }
                self.emit(Inst::Store {
                    ptr: result_slot,
                    value: body_value,
                });
                self.terminate(Terminator::Jump(merge))?;
            } else if result_ty.is_none() {
                result_ty = Some(Type::I64);
            }
            if let Some(next) = next_block {
                test_block = next;
            }
        }
        if sowned {
            self.current = merge;
            // Drop scrutinee after arms (all arms jumped to merge).
            // Insert drop at start of merge by using current.
            self.emit(Inst::DropString { value: sval });
        } else {
            self.current = merge;
        }
        let result_ty = result_ty.unwrap_or(Type::I64);
        if let Inst::Alloca { ty, .. } =
            &mut self.function.blocks[result_block.0 as usize].instructions[result_index]
        {
            *ty = result_ty;
        }
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: result_ty,
        });
        Ok((out, result_ty, result_ty.is_heap()))
    }

    /// Irrefutable tuple/struct match — bind fields and evaluate the single arm body.
    fn lower_tuple_match(
        &mut self,
        sval: Value,
        id: u32,
        sowned: bool,
        arms: &[crate::ast::MatchArm],
    ) -> Result<(Value, Type, bool), IrError> {
        if arms.is_empty() {
            return Err(IrError::new("native IR: empty match"));
        }
        // Use first non-wildcard arm or the only arm.
        let arm = &arms[0];
        match &arm.pattern {
            Pattern::Tuple(parts) => {
                if parts.len() != self.structs.field_count(id) {
                    return Err(IrError::new("native IR: tuple match arity mismatch"));
                }
                for (i, part) in parts.iter().enumerate() {
                    let Pattern::Ident(name) = part else {
                        return Err(IrError::new(
                            "native IR: tuple match only supports identifier bindings",
                        ));
                    };
                    let fty = self.structs.field_type(id, i);
                    let fv = self.value();
                    self.emit(Inst::StructField {
                        out: fv,
                        base: sval,
                        struct_id: id,
                        index: i as u32,
                        ty: fty,
                    });
                    let bound = if fty.is_heap() {
                        self.emit_clone(fv, fty)
                    } else {
                        fv
                    };
                    let slot = self.value();
                    self.emit(Inst::Alloca {
                        out: slot,
                        ty: fty,
                    });
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: bound,
                    });
                    self.locals.insert(name.clone(), (slot, fty));
                    if fty.is_heap() {
                        self.heap_owned.insert(name.clone(), true);
                    }
                }
            }
            Pattern::Wildcard | Pattern::Ident(_) => {
                // Whole-value bind already handled if Ident.
                if let Pattern::Ident(name) = &arm.pattern {
                    let slot = self.value();
                    self.emit(Inst::Alloca {
                        out: slot,
                        ty: Type::Struct(id),
                    });
                    let v = if sowned {
                        sval
                    } else {
                        let c = self.value();
                        self.emit(Inst::StructClone {
                            out: c,
                            base: sval,
                            struct_id: id,
                        });
                        c
                    };
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: v,
                    });
                    self.locals.insert(name.clone(), (slot, Type::Struct(id)));
                    self.heap_owned.insert(name.clone(), true);
                }
            }
            Pattern::Struct { name: _, fields } => {
                // Irrefutable struct destructure: `match p { Point { x, y } => … }`.
                for (fname, fpat) in fields {
                    let (fidx, fty) = self.structs.field(id, fname).ok_or_else(|| {
                        IrError::new(format!(
                            "native IR: unknown field `{fname}` in struct match"
                        ))
                    })?;
                    let Pattern::Ident(bname) = fpat else {
                        return Err(IrError::new(
                            "native IR: struct match fields must be identifier bindings",
                        ));
                    };
                    let fv = self.value();
                    self.emit(Inst::StructField {
                        out: fv,
                        base: sval,
                        struct_id: id,
                        index: fidx,
                        ty: fty,
                    });
                    let bound = if fty.is_heap() {
                        self.emit_clone(fv, fty)
                    } else {
                        fv
                    };
                    let slot = self.value();
                    self.emit(Inst::Alloca {
                        out: slot,
                        ty: fty,
                    });
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: bound,
                    });
                    self.locals.insert(bname.clone(), (slot, fty));
                    if fty.is_heap() {
                        self.heap_owned.insert(bname.clone(), true);
                    }
                }
            }
            _ => {
                return Err(IrError::new(
                    "native IR: unsupported tuple match pattern",
                ));
            }
        }
        let result = self.lower_expr(&arm.body)?;
        if sowned {
            self.emit(Inst::DropStruct {
                value: sval,
                struct_id: id,
            });
        }
        Ok(result)
    }

    fn lower_scalar_match(
        &mut self,
        scrutinee: Value,
        sty: Type,
        arms: &[crate::ast::MatchArm],
    ) -> Result<(Value, Type, bool), IrError> {
        let result_slot = self.value();
        let result_block = self.current;
        let result_index = self.block().instructions.len();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: Type::I64,
        });
        let mut result_ty: Option<Type> = None;
        let merge = self.new_block();
        let saved_locals = self.locals.clone();
        let saved_owned = self.heap_owned.clone();
        let mut test_block = self.current;

        for (i, arm) in arms.iter().enumerate() {
            let is_last = i == arms.len() - 1;
            let (match_values, catch_all, bind_name) =
                self.resolve_scalar_pattern(&arm.pattern, sty)?;
            self.current = test_block;
            let arm_block = self.new_block();
            let next_block = if is_last {
                None
            } else {
                Some(self.new_block())
            };

            if is_last || catch_all {
                self.terminate(Terminator::Jump(arm_block))?;
            } else {
                // Or-pattern: value equals any of the listed literals.
                let mut any = None;
                for lit in &match_values {
                    let expected = self.const_int(*lit, sty);
                    let cmp = self.value();
                    self.emit(Inst::Binary {
                        out: cmp,
                        op: BinOp::Eq,
                        left: scrutinee,
                        right: expected,
                        ty: sty,
                    });
                    any = Some(match any {
                        None => cmp,
                        Some(prev) => {
                            let or = self.value();
                            self.emit(Inst::Binary {
                                out: or,
                                op: BinOp::Or,
                                left: prev,
                                right: cmp,
                                ty: Type::I1,
                            });
                            or
                        }
                    });
                }
                let cond = any.ok_or_else(|| {
                    IrError::new("native IR: scalar match arm has no literals")
                })?;
                self.terminate(Terminator::Branch {
                    condition: cond,
                    then_block: arm_block,
                    else_block: next_block.unwrap(),
                })?;
            }

            self.current = arm_block;
            self.locals = saved_locals.clone();
            self.heap_owned = saved_owned.clone();
            if let Some(name) = bind_name {
                // Whole-scrutinee identifier binding (`x => x + 1`).
                let local = self.value();
                self.emit(Inst::Alloca { out: local, ty: sty });
                self.emit(Inst::Store {
                    ptr: local,
                    value: scrutinee,
                });
                self.locals.insert(name, (local, sty));
            }
            if let Some(guard) = &arm.guard {
                let (gval, gty, gowned) = self.lower_expr(guard)?;
                if gowned || gty != Type::I1 {
                    return Err(IrError::new("native IR: match guard must be bool"));
                }
                let body_block = self.new_block();
                if let Some(next) = next_block {
                    self.terminate(Terminator::Branch {
                        condition: gval,
                        then_block: body_block,
                        else_block: next,
                    })?;
                } else {
                    self.terminate(Terminator::Jump(body_block))?;
                    let _ = gval;
                }
                self.current = body_block;
            }
            let (mut body_value, body_ty, body_owned) = self.lower_expr(&arm.body)?;
            // Diverging arm (return/break inside body): block is already terminated;
            // skip result store and merge jump.
            if self.block().terminator.is_some() {
                if result_ty.is_none() {
                    result_ty = Some(Type::I64);
                }
            } else {
                if body_ty.is_heap() && !body_owned {
                    body_value = self.emit_clone(body_value, body_ty);
                }
                match result_ty {
                    None => result_ty = Some(body_ty),
                    Some(t) if t != body_ty => {
                        return Err(IrError::new(
                            "native IR: match arms disagree on result type",
                        ));
                    }
                    _ => {}
                }
                self.emit(Inst::Store {
                    ptr: result_slot,
                    value: body_value,
                });
                self.terminate(Terminator::Jump(merge))?;
            }
            if let Some(next) = next_block {
                test_block = next;
            }
        }

        self.locals = saved_locals;
        self.heap_owned = saved_owned;
        self.current = merge;
        let result_ty = result_ty.expect("non-empty match");
        if let Inst::Alloca { ty, .. } =
            &mut self.function.blocks[result_block.0 as usize].instructions[result_index]
        {
            *ty = result_ty;
        }
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: result_ty,
        });
        Ok((out, result_ty, result_ty.is_heap()))
    }

    /// Resolve a scalar match pattern to `(literal values, is_catch_all, bind)`.
    ///
    /// Also accepts unit/wildcard-only enum variant patterns when the scrutinee
    /// is an i64 tag (e.g. map[Color]int range keys stored via `map_i_key`).
    fn resolve_scalar_pattern(
        &self,
        pattern: &Pattern,
        sty: Type,
    ) -> Result<(Vec<i64>, bool, Option<String>), IrError> {
        match pattern {
            Pattern::Wildcard => Ok((Vec::new(), true, None)),
            Pattern::Ident(name) => {
                // Uppercase unit enum variant used as an i64 tag (map enum keys).
                if sty == Type::I64 {
                    if let Some(info) = self.structs.variant(name) {
                        if info.arity == 0 {
                            return Ok((vec![info.tag], false, None));
                        }
                    }
                }
                Ok((Vec::new(), true, Some(name.clone())))
            }
            Pattern::Literal(expr) => {
                let v = self.scalar_literal_value(expr, sty)?;
                Ok((vec![v], false, None))
            }
            Pattern::Variant { name, bindings } if sty == Type::I64 => {
                // Tag-only match: payload slots must be wildcards (keys store tag only).
                let info = self.structs.variant(name).ok_or_else(|| {
                    IrError::new(format!(
                        "native IR: unknown variant `{name}` in scalar match"
                    ))
                })?;
                if bindings.len() != info.arity {
                    return Err(IrError::new(
                        "native IR: variant pattern arity mismatch in scalar match",
                    ));
                }
                for b in bindings {
                    if !matches!(b, Pattern::Wildcard) {
                        return Err(IrError::new(
                            "native IR: tag-only enum key match cannot bind payload fields",
                        ));
                    }
                }
                Ok((vec![info.tag], false, None))
            }
            Pattern::Or(parts) => {
                let mut values = Vec::new();
                for part in parts {
                    let (vs, catch, bind) = self.resolve_scalar_pattern(part, sty)?;
                    if catch || bind.is_some() {
                        return Err(IrError::new(
                            "native IR: or-pattern must be literal-only",
                        ));
                    }
                    values.extend(vs);
                }
                Ok((values, false, None))
            }
            other => Err(IrError::new(format!(
                "native IR: unsupported scalar match pattern ({other:?})"
            ))),
        }
    }

    fn scalar_literal_value(&self, expr: &Expr, sty: Type) -> Result<i64, IrError> {
        match (expr, sty) {
            (Expr::Int(n), Type::I64) => Ok(*n),
            (Expr::Bool(b), Type::I1) => Ok(i64::from(*b)),
            (Expr::Bool(b), Type::I64) => Ok(i64::from(*b)),
            _ => Err(IrError::new(
                "native IR: match literal type does not match scrutinee",
            )),
        }
    }

    fn lower_enum_match(
        &mut self,
        sptr: Value,
        enum_id: u32,
        sowned: bool,
        arms: &[crate::ast::MatchArm],
    ) -> Result<(Value, Type, bool), IrError> {
        // Discriminant (slot 0), then the result slot (type patched once known).
        let tag = self.value();
        self.emit(Inst::StructField {
            out: tag,
            base: sptr,
            struct_id: enum_id,
            index: 0,
            ty: Type::I64,
        });
        let result_slot = self.value();
        let result_block = self.current;
        let result_index = self.block().instructions.len();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: Type::I64,
        });
        let mut result_ty: Option<Type> = None;

        let merge = self.new_block();
        let saved_locals = self.locals.clone();
        let saved_owned = self.heap_owned.clone();

        let mut test_block = self.current;
        for (i, arm) in arms.iter().enumerate() {
            let (arm_tag, bindings, nested_checks, catch_all, whole_bind) =
                self.resolve_match_pattern(enum_id, &arm.pattern)?;
            let is_last = i == arms.len() - 1;

            self.current = test_block;
            let arm_block = self.new_block();
            let next_block = if is_last { None } else { Some(self.new_block()) };
            if is_last || catch_all {
                self.terminate(Terminator::Jump(arm_block))?;
            } else {
                let expected = self.const_int(arm_tag, Type::I64);
                let cmp = self.value();
                self.emit(Inst::Binary {
                    out: cmp,
                    op: BinOp::Eq,
                    left: tag,
                    right: expected,
                    ty: Type::I64,
                });
                self.terminate(Terminator::Branch {
                    condition: cmp,
                    then_block: arm_block,
                    else_block: next_block.unwrap(),
                })?;
            }

            self.current = arm_block;
            self.locals = saved_locals.clone();
            self.heap_owned = saved_owned.clone();

            // Nested discriminants: `Ok(Some(v))` vs `Ok(None)`.
            for (path, nested_tag) in &nested_checks {
                let mut base = sptr;
                let mut last_ty = Type::Struct(enum_id);
                for (sid, idx, fty) in path {
                    let v = self.value();
                    self.emit(Inst::StructField {
                        out: v,
                        base,
                        struct_id: *sid,
                        index: *idx,
                        ty: *fty,
                    });
                    base = v;
                    last_ty = *fty;
                }
                let Type::Struct(nested_id) = last_ty else {
                    return Err(IrError::new(
                        "native IR: nested tag check path does not end in an enum",
                    ));
                };
                let nested_tag_val = self.value();
                self.emit(Inst::StructField {
                    out: nested_tag_val,
                    base,
                    struct_id: nested_id,
                    index: 0,
                    ty: Type::I64,
                });
                let want = self.const_int(*nested_tag, Type::I64);
                let cmp = self.value();
                self.emit(Inst::Binary {
                    out: cmp,
                    op: BinOp::Eq,
                    left: nested_tag_val,
                    right: want,
                    ty: Type::I64,
                });
                let body = self.new_block();
                if let Some(next) = next_block {
                    self.terminate(Terminator::Branch {
                        condition: cmp,
                        then_block: body,
                        else_block: next,
                    })?;
                } else {
                    let fail = self.new_block();
                    self.terminate(Terminator::Branch {
                        condition: cmp,
                        then_block: body,
                        else_block: fail,
                    })?;
                    self.current = fail;
                    if sowned {
                        self.emit(Inst::DropStruct {
                            value: sptr,
                            struct_id: enum_id,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                }
                self.current = body;
            }

            if let Some(name) = whole_bind {
                // Bind the whole scrutinee as a borrow of the enum block.
                let local = self.value();
                self.emit(Inst::Alloca {
                    out: local,
                    ty: Type::Struct(enum_id),
                });
                self.emit(Inst::Store {
                    ptr: local,
                    value: sptr,
                });
                self.locals
                    .insert(name, (local, Type::Struct(enum_id)));
            }
            for (name, path) in &bindings {
                // Walk the field path from the scrutinee.
                let mut base = sptr;
                let mut cur_ty = Type::Struct(enum_id);
                let mut last_sid = enum_id;
                let mut last_idx = 0u32;
                for (sid, idx, fty) in path {
                    let v = self.value();
                    self.emit(Inst::StructField {
                        out: v,
                        base,
                        struct_id: *sid,
                        index: *idx,
                        ty: *fty,
                    });
                    base = v;
                    cur_ty = *fty;
                    last_sid = *sid;
                    last_idx = *idx;
                }
                let ty = cur_ty;
                // Heap payload: *move* out of the immediate parent so DropStruct
                // does not free the same handle the binding still uses.
                let bound = if ty.is_heap() && sowned && path.len() == 1 {
                    let null = self.const_int(0, Type::I64);
                    self.emit(Inst::StructFieldStore {
                        base: sptr,
                        struct_id: last_sid,
                        index: last_idx,
                        value: null,
                    });
                    base
                } else if ty.is_heap() && sowned && path.len() > 1 {
                    // Nested heap: null the leaf field in its parent struct.
                    let mut parent = sptr;
                    for (sid, idx, fty) in &path[..path.len() - 1] {
                        let v = self.value();
                        self.emit(Inst::StructField {
                            out: v,
                            base: parent,
                            struct_id: *sid,
                            index: *idx,
                            ty: *fty,
                        });
                        parent = v;
                    }
                    let null = self.const_int(0, Type::I64);
                    self.emit(Inst::StructFieldStore {
                        base: parent,
                        struct_id: last_sid,
                        index: last_idx,
                        value: null,
                    });
                    base
                } else if ty.is_heap() {
                    self.emit_clone(base, ty)
                } else {
                    base
                };
                let local = self.value();
                self.emit(Inst::Alloca { out: local, ty });
                self.emit(Inst::Store {
                    ptr: local,
                    value: bound,
                });
                self.locals.insert(name.clone(), (local, ty));
                if ty.is_heap() {
                    self.heap_owned.insert(name.clone(), true);
                }
            }
            if let Some(guard) = &arm.guard {
                let (gval, gty, gowned) = self.lower_expr(guard)?;
                if gowned {
                    return Err(IrError::new("native IR: owned string match guard"));
                }
                if gty != Type::I1 {
                    return Err(IrError::new("native IR: match guard must be bool"));
                }
                let body_block = self.new_block();
                if let Some(next) = next_block {
                    self.terminate(Terminator::Branch {
                        condition: gval,
                        then_block: body_block,
                        else_block: next,
                    })?;
                } else {
                    let fail = self.new_block();
                    self.terminate(Terminator::Branch {
                        condition: gval,
                        then_block: body_block,
                        else_block: fail,
                    })?;
                    self.current = fail;
                    if sowned {
                        self.emit(Inst::DropStruct {
                            value: sptr,
                            struct_id: enum_id,
                        });
                    }
                    self.terminate(Terminator::Jump(merge))?;
                }
                self.current = body_block;
            }
            let (mut body_value, body_ty, body_owned) = self.lower_expr(&arm.body)?;
            if self.block().terminator.is_some() {
                // Diverging arm already returned/broke; still drop owned scrutinee
                // would be wrong on that path (it may have been moved). Skip merge.
                if result_ty.is_none() {
                    result_ty = Some(Type::I64);
                }
            } else {
                if body_ty.is_heap() && !body_owned {
                    body_value = self.emit_clone(body_value, body_ty);
                }
                match result_ty {
                    None => result_ty = Some(body_ty),
                    Some(t) if t != body_ty => {
                        return Err(IrError::new(
                            "native IR: match arms disagree on result type",
                        ));
                    }
                    _ => {}
                }
                self.emit(Inst::Store {
                    ptr: result_slot,
                    value: body_value,
                });
                if sowned {
                    self.emit(Inst::DropStruct {
                        value: sptr,
                        struct_id: enum_id,
                    });
                }
                self.terminate(Terminator::Jump(merge))?;
            }

            if let Some(next) = next_block {
                test_block = next;
            }
        }

        self.locals = saved_locals;
        self.heap_owned = saved_owned;
        self.current = merge;
        let result_ty = result_ty.expect("non-empty match sets a result type");
        if let Inst::Alloca { ty, .. } =
            &mut self.function.blocks[result_block.0 as usize].instructions[result_index]
        {
            *ty = result_ty;
        }
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: result_slot,
            ty: result_ty,
        });
        Ok((out, result_ty, result_ty.is_heap()))
    }

    /// Resolve an enum match pattern to
    /// `(tag, payload_bindings, nested_tag_checks, is_catch_all, whole_scrutinee_bind)`.
    ///
    /// Payload bindings are `(name, path)` where `path` is a chain of
    /// `(struct_id, field_index, field_ty)` steps from the scrutinee.
    /// Nested tag checks are additional discriminants for patterns like
    /// `Ok(Some(v))` / `Ok(None)`.
    #[allow(clippy::type_complexity)]
    fn resolve_match_pattern(
        &self,
        enum_id: u32,
        pattern: &Pattern,
    ) -> Result<
        (
            i64,
            Vec<(String, Vec<(u32, u32, Type)>)>,
            Vec<(Vec<(u32, u32, Type)>, i64)>,
            bool,
            Option<String>,
        ),
        IrError,
    > {
        match pattern {
            Pattern::Wildcard => Ok((0, Vec::new(), Vec::new(), true, None)),
            Pattern::Ident(name) => {
                // Prefer monomorph of this scrutinee enum (None on Option[T], etc.).
                if let Some(info) = self.structs.variant_in_enum(name, enum_id) {
                    if info.arity == 0 {
                        return Ok((info.tag, Vec::new(), Vec::new(), false, None));
                    }
                    return Err(IrError::new("native IR: variant pattern arity mismatch"));
                }
                // Lowercase identifier binds the whole scrutinee.
                Ok((0, Vec::new(), Vec::new(), true, Some(name.clone())))
            }
            Pattern::Variant { name, bindings } => {
                let info = self.structs.variant_in_enum(name, enum_id).ok_or_else(|| {
                    if self.structs.variant(name).is_some() {
                        IrError::new("native IR: match variant belongs to a different enum")
                    } else {
                        IrError::new(format!("native IR: unknown variant `{name}`"))
                    }
                })?;
                if bindings.len() != info.arity {
                    return Err(IrError::new("native IR: variant pattern arity mismatch"));
                }
                let mut binds = Vec::new();
                let mut nested_checks = Vec::new();
                for (index, binding) in bindings.iter().enumerate() {
                    let slot = (info.slot_base + index) as u32;
                    let slot_ty = self.structs.field_type(enum_id, slot as usize);
                    let prefix = vec![(enum_id, slot, slot_ty)];
                    self.collect_payload_pattern(
                        binding,
                        slot_ty,
                        &prefix,
                        &mut binds,
                        &mut nested_checks,
                    )?;
                }
                Ok((info.tag, binds, nested_checks, false, None))
            }
            _ => Err(IrError::new("native IR: unsupported match pattern")),
        }
    }

    /// Expand a nested payload pattern into field bindings and nested tag checks.
    fn collect_payload_pattern(
        &self,
        binding: &Pattern,
        value_ty: Type,
        prefix: &[(u32, u32, Type)],
        binds: &mut Vec<(String, Vec<(u32, u32, Type)>)>,
        nested_checks: &mut Vec<(Vec<(u32, u32, Type)>, i64)>,
    ) -> Result<(), IrError> {
        match binding {
            Pattern::Wildcard => Ok(()),
            Pattern::Ident(name) => {
                binds.push((name.clone(), prefix.to_vec()));
                Ok(())
            }
            Pattern::Struct { name, fields } => {
                let Type::Struct(sid) = value_ty else {
                    return Err(IrError::new(format!(
                        "native IR: struct pattern `{name}` on non-struct payload"
                    )));
                };
                let _ = name; // name is checked by field resolution / type system
                for (fname, fpat) in fields {
                    let (fidx, fty) = self.structs.field(sid, fname).ok_or_else(|| {
                        IrError::new(format!(
                            "native IR: unknown field `{fname}` in struct pattern `{name}`"
                        ))
                    })?;
                    let mut path = prefix.to_vec();
                    path.push((sid, fidx, fty));
                    self.collect_payload_pattern(fpat, fty, &path, binds, nested_checks)?;
                }
                Ok(())
            }
            Pattern::Variant { name, bindings } => {
                let Type::Struct(inner_id) = value_ty else {
                    return Err(IrError::new(format!(
                        "native IR: nested variant `{name}` on non-enum payload"
                    )));
                };
                if !self.structs.is_enum(inner_id) {
                    return Err(IrError::new(format!(
                        "native IR: nested variant `{name}` on non-enum payload"
                    )));
                }
                let info = self.structs.variant_in_enum(name, inner_id).ok_or_else(|| {
                    IrError::new(format!("native IR: unknown nested variant `{name}`"))
                })?;
                if bindings.len() != info.arity {
                    return Err(IrError::new(
                        "native IR: nested variant pattern arity mismatch",
                    ));
                }
                nested_checks.push((prefix.to_vec(), info.tag));
                for (index, nested) in bindings.iter().enumerate() {
                    let slot = (info.slot_base + index) as u32;
                    let slot_ty = self.structs.field_type(inner_id, slot as usize);
                    let mut path = prefix.to_vec();
                    path.push((inner_id, slot, slot_ty));
                    self.collect_payload_pattern(nested, slot_ty, &path, binds, nested_checks)?;
                }
                Ok(())
            }
            Pattern::Tuple(parts) => {
                let Type::Struct(sid) = value_ty else {
                    return Err(IrError::new(
                        "native IR: tuple pattern on non-tuple payload",
                    ));
                };
                if parts.len() != self.structs.field_count(sid) {
                    return Err(IrError::new("native IR: nested tuple arity mismatch"));
                }
                for (i, part) in parts.iter().enumerate() {
                    let fty = self.structs.field_type(sid, i);
                    let mut path = prefix.to_vec();
                    path.push((sid, i as u32, fty));
                    self.collect_payload_pattern(part, fty, &path, binds, nested_checks)?;
                }
                Ok(())
            }
            other => Err(IrError::new(format!(
                "native IR: unsupported nested payload pattern ({other:?})"
            ))),
        }
    }

    /// If `expr` is a bare owned local, transfer ownership out of it (caller
    /// becomes the sole owner). Otherwise preserve `already_owned` so that
    /// owned temporaries (`None`, `Ok(1)`, `share_int(…)`, call results)
    /// are not mistaken for borrows — cloning those would leak the original.
    fn take_bare_string_local(&mut self, expr: &Expr, already_owned: bool) -> bool {
        let Expr::Ident(name) = expr else {
            return already_owned;
        };
        if self.heap_owned.get(name) == Some(&true) {
            self.heap_owned.insert(name.clone(), false);
            true
        } else {
            already_owned
        }
    }

    /// Move a fat-fn local into a call/kick: clear the binding so later
    /// `fn_has_env(f)` is 0, without freeing the box (callee/task owns it).
    fn move_fnptr_local(&mut self, expr: &Expr) {
        let Expr::Ident(name) = expr else {
            return;
        };
        let Some((slot, ty)) = self.locals.get(name).copied() else {
            return;
        };
        if ty != Type::FnPtr {
            return;
        }
        if self.heap_owned.get(name) != Some(&true) {
            // Still null the slot if the local is live — by-value param move.
        }
        self.heap_owned.insert(name.clone(), false);
        let z = self.const_int(0, Type::I64);
        self.emit(Inst::Store {
            ptr: slot,
            value: z,
        });
    }

    /// True when both expressions name the same local (field-assign consume).
    fn field_base_same_local(a: &Expr, b: &Expr) -> bool {
        match (a, b) {
            (Expr::Ident(x), Expr::Ident(y)) => x == y,
            _ => false,
        }
    }

    fn drop_owned_locals(&mut self) {
        let owned = self
            .heap_owned
            .iter()
            .filter_map(|(name, owned)| owned.then_some(name.clone()))
            .collect::<Vec<_>>();
        for name in owned {
            if let Some((ptr, ty)) = self.locals.get(&name).copied() {
                let value = self.value();
                self.emit(Inst::Load {
                    out: value,
                    ptr,
                    ty,
                });
                self.emit_drop(value, ty);
                self.heap_owned.insert(name, false);
            }
        }
    }

    /// Lower a call into the native runtime bridge (net/http/fmt/chan/…).
    /// Returns `None` when `function` is not a known bridge builtin.
    fn lower_runtime_builtin(
        &mut self,
        function: &str,
        args: &[Expr],
    ) -> Result<Option<(Value, Type, bool)>, IrError> {
        // (c_name, arg_types, ret_type, ret_owned)
        // arg types: I64, F64, Str, ChanI, Arena, I1
        type Spec = (&'static str, &'static [Type], Option<Type>, bool);
        let spec: Option<Spec> = match function {
            "tcp_listen" if args.len() == 1 => {
                Some(("mako_native_tcp_listen", &[Type::I64], Some(Type::I64), false))
            }
            "tcp_connect" if args.len() == 2 => Some((
                "mako_native_tcp_connect_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_accept" if args.len() == 1 => {
                Some(("mako_native_tcp_accept", &[Type::I64], Some(Type::I64), false))
            }
            "tcp_accept_nb" if args.len() == 1 => Some((
                "mako_native_tcp_accept_nb",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_close" if args.len() == 1 => {
                Some(("mako_native_tcp_close", &[Type::I64], Some(Type::I64), false))
            }
            "http_bind" if args.len() == 1 => {
                Some(("mako_native_http_bind", &[Type::I64], Some(Type::I64), false))
            }
            "http_accept" if args.len() == 1 => Some((
                "mako_native_http_accept",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_path" if args.len() == 1 => Some((
                "mako_native_http_path_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_method" if args.len() == 1 => Some((
                "mako_native_http_method_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "json_array_ints3" if args.len() == 3 => Some((
                "mako_native_json_array_ints3",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "json_array_len" if args.len() == 1 => Some((
                "mako_native_json_array_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_array_get_int" if args.len() == 2 => Some((
                "mako_native_json_array_get_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "json_array_get_string" if args.len() == 2 => Some((
                "mako_native_json_array_get_string_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "json_array_push_int" if args.len() == 2 => Some((
                "mako_native_json_array_push_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "json_array_strings2" if args.len() == 2 => Some((
                "mako_native_json_array_strings2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_object" if args.len() == 2 => Some((
                "mako_native_json_object_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_merge" if args.len() == 2 => Some((
                "mako_native_json_merge_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_nest" if args.len() == 2 => Some((
                "mako_native_json_nest_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "regex_match" if args.len() == 2 => Some((
                "mako_native_regex_match_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "regex_find" if args.len() == 2 => Some((
                "mako_native_regex_find_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "chan_select_value" if args.is_empty() => Some((
                "mako_native_chan_select_value",
                &[],
                Some(Type::I64),
                false,
            )),
            "chan_select2" if args.len() == 3 => Some((
                "mako_native_chan_select2",
                &[Type::ChanI, Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),
            "chan_select3" if args.len() == 4 => Some((
                "mako_native_chan_select3",
                &[Type::ChanI, Type::ChanI, Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_echo" if args.len() == 1 => {
                Some(("mako_native_http_echo", &[Type::I64], Some(Type::I64), false))
            }
            "http_serve" if args.len() == 2 => Some((
                "mako_native_http_serve_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_get" if args.len() == 1 => Some((
                "mako_native_http_get_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_println" if args.len() == 1 => Some((
                "mako_native_fmt_println_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_println2" if args.len() == 2 => Some((
                "mako_native_fmt_println2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_printf2" if args.len() == 3 => Some((
                "mako_native_fmt_printf2_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_eprintln" if args.len() == 1 => Some((
                "mako_native_fmt_eprintln_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_sprintf2" if args.len() == 3 => Some((
                "mako_native_fmt_sprintf2_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_errorf" if args.len() == 2 => Some((
                "mako_native_fmt_errorf_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "print_raw" if args.len() == 1 => Some((
                "mako_native_print_raw_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "chan_new" if args.len() == 1 => Some((
                "mako_native_chan_new",
                &[Type::I64],
                Some(Type::ChanI),
                true,
            )),
            "chan_send" if args.len() == 2 => Some((
                "mako_native_chan_send",
                &[Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),
            "chan_recv" if args.len() == 1 => Some((
                "mako_native_chan_recv",
                &[Type::ChanI],
                Some(Type::I64),
                false,
            )),
            "chan_try_send" if args.len() == 2 => Some((
                "mako_native_chan_try_send",
                &[Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),
            "chan_close" if args.len() == 1 => Some((
                "mako_native_chan_close",
                &[Type::ChanI],
                Some(Type::I64),
                false,
            )),
            "uuid_v4" if args.is_empty() => {
                Some(("mako_native_uuid_v4", &[], Some(Type::Str), true))
            }
            "log_info" if args.len() == 1 => {
                Some(("mako_native_log_info_ptr", &[Type::Str], None, false))
            }
            "arena_text" if args.len() == 2 => Some((
                "mako_native_arena_text_ptr",
                &[Type::Arena, Type::Str],
                Some(Type::Str),
                true,
            )),
            "arena_stamp" if args.len() == 2 => Some((
                "mako_native_arena_stamp",
                &[Type::Arena, Type::I64],
                Some(Type::I64),
                false,
            )),
            "arena_ints" if args.len() == 2 => Some((
                "mako_native_arena_ints",
                &[Type::Arena, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "h3_server_available" if args.is_empty() => Some((
                "mako_native_h3_server_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "http_respond" if args.len() == 3 => Some((
                "mako_native_http_respond_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_header" if args.len() == 2 => Some((
                "mako_native_http_header_ptr",
                &[Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "ws_echo_once" if args.len() == 1 => Some((
                "mako_native_ws_echo_once",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sqlite_query_int" if args.len() == 2 => Some((
                "mako_native_sqlite_query_int_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "nb_listen" if args.len() == 1 => Some((
                "mako_native_nb_listen",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_poll2" if args.len() == 3 => Some((
                "mako_native_io_poll2",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_kq_poll2" if args.len() == 3 => Some((
                "mako_native_io_kq_poll2",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_poll4" if args.len() == 5 => Some((
                "mako_native_io_poll4",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_wait" if args.len() == 2 => Some((
                "mako_native_io_wait",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_native_poll2" if args.len() == 3 => Some((
                "mako_native_io_native_poll2",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "metric_inc" if args.len() == 1 => Some((
                "mako_native_metric_inc",
                &[Type::I64],
                None,
                false,
            )),
            "log_warn" if args.len() == 1 => Some((
                "mako_native_log_warn_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "json_i" if args.len() == 2 => Some((
                "mako_native_json_i_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "json_has" if args.len() == 2 => Some((
                "mako_native_json_has_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_path_string" if args.len() == 3 => Some((
                "mako_native_json_path_string_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_array_push_string" if args.len() == 2 => Some((
                "mako_native_json_array_push_string_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_encode_varint" if args.len() == 1 => Some((
                "mako_native_pb_encode_varint",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "pb_encode_key" if args.len() == 2 => Some((
                "mako_native_pb_encode_key",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "pb_encode_simple" if args.len() == 2 => Some((
                "mako_native_pb_encode_simple_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mail_msg_new" if args.is_empty() => Some((
                "mako_native_mail_msg_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "smtp_mock_serve_once" if args.is_empty() => Some((
                "mako_native_smtp_mock_serve_once",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_serve_once" if args.len() == 4 => Some((
                "mako_native_tls_serve_once_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_n" if args.len() == 5 => Some((
                "mako_native_tls_serve_n_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_serve_h2_routes" if args.len() == 6 => Some((
                "mako_native_tls_serve_h2_routes_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_get" if args.len() == 4 => Some((
                "mako_native_tls_get_full_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_get_insecure" if args.len() == 3 => Some((
                "mako_native_tls_get_insecure_full_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_mock_once" if args.len() == 1 => Some((
                "mako_native_redis_mock_once",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "regex_capture" if args.len() == 3 => Some((
                "mako_native_regex_capture_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_settings_len" if args.len() == 1 => Some((
                "mako_native_http2_settings_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_detect" if args.len() == 1 => Some((
                "mako_native_http2_detect",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "llm_api_key" if args.is_empty() => Some((
                "mako_native_llm_api_key",
                &[],
                Some(Type::Str),
                true,
            )),
            "gpu_device_open" if args.is_empty() => Some((
                "mako_native_gpu_device_open",
                &[],
                Some(Type::I64),
                false,
            )),
            "h3_server_new" if args.len() == 2 => Some((
                "mako_native_h3_server_new_certs",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_http2_unary" if args.len() == 3 => Some((
                "mako_native_grpc_http2_unary_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_unary_payload" if args.len() == 1 => Some((
                "mako_native_grpc_http2_unary_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_unary_response" if args.len() == 3 => Some((
                "mako_native_grpc_http2_unary_response_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_response_payload" if args.len() == 1 => Some((
                "mako_native_grpc_http2_unary_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_response_status" if args.len() == 1 => Some((
                "mako_native_grpc_http2_response_status_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_unary_name" if args.len() == 1 => Some((
                "mako_native_grpc_unary_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_unary_id" if args.len() == 1 => Some((
                "mako_native_grpc_unary_id_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_branch" if args.is_empty() => Some((
                "mako_native_sip_branch",
                &[],
                Some(Type::Str),
                true,
            )),
            "str_builder" if args.is_empty() => Some((
                "mako_native_str_builder",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "tmpl_data_new" if args.is_empty() => Some((
                "mako_native_tmpl_data_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "chan_select4" if args.len() == 5 => Some((
                "mako_native_chan_select4",
                &[Type::ChanI, Type::ChanI, Type::ChanI, Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),

            "llm_https_available" if args.is_empty() => Some((
                "mako_native_llm_https_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_empty_settings" if args.is_empty() => Some((
                "mako_native_http2_empty_settings",
                &[],
                Some(Type::Str),
                true,
            )),

            "str_has_prefix" if args.len() == 2 => Some((
                "mako_native_str_has_prefix_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "metric_add" if args.len() == 2 => Some((
                "mako_native_metric_add",
                &[Type::I64, Type::I64],
                None,
                false,
            )),
            "sqlite_query_text" if args.len() == 2 => Some((
                "mako_native_sqlite_query_text_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_get_string" if args.len() == 2 => Some((
                "mako_native_json_get_string_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "json_path_int" if args.len() == 3 => Some((
                "mako_native_json_path_int_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_object_from_map_ss" if args.len() == 1 => Some((
                "mako_native_json_object_from_map_ss",
                &[Type::MapSS],
                Some(Type::Str),
                true,
            )),
            "http_respond_ct" if args.len() == 4 => Some((
                "mako_native_http_respond_ct_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_keepalive" if args.len() == 1 => Some((
                "mako_native_http_keepalive",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_close" if args.len() == 1 => Some((
                "mako_native_http_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_close_listener" if args.len() == 1 => Some((
                "mako_native_http_close_listener",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "pb_decode_varint" if args.len() == 1 => Some((
                "mako_native_pb_decode_varint_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_key_field" if args.len() == 1 => Some((
                "mako_native_pb_key_field_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_simple_name" if args.len() == 1 => Some((
                "mako_native_pb_simple_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "log_error" if args.len() == 1 => Some((
                "mako_native_log_error_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "redis_ping" if args.len() == 2 => Some((
                "mako_native_redis_ping_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mail_msg_set_from" if args.len() == 2 => Some((
                "mako_native_mail_msg_set_from_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "uuid_string" if args.len() == 1 => Some((
                "mako_native_uuid_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "uuid_parse" if args.len() == 1 => Some((
                "mako_native_uuid_parse_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "uuid_parse_ok" if args.len() == 1 => Some((
                "mako_native_uuid_parse_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "uuid_eq" if args.len() == 2 => Some((
                "mako_native_uuid_eq_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "uuid_is_nil" if args.len() == 1 => Some((
                "mako_native_uuid_is_nil_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "uuid_nil" if args.is_empty() => Some((
                "mako_native_uuid_nil",
                &[],
                Some(Type::Str),
                true,
            )),
            "str_has_suffix" if args.len() == 2 => Some((
                "mako_native_str_has_suffix_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "str_index" if args.len() == 2 => Some((
                "mako_native_str_index_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "str_last_index" if args.len() == 2 => Some((
                "mako_native_str_last_index_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "slice_ints" if args.len() == 3 => Some((
                "mako_native_slice_ints",
                &[Type::IntSlice, Type::I64, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "slice_get" if args.len() == 2 => Some((
                "mako_native_slice_get",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "slice_len" if args.len() == 1 => Some((
                "mako_native_slice_len",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "actor_spawn" if args.len() == 1 => Some((
                "mako_native_chan_new",
                &[Type::I64],
                Some(Type::ChanI),
                true,
            )),
            "actor_send" if args.len() == 2 => Some((
                "mako_native_chan_send",
                &[Type::ChanI, Type::I64],
                Some(Type::I64),
                false,
            )),
            "actor_recv" if args.len() == 1 => Some((
                "mako_native_chan_recv",
                &[Type::ChanI],
                Some(Type::I64),
                false,
            )),
            "actor_msg_tag" if args.len() == 1 => Some((
                "mako_native_actor_msg_tag",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "actor_msg_payload" if args.len() == 1 => Some((
                "mako_native_actor_msg_payload",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "actor_stop" if args.len() == 1 => Some((
                "mako_native_actor_stop",
                &[Type::ChanI],
                None,
                false,
            )),
            "http_body" if args.len() == 1 => Some((
                "mako_native_http_body_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_next" if args.len() == 1 => Some((
                "mako_native_http_next",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "pb_varint_len" if args.len() == 1 => Some((
                "mako_native_pb_varint_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_key_wire" if args.len() == 1 => Some((
                "mako_native_pb_key_wire_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_simple_id" if args.len() == 1 => Some((
                "mako_native_pb_simple_id_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_has" if args.len() == 3 => Some((
                "mako_native_json_has_string_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_get_int" if args.len() == 2 => Some((
                "mako_native_json_get_int_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "json_get_object" if args.len() == 2 => Some((
                "mako_native_json_get_object_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "metric_get" if args.len() == 1 => Some((
                "mako_native_metric_get",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "gauge_set" if args.len() == 2 => Some((
                "mako_native_gauge_set",
                &[Type::I64, Type::I64],
                None,
                false,
            )),
            "gauge_add" if args.len() == 2 => Some((
                "mako_native_gauge_add",
                &[Type::I64, Type::I64],
                None,
                false,
            )),
            "gauge_get" if args.len() == 1 => Some((
                "mako_native_gauge_get",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hist_observe" if args.len() == 2 => Some((
                "mako_native_hist_observe",
                &[Type::I64, Type::I64],
                None,
                false,
            )),
            "hist_count" if args.len() == 1 => Some((
                "mako_native_hist_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hist_sum" if args.len() == 1 => Some((
                "mako_native_hist_sum",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hist_avg" if args.len() == 1 => Some((
                "mako_native_hist_avg",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "metrics_export" if args.is_empty() => Some((
                "mako_native_metrics_export",
                &[],
                Some(Type::Str),
                true,
            )),
            "sha256" if args.len() == 1 => Some((
                "mako_native_sha256_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hmac_sha256" if args.len() == 2 => Some((
                "mako_native_hmac_sha256_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "bin_encode_int" if args.len() == 1 => Some((
                "mako_native_bin_encode_int",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "safe_add" if args.len() == 2 => Some((
                "mako_native_safe_add",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "builder_write" if args.len() == 2 => Some((
                "mako_native_builder_write_ptr",
                &[Type::Opaque, Type::Str],
                None,
                false,
            )),
            "builder_write_byte" if args.len() == 2 => Some((
                "mako_native_builder_write_byte",
                &[Type::Opaque, Type::I64],
                None,
                false,
            )),
            "builder_len" if args.len() == 1 => Some((
                "mako_native_builder_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "builder_string" if args.len() == 1 => Some((
                "mako_native_builder_string",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "tmpl_data_set" if args.len() == 3 => Some((
                "mako_native_tmpl_data_set_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tmpl_data_set_list" if args.len() == 3 => Some((
                "mako_native_tmpl_data_set_list_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tmpl_text" if args.len() == 2 => Some((
                "mako_native_tmpl_text_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "tmpl_html" if args.len() == 2 => Some((
                "mako_native_tmpl_html_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "tmpl_data_free" if args.len() == 1 => Some((
                "mako_native_tmpl_data_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "mail_msg_add_to" if args.len() == 2 => Some((
                "mako_native_mail_msg_add_to_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_set_subject" if args.len() == 2 => Some((
                "mako_native_mail_msg_set_subject_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_set_text" if args.len() == 2 => Some((
                "mako_native_mail_msg_set_text_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_set_html" if args.len() == 2 => Some((
                "mako_native_mail_msg_set_html_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_attach" if args.len() == 4 => Some((
                "mako_native_mail_msg_attach_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_build" if args.len() == 1 => Some((
                "mako_native_mail_msg_build",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "mail_msg_free" if args.len() == 1 => Some((
                "mako_native_mail_msg_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "smtp_send_msg" if args.len() == 6 => Some((
                "mako_native_smtp_send_msg_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "smtp_mock_serve_once" if args.is_empty() => Some((
                "mako_native_smtp_mock_serve_once",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_serve_once" if args.len() == 4 => Some((
                "mako_native_tls_serve_once_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_n" if args.len() == 5 => Some((
                "mako_native_tls_serve_n_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_serve_h2_routes" if args.len() == 6 => Some((
                "mako_native_tls_serve_h2_routes_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_get" if args.len() == 4 => Some((
                "mako_native_tls_get_full_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_get_insecure" if args.len() == 3 => Some((
                "mako_native_tls_get_insecure_full_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_settings_len" if args.len() == 1 => Some((
                "mako_native_http2_settings_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hpack_decoded_count" if args.is_empty() => Some((
                "mako_native_hpack_decoded_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "hpack_decoded_name" if args.len() == 1 => Some((
                "mako_native_hpack_decoded_name",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "hpack_decoded_value" if args.len() == 1 => Some((
                "mako_native_hpack_decoded_value",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "evloop_new" if args.is_empty() => Some((
                "mako_native_evloop_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "sip_tag" if args.is_empty() => Some((
                "mako_native_sip_tag",
                &[],
                Some(Type::Str),
                true,
            )),
            "llm_content" if args.len() == 1 => Some((
                "mako_native_llm_content_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_device_backend" if args.len() == 1 => Some((
                "mako_native_gpu_device_backend",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "str_split" if args.len() == 2 => Some((
                "mako_native_str_split_ptr",
                &[Type::Str, Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "str_fields" if args.len() == 1 => Some((
                "mako_native_str_fields_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "str_cut" if args.len() == 2 => Some((
                "mako_native_str_cut_ptr",
                &[Type::Str, Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "str_join" if args.len() == 2 => Some((
                "mako_native_str_join_ptr",
                &[Type::StrSlice, Type::Str],
                Some(Type::Str),
                true,
            )),
            "mail_msg_add_header" if args.len() == 3 => Some((
                "mako_native_mail_msg_add_header_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mono_ns" if args.is_empty() => Some((
                "mako_native_mono_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "nb_read" if args.len() == 1 => Some((
                "mako_native_nb_read_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "sip_headers_append" if args.len() == 3 => Some((
                "mako_native_sip_headers_append_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "h3_server_bind" if args.len() == 3 => Some((
                "mako_native_h3_server_bind",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_new" if args.is_empty() => Some((
                "mako_native_http2_conn_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "str_to_lower" if args.len() == 1 => Some((
                "mako_native_str_to_lower_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_to_upper" if args.len() == 1 => Some((
                "mako_native_str_to_upper_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_repeat" if args.len() == 2 => Some((
                "mako_native_str_repeat_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "str_replace" if args.len() == 3 => Some((
                "mako_native_str_replace_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_count" if args.len() == 2 => Some((
                "mako_native_str_count_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_add_cc" if args.len() == 2 => Some((
                "mako_native_mail_msg_add_cc_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "llm_base_url" if args.is_empty() => Some((
                "mako_native_llm_base_url",
                &[],
                Some(Type::Str),
                true,
            )),
            "llm_default_model" if args.is_empty() => Some((
                "mako_native_llm_default_model",
                &[],
                Some(Type::Str),
                true,
            )),
            "nb_accept" if args.len() == 1 => Some((
                "mako_native_nb_accept",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_cseq_new" if args.is_empty() => Some((
                "mako_native_sip_cseq_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "model_new" if args.len() == 1 => Some((
                "mako_native_model_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "actor_pack" if args.len() == 2 => Some((
                "mako_native_actor_pack",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "str_trim" if args.len() == 2 => Some((
                "mako_native_str_trim_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_trim_space" if args.len() == 1 => Some((
                "mako_native_str_trim_space_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_contains" if args.len() == 2 => Some((
                "mako_native_str_contains_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_call_id_new" if args.len() == 1 => Some((
                "mako_native_sip_call_id_new_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_device_name" if args.len() == 1 => Some((
                "mako_native_gpu_device_name",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "llm_redact_key" if args.len() == 1 => Some((
                "mako_native_llm_redact_key_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "evloop_add" if args.len() == 3 => Some((
                "mako_native_evloop_add",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "evloop_wait" if args.len() == 2 => Some((
                "mako_native_evloop_wait",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "evloop_event_fd" if args.len() == 2 => Some((
                "mako_native_evloop_event_fd",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "smtp_mock_start" if args.len() == 1 => Some((
                "mako_native_smtp_mock_start",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_use" if args.len() == 1 => Some(("mako_native_http2_conn_use", &[Type::I64], Some(Type::I64), false)),
            "http2_conn_set_server" if args.len() == 1 => Some(("mako_native_http2_conn_set_server", &[Type::I64], Some(Type::I64), false)),
            "http2_conn_pump" if args.len() == 1 => Some(("mako_native_http2_conn_pump_ptr", &[Type::Str], Some(Type::Str), true)),
            "http2_conn_header_stream" if args.is_empty() => Some(("mako_native_http2_conn_header_stream", &[], Some(Type::I64), false)),
            "http2_conn_header_block" if args.len() == 1 => Some(("mako_native_http2_conn_header_block", &[Type::I64], Some(Type::Str), true)),
            "http2_conn_free" if args.len() == 1 => Some(("mako_native_http2_conn_free", &[Type::I64], Some(Type::I64), false)),
            "hpack_decode_block" if args.len() == 1 => Some(("mako_native_hpack_decode_block_ptr", &[Type::Str], Some(Type::I64), false)),
            "http2_response" if args.len() == 3 => Some(("mako_native_http2_response_ptr", &[Type::I64, Type::I64, Type::Str], Some(Type::Str), true)),
            "tls_server_available" if args.is_empty() => Some(("mako_native_tls_server_available", &[], Some(Type::I64), false)),
            "tls_server_new" if args.len() == 2 => Some(("mako_native_tls_server_new_ptr", &[Type::Str, Type::Str], Some(Type::Opaque), false)),
            "tls_accept" if args.len() == 2 => Some(("mako_native_tls_accept", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "tls_read" if args.len() == 2 => Some(("mako_native_tls_read", &[Type::Opaque, Type::I64], Some(Type::Str), true)),
            "tls_write" if args.len() == 2 => Some(("mako_native_tls_write_ptr", &[Type::Opaque, Type::Str], Some(Type::I64), false)),
            "tls_conn_close" if args.len() == 1 => Some(("mako_native_tls_conn_close", &[Type::Opaque], Some(Type::I64), false)),
            "h3_server_close" if args.len() == 1 => Some(("mako_native_h3_server_close", &[Type::I64], Some(Type::I64), false)),
            "h3_server_poll" if args.len() == 2 => Some(("mako_native_h3_server_poll", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "h3_response" if args.len() == 5 => Some(("mako_native_h3_response_ptr", &[Type::I64, Type::I64, Type::I64, Type::Str, Type::Str], Some(Type::I64), false)),
            "h3_stream_write" if args.len() == 3 => Some(("mako_native_h3_stream_write_ptr", &[Type::I64, Type::I64, Type::Str], Some(Type::I64), false)),
            "llm_system_user" if args.len() == 3 => Some(("mako_native_llm_system_user_ptr", &[Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "llm_chat_retry" if args.len() == 5 => Some(("mako_native_llm_chat_retry_ptr", &[Type::Str, Type::Str, Type::Str, Type::I64, Type::I64], Some(Type::Str), true)),
            "elapsed_ns" if args.len() == 1 => Some(("mako_native_elapsed_ns", &[Type::I64], Some(Type::I64), false)),
            "llm_is_error" if args.len() == 1 => Some(("mako_native_llm_is_error_ptr", &[Type::Str], Some(Type::I64), false)),
            "llm_error_message" if args.len() == 1 => Some(("mako_native_llm_error_message_ptr", &[Type::Str], Some(Type::Str), true)),
            "llm_last_status" if args.is_empty() => Some(("mako_native_llm_last_status", &[], Some(Type::I64), false)),
            "llm_usage_prompt_tokens" if args.len() == 1 => Some(("mako_native_llm_usage_prompt_tokens_ptr", &[Type::Str], Some(Type::I64), false)),
            "llm_usage_completion_tokens" if args.len() == 1 => Some(("mako_native_llm_usage_completion_tokens_ptr", &[Type::Str], Some(Type::I64), false)),
            "smtp_new" if args.len() == 2 => Some(("mako_native_smtp_new_ptr", &[Type::Str, Type::I64], Some(Type::I64), false)),
            "smtp_connect" if args.len() == 1 => Some(("mako_native_smtp_connect", &[Type::I64], Some(Type::I64), false)),
            "smtp_ehlo" if args.len() == 2 => Some(("mako_native_smtp_ehlo_ptr", &[Type::I64, Type::Str], Some(Type::I64), false)),
            "smtp_send_built" if args.len() == 2 => Some(("mako_native_smtp_send_built", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "smtp_quit" if args.len() == 1 => Some(("mako_native_smtp_quit", &[Type::I64], Some(Type::I64), false)),
            "smtp_close" if args.len() == 1 => Some(("mako_native_smtp_close", &[Type::I64], Some(Type::I64), false)),
            "smtp_mock_stop" if args.is_empty() => Some(("mako_native_smtp_mock_stop", &[], Some(Type::I64), false)),
            "smtp_last_reply" if args.len() == 1 => Some(("mako_native_smtp_last_reply", &[Type::I64], Some(Type::Str), true)),
            "smtp_mock_last_from" if args.is_empty() => Some(("mako_native_smtp_mock_last_from", &[], Some(Type::Str), true)),
            "smtp_mock_last_rcpt" if args.is_empty() => Some(("mako_native_smtp_mock_last_rcpt", &[], Some(Type::Str), true)),
            "smtp_mock_last_message" if args.is_empty() => Some(("mako_native_smtp_mock_last_message", &[], Some(Type::Str), true)),
            "model_set_f32" if args.len() == 7 => Some(("mako_native_model_set_f32_ptr", &[Type::I64, Type::Str, Type::FloatSlice, Type::I64, Type::I64, Type::I64, Type::I64], Some(Type::I64), false)),
            "model_linear_f32" if args.len() == 9 => Some(("mako_native_model_linear_f32", &[Type::I64, Type::I64, Type::I64, Type::Str, Type::Str, Type::I64, Type::I64, Type::I64, Type::I64], Some(Type::I64), false)),
            "model_save" if args.len() == 2 => Some(("mako_native_model_save_ptr", &[Type::I64, Type::Str], Some(Type::I64), false)),
            "model_free" if args.len() == 1 => Some(("mako_native_model_free", &[Type::I64], Some(Type::I64), false)),
            "gpu_device_close" if args.len() == 1 => Some(("mako_native_gpu_device_close", &[Type::I64], Some(Type::I64), false)),
            "gpu_buf_new" if args.len() == 2 => Some(("mako_native_gpu_buf_new", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "gpu_upload_f32" if args.len() == 2 => Some(("mako_native_gpu_upload_f32", &[Type::I64, Type::FloatSlice], Some(Type::I64), false)),
            "gpu_download_f32" if args.len() == 1 => Some(("mako_native_gpu_download_f32", &[Type::I64], Some(Type::FloatSlice), true)),
            "gpu_relu_f32" if args.len() == 2 => Some(("mako_native_gpu_relu_f32", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "evloop_del" if args.len() == 2 => Some(("mako_native_evloop_del", &[Type::I64, Type::I64], Some(Type::I64), false)),
            "nb_write" if args.len() == 2 => Some(("mako_native_nb_write_ptr", &[Type::I64, Type::Str], Some(Type::I64), false)),
            "nb_close" if args.len() == 1 => Some(("mako_native_nb_close", &[Type::I64], Some(Type::I64), false)),
            "sip_via_value" if args.len() == 4 => Some(("mako_native_sip_via_value_ptr", &[Type::Str, Type::Str, Type::I64, Type::Str], Some(Type::Str), true)),
            "sip_from_value" if args.len() == 3 => Some(("mako_native_sip_from_value_ptr", &[Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "sip_to_value" if args.len() == 3 => Some(("mako_native_sip_to_value_ptr", &[Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "sip_cseq_value" if args.len() == 2 => Some(("mako_native_sip_cseq_value_ptr", &[Type::I64, Type::Str], Some(Type::Str), true)),
            "sip_contact_value" if args.len() == 1 => Some(("mako_native_sip_contact_value_ptr", &[Type::Str], Some(Type::Str), true)),
            "sip_uri_build" if args.len() == 3 => Some(("mako_native_sip_uri_build_ptr", &[Type::Str, Type::Str, Type::I64], Some(Type::Str), true)),
            "sip_request" if args.len() == 4 => Some(("mako_native_sip_request_ptr", &[Type::Str, Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "sip_method" if args.len() == 1 => Some(("mako_native_sip_method_ptr", &[Type::Str], Some(Type::Str), true)),
            "sip_request_uri" if args.len() == 1 => Some(("mako_native_sip_request_uri_ptr", &[Type::Str], Some(Type::Str), true)),
            "sip_body" if args.len() == 1 => Some(("mako_native_sip_body_ptr", &[Type::Str], Some(Type::Str), true)),
            "sip_reply" if args.len() == 5 => Some(("mako_native_sip_reply_ptr", &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "sip_status_code" if args.len() == 1 => Some(("mako_native_sip_status_code_ptr", &[Type::Str], Some(Type::I64), false)),
            "sdp_build_audio" if args.len() == 5 => Some(("mako_native_sdp_build_audio_ptr", &[Type::Str, Type::Str, Type::Str, Type::I64, Type::Str], Some(Type::Str), true)),
            "sdp_media_port" if args.len() == 2 => Some(("mako_native_sdp_media_port_ptr", &[Type::Str, Type::I64], Some(Type::I64), false)),
            "sdp_connection_addr" if args.len() == 1 => Some(("mako_native_sdp_connection_addr_ptr", &[Type::Str], Some(Type::Str), true)),
            "rtp_pack" if args.len() == 6 => Some(("mako_native_rtp_pack_ptr", &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::Str], Some(Type::Str), true)),
            "rtp_seq" if args.len() == 1 => Some(("mako_native_rtp_seq_ptr", &[Type::Str], Some(Type::I64), false)),
            "rtp_ssrc" if args.len() == 1 => Some(("mako_native_rtp_ssrc_ptr", &[Type::Str], Some(Type::I64), false)),
            "cmap_new" if args.is_empty() => Some(("mako_native_cmap_new", &[], Some(Type::Opaque), true)),
            "cmap_set" if args.len() == 3 => Some(("mako_native_cmap_set_ptr", &[Type::Opaque, Type::Str, Type::Str], None, false)),
            "cmap_get" if args.len() == 2 => Some(("mako_native_cmap_get_ptr", &[Type::Opaque, Type::Str], Some(Type::Str), true)),
            "sql_open_sqlite" if args.len() == 1 => Some(("mako_native_sql_open_sqlite_ptr", &[Type::Str], Some(Type::Opaque), false)),
            "sql_exec_plain" if args.len() == 2 => Some(("mako_native_sql_exec_plain_ptr", &[Type::Opaque, Type::Str], Some(Type::I64), false)),
            "sql_exec_str4" if args.len() == 6 => Some(("mako_native_sql_exec_str4_ptr", &[Type::Opaque, Type::Str, Type::Str, Type::Str, Type::Str, Type::Str], Some(Type::I64), false)),
            "sql_query_str" if args.len() == 3 => Some(("mako_native_sql_query_str_ptr", &[Type::Opaque, Type::Str, Type::Str], Some(Type::Str), true)),
            "format_int" if args.len() == 1 => Some(("mako_native_int_to_string_ptr", &[Type::I64], Some(Type::Str), true)),
            "int_to_string" if args.len() == 1 => Some(("mako_native_int_to_string_ptr", &[Type::I64], Some(Type::Str), true)),
            "http_forward" if args.len() == 5 => Some(("mako_native_http_forward_ptr", &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str], Some(Type::Str), true)),
            "http_forward_full" if args.len() == 7 => Some((
                "mako_native_http_forward_full_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_forward_fd" if args.len() == 7 => Some((
                "mako_native_http_forward_fd_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_accept_start" if args.len() == 2 => Some((
                "mako_native_tls_accept_start",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_proxy_raw" if args.len() == 4 => Some((
                "mako_native_http_proxy_raw_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "proxy_io_ok" if args.len() == 1 => Some((
                "mako_native_proxy_io_ok",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "proxy_io_bytes_written" if args.len() == 1 => Some((
                "mako_native_proxy_io_bytes_written",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "proxy_io_bytes_read" if args.len() == 1 => Some((
                "mako_native_proxy_io_bytes_read",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_forward_ok" if args.len() == 1 => Some((
                "mako_native_http_forward_ok",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_forward_status" if args.len() == 1 => Some((
                "mako_native_http_forward_status",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_forward_body_len" if args.len() == 1 => Some((
                "mako_native_http_forward_body_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_forward_total_bytes" if args.len() == 1 => Some((
                "mako_native_http_forward_total_bytes",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "h3_accept_stream" if args.len() == 1 => Some(("mako_native_h3_accept_stream", &[Type::I64], Some(Type::I64), false)),
            "quiche_available" if args.is_empty() => Some(("mako_native_quiche_available", &[], Some(Type::I64), false)),
            "quiche_version" if args.is_empty() => Some(("mako_native_quiche_version", &[], Some(Type::Str), true)),
            "quiche_handshake" if args.len() == 4 => Some(("mako_native_quiche_handshake_ptr", &[Type::Str, Type::I64, Type::Str, Type::I64], Some(Type::Str), true)),
            "quiche_h3_get" if args.len() == 5 => Some(("mako_native_quiche_h3_get_ptr", &[Type::Str, Type::I64, Type::Str, Type::Str, Type::I64], Some(Type::Str), true)),
            "quiche_h3_post" if args.len() == 6 => Some(("mako_native_quiche_h3_post_ptr", &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str, Type::I64], Some(Type::Str), true)),
            "quiche_h3_get_two" if args.len() == 6 => Some(("mako_native_quiche_h3_get_two_ptr", &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str, Type::I64], Some(Type::Str), true)),
            "sql_close" if args.len() == 1 => Some(("mako_native_sql_close", &[Type::Opaque], Some(Type::I64), false)),
            "h3_stream_method" if args.len() == 2 => Some(("mako_native_h3_stream_method", &[Type::I64, Type::I64], Some(Type::Str), true)),
            "h3_stream_path" if args.len() == 2 => Some(("mako_native_h3_stream_path", &[Type::I64, Type::I64], Some(Type::Str), true)),
            "h3_stream_body" if args.len() == 2 => Some(("mako_native_h3_stream_body", &[Type::I64, Type::I64], Some(Type::Str), true)),
            // High-frequency testing/stdlib helpers (bridge in native_bridge.c).
            "env_get_or" if args.len() == 2 => Some((
                "mako_native_env_get_or_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "path_clean" if args.len() == 1 => Some((
                "mako_native_path_clean_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sha1" if args.len() == 1 => Some((
                "mako_native_sha1_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "would_overflow_add" if args.len() == 2 => Some((
                "mako_native_would_overflow_add",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "would_overflow_sub" if args.len() == 2 => Some((
                "mako_native_would_overflow_sub",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "would_overflow_mul" if args.len() == 2 => Some((
                "mako_native_would_overflow_mul",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "chan_cap" if args.len() == 1 => Some((
                "mako_native_chan_cap",
                &[Type::ChanI],
                Some(Type::I64),
                false,
            )),
            "chan_len" if args.len() == 1 => Some((
                "mako_native_chan_len",
                &[Type::ChanI],
                Some(Type::I64),
                false,
            )),
            "sql_ok" if args.len() == 1 => Some((
                "mako_native_sql_ok",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "time_sleep_ms" if args.len() == 1 => Some((
                "mako_native_time_sleep_ms",
                &[Type::I64],
                None,
                false,
            )),
            "tcp_listen_addr" if args.len() == 2 => Some((
                "mako_native_tcp_listen_addr_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "aead_available" if args.len() == 0 => Some((
                "mako_native_aead_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "alloc_report_json" if args.len() == 0 => Some((
                "mako_native_alloc_report_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "alloc_track_reset" if args.len() == 0 => Some((
                "mako_native_alloc_track_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "append_file" if args.len() == 2 => Some((
                "mako_native_append_file_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "auth_bearer" if args.len() == 1 => Some((
                "mako_native_auth_bearer_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "avro_encode_long" if args.len() == 1 => Some((
                "mako_native_avro_encode_long_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "binary_put_u16be" if args.len() == 1 => Some((
                "mako_native_binary_put_u16be_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "checked_add" if args.len() == 2 => Some((
                "mako_native_checked_add",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "conn_pool_slot" if args.len() == 2 => Some((
                "mako_native_conn_pool_slot_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "const_eq" if args.len() == 2 => Some((
                "mako_native_const_eq_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "cookie_get" if args.len() == 2 => Some((
                "mako_native_cookie_get_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "dap_initialize_response" if args.len() == 1 => Some((
                "mako_native_dap_initialize_response_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "ecs_world_new" if args.len() == 1 => Some((
                "mako_native_ecs_world_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "elapsed_ms" if args.len() == 1 => Some((
                "mako_native_elapsed_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "exec_output" if args.len() == 1 => Some((
                "mako_native_exec_output_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintf" if args.len() == 2 => Some((
                "mako_native_fmt_sprintf_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "format_bool" if args.len() == 1 => Some((
                "mako_native_format_bool_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "fsm_rule" if args.len() == 3 => Some((
                "mako_native_fsm_rule_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fx_from_int" if args.len() == 2 => Some((
                "mako_native_fx_from_int",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_fixed_steps" if args.len() == 3 => Some((
                "mako_native_game_fixed_steps",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_available" if args.len() == 0 => Some((
                "mako_native_gpu_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "gpu_set_prefer_host" if args.len() == 1 => Some((
                "mako_native_gpu_set_prefer_host",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_field" if args.len() == 1 => Some((
                "mako_native_graphql_field_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_encode_message" if args.len() == 1 => Some((
                "mako_native_grpc_encode_message_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "h3_server_fd" if args.len() == 1 => Some((
                "mako_native_h3_server_fd",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hex_decode" if args.len() == 1 => Some((
                "mako_native_hex_decode_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hex_encode" if args.len() == 1 => Some((
                "mako_native_hex_encode_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hpack_encode_indexed" if args.len() == 1 => Some((
                "mako_native_hpack_encode_indexed_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "hpack_encode_literal" if args.len() == 2 => Some((
                "mako_native_hpack_encode_literal_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_client_preface" if args.len() == 0 => Some((
                "mako_native_http2_client_preface_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "http2_conn_recv" if args.len() == 1 => Some((
                "mako_native_http2_conn_recv_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_conn_reset" if args.len() == 0 => Some((
                "mako_native_http2_conn_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_goaway_frame" if args.len() == 2 => Some((
                "mako_native_http2_goaway_frame_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_headers_frame" if args.len() == 3 => Some((
                "mako_native_http2_headers_frame_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_response_ct" if args.len() == 4 => Some((
                "mako_native_http2_response_ct_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_health_json" if args.len() == 2 => Some((
                "mako_native_http_health_json_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_last_status" if args.len() == 0 => Some((
                "mako_native_http_last_status",
                &[],
                Some(Type::I64),
                false,
            )),
            "http_respond_json" if args.len() == 3 => Some((
                "mako_native_http_respond_json_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_shutdown_reset" if args.len() == 0 => Some((
                "mako_native_http_shutdown_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "https_last_status" if args.len() == 0 => Some((
                "mako_native_https_last_status",
                &[],
                Some(Type::I64),
                false,
            )),
            "io_backoff_ms" if args.len() == 3 => Some((
                "mako_native_io_backoff_ms",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "job_schedule" if args.len() == 2 => Some((
                "mako_native_job_schedule_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "jpeg_encode_gray" if args.len() == 3 => Some((
                "mako_native_jpeg_encode_gray_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_encode_gray_huff" if args.len() == 3 => Some((
                "mako_native_jpeg_encode_gray_huff_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_encode_gray_baseline" if args.len() == 3 => Some((
                "mako_native_jpeg_encode_gray_baseline_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_is_baseline_huff" if args.len() == 1 => Some((
                "mako_native_jpeg_is_baseline_huff_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_encode_gray_jfif" if args.len() == 3 => Some((
                "mako_native_jpeg_encode_gray_jfif_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "llm_message" if args.len() == 2 => Some((
                "mako_native_llm_message_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "mail_parse_address" if args.len() == 1 => Some((
                "mako_native_mail_parse_address_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "mongo_connect_url" if args.len() == 1 => Some((
                "mako_native_mongo_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "mono_res_ns" if args.len() == 0 => Some((
                "mako_native_mono_res_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "msgpack_encode_int" if args.len() == 1 => Some((
                "mako_native_msgpack_encode_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "multipart_boundary" if args.len() == 1 => Some((
                "mako_native_multipart_boundary_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "openapi_route" if args.len() == 3 => Some((
                "mako_native_openapi_route_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "parse_ip_ok" if args.len() == 1 => Some((
                "mako_native_parse_ip_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "path_base" if args.len() == 1 => Some((
                "mako_native_path_base_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_encode_bytes" if args.len() == 1 => Some((
                "mako_native_pb_encode_bytes_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_encode_nested" if args.len() == 4 => Some((
                "mako_native_pb_encode_nested_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "pb_zigzag_encode" if args.len() == 1 => Some((
                "mako_native_pb_zigzag_encode",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_max_slots" if args.len() == 0 => Some((
                "mako_native_plugin_max_slots",
                &[],
                Some(Type::I64),
                false,
            )),
            "profile_sample_clear" if args.len() == 0 => Some((
                "mako_native_profile_sample_clear",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_hkdf_expand_label_hex" if args.len() == 3 => Some((
                "mako_native_quic_hkdf_expand_label_hex_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quiche_start_server" if args.len() == 5 => Some((
                "mako_native_quiche_start_server_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "rate_allow" if args.len() == 3 => Some((
                "mako_native_rate_allow_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "redis_mock_kv" if args.len() == 2 => Some((
                "mako_native_redis_mock_kv",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "reflect_type_schema" if args.len() == 1 => Some((
                "mako_native_reflect_type_schema_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "remove_all" if args.len() == 1 => Some((
                "mako_native_remove_all_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "reqctx_new" if args.len() == 0 => Some((
                "mako_native_reqctx_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "ring_new" if args.len() == 1 => Some((
                "mako_native_ring_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "router_new" if args.len() == 0 => Some((
                "mako_native_router_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "runtime_stats_reset" if args.len() == 0 => Some((
                "mako_native_runtime_stats_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "sched_set_workers" if args.len() == 1 => Some((
                "mako_native_sched_set_workers",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sha512" if args.len() == 1 => Some((
                "mako_native_sha512_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "signal_watch" if args.len() == 1 => Some((
                "mako_native_signal_watch_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_md5_hex" if args.len() == 1 => Some((
                "mako_native_sip_md5_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_ok" if args.len() == 1 => Some((
                "mako_native_sip_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "slog_redact" if args.len() == 1 => Some((
                "mako_native_slog_redact_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_set_level" if args.len() == 1 => Some((
                "mako_native_slog_set_level_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sql_check_typed" if args.len() == 4 => Some((
                "mako_native_sql_check_typed_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sse_event" if args.len() == 2 => Some((
                "mako_native_sse_event_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_slice_eq" if args.len() == 4 => Some((
                "mako_native_str_slice_eq_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_available" if args.len() == 0 => Some((
                "mako_native_syscall_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "t_run" if args.len() == 1 => Some((
                "mako_native_t_run_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tcp_listen_backlog" if args.len() == 3 => Some((
                "mako_native_tcp_listen_backlog_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_local_addr" if args.len() == 1 => Some((
                "mako_native_tcp_local_addr_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "tcp_pool_open" if args.len() == 4 => Some((
                "mako_native_tcp_pool_open_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_set_he_delay_ms" if args.len() == 1 => Some((
                "mako_native_tcp_set_he_delay_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_date" if args.len() == 7 => Some((
                "mako_native_time_date",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_aead_seal" if args.len() == 4 => Some((
                "mako_native_tls_aead_seal_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_client_available" if args.len() == 0 => Some((
                "mako_native_tls_client_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_client_hello" if args.len() == 1 => Some((
                "mako_native_tls_client_hello_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_handshake_ok" if args.len() == 3 => Some((
                "mako_native_tls_handshake_ok_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tmpl_new" if args.len() == 1 => Some((
                "mako_native_tmpl_new_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "trace_clear" if args.len() == 0 => Some((
                "mako_native_trace_clear",
                &[],
                Some(Type::I64),
                false,
            )),
            "trace_id" if args.len() == 0 => Some((
                "mako_native_trace_id_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "udp_bind" if args.len() == 1 => Some((
                "mako_native_udp_bind",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "url_scheme" if args.len() == 1 => Some((
                "mako_native_url_scheme_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "utf8_rune_error" if args.len() == 0 => Some((
                "mako_native_utf8_rune_error",
                &[],
                Some(Type::I64),
                false,
            )),
            "validate_required" if args.len() == 1 => Some((
                "mako_native_validate_required_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "watch_available" if args.len() == 0 => Some((
                "mako_native_watch_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_accept_key" if args.len() == 1 => Some((
                "mako_native_ws_accept_key_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "yaml_get_string" if args.len() == 2 => Some((
                "mako_native_yaml_get_string_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "zip_write_file" if args.len() == 3 => Some((
                "mako_native_zip_write_file_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "auth_check_bearer" if args.len() == 2 => Some((
                "mako_native_auth_check_bearer_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "avro_decode_long" if args.len() == 1 => Some((
                "mako_native_avro_decode_long_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_u16be" if args.len() == 1 => Some((
                "mako_native_binary_u16be_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "checked_sub" if args.len() == 2 => Some((
                "mako_native_checked_sub",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "conn_pool_next" if args.len() == 1 => Some((
                "mako_native_conn_pool_next",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "cookie_make" if args.len() == 3 => Some((
                "mako_native_cookie_make_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "crypto_eq" if args.len() == 2 => Some((
                "mako_native_crypto_eq_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "dap_stopped_event" if args.len() == 2 => Some((
                "mako_native_dap_stopped_event_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "dns_ip_family" if args.len() == 1 => Some((
                "mako_native_dns_ip_family_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "ecs_spawn" if args.len() == 1 => Some((
                "mako_native_ecs_spawn",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fmt_sprintf3" if args.len() == 4 => Some((
                "mako_native_fmt_sprintf3_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintf_d" if args.len() == 2 => Some((
                "mako_native_fmt_sprintf_d_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "frame_alloc_new" if args.len() == 1 => Some((
                "mako_native_frame_alloc_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fx_to_int" if args.len() == 2 => Some((
                "mako_native_fx_to_int",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_fixed_remainder" if args.len() == 3 => Some((
                "mako_native_game_fixed_remainder",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_backend" if args.len() == 0 => Some((
                "mako_native_gpu_backend_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "gpu_gelu_f32" if args.len() == 2 => Some((
                "mako_native_gpu_gelu_f32",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_arg" if args.len() == 2 => Some((
                "mako_native_graphql_arg_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_message_len" if args.len() == 1 => Some((
                "mako_native_grpc_message_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hpack_decode_clear" if args.len() == 0 => Some((
                "mako_native_hpack_decode_clear",
                &[],
                Some(Type::I64),
                false,
            )),
            "hpack_decode_indexed" if args.len() == 1 => Some((
                "mako_native_hpack_decode_indexed_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_conn_is_server" if args.len() == 0 => Some((
                "mako_native_http2_conn_is_server",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_continuation_frame" if args.len() == 3 => Some((
                "mako_native_http2_continuation_frame_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_frame_type" if args.len() == 1 => Some((
                "mako_native_http2_frame_type_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_is_goaway" if args.len() == 1 => Some((
                "mako_native_http2_is_goaway_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_window_conn" if args.len() == 0 => Some((
                "mako_native_http2_window_conn",
                &[],
                Some(Type::I64),
                false,
            )),
            "http_post" if args.len() == 2 => Some((
                "mako_native_http_post_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_request" if args.len() == 4 => Some((
                "mako_native_http_request_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_requested" if args.len() == 0 => Some((
                "mako_native_http_shutdown_requested",
                &[],
                Some(Type::I64),
                false,
            )),
            "io_should_pause" if args.len() == 3 => Some((
                "mako_native_io_should_pause",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "job_due" if args.len() == 1 => Some((
                "mako_native_job_due_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_has_app8" if args.len() == 1 => Some((
                "mako_native_jpeg_has_app8_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_has_eoi" if args.len() == 1 => Some((
                "mako_native_jpeg_has_eoi_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_has_soi" if args.len() == 1 => Some((
                "mako_native_jpeg_has_soi_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_huff_block" if args.len() == 1 => Some((
                "mako_native_jpeg_huff_block_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_is_baseline_gray" if args.len() == 1 => Some((
                "mako_native_jpeg_is_baseline_gray_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_jfif" if args.len() == 1 => Some((
                "mako_native_jpeg_is_jfif_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_mako_raw" if args.len() == 1 => Some((
                "mako_native_jpeg_is_mako_raw_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_density_units" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_density_units_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_major" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_major_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_roundtrip_ok" if args.len() == 1 => Some((
                "mako_native_jpeg_roundtrip_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_components" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_components_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_precision" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_precision_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_quant_table" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_quant_table_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jwt_verify_jwks" if args.len() == 2 => Some((
                "mako_native_jwt_verify_jwks_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "jwt_verify_rs256" if args.len() == 2 => Some((
                "mako_native_jwt_verify_rs256_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "leak_mark" if args.len() == 0 => Some((
                "mako_native_leak_mark",
                &[],
                Some(Type::I64),
                false,
            )),
            "llm_messages_append" if args.len() == 2 => Some((
                "mako_native_llm_messages_append_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "mail_address_ok" if args.len() == 1 => Some((
                "mako_native_mail_address_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "mkdir_all" if args.len() == 1 => Some((
                "mako_native_mkdir_all_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "model_tensor_count" if args.len() == 1 => Some((
                "mako_native_model_tensor_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "mongo_find_one_request" if args.len() == 2 => Some((
                "mako_native_mongo_find_one_request_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "mono_overhead_ns" if args.len() == 0 => Some((
                "mako_native_mono_overhead_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "msgpack_decode_int" if args.len() == 1 => Some((
                "mako_native_msgpack_decode_int_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "multipart_form_value" if args.len() == 3 => Some((
                "mako_native_multipart_form_value_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "openapi_doc" if args.len() == 3 => Some((
                "mako_native_openapi_doc_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "path_dir" if args.len() == 1 => Some((
                "mako_native_path_dir_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_bytes_len" if args.len() == 1 => Some((
                "mako_native_pb_bytes_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_nested_inner" if args.len() == 1 => Some((
                "mako_native_pb_nested_inner_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_zigzag_decode" if args.len() == 1 => Some((
                "mako_native_pb_zigzag_decode",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "pbkdf2_sha256" if args.len() == 4 => Some((
                "mako_native_pbkdf2_sha256_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "plugin_abi_version" if args.len() == 0 => Some((
                "mako_native_plugin_abi_version",
                &[],
                Some(Type::I64),
                false,
            )),
            "plugin_open" if args.len() == 1 => Some((
                "mako_native_plugin_open_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "profile_sample_count" if args.len() == 0 => Some((
                "mako_native_profile_sample_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_hkdf_expand_label" if args.len() == 3 => Some((
                "mako_native_quic_hkdf_expand_label_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quiche_stop_server" if args.len() == 1 => Some((
                "mako_native_quiche_stop_server",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "random_bytes" if args.len() == 1 => Some((
                "mako_native_random_bytes_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "rate_remaining" if args.len() == 3 => Some((
                "mako_native_rate_remaining_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "redis_set" if args.len() == 4 => Some((
                "mako_native_redis_set_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_struct_num_fields" if args.len() == 1 => Some((
                "mako_native_reflect_struct_num_fields_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "reqctx_count" if args.len() == 1 => Some((
                "mako_native_reqctx_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "ring_cap" if args.len() == 1 => Some((
                "mako_native_ring_cap",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "router_count" if args.len() == 1 => Some((
                "mako_native_router_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "runtime_stats_json" if args.len() == 0 => Some((
                "mako_native_runtime_stats_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "sched_workers" if args.len() == 0 => Some((
                "mako_native_sched_workers",
                &[],
                Some(Type::I64),
                false,
            )),
            "sg_gather3" if args.len() == 3 => Some((
                "mako_native_sg_gather3_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "signal_ignore" if args.len() == 1 => Some((
                "mako_native_signal_ignore_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_digest_response" if args.len() == 6 => Some((
                "mako_native_sip_digest_response_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_is_request" if args.len() == 1 => Some((
                "mako_native_sip_is_request_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),

            "slog_with_redacted" if args.len() == 3 => Some((
                "mako_native_slog_with_redacted_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sse_retry" if args.len() == 1 => Some((
                "mako_native_sse_retry_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "str_slice_ci_eq" if args.len() == 4 => Some((
                "mako_native_str_slice_ci_eq_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "t_run_nested" if args.len() == 1 => Some((
                "mako_native_t_run_nested_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tcp_get_he_delay_ms" if args.len() == 0 => Some((
                "mako_native_tcp_get_he_delay_ms",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_keepalive" if args.len() == 4 => Some((
                "mako_native_tcp_keepalive",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_peer_addr" if args.len() == 1 => Some((
                "mako_native_tcp_peer_addr_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "tcp_pool_idle" if args.len() == 1 => Some((
                "mako_native_tcp_pool_idle",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_aead_open" if args.len() == 4 => Some((
                "mako_native_tls_aead_open_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_client_hello_legacy_version" if args.len() == 1 => Some((
                "mako_native_tls_client_hello_legacy_version_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_handshake_version" if args.len() == 3 => Some((
                "mako_native_tls_handshake_version_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tmpl_execute" if args.len() == 2 => Some((
                "mako_native_tmpl_execute_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "toml_get_string" if args.len() == 2 => Some((
                "mako_native_toml_get_string_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "trace_export_otlp_json" if args.len() == 0 => Some((
                "mako_native_trace_export_otlp_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "trace_set" if args.len() == 1 => Some((
                "mako_native_trace_set_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "udp_local_port" if args.len() == 1 => Some((
                "mako_native_udp_local_port",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "url_host" if args.len() == 1 => Some((
                "mako_native_url_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "utf8_rune_self" if args.len() == 0 => Some((
                "mako_native_utf8_rune_self",
                &[],
                Some(Type::I64),
                false,
            )),
            "validate_min_len" if args.len() == 2 => Some((
                "mako_native_validate_min_len_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ws_client_request" if args.len() == 3 => Some((
                "mako_native_ws_client_request_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "yaml_get_int" if args.len() == 2 => Some((
                "mako_native_yaml_get_int_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "zip_first_name" if args.len() == 1 => Some((
                "mako_native_zip_first_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "zip_read_file" if args.len() == 2 => Some((
                "mako_native_zip_read_file_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_get_level" if args.len() == 0 => Some((
                "mako_native_slog_get_level",
                &[],
                Some(Type::I64),
                false,
            )),
            // High-frequency testing helpers (round 3).
            "path_ext" if args.len() == 1 => Some((
                "mako_native_path_ext_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "random_int" if args.len() == 2 => Some((
                "mako_native_random_int",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "wall_ms" if args.len() == 0 => Some((
                "mako_native_wall_ms",
                &[],
                Some(Type::I64),
                false,
            )),
            "fmt_sprint" if args.len() == 1 => Some((
                "mako_native_fmt_sprint_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprint2" if args.len() == 2 => Some((
                "mako_native_fmt_sprint2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprint3" if args.len() == 3 => Some((
                "mako_native_fmt_sprint3_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintln" if args.len() == 1 => Some((
                "mako_native_fmt_sprintln_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintln2" if args.len() == 2 => Some((
                "mako_native_fmt_sprintln2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_errorf2" if args.len() == 3 => Some((
                "mako_native_fmt_errorf2_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_print" if args.len() == 1 => Some((
                "mako_native_fmt_print_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_println" if args.len() == 1 => Some((
                "mako_native_fmt_println_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_printf" if args.len() == 2 => Some((
                "mako_native_fmt_printf_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_eprint" if args.len() == 1 => Some((
                "mako_native_fmt_eprint_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_eprintln" if args.len() == 1 => Some((
                "mako_native_fmt_eprintln_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fmt_sprintf_f" if args.len() == 3 => Some((
                "mako_native_fmt_sprintf_f_ptr",
                &[Type::Str, Type::F64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintf_dd" if args.len() == 3 => Some((
                "mako_native_fmt_sprintf_dd_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "fmt_sprintf4" if args.len() == 5 => Some((
                "mako_native_fmt_sprintf4_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fmt_print2" if args.len() == 2 => Some((
                "mako_native_fmt_print2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "argon2id_hash" if args.len() == 1 => Some((
                "mako_native_argon2id_hash_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "argon2id_verify" if args.len() == 2 => Some((
                "mako_native_argon2id_verify_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "cmap_has" if args.len() == 2 => Some((
                "mako_native_cmap_has_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "cmap_del" if args.len() == 2 => Some((
                "mako_native_cmap_del_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "cmap_len" if args.len() == 1 => Some((
                "mako_native_cmap_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "cmap_incr" if args.len() == 3 => Some((
                "mako_native_cmap_incr_ptr",
                &[Type::Opaque, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "btree_new" if args.len() == 0 => Some((
                "mako_native_btree_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "btree_put" if args.len() == 3 => Some((
                "mako_native_btree_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "btree_get" if args.len() == 2 => Some((
                "mako_native_btree_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "btree_free" if args.len() == 1 => Some((
                "mako_native_btree_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "btree_range" if args.len() == 3 => Some((
                "mako_native_btree_range",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pbtree_new" if args.len() == 0 => Some((
                "mako_native_pbtree_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "pbtree_put" if args.len() == 3 => Some((
                "mako_native_pbtree_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pbtree_get" if args.len() == 2 => Some((
                "mako_native_pbtree_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pbtree_len" if args.len() == 1 => Some((
                "mako_native_pbtree_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pbtree_pages" if args.len() == 1 => Some((
                "mako_native_pbtree_pages",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pbtree_free" if args.len() == 1 => Some((
                "mako_native_pbtree_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            // WAL / LSM / bloom / page manager (domain tracks)
            "wal_open" if args.len() == 1 => Some((
                "mako_native_wal_open_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                true,
            )),
            "wal_close" if args.len() == 1 => Some((
                "mako_native_wal_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_new" if args.len() == 1 => Some((
                "mako_native_lsm_new",
                &[Type::I64],
                Some(Type::Opaque),
                true,
            )),
            "lsm_free" if args.len() == 1 => Some((
                "mako_native_lsm_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_attach_run" if args.len() == 2 => Some((
                "mako_native_lsm_attach_run",
                &[Type::Opaque, Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_put" if args.len() == 3 => Some((
                "mako_native_lsm_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "lsm_get" if args.len() == 2 => Some((
                "mako_native_lsm_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "lsm_flush" if args.len() == 1 => Some((
                "mako_native_lsm_flush",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_flushes" if args.len() == 1 => Some((
                "mako_native_lsm_flushes",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_compact" if args.len() == 2 => Some((
                "mako_native_lsm_compact_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "lsm_compact_down" if args.len() == 2 => Some((
                "mako_native_lsm_compact_down_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "lsm_compactions" if args.len() == 1 => Some((
                "mako_native_lsm_compactions",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_sst_levels" if args.len() == 1 => Some((
                "mako_native_lsm_sst_levels",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "lsm_level_len" if args.len() == 2 => Some((
                "mako_native_lsm_level_len",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "bloom_new" if args.len() == 0 => Some((
                "mako_native_bloom_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "bloom_add" if args.len() == 2 => Some((
                "mako_native_bloom_add",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "bloom_len" if args.len() == 1 => Some((
                "mako_native_bloom_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "bloom_maybe" if args.len() == 2 => Some((
                "mako_native_bloom_maybe",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "bloom_clear" if args.len() == 1 => Some((
                "mako_native_bloom_clear",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "bloom_free" if args.len() == 1 => Some((
                "mako_native_bloom_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_open" if args.len() == 1 => Some((
                "mako_native_pman_open_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                true,
            )),
            "pman_alloc" if args.len() == 1 => Some((
                "mako_native_pman_alloc",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_set" if args.len() == 4 => Some((
                "mako_native_pman_set",
                &[Type::Opaque, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pman_get" if args.len() == 3 => Some((
                "mako_native_pman_get",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pman_close" if args.len() == 1 => Some((
                "mako_native_pman_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),

            // ---- auto domain-track bridges ----
            "range_val_at" if args.len() == 1 => Some((
                "mako_native_range_val_at",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "range_key_at" if args.len() == 1 => Some((
                "mako_native_range_key_at",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "range_len" if args.len() == 0 => Some((
                "mako_native_range_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "range_cap" if args.len() == 0 => Some((
                "mako_native_range_cap",
                &[],
                Some(Type::I64),
                false,
            )),
            "range_next" if args.len() == 0 => Some((
                "mako_native_range_next",
                &[],
                Some(Type::I64),
                false,
            )),
            "range_rewind" if args.len() == 0 => Some((
                "mako_native_range_rewind",
                &[],
                Some(Type::I64),
                false,
            )),
            "btree_put_str" if args.len() == 3 => Some((
                "mako_native_btree_put_str_ptr",
                &[Type::Opaque, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "btree_get_str" if args.len() == 2 => Some((
                "mako_native_btree_get_str_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "btree_save" if args.len() == 2 => Some((
                "mako_native_btree_save_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "btree_load" if args.len() == 1 => Some((
                "mako_native_btree_load_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                true,
            )),
            "bloom_add_str" if args.len() == 2 => Some((
                "mako_native_bloom_add_str_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "bloom_maybe_str" if args.len() == 2 => Some((
                "mako_native_bloom_maybe_str_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "domain_reg_del" if args.len() == 1 => Some((
                "mako_native_domain_reg_del",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "domain_reg_put_bloom" if args.len() == 1 => Some((
                "mako_native_domain_reg_put_bloom",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "domain_reg_put_btree" if args.len() == 1 => Some((
                "mako_native_domain_reg_put_btree",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "domain_reg_put_pman" if args.len() == 1 => Some((
                "mako_native_domain_reg_put_pman",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "domain_reg_get_bloom" if args.len() == 1 => Some((
                "mako_native_domain_reg_get_bloom",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "domain_reg_get_btree" if args.len() == 1 => Some((
                "mako_native_domain_reg_get_btree",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "domain_reg_get_pman" if args.len() == 1 => Some((
                "mako_native_domain_reg_get_pman",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "pman_pages" if args.len() == 1 => Some((
                "mako_native_pman_pages",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_reads" if args.len() == 1 => Some((
                "mako_native_pman_reads",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_writes" if args.len() == 1 => Some((
                "mako_native_pman_writes",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_sync" if args.len() == 1 => Some((
                "mako_native_pman_sync",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pman_read_page" if args.len() == 2 => Some((
                "mako_native_pman_read_page_ptr",
                &[Type::Opaque, Type::I64],
                Some(Type::Str),
                true,
            )),
            "pman_write_page" if args.len() == 3 => Some((
                "mako_native_pman_write_page_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "hot_reload_watch" if args.len() == 1 => Some((
                "mako_native_hot_reload_watch_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hot_reload_unwatch" if args.len() == 1 => Some((
                "mako_native_hot_reload_unwatch_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hot_reload_changed" if args.len() == 1 => Some((
                "mako_native_hot_reload_changed_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "file_mtime_ns" if args.len() == 1 => Some((
                "mako_native_file_mtime_ns_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "mvcc_new" if args.len() == 0 => Some((
                "mako_native_mvcc_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "mvcc_free" if args.len() == 1 => Some((
                "mako_native_mvcc_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mvcc_begin" if args.len() == 1 => Some((
                "mako_native_mvcc_begin",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mvcc_put" if args.len() == 3 => Some((
                "mako_native_mvcc_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "mvcc_get" if args.len() == 3 => Some((
                "mako_native_mvcc_get",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "mvcc_versions" if args.len() == 1 => Some((
                "mako_native_mvcc_versions",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "multimap_new" if args.len() == 0 => Some((
                "mako_native_multimap_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "multimap_free" if args.len() == 1 => Some((
                "mako_native_multimap_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "multimap_put" if args.len() == 3 => Some((
                "mako_native_multimap_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "multimap_get" if args.len() == 2 => Some((
                "mako_native_multimap_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "multimap_get_all" if args.len() == 2 => Some((
                "mako_native_multimap_get_all",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "multimap_len" if args.len() == 1 => Some((
                "mako_native_multimap_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "multimap_range" if args.len() == 3 => Some((
                "mako_native_multimap_range",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "rollback_new" if args.len() == 1 => Some((
                "mako_native_rollback_new",
                &[Type::I64],
                Some(Type::Opaque),
                true,
            )),
            "rollback_free" if args.len() == 1 => Some((
                "mako_native_rollback_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "rollback_push" if args.len() == 3 => Some((
                "mako_native_rollback_push_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "rollback_get" if args.len() == 3 => Some((
                "mako_native_rollback_get",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "rollback_len" if args.len() == 1 => Some((
                "mako_native_rollback_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "rollback_restore_slot0" if args.len() == 2 => Some((
                "mako_native_rollback_restore_slot0",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "store_new" if args.len() == 1 => Some((
                "mako_native_store_new",
                &[Type::I64],
                Some(Type::Opaque),
                true,
            )),
            "store_free" if args.len() == 1 => Some((
                "mako_native_store_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "store_attach_wal" if args.len() == 2 => Some((
                "mako_native_store_attach_wal",
                &[Type::Opaque, Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "store_begin" if args.len() == 1 => Some((
                "mako_native_store_begin",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "store_put" if args.len() == 3 => Some((
                "mako_native_store_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "store_get" if args.len() == 2 => Some((
                "mako_native_store_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "store_commit" if args.len() == 1 => Some((
                "mako_native_store_commit",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "store_recover_wal" if args.len() == 2 => Some((
                "mako_native_store_recover_wal",
                &[Type::Opaque, Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sst_free" if args.len() == 1 => Some((
                "mako_native_sst_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sst_get" if args.len() == 2 => Some((
                "mako_native_sst_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sst_len" if args.len() == 1 => Some((
                "mako_native_sst_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sst_range" if args.len() == 3 => Some((
                "mako_native_sst_range",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gfx_window_open" if args.len() == 3 => Some((
                "mako_native_gfx_window_open_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Opaque),
                true,
            )),
            "gfx_window_close" if args.len() == 1 => Some((
                "mako_native_gfx_window_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gfx_window_width" if args.len() == 1 => Some((
                "mako_native_gfx_window_width",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gfx_window_height" if args.len() == 1 => Some((
                "mako_native_gfx_window_height",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gfx_shader_compile" if args.len() == 1 => Some((
                "mako_native_gfx_shader_compile_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "audio_mix" if args.len() == 2 => Some((
                "mako_native_audio_mix",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "physics_step_x" if args.len() == 3 => Some((
                "mako_native_physics_step_x",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "physics_step_v" if args.len() == 3 => Some((
                "mako_native_physics_step_v",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ai_rope_cos" if args.len() == 1 => Some((
                "mako_native_ai_rope_cos",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "ai_rope_apply_x" if args.len() == 3 => Some((
                "mako_native_ai_rope_apply_x",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "kv_cache_new" if args.len() == 1 => Some((
                "mako_native_kv_cache_new",
                &[Type::I64],
                Some(Type::Opaque),
                true,
            )),
            "kv_cache_free" if args.len() == 1 => Some((
                "mako_native_kv_cache_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "kv_cache_append" if args.len() == 3 => Some((
                "mako_native_kv_cache_append",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "kv_cache_get_k" if args.len() == 2 => Some((
                "mako_native_kv_cache_get_k",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "kv_cache_get_v" if args.len() == 2 => Some((
                "mako_native_kv_cache_get_v",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "kv_cache_len" if args.len() == 1 => Some((
                "mako_native_kv_cache_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "f32_to_f16_bits" if args.len() == 1 => Some((
                "mako_native_f32_to_f16_bits",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_set_loc" if args.len() == 2 => Some((
                "mako_native_debug_set_loc_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_frame_json" if args.len() == 0 => Some((
                "mako_native_debug_frame_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "snap_encode4" if args.len() == 4 => Some((
                "mako_native_snap_encode4_ptr",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "argon2_available" if args.len() == 0 => Some((
                "mako_native_argon2_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "fmt_printf3" if args.len() == 4 => Some((
                "mako_native_fmt_printf3_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sst_build4" if args.len() == 9 => Some((
                "mako_native_sst_build4_ptr",
                &[
                    Type::Str, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64, Type::I64,
                ],
                Some(Type::Opaque),
                true,
            )),
            "sst_build8" if args.len() == 17 => Some((
                "mako_native_sst_build8_ptr",
                &[
                    Type::Str, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64, Type::I64, Type::I64,
                ],
                Some(Type::Opaque),
                true,
            )),
            "sst_build_n" if args.len() == 18 => Some((
                "mako_native_sst_build_n_ptr",
                &[
                    Type::Str, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64, Type::I64, Type::I64, Type::I64,
                ],
                Some(Type::Opaque),
                true,
            )),
            "gemm2x2" if args.len() == 8 => Some((
                "mako_native_gemm2x2",
                &[
                    Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64,
                    Type::I64,
                ],
                Some(Type::I64),
                false,
            )),
            "gemm_c01" if args.len() == 0 => Some((
                "mako_native_gemm_c01",
                &[],
                Some(Type::I64),
                false,
            )),
            "gemm_c10" if args.len() == 0 => Some((
                "mako_native_gemm_c10",
                &[],
                Some(Type::I64),
                false,
            )),
            "gemm_c11" if args.len() == 0 => Some((
                "mako_native_gemm_c11",
                &[],
                Some(Type::I64),
                false,
            )),
            "debug_file" if args.len() == 0 => Some((
                "mako_native_debug_file_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "debug_line" if args.len() == 0 => Some((
                "mako_native_debug_line",
                &[],
                Some(Type::I64),
                false,
            )),
            "builder_write_slice" if args.len() == 4 => Some((
                "mako_native_builder_write_slice_ptr",
                &[Type::Opaque, Type::Str, Type::I64, Type::I64],
                None,
                false,
            )),
            "str_hash64" if args.len() == 1 => Some((
                "mako_native_str_hash64_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "str_slice_ci_index" if args.len() == 4 => Some((
                "mako_native_str_slice_ci_index_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "str_slice_ci_starts" if args.len() == 3 => Some((
                "mako_native_str_slice_ci_starts_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "file_open" if args.len() == 3 => Some((
                "mako_native_file_open_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "file_close" if args.len() == 1 => Some((
                "mako_native_file_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "file_append2" if args.len() == 3 => Some((
                "mako_native_file_append2_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "file_append3" if args.len() == 4 => Some((
                "mako_native_file_append3_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "file_seek" if args.len() == 3 => Some((
                "mako_native_file_seek",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "file_read_exact" if args.len() == 2 => Some((
                "mako_native_file_read_exact_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "fdatasync" if args.len() == 1 => Some((
                "mako_native_fdatasync",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fallocate" if args.len() == 2 => Some((
                "mako_native_fallocate",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "fmt_println2" if args.len() == 2 => Some((
                "mako_native_fmt_println2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "yaml_has" if args.len() == 2 => Some((
                "mako_native_yaml_has_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "toml_has" if args.len() == 2 => Some((
                "mako_native_toml_has_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "toml_get_in" if args.len() == 3 => Some((
                "mako_native_toml_get_in_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "toml_get_int_in" if args.len() == 3 => Some((
                "mako_native_toml_get_int_in_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "toml_keys" if args.len() == 1 => Some((
                "mako_native_toml_keys_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "toml_pair" if args.len() == 2 => Some((
                "mako_native_toml_pair_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "toml_pair_int" if args.len() == 2 => Some((
                "mako_native_toml_pair_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "toml_pair_bool" if args.len() == 2 => Some((
                "mako_native_toml_pair_bool_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "toml_section" if args.len() == 1 => Some((
                "mako_native_toml_section_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "toml_merge" if args.len() == 2 => Some((
                "mako_native_toml_merge_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "uuid_v5" if args.len() == 2 => Some((
                "mako_native_uuid_v5_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "uuid_v3" if args.len() == 2 => Some((
                "mako_native_uuid_v3_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "ulid_new" if args.is_empty() => Some((
                "mako_native_ulid_new",
                &[],
                Some(Type::Str),
                true,
            )),
            "ulid_string" if args.len() == 1 => Some((
                "mako_native_ulid_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ulid_parse" if args.len() == 1 => Some((
                "mako_native_ulid_parse_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "uuid_ns_dns" if args.is_empty() => Some((
                "mako_native_uuid_ns_dns",
                &[],
                Some(Type::Str),
                true,
            )),
            "uuid_ns_url" if args.is_empty() => Some((
                "mako_native_uuid_ns_url",
                &[],
                Some(Type::Str),
                true,
            )),
            "uuid_ns_oid" if args.is_empty() => Some((
                "mako_native_uuid_ns_oid",
                &[],
                Some(Type::Str),
                true,
            )),
            "uuid_ns_x500" if args.is_empty() => Some((
                "mako_native_uuid_ns_x500",
                &[],
                Some(Type::Str),
                true,
            )),
            "chan_str_try_send" if args.len() == 2 => Some((
                "mako_native_chan_str_try_send",
                &[Type::ChanS, Type::Str],
                Some(Type::I64),
                false,
            )),
            "chan_str_try_send_take" if args.len() == 2 => Some((
                "mako_native_chan_str_try_send_take",
                &[Type::ChanS, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sql_open_postgres" if args.len() == 1 => Some((
                "mako_native_sql_open_postgres_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "buf_get" if args.len() == 1 => Some((
                "mako_native_buf_get_ptr",
                &[Type::I64],
                Some(Type::ByteSlice),
                true,
            )),
            "as_bytes" if args.len() == 1 => Some((
                "mako_native_as_bytes_ptr",
                &[Type::Str],
                Some(Type::ByteSlice),
                true,
            )),
            "bytes_view" if args.len() == 3 => Some((
                "mako_native_bytes_view_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::ByteSlice),
                true,
            )),
            "bytes_as_str" if args.len() == 1 => Some((
                "mako_native_bytes_as_str_ptr",
                &[Type::ByteSlice],
                Some(Type::Str),
                true,
            )),
            "bytes_is_view" if args.len() == 1 => Some((
                "mako_native_bytes_is_view_ptr",
                &[Type::ByteSlice],
                Some(Type::I64),
                false,
            )),
            "chan_str_send_take" if args.len() == 2 => Some((
                "mako_native_chan_str_send_take",
                &[Type::ChanS, Type::Str],
                Some(Type::I64),
                false,
            )),
            "list_new_int" if args.len() == 0 => Some((
                "mako_native_list_new_int_ptr",
                &[],
                Some(Type::IntSlice),
                true,
            )),
            "yaml_get_bool" if args.len() == 2 => Some((
                "mako_native_yaml_get_bool_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "toml_get_int" if args.len() == 2 => Some((
                "mako_native_toml_get_int_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "validate_max_len" if args.len() == 2 => Some((
                "mako_native_validate_max_len_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "utf8_max_rune" if args.len() == 0 => Some((
                "mako_native_utf8_max_rune",
                &[],
                Some(Type::I64),
                false,
            )),
            "url_path" if args.len() == 1 => Some((
                "mako_native_url_path_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "zip_deflate_available" if args.len() == 0 => Some((
                "mako_native_zip_deflate_available",
                &[],
                Some(Type::I64),
                false,
            )),
            // path / time / math / print (round 3b)
            "path_is_abs" if args.len() == 1 => Some((
                "mako_native_path_is_abs_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "time_unix" if args.len() == 0 => Some((
                "mako_native_time_unix",
                &[],
                Some(Type::I64),
                false,
            )),
            "abs" if args.len() == 1 => Some((
                "mako_native_abs",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "min" if args.len() == 2 => Some((
                "mako_native_min",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "max" if args.len() == 2 => Some((
                "mako_native_max",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "clamp" if args.len() == 3 => Some((
                "mako_native_clamp",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "math_sqrt" if args.len() == 1 => Some((
                "mako_native_math_sqrt",
                &[Type::F64],
                Some(Type::F64),
                false,
            )),
            "math_floor" if args.len() == 1 => Some((
                "mako_native_math_floor",
                &[Type::F64],
                Some(Type::F64),
                false,
            )),
            "getcwd" if args.len() == 0 => Some((
                "mako_native_getcwd_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "is_dir" if args.len() == 1 => Some((
                "mako_native_is_dir_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "eprint" if args.len() == 1 => Some((
                "mako_native_eprint_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "eprintln" if args.len() == 1 => Some((
                "mako_native_eprintln_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "mono_us" if args.len() == 0 => Some((
                "mako_native_mono_us",
                &[],
                Some(Type::I64),
                false,
            )),
            "mono_ms" if args.len() == 0 => Some((
                "mako_native_mono_ms",
                &[],
                Some(Type::I64),
                false,
            )),
            "mono_res_ns" if args.len() == 0 => Some((
                "mako_native_mono_res_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "mono_overhead_ns" if args.len() == 0 => Some((
                "mako_native_mono_overhead_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "sleep_us" if args.len() == 1 => Some((
                "mako_native_sleep_us",
                &[Type::I64],
                None,
                false,
            )),
            "elapsed_us" if args.len() == 1 => Some((
                "mako_native_elapsed_us",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "elapsed_mono_ms" if args.len() == 1 => Some((
                "mako_native_elapsed_mono_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "deadline_ms" if args.len() == 1 => Some((
                "mako_native_deadline_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "deadline_expired" if args.len() == 1 => Some((
                "mako_native_deadline_expired",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "deadline_remaining_ms" if args.len() == 1 => Some((
                "mako_native_deadline_remaining_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "deadline_remaining_ns" if args.len() == 1 => Some((
                "mako_native_deadline_remaining_ns",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "deadline_ns" if args.len() == 1 => Some((
                "mako_native_deadline_ns",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sleep_until_ns" if args.len() == 1 => Some((
                "mako_native_sleep_until_ns",
                &[Type::I64],
                None,
                false,
            )),
            "format_int_hex" if args.len() == 1 => Some((
                "mako_native_format_int_hex_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_hex_upper" if args.len() == 1 => Some((
                "mako_native_format_int_hex_upper_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_hex_prefix" if args.len() == 1 => Some((
                "mako_native_format_int_hex_prefix_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_bin" if args.len() == 1 => Some((
                "mako_native_format_int_bin_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_oct" if args.len() == 1 => Some((
                "mako_native_format_int_oct_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_dec" if args.len() == 1 => Some((
                "mako_native_format_int_dec_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_base" if args.len() == 2 => Some((
                "mako_native_format_int_base_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_int_hex_pad" if args.len() == 2 => Some((
                "mako_native_format_int_hex_pad_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "format_pad" if args.len() == 3 => Some((
                "mako_native_format_pad_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "wall_us" if args.len() == 0 => Some((
                "mako_native_wall_us",
                &[],
                Some(Type::I64),
                false,
            )),
            "wall_ns" if args.len() == 0 => Some((
                "mako_native_wall_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_format" if args.len() == 1 => Some((
                "mako_native_time_format_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "mutex_new" if args.len() == 0 => Some((
                "mako_native_mutex_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "mutex_lock" if args.len() == 1 => Some((
                "mako_native_mutex_lock",
                &[Type::Opaque],
                None,
                false,
            )),
            "mutex_unlock" if args.len() == 1 => Some((
                "mako_native_mutex_unlock",
                &[Type::Opaque],
                None,
                false,
            )),
            "spin_until_ns" if args.len() == 1 => Some((
                "mako_native_spin_until_ns",
                &[Type::I64],
                None,
                false,
            )),
            "ints_contains" if args.len() == 2 => Some((
                "mako_native_ints_contains_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ints_index" if args.len() == 2 => Some((
                "mako_native_ints_index_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ints_copy" if args.len() == 1 => Some((
                "mako_native_ints_copy_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "strings_contains" if args.len() == 2 => Some((
                "mako_native_strings_contains_ptr",
                &[Type::StrSlice, Type::Str],
                Some(Type::I64),
                false,
            )),
            // ---- simple stdlib / goext bridges (native_bridge.c) ----
            "fmt_eprintf" if args.len() == 2 => Some((
                "mako_native_fmt_eprintf_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "is_file" if args.len() == 1 => Some((
                "mako_native_is_file_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "session_id_new" if args.len() == 0 => Some((
                "mako_native_session_id_new_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "png_available" if args.len() == 0 => Some((
                "mako_native_png_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "binary_put_u32be" if args.len() == 1 => Some((
                "mako_native_binary_put_u32be_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "binary_put_u16be" if args.len() == 1 => Some((
                "mako_native_binary_put_u16be_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "bcrypt_hash" if args.len() == 2 => Some((
                "mako_native_bcrypt_hash_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "bcrypt_verify" if args.len() == 2 => Some((
                "mako_native_bcrypt_verify_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "bcrypt_available" if args.len() == 0 => Some((
                "mako_native_bcrypt_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "str_as_view" if args.len() == 1 => Some((
                // Non-owning view: return the same header pointer, ret_owned=false.
                "mako_native_str_as_view_ptr",
                &[Type::Str],
                Some(Type::Str),
                false,
            )),
            "str_to_owned" if args.len() == 1 => Some((
                // View → owned string (clone).
                "mako_native_string_clone_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_is_loopback" if args.len() == 1 => Some((
                "mako_native_dns_is_loopback_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hmac_sha256_raw" if args.len() == 2 => Some((
                "mako_native_hmac_sha256_raw_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "csrf_token" if args.len() == 0 => Some((
                "mako_native_csrf_token_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "rwmutex_new" if args.len() == 0 => Some((
                "mako_native_rwmutex_new",
                &[],
                Some(Type::Opaque),
                true,
            )),
            "rwmutex_rlock" if args.len() == 1 => Some((
                "mako_native_rwmutex_rlock",
                &[Type::Opaque],
                None,
                false,
            )),
            "rwmutex_runlock" if args.len() == 1 => Some((
                "mako_native_rwmutex_runlock",
                &[Type::Opaque],
                None,
                false,
            )),
            "rwmutex_lock" if args.len() == 1 => Some((
                "mako_native_rwmutex_lock",
                &[Type::Opaque],
                None,
                false,
            )),
            "rwmutex_unlock" if args.len() == 1 => Some((
                "mako_native_rwmutex_unlock",
                &[Type::Opaque],
                None,
                false,
            )),
            "list_push_int" if args.len() == 2 => Some((
                "mako_native_list_push_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "list_pop_int" if args.len() == 1 => Some((
                "mako_native_list_pop_int_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "list_popped_int" if args.len() == 0 => Some((
                "mako_native_list_popped_int",
                &[],
                Some(Type::I64),
                false,
            )),
            "list_get_int" if args.len() == 2 => Some((
                "mako_native_list_get_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_len_int" if args.len() == 1 => Some((
                "mako_native_list_len_int_ptr",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "list_clear_int" if args.len() == 1 => Some((
                "mako_native_list_clear_int_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "list_insert_int" if args.len() == 3 => Some((
                "mako_native_list_insert_int_ptr",
                &[Type::IntSlice, Type::I64, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "list_remove_int" if args.len() == 2 => Some((
                "mako_native_list_remove_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "list_sum_int" if args.len() == 1 => Some((
                "mako_native_list_sum_int_ptr",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "stack_peek_int" if args.len() == 1 => Some((
                "mako_native_stack_peek_int_ptr",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "strings_copy" if args.len() == 1 => Some((
                "mako_native_strings_copy_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "list_new_str" if args.len() == 0 => Some((
                "mako_native_list_new_str_ptr",
                &[],
                Some(Type::StrSlice),
                true,
            )),
            "list_push_str" if args.len() == 2 => Some((
                "mako_native_list_push_str_ptr",
                &[Type::StrSlice, Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "list_pop_str" if args.len() == 1 => Some((
                "mako_native_list_pop_str_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "list_popped_str" if args.len() == 0 => Some((
                "mako_native_list_popped_str_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "list_get_str" if args.len() == 2 => Some((
                "mako_native_list_get_str_ptr",
                &[Type::StrSlice, Type::I64],
                Some(Type::Str),
                true,
            )),
            "list_len_str" if args.len() == 1 => Some((
                "mako_native_list_len_str_ptr",
                &[Type::StrSlice],
                Some(Type::I64),
                false,
            )),
            "list_clear_str" if args.len() == 1 => Some((
                "mako_native_list_clear_str_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "list_insert_str" if args.len() == 3 => Some((
                "mako_native_list_insert_str_ptr",
                &[Type::StrSlice, Type::I64, Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "list_remove_str" if args.len() == 2 => Some((
                "mako_native_list_remove_str_ptr",
                &[Type::StrSlice, Type::I64],
                Some(Type::StrSlice),
                true,
            )),
            "strings_index" if args.len() == 2 => Some((
                "mako_native_strings_index_ptr",
                &[Type::StrSlice, Type::Str],
                Some(Type::I64),
                false,
            )),
            "queue_pop_int" if args.len() == 1 => Some((
                "mako_native_queue_pop_int_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "queue_popped_int" if args.len() == 0 => Some((
                "mako_native_queue_popped_int",
                &[],
                Some(Type::I64),
                false,
            )),
            "slices_reverse_strs" if args.len() == 1 => Some((
                "mako_native_slices_reverse_strs_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "slices_unique_strs" if args.len() == 1 => Some((
                "mako_native_slices_unique_strs_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "slices_reverse_ints" if args.len() == 1 => Some((
                "mako_native_slices_reverse_ints_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "slices_unique_ints" if args.len() == 1 => Some((
                "mako_native_slices_unique_ints_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            // Alias: slices_reverse == slices_reverse_ints (Go-style name).
            "slices_reverse" if args.len() == 1 => Some((
                "mako_native_slices_reverse_ints_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "sha256_raw" if args.len() == 1 => Some((
                "mako_native_sha256_raw_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "list_min_int" if args.len() == 1 => Some((
                "mako_native_list_min_int_ptr",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "list_max_int" if args.len() == 1 => Some((
                "mako_native_list_max_int_ptr",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "str_slice_contains" if args.len() == 4 => Some((
                "mako_native_str_slice_contains_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "wait_group_new" if args.len() == 0 => Some((
                "mako_native_wait_group_new",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "wait_group_add" if args.len() == 2 => Some((
                "mako_native_wait_group_add",
                &[Type::Opaque, Type::I64],
                None,
                false,
            )),
            "wait_group_done" if args.len() == 1 => Some((
                "mako_native_wait_group_done",
                &[Type::Opaque],
                None,
                false,
            )),
            "wait_group_wait" if args.len() == 1 => Some((
                "mako_native_wait_group_wait",
                &[Type::Opaque],
                None,
                false,
            )),
            "atomic_new" if args.len() == 1 => Some((
                "mako_native_atomic_new",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "atomic_add" if args.len() == 2 => Some((
                "mako_native_atomic_add",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "atomic_load" if args.len() == 1 => Some((
                "mako_native_atomic_load",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "atomic_store" if args.len() == 2 => Some((
                "mako_native_atomic_store",
                &[Type::Opaque, Type::I64],
                None,
                false,
            )),
            "atomic_cas" if args.len() == 3 => Some((
                "mako_native_atomic_cas",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "map_si_set_take" if args.len() == 3 => Some((
                "mako_native_map_si_set_take_ptr",
                &[Type::MapSI, Type::Str, Type::I64],
                None,
                false,
            )),
            "syscall_getpid" if args.len() == 0 => Some((
                "mako_native_syscall_getpid",
                &[],
                Some(Type::I64),
                false,
            )),
            "path_size" if args.len() == 1 => Some((
                "mako_native_path_size_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "url_query" if args.len() == 1 => Some((
                "mako_native_url_query_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ring_len" if args.len() == 1 => Some((
                "mako_native_ring_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "signal_fired" if args.len() == 1 => Some((
                "mako_native_signal_fired_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "checked_mul" if args.len() == 2 => Some((
                "mako_native_checked_mul",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "watch_new" if args.len() == 0 => Some((
                "mako_native_watch_new",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "zip_create" if args.len() == 0 => Some((
                "mako_native_zip_create",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "buf_reader_from_string" if args.len() == 1 => Some((
                "mako_native_buf_reader_from_string_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "limits_new" if args.len() == 3 => Some((
                "mako_native_limits_new",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "http_header_ok" if args.len() == 2 => Some((
                "mako_native_http_header_ok_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mime_type" if args.len() == 1 => Some((
                "mako_native_mime_type_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "watch_add" if args.len() == 2 => Some((
                "mako_native_watch_add_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "zip_add" if args.len() == 3 => Some((
                "mako_native_zip_add_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "buf_read_line" if args.len() == 1 => Some((
                "mako_native_buf_read_line_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "limits_try_mem" if args.len() == 2 => Some((
                "mako_native_limits_try_mem",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tar_write_file" if args.len() == 3 => Some((
                "mako_native_tar_write_file_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "secret_from_str" if args.len() == 1 => Some((
                "mako_native_secret_from_str_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "secret_len" if args.len() == 1 => Some((
                "mako_native_secret_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "secret_eq_str" if args.len() == 2 => Some((
                "mako_native_secret_eq_str_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_value_new" if args.len() == 1 => Some((
                "mako_native_reflect_value_new_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "reflect_value_get" if args.len() == 2 => Some((
                "mako_native_reflect_value_get_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_value_set" if args.len() == 3 => Some((
                "mako_native_reflect_value_set_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_value_num_fields" if args.len() == 1 => Some((
                "mako_native_reflect_value_num_fields",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_begin" if args.len() == 1 => Some((
                "mako_native_sql_begin",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_last_insert_id" if args.len() == 1 => Some((
                "mako_native_sql_last_insert_id",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_query_int" if args.len() == 3 => Some((
                "mako_native_sql_query_int_ptr",
                &[Type::Opaque, Type::Str, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "sql_commit" if args.len() == 1 => Some((
                "mako_native_sql_commit",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_exec" if args.len() == 3 => Some((
                "mako_native_sql_exec_ptr",
                &[Type::Opaque, Type::Str, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "sql_rows_affected" if args.len() == 1 => Some((
                "mako_native_sql_rows_affected",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_migration_applied" if args.len() == 2 => Some((
                "mako_native_sql_migration_applied",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_query_str2" if args.len() == 4 => Some((
                "mako_native_sql_query_str2_ptr",
                &[Type::Opaque, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sql_query_rows" if args.len() == 3 => Some((
                "mako_native_sql_query_rows_ptr",
                &[Type::Opaque, Type::Str, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "hmac_sha1" if args.len() == 2 => Some((
                "mako_native_hmac_sha1_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "hmac_sha1_raw" if args.len() == 2 => Some((
                "mako_native_hmac_sha1_raw_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "unicode_is_upper" if args.len() == 1 => Some((
                "mako_native_unicode_is_upper",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_before" if args.len() == 2 => Some((
                "mako_native_time_before",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "syscall_errno" if args.len() == 0 => Some((
                "mako_native_syscall_errno",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_accept" if args.len() == 1 => Some((
                "mako_native_ws_accept",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "queue_pop_str" if args.len() == 1 => Some((
                "mako_native_queue_pop_str_ptr",
                &[Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "secret_drop" if args.len() == 1 => Some((
                "mako_native_secret_drop",
                &[Type::Opaque],
                None,
                false,
            )),
            "reqctx_get" if args.len() == 2 => Some((
                "mako_native_reqctx_get_value_ptr",
                &[Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "reqctx_has" if args.len() == 2 => Some((
                "mako_native_reqctx_get_value_exists",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_value_of_type" if args.len() == 1 => Some((
                "mako_native_reflect_value_of_type_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "reflect_value_clone" if args.len() == 1 => Some((
                "mako_native_reflect_value_clone",
                &[Type::Opaque],
                Some(Type::Opaque),
                false,
            )),
            "reflect_value_set_at" if args.len() == 3 => Some((
                "mako_native_reflect_value_set_at_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_request_parse" if args.len() == 1 => Some((
                "mako_native_http_request_parse_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "http_request_method" if args.len() == 1 => Some((
                "mako_native_http_request_method_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_request_path" if args.len() == 1 => Some((
                "mako_native_http_request_path_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_request_body" if args.len() == 1 => Some((
                "mako_native_http_request_body_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "slices_unique" if args.len() == 1 => Some((
                "mako_native_slices_unique_ints_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "list_concat_int" if args.len() == 2 => Some((
                "mako_native_list_concat_int_ptr",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "xor_bytes" if args.len() == 2 => Some((
                "mako_native_xor_bytes_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "str_slice_index" if args.len() == 4 => Some((
                "mako_native_str_slice_index_ptr",
                &[Type::Str, Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "map_ss_set_take" if args.len() == 3 => Some((
                "mako_native_map_ss_set_take_ptr",
                &[Type::MapSS, Type::Str, Type::Str],
                None,
                false,
            )),
            "url_query_escape" if args.len() == 1 => Some((
                "mako_native_url_query_escape_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "aes_ctr" if args.len() == 3 => Some((
                "mako_native_aes_ctr_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "aes_gcm_seal" if args.len() == 4 => Some((
                "mako_native_aes_gcm_seal_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "auth_basic_header" if args.len() == 2 => Some((
                "mako_native_auth_basic_header_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "avro_long_len" if args.len() == 1 => Some((
                "mako_native_avro_long_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_u32be" if args.len() == 1 => Some((
                "mako_native_binary_u32be_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "cache_put" if args.len() == 3 => Some((
                "mako_native_cache_put_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "cassandra_connect_url" if args.len() == 1 => Some((
                "mako_native_cassandra_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "chan_str_select2" if args.len() == 3 => Some((
                "mako_native_chan_str_select2",
                &[Type::Opaque, Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "csrf_check" if args.len() == 2 => Some((
                "mako_native_csrf_check_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "csv_split_line" if args.len() == 1 => Some((
                "mako_native_csv_split_line_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "dap_threads_response" if args.len() == 1 => Some((
                "mako_native_dap_threads_response_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "dns_is_private" if args.len() == 1 => Some((
                "mako_native_dns_is_private_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "ecs_alive" if args.len() == 2 => Some((
                "mako_native_ecs_alive",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "file_mtime" if args.len() == 1 => Some((
                "mako_native_file_mtime_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "filepath_walk" if args.len() == 1 => Some((
                "mako_native_filepath_walk_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "frame_cap" if args.len() == 1 => Some((
                "mako_native_frame_cap",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fsm_transition" if args.len() == 3 => Some((
                "mako_native_fsm_transition_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "fx_mul" if args.len() == 3 => Some((
                "mako_native_fx_mul",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_alpha" if args.len() == 3 => Some((
                "mako_native_game_alpha",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_udp_recv" if args.len() == 1 => Some((
                "mako_native_game_udp_recv_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "gif_encode_rgb" if args.len() == 3 => Some((
                "mako_native_gif_encode_rgb_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_device_vendor" if args.len() == 1 => Some((
                "mako_native_gpu_device_vendor_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "gpu_silu_f32" if args.len() == 2 => Some((
                "mako_native_gpu_silu_f32",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_is_mutation" if args.len() == 1 => Some((
                "mako_native_graphql_is_mutation_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_message_payload" if args.len() == 1 => Some((
                "mako_native_grpc_message_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hpack_static_name" if args.len() == 1 => Some((
                "mako_native_hpack_static_name_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_post_timeout" if args.len() == 3 => Some((
                "mako_native_http_post_timeout_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_ready" if args.len() == 0 => Some((
                "mako_native_http_shutdown_ready",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_concat_frames" if args.len() == 2 => Some((
                "mako_native_http2_concat_frames_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_frame_payload" if args.len() == 1 => Some((
                "mako_native_http2_frame_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_is_ping" if args.len() == 1 => Some((
                "mako_native_http2_is_ping_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_settings_ack" if args.len() == 0 => Some((
                "mako_native_http2_settings_ack_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "http2_window_blocked" if args.len() == 1 => Some((
                "mako_native_http2_window_blocked",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "job_delay_ms" if args.len() == 1 => Some((
                "mako_native_job_delay_ms_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_app7_length" if args.len() == 1 => Some((
                "mako_native_jpeg_app7_length_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_app8_length" if args.len() == 1 => Some((
                "mako_native_jpeg_app8_length_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_decode_gray" if args.len() == 1 => Some((
                "mako_native_jpeg_decode_gray_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_has_app9" if args.len() == 1 => Some((
                "mako_native_jpeg_has_app9_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_mako_complete" if args.len() == 1 => Some((
                "mako_native_jpeg_is_mako_complete_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_minor" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_minor_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_thumb_width" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_thumb_width_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_x_density" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_x_density_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_matches_app7" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_matches_app7_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_width" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_width_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jwt_verify" if args.len() == 2 => Some((
                "mako_native_jwt_verify_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "lb_pick2" if args.len() == 3 => Some((
                "mako_native_lb_pick2_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "leak_bytes_since" if args.len() == 1 => Some((
                "mako_native_leak_bytes_since",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "leak_scope_enter" if args.len() == 0 => Some((
                "mako_native_leak_scope_enter",
                &[],
                Some(Type::I64),
                false,
            )),
            "list_eq_int" if args.len() == 2 => Some((
                "mako_native_list_eq_int",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "llm_chat_body" if args.len() == 3 => Some((
                "mako_native_llm_chat_body_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mail_simple" if args.len() == 4 => Some((
                "mako_native_mail_simple_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "model_tensor_elems" if args.len() == 2 => Some((
                "mako_native_model_tensor_elems_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "msgpack_int_hex" if args.len() == 1 => Some((
                "mako_native_msgpack_int_hex_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "msgpack_is_nil" if args.len() == 1 => Some((
                "mako_native_msgpack_is_nil_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "multipart_file_name" if args.len() == 3 => Some((
                "mako_native_multipart_file_name_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_encode_field_varint" if args.len() == 2 => Some((
                "mako_native_pb_encode_field_varint_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "pb_encode_repeated_varint" if args.len() == 3 => Some((
                "mako_native_pb_encode_repeated_varint_ptr",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "pb_encode_sint" if args.len() == 1 => Some((
                "mako_native_pb_encode_sint_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "plugin_alive" if args.len() == 1 => Some((
                "mako_native_plugin_alive",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_api_version" if args.len() == 0 => Some((
                "mako_native_plugin_api_version_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "png_encode_gray" if args.len() == 3 => Some((
                "mako_native_png_encode_gray_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "profile_sample_once" if args.len() == 1 => Some((
                "mako_native_profile_sample_once_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_initial_client_secret_hex" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_secret_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_get" if args.len() == 3 => Some((
                "mako_native_redis_get_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_struct_field_name" if args.len() == 2 => Some((
                "mako_native_reflect_struct_field_name_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "reqctx_has" if args.len() == 2 => Some((
                "mako_native_reqctx_has_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ring_pop" if args.len() == 1 => Some((
                "mako_native_ring_pop",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "router_add" if args.len() == 4 => Some((
                "mako_native_router_add_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "rpc_frame" if args.len() == 2 => Some((
                "mako_native_rpc_frame_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_digest_response_ha1" if args.len() == 4 => Some((
                "mako_native_sip_digest_response_ha1_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_is_response" if args.len() == 1 => Some((
                "mako_native_sip_is_response_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sock_error" if args.len() == 1 => Some((
                "mako_native_sock_error",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "str_at_eq" if args.len() == 3 => Some((
                "mako_native_str_at_eq_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_getppid" if args.len() == 0 => Some((
                "mako_native_syscall_getppid",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_connect_timeout" if args.len() == 3 => Some((
                "mako_native_tcp_connect_timeout_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_pool_open_count" if args.len() == 1 => Some((
                "mako_native_tcp_pool_open_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_set_timeout" if args.len() == 2 => Some((
                "mako_native_tcp_set_timeout",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_year" if args.len() == 1 => Some((
                "mako_native_time_year",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_client_hello_random" if args.len() == 1 => Some((
                "mako_native_tls_client_hello_random_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_conn_fd" if args.len() == 1 => Some((
                "mako_native_tls_conn_fd",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tls_post" if args.len() == 5 => Some((
                "mako_native_tls_post_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_appdata_seal" if args.len() == 3 => Some((
                "mako_native_tls_record_appdata_seal_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_server_free" if args.len() == 1 => Some((
                "mako_native_tls_server_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tmpl_free" if args.len() == 1 => Some((
                "mako_native_tmpl_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "trace_begin" if args.len() == 1 => Some((
                "mako_native_trace_begin_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "udp_send_to" if args.len() == 4 => Some((
                "mako_native_udp_send_to_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "unix_socket_pair" if args.len() == 0 => Some((
                "mako_native_unix_socket_pair",
                &[],
                Some(Type::I64),
                false,
            )),
            "utf8_utf_max" if args.len() == 0 => Some((
                "mako_native_utf8_utf_max",
                &[],
                Some(Type::I64),
                false,
            )),
            "validate_int_range" if args.len() == 3 => Some((
                "mako_native_validate_int_range",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ws_upgrade_request_ok" if args.len() == 1 => Some((
                "mako_native_ws_upgrade_request_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "yaml_pair" if args.len() == 2 => Some((
                "mako_native_yaml_pair_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),

            "aes_gcm_open" if args.len() == 4 => Some((
                "mako_native_aes_gcm_open_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "atomic_write_file" if args.len() == 2 => Some((
                "mako_native_atomic_write_file_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "auth_check_basic" if args.len() == 3 => Some((
                "mako_native_auth_check_basic_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "avro_decode_bool" if args.len() == 1 => Some((
                "mako_native_avro_decode_bool_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_put_u64be" if args.len() == 1 => Some((
                "mako_native_binary_put_u64be_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "cache_has" if args.len() == 1 => Some((
                "mako_native_cache_has_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "cassandra_select" if args.len() == 3 => Some((
                "mako_native_cassandra_select_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "cbor_int_hex" if args.len() == 1 => Some((
                "mako_native_cbor_int_hex_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "dap_request_command" if args.len() == 1 => Some((
                "mako_native_dap_request_command_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_normalize_host" if args.len() == 1 => Some((
                "mako_native_dns_normalize_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_add" if args.len() == 4 => Some((
                "mako_native_ecs_add",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "filepath_walk_n" if args.len() == 2 => Some((
                "mako_native_filepath_walk_n_ptr",
                &[Type::Str, Type::I64],
                Some(Type::StrSlice),
                true,
            )),
            "frame_used" if args.len() == 1 => Some((
                "mako_native_frame_used",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fsm_is" if args.len() == 2 => Some((
                "mako_native_fsm_is_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "fx_div" if args.len() == 3 => Some((
                "mako_native_fx_div",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_frame_budget_ok" if args.len() == 2 => Some((
                "mako_native_game_frame_budget_ok",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gif_encode_rgb_lzw" if args.len() == 3 => Some((
                "mako_native_gif_encode_rgb_lzw_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "gif_width" if args.len() == 1 => Some((
                "mako_native_gif_width_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gpu_device_is_gpu" if args.len() == 1 => Some((
                "mako_native_gpu_device_is_gpu",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_layernorm_f32" if args.len() == 7 => Some((
                "mako_native_gpu_layernorm_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::F64],
                Some(Type::I64),
                false,
            )),
            "graphql_data" if args.len() == 2 => Some((
                "mako_native_graphql_data_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_unary_request" if args.len() == 2 => Some((
                "mako_native_grpc_unary_request_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "hpack_static_value" if args.len() == 1 => Some((
                "mako_native_hpack_static_value_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_remaining" if args.len() == 0 => Some((
                "mako_native_http_shutdown_remaining",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_frame_at" if args.len() == 2 => Some((
                "mako_native_http2_frame_at_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_frame_stream" if args.len() == 1 => Some((
                "mako_native_http2_frame_stream_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_header_block" if args.len() == 2 => Some((
                "mako_native_http2_header_block_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_is_settings_ack" if args.len() == 1 => Some((
                "mako_native_http2_is_settings_ack_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_stream_apply" if args.len() == 1 => Some((
                "mako_native_http2_stream_apply_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "https_get" if args.len() == 3 => Some((
                "mako_native_https_get_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "io_set_nonblocking" if args.len() == 2 => Some((
                "mako_native_io_set_nonblocking",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "job_cancel" if args.len() == 1 => Some((
                "mako_native_job_cancel_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_app7_payload_len" if args.len() == 1 => Some((
                "mako_native_jpeg_app7_payload_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_app9_length" if args.len() == 1 => Some((
                "mako_native_jpeg_app9_length_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_mako_dct" if args.len() == 1 => Some((
                "mako_native_jpeg_is_mako_dct_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_thumb_height" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_thumb_height_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_y_density" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_y_density_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_height" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_height_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_sampling" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_sampling_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_width" if args.len() == 1 => Some((
                "mako_native_jpeg_width_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "lb_pick3" if args.len() == 4 => Some((
                "mako_native_lb_pick3_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "leak_detected" if args.len() == 1 => Some((
                "mako_native_leak_detected",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "leak_scope_exit" if args.len() == 0 => Some((
                "mako_native_leak_scope_exit",
                &[],
                Some(Type::I64),
                false,
            )),
            "list_fill_int" if args.len() == 2 => Some((
                "mako_native_list_fill_int_ptr",
                &[Type::I64, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_body_with_tools" if args.len() == 2 => Some((
                "mako_native_llm_body_with_tools_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "mail_header_get" if args.len() == 2 => Some((
                "mako_native_mail_header_get_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "model_tensor_ndim" if args.len() == 2 => Some((
                "mako_native_model_tensor_ndim_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "msgpack_encode_nil" if args.len() == 0 => Some((
                "mako_native_msgpack_encode_nil_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "multipart_file_content_type" if args.len() == 3 => Some((
                "mako_native_multipart_file_content_type_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_decode_sint" if args.len() == 1 => Some((
                "mako_native_pb_decode_sint_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pb_repeated_count" if args.len() == 2 => Some((
                "mako_native_pb_repeated_count_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_count" if args.len() == 0 => Some((
                "mako_native_plugin_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "plugin_name" if args.len() == 1 => Some((
                "mako_native_plugin_name_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "png_width" if args.len() == 1 => Some((
                "mako_native_png_width_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "profile_sample_len" if args.len() == 0 => Some((
                "mako_native_profile_sample_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_initial_client_key_hex" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_key_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_exists" if args.len() == 3 => Some((
                "mako_native_redis_exists_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "ring_push" if args.len() == 2 => Some((
                "mako_native_ring_push",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "router_group" if args.len() == 2 => Some((
                "mako_native_router_group_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "rpc_method" if args.len() == 1 => Some((
                "mako_native_rpc_method_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_version" if args.len() == 1 => Some((
                "mako_native_sip_version_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_www_authenticate" if args.len() == 2 => Some((
                "mako_native_sip_www_authenticate_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_debug" if args.len() == 1 => Some((
                "mako_native_slog_debug_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "str_byte_at" if args.len() == 2 => Some((
                "mako_native_str_byte_at_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "syscall_getuid" if args.len() == 0 => Some((
                "mako_native_syscall_getuid",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_pool_acquire" if args.len() == 1 => Some((
                "mako_native_tcp_pool_acquire",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_read" if args.len() == 1 => Some((
                "mako_native_tcp_read_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "tcp_write_all" if args.len() == 2 => Some((
                "mako_native_tcp_write_all_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "time_month" if args.len() == 1 => Some((
                "mako_native_time_month",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_client_hello_has_aes128_gcm" if args.len() == 1 => Some((
                "mako_native_tls_client_hello_has_aes128_gcm_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_record_type" if args.len() == 1 => Some((
                "mako_native_tls_record_type_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tmpl_html_execute" if args.len() == 2 => Some((
                "mako_native_tmpl_html_execute_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "trace_span_id" if args.len() == 0 => Some((
                "mako_native_trace_span_id_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "udp_recv" if args.len() == 2 => Some((
                "mako_native_udp_recv_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "unix_socket_pair_peer" if args.len() == 0 => Some((
                "mako_native_unix_socket_pair_peer",
                &[],
                Some(Type::I64),
                false,
            )),
            "utf8_valid" if args.len() == 1 => Some((
                "mako_native_utf8_valid_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "validate_email" if args.len() == 1 => Some((
                "mako_native_validate_email_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_accept_ok" if args.len() == 2 => Some((
                "mako_native_ws_client_accept_ok_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "yaml_pair_int" if args.len() == 2 => Some((
                "mako_native_yaml_pair_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),

            "auth_role_has" if args.len() == 2 => Some((
                "mako_native_auth_role_has_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "avro_encode_bool" if args.len() == 1 => Some((
                "mako_native_avro_encode_bool_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "avro_long_hex" if args.len() == 1 => Some((
                "mako_native_avro_long_hex_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "binary_u64be" if args.len() == 1 => Some((
                "mako_native_binary_u64be_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "cache_get" if args.len() == 1 => Some((
                "mako_native_cache_get_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "clickhouse_connect_url" if args.len() == 1 => Some((
                "mako_native_clickhouse_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dap_handle_request" if args.len() == 1 => Some((
                "mako_native_dap_handle_request_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_join_host_port" if args.len() == 2 => Some((
                "mako_native_dns_join_host_port_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "ecs_has" if args.len() == 3 => Some((
                "mako_native_ecs_has",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ffi_abi_name" if args.len() == 0 => Some((
                "mako_native_ffi_abi_name_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "fsm_can" if args.len() == 3 => Some((
                "mako_native_fsm_can_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "gif_decode_rgb_lzw" if args.len() == 1 => Some((
                "mako_native_gif_decode_rgb_lzw_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gif_height" if args.len() == 1 => Some((
                "mako_native_gif_height_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gpu_attention_f32" if args.len() == 6 => Some((
                "mako_native_gpu_attention_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_buf_cap" if args.len() == 1 => Some((
                "mako_native_gpu_buf_cap",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_error" if args.len() == 1 => Some((
                "mako_native_graphql_error_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_content_type" if args.len() == 0 => Some((
                "mako_native_grpc_content_type_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "hpack_literal_name" if args.len() == 1 => Some((
                "mako_native_hpack_literal_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_expired" if args.len() == 0 => Some((
                "mako_native_http_shutdown_expired",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_initial_window" if args.len() == 0 => Some((
                "mako_native_http2_conn_initial_window",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_frame_len" if args.len() == 1 => Some((
                "mako_native_http2_frame_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_server_preface" if args.len() == 0 => Some((
                "mako_native_http2_server_preface_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "http2_window_of" if args.len() == 1 => Some((
                "mako_native_http2_window_of",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "https_last_header" if args.len() == 1 => Some((
                "mako_native_https_last_header_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "io_read_ready" if args.len() == 2 => Some((
                "mako_native_io_read_ready",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "jpeg_app7_len_matches_payload" if args.len() == 1 => Some((
                "mako_native_jpeg_app7_len_matches_payload_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_encode_gray_dct" if args.len() == 3 => Some((
                "mako_native_jpeg_encode_gray_dct_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_height" if args.len() == 1 => Some((
                "mako_native_jpeg_height_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_mako_huff" if args.len() == 1 => Some((
                "mako_native_jpeg_is_mako_huff_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_is_mako_jfif" if args.len() == 1 => Some((
                "mako_native_jpeg_is_mako_jfif_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_sof0_component_id" if args.len() == 1 => Some((
                "mako_native_jpeg_sof0_component_id_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "leak_assert_clear" if args.len() == 1 => Some((
                "mako_native_leak_assert_clear",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "leak_check" if args.len() == 0 => Some((
                "mako_native_leak_check",
                &[],
                Some(Type::I64),
                false,
            )),
            "leak_assert_scope" if args.len() == 0 => Some((
                "mako_native_leak_assert_scope",
                &[],
                Some(Type::I64),
                false,
            )),
            "list_range_int" if args.len() == 2 => Some((
                "mako_native_list_range_int_ptr",
                &[Type::I64, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_finish_reason" if args.len() == 1 => Some((
                "mako_native_llm_finish_reason_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "mail_msg_add_bcc" if args.len() == 2 => Some((
                "mako_native_mail_msg_add_bcc_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "model_tensor_dim" if args.len() == 3 => Some((
                "mako_native_model_tensor_dim_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "msgpack_decode_bool" if args.len() == 1 => Some((
                "mako_native_msgpack_decode_bool_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "multipart_file_value" if args.len() == 3 => Some((
                "mako_native_multipart_file_value_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pb_repeated_at" if args.len() == 3 => Some((
                "mako_native_pb_repeated_at_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_version" if args.len() == 1 => Some((
                "mako_native_plugin_version_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "png_height" if args.len() == 1 => Some((
                "mako_native_png_height_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "profile_samples_json" if args.len() == 0 => Some((
                "mako_native_profile_samples_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "quic_initial_client_iv_hex" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_iv_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_del" if args.len() == 3 => Some((
                "mako_native_redis_del_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "rename" if args.len() == 2 => Some((
                "mako_native_rename_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ring_peek" if args.len() == 1 => Some((
                "mako_native_ring_peek",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "rpc_payload" if args.len() == 1 => Some((
                "mako_native_rpc_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "seal_at_rest" if args.len() == 3 => Some((
                "mako_native_seal_at_rest_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_method_is" if args.len() == 2 => Some((
                "mako_native_sip_method_is_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_proxy_authenticate" if args.len() == 2 => Some((
                "mako_native_sip_proxy_authenticate_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_info" if args.len() == 1 => Some((
                "mako_native_slog_info_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "syscall_geteuid" if args.len() == 0 => Some((
                "mako_native_syscall_geteuid",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_pool_release" if args.len() == 3 => Some((
                "mako_native_tcp_pool_release",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_read_n" if args.len() == 2 => Some((
                "mako_native_tcp_read_n_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "template_execute" if args.len() == 3 => Some((
                "mako_native_template_execute_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "time_day" if args.len() == 1 => Some((
                "mako_native_time_day",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_record_version" if args.len() == 1 => Some((
                "mako_native_tls_record_version_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_server_hello" if args.len() == 1 => Some((
                "mako_native_tls_server_hello_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "trace_end" if args.len() == 0 => Some((
                "mako_native_trace_end",
                &[],
                Some(Type::I64),
                false,
            )),
            "udp_close" if args.len() == 1 => Some((
                "mako_native_udp_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "utf8_rune_len" if args.len() == 1 => Some((
                "mako_native_utf8_rune_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "yaml_pair_bool" if args.len() == 2 => Some((
                "mako_native_yaml_pair_bool_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),

            "alloc_track_alloc" if args.len() == 1 => Some((
                "mako_native_alloc_track_alloc",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "authz_allow_role" if args.len() == 2 => Some((
                "mako_native_authz_allow_role_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "avro_encode_string" if args.len() == 1 => Some((
                "mako_native_avro_encode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "clickhouse_select" if args.len() == 3 => Some((
                "mako_native_clickhouse_select_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "copy_file" if args.len() == 2 => Some((
                "mako_native_copy_file_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "dap_request_seq" if args.len() == 1 => Some((
                "mako_native_dap_request_seq_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "dns_split_host" if args.len() == 1 => Some((
                "mako_native_dns_split_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_get" if args.len() == 3 => Some((
                "mako_native_ecs_get",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gif_decode_rgb" if args.len() == 1 => Some((
                "mako_native_gif_decode_rgb_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_buf_len" if args.len() == 1 => Some((
                "mako_native_gpu_buf_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_request" if args.len() == 1 => Some((
                "mako_native_graphql_request_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_status_trailer" if args.len() == 1 => Some((
                "mako_native_grpc_status_trailer_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "hpack_literal_value" if args.len() == 1 => Some((
                "mako_native_hpack_literal_value_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "html_template_execute" if args.len() == 3 => Some((
                "mako_native_html_template_execute_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "html_template_nested" if args.len() == 5 => Some((
                "mako_native_html_template_nested_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "html_template_range" if args.len() == 3 => Some((
                "mako_native_html_template_range_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_active_connections" if args.len() == 0 => Some((
                "mako_native_http_active_connections",
                &[],
                Some(Type::I64),
                false,
            )),
            "http_compress_if_accepted" if args.len() == 2 => Some((
                "mako_native_http_compress_if_accepted_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_conn_max_frame_size" if args.len() == 0 => Some((
                "mako_native_http2_conn_max_frame_size",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_preface_received" if args.len() == 0 => Some((
                "mako_native_http2_conn_preface_received",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_frame_flags" if args.len() == 1 => Some((
                "mako_native_http2_frame_flags_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_goaway_last_stream" if args.len() == 1 => Some((
                "mako_native_http2_goaway_last_stream_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_window_consume" if args.len() == 2 => Some((
                "mako_native_http2_window_consume",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_write_ready" if args.len() == 2 => Some((
                "mako_native_io_write_ready",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "jpeg_has_app7" if args.len() == 1 => Some((
                "mako_native_jpeg_has_app7_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "jpeg_jfif_app0_length" if args.len() == 1 => Some((
                "mako_native_jpeg_jfif_app0_length_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "lfq_new" if args.len() == 1 => Some((
                "mako_native_lfq_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_binary_search_int" if args.len() == 2 => Some((
                "mako_native_list_binary_search_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_usage_total_tokens" if args.len() == 1 => Some((
                "mako_native_llm_usage_total_tokens_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_envelope_from" if args.len() == 1 => Some((
                "mako_native_mail_msg_envelope_from_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "model_load" if args.len() == 2 => Some((
                "mako_native_model_load_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "model_load_gguf" if args.len() == 2 => Some((
                "mako_native_model_load_gguf_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "msgpack_encode_bool" if args.len() == 1 => Some((
                "mako_native_msgpack_encode_bool_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "multipart_file_size" if args.len() == 3 => Some((
                "mako_native_multipart_file_size_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "oidc_discovery" if args.len() == 3 => Some((
                "mako_native_oidc_discovery_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "open_at_rest" if args.len() == 3 => Some((
                "mako_native_open_at_rest_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_kind" if args.len() == 1 => Some((
                "mako_native_plugin_kind_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "plugin_last_error" if args.len() == 0 => Some((
                "mako_native_plugin_last_error",
                &[],
                Some(Type::I64),
                false,
            )),
            "png_decode_gray" if args.len() == 1 => Some((
                "mako_native_png_decode_gray_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "profile_sample_start" if args.len() == 1 => Some((
                "mako_native_profile_sample_start",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "quic_initial_client_key" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_key_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_type_count" if args.len() == 0 => Some((
                "mako_native_reflect_type_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "set_union_int" if args.len() == 2 => Some((
                "mako_native_set_union_int_ptr",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "sg_gather2" if args.len() == 2 => Some((
                "mako_native_sg_gather2_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_first_message_len" if args.len() == 1 => Some((
                "mako_native_sip_first_message_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_header" if args.len() == 2 => Some((
                "mako_native_sip_header_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_warn" if args.len() == 1 => Some((
                "mako_native_slog_warn_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "syscall_getgid" if args.len() == 0 => Some((
                "mako_native_syscall_getgid",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_pool_close" if args.len() == 1 => Some((
                "mako_native_tcp_pool_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_shutdown" if args.len() == 2 => Some((
                "mako_native_tcp_shutdown",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_hour" if args.len() == 1 => Some((
                "mako_native_time_hour",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_record_len" if args.len() == 1 => Some((
                "mako_native_tls_record_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_once_h2" if args.len() == 4 => Some((
                "mako_native_tls_serve_once_h2_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_server_hello_random" if args.len() == 1 => Some((
                "mako_native_tls_server_hello_random_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "trace_current" if args.len() == 0 => Some((
                "mako_native_trace_current_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "trace_export_otlp_pb_len" if args.len() == 0 => Some((
                "mako_native_trace_export_otlp_pb_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "udp_bind_addr" if args.len() == 2 => Some((
                "mako_native_udp_bind_addr_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "unix_write" if args.len() == 2 => Some((
                "mako_native_unix_write_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "utf8_valid_rune" if args.len() == 1 => Some((
                "mako_native_utf8_valid_rune",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "yaml_merge" if args.len() == 2 => Some((
                "mako_native_yaml_merge_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),

            "auth_session_cookie" if args.len() == 3 => Some((
                "mako_native_auth_session_cookie_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "avro_decode_string" if args.len() == 1 => Some((
                "mako_native_avro_decode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_split_port" if args.len() == 1 => Some((
                "mako_native_dns_split_port_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "ecs_query_count" if args.len() == 2 => Some((
                "mako_native_ecs_query_count",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "elastic_connect_url" if args.len() == 1 => Some((
                "mako_native_elastic_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_buf_write" if args.len() == 2 => Some((
                "mako_native_gpu_buf_write_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_status_code" if args.len() == 1 => Some((
                "mako_native_grpc_status_code_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hpack_huffman_encode" if args.len() == 1 => Some((
                "mako_native_hpack_huffman_encode_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "html_template_with" if args.len() == 3 => Some((
                "mako_native_html_template_with_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_content_encoding" if args.len() == 1 => Some((
                "mako_native_http_content_encoding_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_drain_conn" if args.len() == 1 => Some((
                "mako_native_http_shutdown_drain_conn",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_header_table_size" if args.len() == 0 => Some((
                "mako_native_http2_conn_header_table_size",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_settings_exchanged" if args.len() == 0 => Some((
                "mako_native_http2_conn_settings_exchanged",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_goaway_error" if args.len() == 1 => Some((
                "mako_native_http2_goaway_error_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_window_update_frame" if args.len() == 2 => Some((
                "mako_native_http2_window_update_frame_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "install_graceful_shutdown" if args.len() == 1 => Some((
                "mako_native_install_graceful_shutdown",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "io_try_write" if args.len() == 2 => Some((
                "mako_native_io_try_write_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "leak_report_json" if args.len() == 1 => Some((
                "mako_native_leak_report_json_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "lfq_try_push" if args.len() == 2 => Some((
                "mako_native_lfq_try_push",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_take_int" if args.len() == 2 => Some((
                "mako_native_list_take_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_tool_call_count" if args.len() == 1 => Some((
                "mako_native_llm_tool_call_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "mail_msg_rcpt_count" if args.len() == 1 => Some((
                "mako_native_mail_msg_rcpt_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "model_load_safetensors" if args.len() == 2 => Some((
                "mako_native_model_load_safetensors_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "msgpack_encode_string" if args.len() == 1 => Some((
                "mako_native_msgpack_encode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "multipart_file_allowed" if args.len() == 5 => Some((
                "mako_native_multipart_file_allowed_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "oidc_token" if args.len() == 4 => Some((
                "mako_native_oidc_token_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "plugin_last_error_str" if args.len() == 0 => Some((
                "mako_native_plugin_last_error_str_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "plugin_plugin_abi" if args.len() == 1 => Some((
                "mako_native_plugin_plugin_abi",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "profile_sample_stop" if args.len() == 0 => Some((
                "mako_native_profile_sample_stop",
                &[],
                Some(Type::I64),
                false,
            )),
            "profile_samples_pprof_text" if args.len() == 0 => Some((
                "mako_native_profile_samples_pprof_text_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "quic_detect" if args.len() == 1 => Some((
                "mako_native_quic_detect_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_initial_client_iv" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_iv_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_struct_field_type" if args.len() == 2 => Some((
                "mako_native_reflect_struct_field_type_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "reflect_type_of_int" if args.len() == 1 => Some((
                "mako_native_reflect_type_of_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "rmdir" if args.len() == 1 => Some((
                "mako_native_rmdir_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "scram_gs2_header" if args.len() == 1 => Some((
                "mako_native_scram_gs2_header_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "set_has_int" if args.len() == 2 => Some((
                "mako_native_set_has_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_header_count" if args.len() == 2 => Some((
                "mako_native_sip_header_count_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_strip_via" if args.len() == 1 => Some((
                "mako_native_sip_strip_via_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_error" if args.len() == 1 => Some((
                "mako_native_slog_error_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "syscall_getegid" if args.len() == 0 => Some((
                "mako_native_syscall_getegid",
                &[],
                Some(Type::I64),
                false,
            )),
            "tcp_linger" if args.len() == 3 => Some((
                "mako_native_tcp_linger",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_minute" if args.len() == 1 => Some((
                "mako_native_time_minute",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_certificate" if args.len() == 1 => Some((
                "mako_native_tls_certificate_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_settings_exchange" if args.len() == 3 => Some((
                "mako_native_tls_h2_settings_exchange_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_appdata_open" if args.len() == 3 => Some((
                "mako_native_tls_record_appdata_open_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_new" if args.len() == 0 => Some((
                "mako_native_tok_new",
                &[],
                Some(Type::I64),
                false,
            )),
            "trace_export_otlp_pb" if args.len() == 0 => Some((
                "mako_native_trace_export_otlp_pb_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "udp_last_sender_host" if args.len() == 0 => Some((
                "mako_native_udp_last_sender_host_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "unix_read" if args.len() == 2 => Some((
                "mako_native_unix_read_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "utf8_encode_rune" if args.len() == 1 => Some((
                "mako_native_utf8_encode_rune_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "yaml_keys" if args.len() == 1 => Some((
                "mako_native_yaml_keys_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),

            "alloc_track_free" if args.len() == 1 => Some((
                "mako_native_alloc_track_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "auth_token_sign" if args.len() == 2 => Some((
                "mako_native_auth_token_sign_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "avro_encode_array_long" if args.len() == 1 => Some((
                "mako_native_avro_encode_array_long_ptr",
                &[Type::IntSlice],
                Some(Type::Str),
                true,
            )),
            "csv_join_row" if args.len() == 1 => Some((
                "mako_native_csv_join_row_ptr",
                &[Type::StrSlice],
                Some(Type::Str),
                true,
            )),
            "dns_lookup_count" if args.len() == 1 => Some((
                "mako_native_dns_lookup_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "ecs_query_first" if args.len() == 2 => Some((
                "mako_native_ecs_query_first",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "elastic_search_request" if args.len() == 2 => Some((
                "mako_native_elastic_search_request_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "gob_encode_strs" if args.len() == 1 => Some((
                "mako_native_gob_encode_strs_ptr",
                &[Type::StrSlice],
                Some(Type::Str),
                true,
            )),
            "gpu_buf_read" if args.len() == 2 => Some((
                "mako_native_gpu_buf_read_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "grpc_default_max_message" if args.len() == 0 => Some((
                "mako_native_grpc_default_max_message",
                &[],
                Some(Type::I64),
                false,
            )),
            "gzip_available" if args.len() == 0 => Some((
                "mako_native_gzip_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "hpack_huffman_decode" if args.len() == 1 => Some((
                "mako_native_hpack_huffman_decode_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "http_shutdown_begin" if args.len() == 1 => Some((
                "mako_native_http_shutdown_begin",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_closing" if args.len() == 0 => Some((
                "mako_native_http2_conn_closing",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_enable_push" if args.len() == 0 => Some((
                "mako_native_http2_conn_enable_push",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_ping_frame" if args.len() == 2 => Some((
                "mako_native_http2_ping_frame_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_recv_window_of" if args.len() == 1 => Some((
                "mako_native_http2_recv_window_of",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "lfq_len" if args.len() == 1 => Some((
                "mako_native_lfq_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_drop_int" if args.len() == 2 => Some((
                "mako_native_list_drop_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_tool_call_name" if args.len() == 2 => Some((
                "mako_native_llm_tool_call_name_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mail_msg_rcpt_at" if args.len() == 2 => Some((
                "mako_native_mail_msg_rcpt_at_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "msgpack_decode_string" if args.len() == 1 => Some((
                "mako_native_msgpack_decode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "otlp_export_traces_json" if args.len() == 2 => Some((
                "mako_native_otlp_export_traces_json_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_path" if args.len() == 1 => Some((
                "mako_native_plugin_path_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "profile_sample_thread_count" if args.len() == 0 => Some((
                "mako_native_profile_sample_thread_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "profile_sample_wall_ns" if args.len() == 0 => Some((
                "mako_native_profile_sample_wall_ns",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_initial_protect" if args.len() == 4 => Some((
                "mako_native_quic_initial_protect_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_long_header" if args.len() == 1 => Some((
                "mako_native_quic_long_header_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_struct_has_field" if args.len() == 2 => Some((
                "mako_native_reflect_struct_has_field_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_type_of_string" if args.len() == 1 => Some((
                "mako_native_reflect_type_of_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "scram_cbind_b64" if args.len() == 2 => Some((
                "mako_native_scram_cbind_b64_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "set_intersect_int" if args.len() == 2 => Some((
                "mako_native_set_intersect_int_ptr",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "signal_on_term" if args.len() == 0 => Some((
                "mako_native_signal_on_term",
                &[],
                Some(Type::I64),
                false,
            )),
            "sip_via_branch" if args.len() == 1 => Some((
                "mako_native_sip_via_branch_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_set_json" if args.len() == 1 => Some((
                "mako_native_slog_set_json",
                &[Type::I64],
                None,
                false,
            )),
            "slog_with" if args.len() == 4 => Some((
                "mako_native_slog_with_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                None,
                false,
            )),
            "syscall_hostname" if args.len() == 0 => Some((
                "mako_native_syscall_hostname_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "temp_file" if args.len() == 1 => Some((
                "mako_native_temp_file_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "time_second" if args.len() == 1 => Some((
                "mako_native_time_second",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_certificate_der" if args.len() == 1 => Some((
                "mako_native_tls_certificate_der_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_get" if args.len() == 4 => Some((
                "mako_native_tls_h2_get_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_seq_reset" if args.len() == 0 => Some((
                "mako_native_tls_record_seq_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "tok_set" if args.len() == 3 => Some((
                "mako_native_tok_set_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "udp_recv_from" if args.len() == 2 => Some((
                "mako_native_udp_recv_from_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "unix_close" if args.len() == 1 => Some((
                "mako_native_unix_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "utf8_decode_rune" if args.len() == 2 => Some((
                "mako_native_utf8_decode_rune_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "yaml_get_list" if args.len() == 2 => Some((
                "mako_native_yaml_get_list_ptr",
                &[Type::Str, Type::Str],
                Some(Type::StrSlice),
                true,
            )),

            "auth_token_subject" if args.len() == 1 => Some((
                "mako_native_auth_token_subject_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "avro_decode_array_long" if args.len() == 1 => Some((
                "mako_native_avro_decode_array_long_ptr",
                &[Type::Str],
                Some(Type::IntSlice),
                true,
            )),
            "dns_lookup_all" if args.len() == 1 => Some((
                "mako_native_dns_lookup_all_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_archetype" if args.len() == 2 => Some((
                "mako_native_ecs_archetype",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gfx_poll" if args.len() == 1 => Some((
                "mako_native_gfx_poll",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gob_decode_strs" if args.len() == 1 => Some((
                "mako_native_gob_decode_strs_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "gob_encode_string" if args.len() == 1 => Some((
                "mako_native_gob_encode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_buf_free" if args.len() == 1 => Some((
                "mako_native_gpu_buf_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "grpc_message_within_limit" if args.len() == 2 => Some((
                "mako_native_grpc_message_within_limit_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gzip_decompress" if args.len() == 1 => Some((
                "mako_native_gzip_decompress_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hpack_dyn_clear" if args.len() == 0 => Some((
                "mako_native_hpack_dyn_clear",
                &[],
                None,
                false,
            )),
            "http_shutdown_deadline" if args.len() == 0 => Some((
                "mako_native_http_shutdown_deadline",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_settings_ack_needed" if args.len() == 0 => Some((
                "mako_native_http2_conn_settings_ack_needed",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_unacked" if args.len() == 0 => Some((
                "mako_native_http2_conn_unacked",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_is_window_update" if args.len() == 1 => Some((
                "mako_native_http2_is_window_update_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_recv_window_conn" if args.len() == 0 => Some((
                "mako_native_http2_recv_window_conn",
                &[],
                Some(Type::I64),
                false,
            )),
            "lfq_try_pop" if args.len() == 1 => Some((
                "mako_native_lfq_try_pop",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_zip_int" if args.len() == 2 => Some((
                "mako_native_list_zip_int_ptr",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "llm_tool_call_args" if args.len() == 2 => Some((
                "mako_native_llm_tool_call_args_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "msgpack_encode_array_int" if args.len() == 1 => Some((
                "mako_native_msgpack_encode_array_int_ptr",
                &[Type::IntSlice],
                Some(Type::Str),
                true,
            )),
            "otlp_export_traces_pb" if args.len() == 2 => Some((
                "mako_native_otlp_export_traces_pb_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_close" if args.len() == 1 => Some((
                "mako_native_plugin_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "plugin_log_count" if args.len() == 0 => Some((
                "mako_native_plugin_log_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "profile_sample_cpu_us" if args.len() == 0 => Some((
                "mako_native_profile_sample_cpu_us",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_initial_unprotect" if args.len() == 4 => Some((
                "mako_native_quic_initial_unprotect_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_short_header" if args.len() == 1 => Some((
                "mako_native_quic_short_header_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "realpath" if args.len() == 1 => Some((
                "mako_native_realpath_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_kind_of_int" if args.len() == 1 => Some((
                "mako_native_reflect_kind_of_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "scram_client_final_without_proof" if args.len() == 2 => Some((
                "mako_native_scram_client_final_without_proof_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "set_diff_int" if args.len() == 2 => Some((
                "mako_native_set_diff_int_ptr",
                &[Type::IntSlice, Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "shutdown_requested" if args.len() == 0 => Some((
                "mako_native_shutdown_requested",
                &[],
                Some(Type::I64),
                false,
            )),
            "sip_addr_tag" if args.len() == 1 => Some((
                "mako_native_sip_addr_tag_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "smtp_auth_plain" if args.len() == 2 => Some((
                "mako_native_smtp_auth_plain_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "syscall_uname_sysname" if args.len() == 0 => Some((
                "mako_native_syscall_uname_sysname_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "time_millisecond" if args.len() == 1 => Some((
                "mako_native_time_millisecond",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_certificate_verify" if args.len() == 2 => Some((
                "mako_native_tls_certificate_verify_ptr",
                &[Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_post" if args.len() == 5 => Some((
                "mako_native_tls_h2_post_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_write_seq" if args.len() == 0 => Some((
                "mako_native_tls_record_write_seq",
                &[],
                Some(Type::I64),
                false,
            )),
            "tok_id" if args.len() == 2 => Some((
                "mako_native_tok_id_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "toml_get_bool" if args.len() == 2 => Some((
                "mako_native_toml_get_bool_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "udp_last_sender_port" if args.len() == 0 => Some((
                "mako_native_udp_last_sender_port",
                &[],
                Some(Type::I64),
                false,
            )),
            "utf8_decode_size" if args.len() == 2 => Some((
                "mako_native_utf8_decode_size_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "xml_escape" if args.len() == 1 => Some((
                "mako_native_xml_escape_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),

            "auth_token_check" if args.len() == 2 => Some((
                "mako_native_auth_token_check_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ecs_system_add" if args.len() == 3 => Some((
                "mako_native_ecs_system_add",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gfx_backend_name" if args.len() == 0 => Some((
                "mako_native_gfx_backend_name_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "gob_decode_string" if args.len() == 1 => Some((
                "mako_native_gob_decode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gpu_add_f32" if args.len() == 3 => Some((
                "mako_native_gpu_add_f32",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_is_query" if args.len() == 1 => Some((
                "mako_native_graphql_is_query_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_http2_unary_response_status" if args.len() == 4 => Some((
                "mako_native_grpc_http2_unary_response_status_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "heap_push_int" if args.len() == 2 => Some((
                "mako_native_heap_push_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "hpack_dyn_insert" if args.len() == 2 => Some((
                "mako_native_hpack_dyn_insert_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "html_escape" if args.len() == 1 => Some((
                "mako_native_html_escape_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_conn_auto_settings_ack" if args.len() == 0 => Some((
                "mako_native_http2_conn_auto_settings_ack_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "http2_data_frame" if args.len() == 3 => Some((
                "mako_native_http2_data_frame_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_settings_max_concurrent" if args.len() == 1 => Some((
                "mako_native_http2_settings_max_concurrent_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_window_update_increment" if args.len() == 1 => Some((
                "mako_native_http2_window_update_increment_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "list_find_int" if args.len() == 2 => Some((
                "mako_native_list_find_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_sse_data" if args.len() == 1 => Some((
                "mako_native_llm_sse_data_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "lookup_host" if args.len() == 1 => Some((
                "mako_native_lookup_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "metrics_export_otlp_json" if args.len() == 0 => Some((
                "mako_native_metrics_export_otlp_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "msgpack_decode_array_int" if args.len() == 1 => Some((
                "mako_native_msgpack_decode_array_int_ptr",
                &[Type::Str],
                Some(Type::IntSlice),
                true,
            )),
            "plugin_call" if args.len() == 3 => Some((
                "mako_native_plugin_call_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_last_log" if args.len() == 0 => Some((
                "mako_native_plugin_last_log_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "profile_http_route" if args.len() == 1 => Some((
                "mako_native_profile_http_route_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hot_site_enable" if args.len() == 1 => Some((
                "mako_native_hot_site_enable",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_site_enabled" if args.len() == 0 => Some((
                "mako_native_hot_site_enabled",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_site_clear" if args.len() == 0 => Some((
                "mako_native_hot_site_clear",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_site_hit" if args.len() == 1 => Some((
                "mako_native_hot_site_hit",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_site_count" if args.len() == 1 => Some((
                "mako_native_hot_site_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_site_total" if args.len() == 0 => Some((
                "mako_native_hot_site_total",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_site_top_id" if args.len() == 0 => Some((
                "mako_native_hot_site_top_id",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_site_top_count" if args.len() == 0 => Some((
                "mako_native_hot_site_top_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_sites_json" if args.len() == 0 => Some((
                "mako_native_hot_sites_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "quic_initial_client_hp_hex" if args.len() == 1 => Some((
                "mako_native_quic_initial_client_hp_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_long_type" if args.len() == 1 => Some((
                "mako_native_quic_long_type_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_value_string_int" if args.len() == 1 => Some((
                "mako_native_reflect_value_string_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "scram_tls_unique_cbind" if args.len() == 1 => Some((
                "mako_native_scram_tls_unique_cbind_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "sg_slice" if args.len() == 3 => Some((
                "mako_native_sg_slice_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "should_stop_accepting" if args.len() == 0 => Some((
                "mako_native_should_stop_accepting",
                &[],
                Some(Type::I64),
                false,
            )),
            "sip_content_length" if args.len() == 1 => Some((
                "mako_native_sip_content_length_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "smtp_send_soft" if args.len() == 3 => Some((
                "mako_native_smtp_send_soft_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_uname_machine" if args.len() == 0 => Some((
                "mako_native_syscall_uname_machine_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "temp_dir" if args.len() == 0 => Some((
                "mako_native_temp_dir_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "time_weekday" if args.len() == 1 => Some((
                "mako_native_time_weekday",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_certificate_verify_scheme" if args.len() == 1 => Some((
                "mako_native_tls_certificate_verify_scheme_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_record_read_seq" if args.len() == 0 => Some((
                "mako_native_tls_record_read_seq",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_serve_h2_n" if args.len() == 5 => Some((
                "mako_native_tls_serve_h2_n_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tok_token" if args.len() == 2 => Some((
                "mako_native_tok_token_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "toml_get_float" if args.len() == 2 => Some((
                "mako_native_toml_get_float_ptr",
                &[Type::Str, Type::Str],
                Some(Type::F64),
                false,
            )),
            "ulid_timestamp_ms" if args.len() == 1 => Some((
                "mako_native_ulid_timestamp_ms_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_parse" if args.len() == 1 => Some((
                "mako_native_http_parse_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "http_parsed_ok" if args.len() == 1 => Some((
                "mako_native_http_parsed_ok",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "http_parsed_content_length" if args.len() == 1 => Some((
                "mako_native_http_parsed_content_length",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "http_parsed_chunked" if args.len() == 1 => Some((
                "mako_native_http_parsed_chunked",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "http_parsed_method" if args.len() == 1 => Some((
                "mako_native_http_parsed_method_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_parsed_path" if args.len() == 1 => Some((
                "mako_native_http_parsed_path_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_parsed_host" if args.len() == 1 => Some((
                "mako_native_http_parsed_host_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_parsed_headers" if args.len() == 1 => Some((
                "mako_native_http_parsed_headers_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_parsed_body" if args.len() == 1 => Some((
                "mako_native_http_parsed_body_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "http_parsed_header" if args.len() == 2 => Some((
                "mako_native_http_parsed_header_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::Str),
                true,
            )),
            "udp_last_sender" if args.len() == 0 => Some((
                "mako_native_udp_last_sender_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "utf8_full_rune" if args.len() == 1 => Some((
                "mako_native_utf8_full_rune_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),

            "bytes_to_hex" if args.len() == 1 => Some((
                "mako_native_bytes_to_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_lookup_ipv4" if args.len() == 1 => Some((
                "mako_native_dns_lookup_ipv4_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_set" if args.len() == 4 => Some((
                "mako_native_ecs_set",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gob_encode_int" if args.len() == 1 => Some((
                "mako_native_gob_encode_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "gpu_metal_ok" if args.len() == 0 => Some((
                "mako_native_gpu_metal_ok",
                &[],
                Some(Type::I64),
                false,
            )),
            "gpu_mul_f32" if args.len() == 3 => Some((
                "mako_native_gpu_mul_f32",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_operation_name" if args.len() == 1 => Some((
                "mako_native_graphql_operation_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_stream_data" if args.len() == 4 => Some((
                "mako_native_grpc_http2_stream_data_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "heap_peek_int" if args.len() == 1 => Some((
                "mako_native_heap_peek_int",
                &[Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "hpack_dyn_len" if args.len() == 0 => Some((
                "mako_native_hpack_dyn_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_max_concurrent" if args.len() == 0 => Some((
                "mako_native_http2_conn_max_concurrent",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_conn_send_settings" if args.len() == 0 => Some((
                "mako_native_http2_conn_send_settings",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_rst_stream_frame" if args.len() == 2 => Some((
                "mako_native_http2_rst_stream_frame_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "list_count_int" if args.len() == 2 => Some((
                "mako_native_list_count_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_sse_delta" if args.len() == 1 => Some((
                "mako_native_llm_sse_delta_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_call1" if args.len() == 2 => Some((
                "mako_native_plugin_call1_ptr",
                &[Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_close_all" if args.len() == 0 => Some((
                "mako_native_plugin_close_all",
                &[],
                Some(Type::I64),
                false,
            )),
            "profile_snapshot_json" if args.len() == 0 => Some((
                "mako_native_profile_snapshot_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "quic_initial_hp_mask_hex" if args.len() == 2 => Some((
                "mako_native_quic_initial_hp_mask_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_is_retry" if args.len() == 1 => Some((
                "mako_native_quic_is_retry_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "reflect_value_string_str" if args.len() == 1 => Some((
                "mako_native_reflect_value_string_str_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "scram_plus_client_final_bare" if args.len() == 2 => Some((
                "mako_native_scram_plus_client_final_bare_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::Str),
                true,
            )),
            "server_shutdown_begin" if args.len() == 1 => Some((
                "mako_native_server_shutdown_begin",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_msg_complete" if args.len() == 1 => Some((
                "mako_native_sip_msg_complete_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "smtp_send_dialog" if args.len() == 5 => Some((
                "mako_native_smtp_send_dialog_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "symlink" if args.len() == 2 => Some((
                "mako_native_symlink_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_uname_json" if args.len() == 0 => Some((
                "mako_native_syscall_uname_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "time_yearday" if args.len() == 1 => Some((
                "mako_native_time_yearday",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_certificate_verify_sig" if args.len() == 1 => Some((
                "mako_native_tls_certificate_verify_sig_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_get_twice" if args.len() == 4 => Some((
                "mako_native_tls_h2_get_twice_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_appdata_seal_seq" if args.len() == 3 => Some((
                "mako_native_tls_record_appdata_seal_seq_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_encode" if args.len() == 2 => Some((
                "mako_native_tok_encode_ptr",
                &[Type::I64, Type::Str],
                Some(Type::IntSlice),
                true,
            )),
            "utf8_rune_start" if args.len() == 1 => Some((
                "mako_native_utf8_rune_start",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "xml_tag_text" if args.len() == 2 => Some((
                "mako_native_xml_tag_text_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),

            "chacha20_poly1305_seal" if args.len() == 4 => Some((
                "mako_native_chacha20_poly1305_seal_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "dns_lookup_ipv6" if args.len() == 1 => Some((
                "mako_native_dns_lookup_ipv6_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_remove" if args.len() == 3 => Some((
                "mako_native_ecs_remove",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gob_decode_int" if args.len() == 1 => Some((
                "mako_native_gob_decode_int_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gpu_cuda_ok" if args.len() == 0 => Some((
                "mako_native_gpu_cuda_ok",
                &[],
                Some(Type::I64),
                false,
            )),
            "gpu_scale_f32" if args.len() == 3 => Some((
                "mako_native_gpu_scale_f32",
                &[Type::I64, Type::I64, Type::F64],
                Some(Type::I64),
                false,
            )),
            "graphql_has_field" if args.len() == 2 => Some((
                "mako_native_graphql_has_field_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "grpc_http2_stream_data_count" if args.len() == 2 => Some((
                "mako_native_grpc_http2_stream_data_count_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "heap_pop_int" if args.len() == 1 => Some((
                "mako_native_heap_pop_int_ptr",
                &[Type::IntSlice],
                Some(Type::IntSlice),
                true,
            )),
            "hex_to_bytes" if args.len() == 1 => Some((
                "mako_native_hex_to_bytes_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hot_reload_plugin_watch" if args.len() == 1 => Some((
                "mako_native_hot_reload_plugin_watch_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "hpack_dyn_name" if args.len() == 0 => Some((
                "mako_native_hpack_dyn_name_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "http2_conn_goaway_last" if args.len() == 0 => Some((
                "mako_native_http2_conn_goaway_last",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_is_rst_stream" if args.len() == 1 => Some((
                "mako_native_http2_is_rst_stream_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_stream_body" if args.len() == 1 => Some((
                "mako_native_http2_stream_body_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "list_any_eq_int" if args.len() == 2 => Some((
                "mako_native_list_any_eq_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_stream_append" if args.len() == 2 => Some((
                "mako_native_llm_stream_append_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_info_json" if args.len() == 1 => Some((
                "mako_native_plugin_info_json_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "process_rss_bytes" if args.len() == 0 => Some((
                "mako_native_process_rss_bytes",
                &[],
                Some(Type::I64),
                false,
            )),
            "quic_initial_hp_mask" if args.len() == 2 => Some((
                "mako_native_quic_initial_hp_mask_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_is_version_negotiation" if args.len() == 1 => Some((
                "mako_native_quic_is_version_negotiation_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "readlink" if args.len() == 1 => Some((
                "mako_native_readlink_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_len_string" if args.len() == 1 => Some((
                "mako_native_reflect_len_string_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "server_drain" if args.len() == 1 => Some((
                "mako_native_server_drain",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_msg_needed" if args.len() == 1 => Some((
                "mako_native_sip_msg_needed_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "smtp_send_auth" if args.len() == 7 => Some((
                "mako_native_smtp_send_auth_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_pagesize" if args.len() == 0 => Some((
                "mako_native_syscall_pagesize",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_parse_rfc3339" if args.len() == 1 => Some((
                "mako_native_time_parse_rfc3339_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_finished_verify_data_hex" if args.len() == 2 => Some((
                "mako_native_tls_finished_verify_data_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_mux" if args.len() == 3 => Some((
                "mako_native_tls_h2_mux_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_record_appdata_open_seq" if args.len() == 3 => Some((
                "mako_native_tls_record_appdata_open_seq_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_decode" if args.len() == 2 => Some((
                "mako_native_tok_decode_ptr",
                &[Type::I64, Type::IntSlice],
                Some(Type::Str),
                true,
            )),
            "utf8_decode_last_rune" if args.len() == 1 => Some((
                "mako_native_utf8_decode_last_rune_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),

            "cbor_encode_int" if args.len() == 1 => Some((
                "mako_native_cbor_encode_int_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "chacha20_poly1305_open" if args.len() == 4 => Some((
                "mako_native_chacha20_poly1305_open_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "ecs_despawn" if args.len() == 2 => Some((
                "mako_native_ecs_despawn",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_fill_f32" if args.len() == 3 => Some((
                "mako_native_gpu_fill_f32",
                &[Type::I64, Type::I64, Type::F64],
                Some(Type::I64),
                false,
            )),
            "gpu_vulkan_ok" if args.len() == 0 => Some((
                "mako_native_gpu_vulkan_ok",
                &[],
                Some(Type::I64),
                false,
            )),
            "graphql_request_vars" if args.len() == 2 => Some((
                "mako_native_graphql_request_vars_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_stream_two" if args.len() == 5 => Some((
                "mako_native_grpc_http2_stream_two_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "heap_popped_int" if args.len() == 0 => Some((
                "mako_native_heap_popped_int",
                &[],
                Some(Type::I64),
                false,
            )),
            "hpack_dyn_name_at" if args.len() == 1 => Some((
                "mako_native_hpack_dyn_name_at_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_conn_send_goaway" if args.len() == 0 => Some((
                "mako_native_http2_conn_send_goaway",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_rst_stream_error" if args.len() == 1 => Some((
                "mako_native_http2_rst_stream_error_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_stream_body_done" if args.len() == 1 => Some((
                "mako_native_http2_stream_body_done",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "httptest_get" if args.len() == 1 => Some((
                "mako_native_httptest_get_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "list_all_eq_int" if args.len() == 2 => Some((
                "mako_native_list_all_eq_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_json_extract" if args.len() == 1 => Some((
                "mako_native_llm_json_extract_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "plugin_find" if args.len() == 1 => Some((
                "mako_native_plugin_find_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "process_cpu_user_us" if args.len() == 0 => Some((
                "mako_native_process_cpu_user_us",
                &[],
                Some(Type::I64),
                false,
            )),
            "pwrite" if args.len() == 3 => Some((
                "mako_native_pwrite_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "quic_header_protect_apply" if args.len() == 3 => Some((
                "mako_native_quic_header_protect_apply_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_version" if args.len() == 1 => Some((
                "mako_native_quic_version_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "regex_valid" if args.len() == 1 => Some((
                "mako_native_regex_valid_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "register_listener" if args.len() == 1 => Some((
                "mako_native_register_listener",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sdp_ok" if args.len() == 1 => Some((
                "mako_native_sdp_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "smtp_set_timeout_ms" if args.len() == 2 => Some((
                "mako_native_smtp_set_timeout_ms",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "syscall_ncpu" if args.len() == 0 => Some((
                "mako_native_syscall_ncpu",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_parse_date" if args.len() == 1 => Some((
                "mako_native_time_parse_date_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_grpc_once" if args.len() == 3 => Some((
                "mako_native_tls_serve_grpc_once_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_transcript_reset" if args.len() == 0 => Some((
                "mako_native_tls_transcript_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "tok_load_json" if args.len() == 2 => Some((
                "mako_native_tok_load_json_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "utf8_decode_last_size" if args.len() == 0 => Some((
                "mako_native_utf8_decode_last_size",
                &[],
                Some(Type::I64),
                false,
            )),

            "cbor_decode_int" if args.len() == 1 => Some((
                "mako_native_cbor_decode_int_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "close_listeners" if args.len() == 0 => Some((
                "mako_native_close_listeners",
                &[],
                Some(Type::I64),
                false,
            )),
            "file_size" if args.len() == 1 => Some((
                "mako_native_file_size",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_f32_count" if args.len() == 1 => Some((
                "mako_native_gpu_f32_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "graphql_data2" if args.len() == 4 => Some((
                "mako_native_graphql_data2_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "grpc_http2_client_stream_flow" if args.len() == 8 => Some((
                "mako_native_grpc_http2_client_stream_flow_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::Str, Type::I64, Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "hpack_dyn_value_at" if args.len() == 1 => Some((
                "mako_native_hpack_dyn_value_at_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_conn_goaway" if args.len() == 1 => Some((
                "mako_native_http2_conn_goaway_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_is_priority" if args.len() == 1 => Some((
                "mako_native_http2_is_priority_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_stream_state_of" if args.len() == 1 => Some((
                "mako_native_http2_stream_state_of",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "httptest_status" if args.len() == 0 => Some((
                "mako_native_httptest_status",
                &[],
                Some(Type::I64),
                false,
            )),
            "list_concat_str" if args.len() == 2 => Some((
                "mako_native_list_concat_str_ptr",
                &[Type::StrSlice, Type::StrSlice],
                Some(Type::StrSlice),
                true,
            )),
            "list_take_while_lt_int" if args.len() == 2 => Some((
                "mako_native_list_take_while_lt_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_estimate_tokens" if args.len() == 1 => Some((
                "mako_native_llm_estimate_tokens_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "path_file_size" if args.len() == 1 => Some((
                "mako_native_path_file_size_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "plugin_reload" if args.len() == 1 => Some((
                "mako_native_plugin_reload",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "quic_dcid_len" if args.len() == 1 => Some((
                "mako_native_quic_dcid_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_header_protect_remove" if args.len() == 3 => Some((
                "mako_native_quic_header_protect_remove_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "regex_quote_meta" if args.len() == 1 => Some((
                "mako_native_regex_quote_meta_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_version" if args.len() == 1 => Some((
                "mako_native_sdp_version_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "seal_file_at_rest" if args.len() == 4 => Some((
                "mako_native_seal_file_at_rest_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "smtp_starttls_available" if args.len() == 0 => Some((
                "mako_native_smtp_starttls_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "snap_diff" if args.len() == 2 => Some((
                "mako_native_snap_diff_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "stack_trace" if args.len() == 0 => Some((
                "mako_native_stack_trace_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "syscall_getrlimit_nofile" if args.len() == 0 => Some((
                "mako_native_syscall_getrlimit_nofile",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_format_date" if args.len() == 1 => Some((
                "mako_native_time_format_date_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "tls_grpc_unary" if args.len() == 6 => Some((
                "mako_native_tls_grpc_unary_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_transcript_len" if args.len() == 0 => Some((
                "mako_native_tls_transcript_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "tok_size" if args.len() == 1 => Some((
                "mako_native_tok_size",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_is_letter" if args.len() == 1 => Some((
                "mako_native_unicode_is_letter",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_type" if args.len() == 1 => Some((
                "mako_native_cbor_type_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "crash_report_install" if args.len() == 1 => Some((
                "mako_native_crash_report_install_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gpu_opencl_ok" if args.len() == 0 => Some((
                "mako_native_gpu_opencl_ok",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_priority_frame" if args.len() == 4 => Some((
                "mako_native_http2_priority_frame_ptr",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "http2_stream_half_closed_remote" if args.len() == 1 => Some((
                "mako_native_http2_stream_half_closed_remote",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_stream_reset" if args.len() == 0 => Some((
                "mako_native_http2_stream_reset",
                &[],
                None,
                false,
            )),
            "httptest_header" if args.len() == 1 => Some((
                "mako_native_httptest_header_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "list_map_add_int" if args.len() == 2 => Some((
                "mako_native_list_map_add_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_retry_delay_ms" if args.len() == 3 => Some((
                "mako_native_llm_retry_delay_ms",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "open_file_at_rest" if args.len() == 3 => Some((
                "mako_native_open_file_at_rest_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pem_count_blocks" if args.len() == 1 => Some((
                "mako_native_pem_count_blocks_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "plugin_manifest_artifact" if args.len() == 1 => Some((
                "mako_native_plugin_manifest_artifact_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "pread" if args.len() == 3 => Some((
                "mako_native_pread_ptr",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_dcid" if args.len() == 1 => Some((
                "mako_native_quic_dcid_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_initial_packet_protect" if args.len() == 5 => Some((
                "mako_native_quic_initial_packet_protect_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_media_count" if args.len() == 1 => Some((
                "mako_native_sdp_media_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "snap_get" if args.len() == 2 => Some((
                "mako_native_snap_get_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "syscall_pipe" if args.len() == 0 => Some((
                "mako_native_syscall_pipe",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_format_clock" if args.len() == 1 => Some((
                "mako_native_time_format_clock_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "time_offset_named" if args.len() == 1 => Some((
                "mako_native_time_offset_named_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_grpc_stream" if args.len() == 3 => Some((
                "mako_native_tls_serve_grpc_stream_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_transcript_append" if args.len() == 1 => Some((
                "mako_native_tls_transcript_append_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tok_free" if args.len() == 1 => Some((
                "mako_native_tok_free",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_is_digit" if args.len() == 1 => Some((
                "mako_native_unicode_is_digit",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_is_null" if args.len() == 1 => Some((
                "mako_native_cbor_is_null_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "fsync" if args.len() == 1 => Some((
                "mako_native_fsync",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_map_mul_int" if args.len() == 2 => Some((
                "mako_native_list_map_mul_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_should_retry" if args.len() == 1 => Some((
                "mako_native_llm_should_retry_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "pem_has_block" if args.len() == 2 => Some((
                "mako_native_pem_has_block_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "plugin_manifest_lib_path" if args.len() == 1 => Some((
                "mako_native_plugin_manifest_lib_path_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_scid_len" if args.len() == 1 => Some((
                "mako_native_quic_scid_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "regex_find_all" if args.len() == 3 => Some((
                "mako_native_regex_find_all_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::StrSlice),
                true,
            )),
            "sdp_media_type" if args.len() == 2 => Some((
                "mako_native_sdp_media_type_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "snap_apply_delta" if args.len() == 2 => Some((
                "mako_native_snap_apply_delta_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "syscall_pipe_read_fd" if args.len() == 0 => Some((
                "mako_native_syscall_pipe_read_fd",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_format_local" if args.len() == 1 => Some((
                "mako_native_time_format_local_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "time_format_offset" if args.len() == 2 => Some((
                "mako_native_time_format_offset_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "tls_transcript_finished_hex" if args.len() == 1 => Some((
                "mako_native_tls_transcript_finished_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "unicode_is_space" if args.len() == 1 => Some((
                "mako_native_unicode_is_space",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_priority_dep" if args.len() == 1 => Some((
                "mako_native_http2_priority_dep_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_stream_apply_local" if args.len() == 1 => Some((
                "mako_native_http2_stream_apply_local_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gpu_matmul_f32" if args.len() == 6 => Some((
                "mako_native_gpu_matmul_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_mha_f32" if args.len() == 7 => Some((
                "mako_native_gpu_mha_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),

            "watch_poll" if args.len() == 2 => Some((
                "mako_native_watch_poll_ptr",
                &[Type::Opaque, Type::I64],
                Some(Type::Str),
                true,
            )),
            "watch_close" if args.len() == 1 => Some((
                "mako_native_watch_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "buf_reader_close" if args.len() == 1 => Some((
                "mako_native_buf_reader_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "buf_read" if args.len() == 2 => Some((
                "mako_native_buf_read_ptr",
                &[Type::Opaque, Type::I64],
                Some(Type::Str),
                true,
            )),
            "buf_flush" if args.len() == 1 => Some((
                "mako_native_buf_flush",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "buf_write" if args.len() == 2 => Some((
                "mako_native_buf_write_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "buf_write_byte" if args.len() == 2 => Some((
                "mako_native_buf_write_byte",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "buf_writer_close" if args.len() == 1 => Some((
                "mako_native_buf_writer_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "zip_write_to" if args.len() == 2 => Some((
                "mako_native_zip_write_to_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "zip_close" if args.len() == 1 => Some((
                "mako_native_zip_close",
                &[Type::Opaque],
                None,
                false,
            )),
            "zip_list" if args.len() == 1 => Some((
                "mako_native_zip_list_ptr",
                &[Type::Str],
                Some(Type::StrSlice),
                true,
            )),
            "limits_mem_used" if args.len() == 1 => Some((
                "mako_native_limits_mem_used",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "limits_check_time" if args.len() == 1 => Some((
                "mako_native_limits_check_time",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "limits_free" if args.len() == 1 => Some((
                "mako_native_limits_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "limits_open_conns" if args.len() == 1 => Some((
                "mako_native_limits_open_conns",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "limits_release_conn" if args.len() == 1 => Some((
                "mako_native_limits_release_conn",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "limits_release_mem" if args.len() == 2 => Some((
                "mako_native_limits_release_mem",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "limits_try_conn" if args.len() == 1 => Some((
                "mako_native_limits_try_conn",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tar_first_name" if args.len() == 1 => Some((
                "mako_native_tar_first_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "base32_encode" if args.len() == 1 => Some((
                "mako_native_base32_encode_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "gzip_compress" if args.len() == 1 => Some((
                "mako_native_gzip_compress_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "rand_intn" if args.len() == 1 => Some((
                "mako_native_rand_intn",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "rand_seed" if args.len() == 1 => Some((
                "mako_native_rand_seed",
                &[Type::I64],
                None,
                false,
            )),
            "bytes_buffer_len" if args.len() == 1 => Some((
                "mako_native_bytes_buffer_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "bytes_buffer_reset" if args.len() == 1 => Some((
                "mako_native_bytes_buffer_reset",
                &[Type::Opaque],
                None,
                false,
            )),
            "bytes_buffer_string" if args.len() == 1 => Some((
                "mako_native_bytes_buffer_string_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "bytes_buffer_write" if args.len() == 2 => Some((
                "mako_native_bytes_buffer_write_ptr",
                &[Type::Opaque, Type::Str],
                None,
                false,
            )),
            "context_expired" if args.len() == 1 => Some((
                "mako_native_context_expired",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "flag_int" if args.len() == 2 => Some((
                "mako_native_flag_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "flag_string" if args.len() == 2 => Some((
                "mako_native_flag_string_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "session_cancelled" if args.len() == 1 => Some((
                "mako_native_session_cancelled_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "session_cancel" if args.len() == 1 => Some((
                "mako_native_session_cancel_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "session_cancel_clear" if args.len() == 1 => Some((
                "mako_native_session_cancel_clear_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "session_cancel_token" if args.len() == 0 => Some((
                "mako_native_session_cancel_token_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "binary_u16le" if args.len() == 1 => Some((
                "mako_native_binary_u16le_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_u32le" if args.len() == 1 => Some((
                "mako_native_binary_u32le_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_u64le" if args.len() == 1 => Some((
                "mako_native_binary_u64le_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "binary_put_u16le" if args.len() == 1 => Some((
                "mako_native_binary_put_u16le_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "binary_put_u32le" if args.len() == 1 => Some((
                "mako_native_binary_put_u32le_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "binary_put_u64le" if args.len() == 1 => Some((
                "mako_native_binary_put_u64le_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "jpeg_dct_dc" if args.len() == 1 => Some((
                "mako_native_jpeg_dct_dc_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "gob_encode_map_ss" if args.len() == 1 => Some((
                "mako_native_gob_encode_map_ss_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "html_template_if" if args.len() == 4 => Some((
                "mako_native_html_template_if_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "html_template_execute2" if args.len() == 5 => Some((
                "mako_native_html_template_execute2_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "smtp_format_message" if args.len() == 4 => Some((
                "mako_native_smtp_format_message_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),

            "cbor_encode_null" if args.len() == 0 => Some((
                "mako_native_cbor_encode_null_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "file_truncate" if args.len() == 2 => Some((
                "mako_native_file_truncate",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_bias_add_f32" if args.len() == 5 => Some((
                "mako_native_gpu_bias_add_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_header_assembling" if args.len() == 0 => Some((
                "mako_native_http2_conn_header_assembling",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_priority_weight" if args.len() == 1 => Some((
                "mako_native_http2_priority_weight_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "list_filter_lt_int" if args.len() == 2 => Some((
                "mako_native_list_filter_lt_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_body_force_stream" if args.len() == 1 => Some((
                "mako_native_llm_body_force_stream_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "model_tensor_buf" if args.len() == 2 => Some((
                "mako_native_model_tensor_buf_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "netcode_lag_comp_tick" if args.len() == 3 => Some((
                "mako_native_netcode_lag_comp_tick",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pem_extract_block" if args.len() == 2 => Some((
                "mako_native_pem_extract_block_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_initial_packet_unprotect" if args.len() == 4 => Some((
                "mako_native_quic_initial_packet_unprotect_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_scid" if args.len() == 1 => Some((
                "mako_native_quic_scid_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "regex_replace" if args.len() == 3 => Some((
                "mako_native_regex_replace_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_media_proto" if args.len() == 2 => Some((
                "mako_native_sdp_media_proto_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "syscall_pipe_write_fd" if args.len() == 0 => Some((
                "mako_native_syscall_pipe_write_fd",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_add_ms" if args.len() == 2 => Some((
                "mako_native_time_add_ms",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_in_offset" if args.len() == 2 => Some((
                "mako_native_time_in_offset",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_client_handshake_traffic_secret_hex" if args.len() == 2 => Some((
                "mako_native_tls_client_handshake_traffic_secret_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_grpc_stream" if args.len() == 8 => Some((
                "mako_native_tls_grpc_stream_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "unicode_is_punct" if args.len() == 1 => Some((
                "mako_native_unicode_is_punct",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_decode_bool" if args.len() == 1 => Some((
                "mako_native_cbor_decode_bool_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "duration_hours" if args.len() == 1 => Some((
                "mako_native_duration_hours",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "gpu_saxpy_f32" if args.len() == 4 => Some((
                "mako_native_gpu_saxpy_f32",
                &[Type::I64, Type::I64, Type::I64, Type::F64],
                Some(Type::I64),
                false,
            )),
            "http2_conn_active_streams" if args.len() == 0 => Some((
                "mako_native_http2_conn_active_streams",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_priority_exclusive" if args.len() == 1 => Some((
                "mako_native_http2_priority_exclusive_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "list_filter_gt_int" if args.len() == 2 => Some((
                "mako_native_list_filter_gt_int_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "llm_embed_body" if args.len() == 2 => Some((
                "mako_native_llm_embed_body_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "netcode_interp" if args.len() == 3 => Some((
                "mako_native_netcode_interp",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "pem_load_file" if args.len() == 1 => Some((
                "mako_native_pem_load_file_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_crypto_payload" if args.len() == 3 => Some((
                "mako_native_quic_crypto_payload_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_payload_offset" if args.len() == 1 => Some((
                "mako_native_quic_payload_offset_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "regex_replace_all" if args.len() == 3 => Some((
                "mako_native_regex_replace_all_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_attr" if args.len() == 2 => Some((
                "mako_native_sdp_attr_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "syscall_write" if args.len() == 2 => Some((
                "mako_native_syscall_write_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_serve_h2_wu" if args.len() == 5 => Some((
                "mako_native_tls_serve_h2_wu_ptr",
                &[Type::I64, Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_server_handshake_traffic_secret_hex" if args.len() == 2 => Some((
                "mako_native_tls_server_handshake_traffic_secret_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_load_bpe" if args.len() == 3 => Some((
                "mako_native_tok_load_bpe_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "unicode_is_symbol" if args.len() == 1 => Some((
                "mako_native_unicode_is_symbol",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_encode_bool" if args.len() == 1 => Some((
                "mako_native_cbor_encode_bool_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "gpu_sum_f32" if args.len() == 1 => Some((
                "mako_native_gpu_sum_f32",
                &[Type::I64],
                Some(Type::F64),
                false,
            )),
            "gpu_softmax_rows_f32" if args.len() == 4 => Some((
                "mako_native_gpu_softmax_rows_f32",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_reload_watch_count" if args.len() == 0 => Some((
                "mako_native_hot_reload_watch_count",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_priority_apply" if args.len() == 1 => Some((
                "mako_native_http2_priority_apply_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "list_fold_add_int" if args.len() == 2 => Some((
                "mako_native_list_fold_add_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_embedding_dim" if args.len() == 1 => Some((
                "mako_native_llm_embedding_dim_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "metrics_export_prom" if args.len() == 0 => Some((
                "mako_native_metrics_export_prom_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "quic_payload_crypto_data" if args.len() == 1 => Some((
                "mako_native_quic_payload_crypto_data_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_spin_bit" if args.len() == 1 => Some((
                "mako_native_quic_spin_bit_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_reason" if args.len() == 1 => Some((
                "mako_native_sip_reason_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "syscall_read" if args.len() == 2 => Some((
                "mako_native_syscall_read_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "time_sub_ms" if args.len() == 2 => Some((
                "mako_native_time_sub_ms",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_derive_secret_hex" if args.len() == 3 => Some((
                "mako_native_tls_derive_secret_hex_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_h2_window_get" if args.len() == 4 => Some((
                "mako_native_tls_h2_window_get_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_merge_count" if args.len() == 1 => Some((
                "mako_native_tok_merge_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_is_control" if args.len() == 1 => Some((
                "mako_native_unicode_is_control",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_encode_string" if args.len() == 1 => Some((
                "mako_native_cbor_encode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "duration_days" if args.len() == 1 => Some((
                "mako_native_duration_days",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_reload_stamp" if args.len() == 0 => Some((
                "mako_native_hot_reload_stamp",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_priority_dep" if args.len() == 1 => Some((
                "mako_native_http2_stream_priority_dep",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "list_fold_mul_int" if args.len() == 2 => Some((
                "mako_native_list_fold_mul_int",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "llm_embedding_json" if args.len() == 1 => Some((
                "mako_native_llm_embedding_json_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_key_phase" if args.len() == 1 => Some((
                "mako_native_quic_key_phase_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_payload_crypto_data_len" if args.len() == 1 => Some((
                "mako_native_quic_payload_crypto_data_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_uri_scheme" if args.len() == 1 => Some((
                "mako_native_sip_uri_scheme_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "syscall_close" if args.len() == 1 => Some((
                "mako_native_syscall_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_client_application_traffic_secret_hex" if args.len() == 2 => Some((
                "mako_native_tls_client_application_traffic_secret_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tok_encode_bpe" if args.len() == 2 => Some((
                "mako_native_tok_encode_bpe_ptr",
                &[Type::I64, Type::Str],
                Some(Type::IntSlice),
                true,
            )),
            "trace_export_json" if args.len() == 0 => Some((
                "mako_native_trace_export_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "unicode_is_print" if args.len() == 1 => Some((
                "mako_native_unicode_is_print",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "cbor_decode_string" if args.len() == 1 => Some((
                "mako_native_cbor_decode_string_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "hot_reload_note_swap" if args.len() == 0 => Some((
                "mako_native_hot_reload_note_swap",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_priority_weight" if args.len() == 1 => Some((
                "mako_native_http2_stream_priority_weight",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "quic_ack_frame" if args.len() == 3 => Some((
                "mako_native_quic_ack_frame_ptr",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_vn_version_count" if args.len() == 1 => Some((
                "mako_native_quic_vn_version_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_uri_user" if args.len() == 1 => Some((
                "mako_native_sip_uri_user_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "stack_peek_str" if args.len() == 1 => Some((
                "mako_native_stack_peek_str_ptr",
                &[Type::StrSlice],
                Some(Type::Str),
                true,
            )),
            "syscall_access" if args.len() == 2 => Some((
                "mako_native_syscall_access_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_after" if args.len() == 2 => Some((
                "mako_native_time_after",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_make_self_signed" if args.len() == 4 => Some((
                "mako_native_tls_make_self_signed_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_server_application_traffic_secret_hex" if args.len() == 2 => Some((
                "mako_native_tls_server_application_traffic_secret_hex_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "unicode_is_graphic" if args.len() == 1 => Some((
                "mako_native_unicode_is_graphic",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),

            "http_route_match" if args.len() == 3 => Some((
                "mako_native_http_route_match_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "hkdf_sha256" if args.len() == 4 => Some((
                "mako_native_hkdf_sha256_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "unsafe_index" if args.len() == 2 => Some((
                "mako_native_unsafe_index_ints_ptr",
                &[Type::IntSlice, Type::I64],
                Some(Type::I64),
                false,
            )),
            "reqctx_set" if args.len() == 3 => Some((
                "mako_native_reqctx_set_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "http_route_param" if args.len() == 3 => Some((
                "mako_native_http_route_param_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_client_new_insecure" if args.len() == 0 => Some((
                "mako_native_tls_client_new_insecure",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "middleware_require_context" if args.len() == 2 => Some((
                "mako_native_middleware_require_context_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "router_match" if args.len() == 2 => Some((
                "mako_native_router_match_ptr",
                &[Type::I64, Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "router_match_path" if args.len() == 3 => Some((
                "mako_native_router_match_path_ptr",
                &[Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_uri_host" if args.len() == 1 => Some((
                "mako_native_sip_uri_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_is_ack" if args.len() == 1 => Some((
                "mako_native_quic_is_ack_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_vn_version_at" if args.len() == 2 => Some((
                "mako_native_quic_vn_version_at_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_server_reload" if args.len() == 3 => Some((
                "mako_native_tls_server_reload_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_reset" if args.len() == 0 => Some((
                "mako_native_tls_hs_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_state" if args.len() == 0 => Some((
                "mako_native_http2_stream_state",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_priority_exclusive" if args.len() == 1 => Some((
                "mako_native_http2_stream_priority_exclusive",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_reload_swap_count" if args.len() == 0 => Some((
                "mako_native_hot_reload_swap_count",
                &[],
                Some(Type::I64),
                false,
            )),

            "tls_client_free" if args.len() == 1 => Some((
                "mako_native_tls_client_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "middleware_allow_methods" if args.len() == 2 => Some((
                "mako_native_middleware_allow_methods_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "router_param" if args.len() == 3 => Some((
                "mako_native_router_param_ptr",
                &[Type::I64, Type::Opaque, Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_client_new" if args.len() == 1 => Some((
                "mako_native_tls_client_new_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "middleware_next" if args.len() == 2 => Some((
                "mako_native_middleware_next_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "middleware_ran" if args.len() == 2 => Some((
                "mako_native_middleware_ran_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "middleware_trace" if args.len() == 1 => Some((
                "mako_native_middleware_trace_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "tls_connect" if args.len() == 3 => Some((
                "mako_native_tls_connect_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "queue_popped_str" if args.is_empty() => Some((
                "mako_native_queue_popped_str",
                &[],
                Some(Type::Str),
                true,
            )),
            "time_equal" if args.len() == 2 => Some((
                "mako_native_time_equal",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_is_lower" if args.len() == 1 => Some((
                "mako_native_unicode_is_lower",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "syscall_errno_str" if args.is_empty() => Some((
                "mako_native_syscall_errno_str",
                &[],
                Some(Type::Str),
                true,
            )),
            "crash_report_installed" if args.is_empty() => Some((
                "mako_native_crash_report_installed",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_id" if args.is_empty() => Some((
                "mako_native_http2_stream_id",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_priority_child_count" if args.len() == 1 => Some((
                "mako_native_http2_stream_priority_child_count",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_stream_body_len" if args.len() == 1 => Some((
                "mako_native_http2_stream_body_len",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "quic_ack_largest" if args.len() == 1 => Some((
                "mako_native_quic_ack_largest_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_vn_select" if args.len() == 2 => Some((
                "mako_native_quic_vn_select_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_uri_port" if args.len() == 1 => Some((
                "mako_native_sip_uri_port_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "frame_alloc" if args.len() == 2 => Some((
                "mako_native_frame_alloc",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "ws_recv" if args.len() == 2 => Some((
                "mako_native_ws_recv",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "uuid_version" if args.len() == 1 => Some((
                "mako_native_uuid_version_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_state" if args.is_empty() => Some((
                "mako_native_tls_hs_state",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_peer_cn" if args.len() == 1 => Some((
                "mako_native_tls_peer_cn",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "tls_make_csr" if args.len() == 4 => Some((
                "mako_native_tls_make_csr_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_server_sni_add" if args.len() == 4 => Some((
                "mako_native_tls_server_sni_add_ptr",
                &[Type::Opaque, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "nghttp2_available" if args.is_empty() => Some((
                "mako_native_nghttp2_available",
                &[],
                Some(Type::I64),
                false,
            )),
            "sql_rows_ok" if args.len() == 1 => Some((
                "mako_native_sql_rows_ok",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_rollback" if args.len() == 1 => Some((
                "mako_native_sql_rollback",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_migrate" if args.len() == 3 => Some((
                "mako_native_sql_migrate_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "slog_is_json" if args.is_empty() => Some((
                "mako_native_slog_is_json",
                &[],
                Some(Type::I64),
                false,
            )),
            "slog_with2" if args.len() == 6 => Some((
                "mako_native_slog_with2_6_ptr",
                &[
                    Type::Str,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                ],
                None,
                false,
            )),
            "slog_set_service" if args.len() == 1 => Some((
                "mako_native_slog_set_service_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "uuid_variant" if args.len() == 1 => Some((
                "mako_native_uuid_variant_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "unicode_to_lower" if args.len() == 1 => Some((
                "mako_native_unicode_to_lower",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_trunc_day" if args.len() == 1 => Some((
                "mako_native_time_trunc_day",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "ws_send_text" if args.len() == 2 => Some((
                "mako_native_ws_send_text_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sql_rows_cols" if args.len() == 1 => Some((
                "mako_native_sql_rows_cols",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_prepare" if args.len() == 2 => Some((
                "mako_native_sql_prepare_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_dialog_id" if args.len() == 3 => Some((
                "mako_native_sip_dialog_id_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "reflect_value_schema" if args.len() == 1 => Some((
                "mako_native_reflect_value_schema",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "reflect_value_from_2_int" if args.len() == 3 => Some((
                "mako_native_reflect_value_from_2_int_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "reflect_value_from_2" if args.len() == 3 => Some((
                "mako_native_reflect_value_from_2_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "quic_has_crypto" if args.len() == 1 => Some((
                "mako_native_quic_has_crypto_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_ack_delay" if args.len() == 1 => Some((
                "mako_native_quic_ack_delay_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "predict_new" if args.len() == 1 => Some((
                "mako_native_predict_new",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "pg_ok" if args.len() == 1 => Some((
                "mako_native_pg_ok",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mysql_ok" if args.len() == 1 => Some((
                "mako_native_mysql_ok",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pcache_get" if args.len() == 2 => Some((
                "mako_native_pcache_get",
                &[Type::Opaque, Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "page_size" if args.len() == 1 => Some((
                "mako_native_page_size",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mmap_size" if args.len() == 1 => Some((
                "mako_native_mmap_size",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "nghttp2_get" if args.len() == 4 => Some((
                "mako_native_nghttp2_get_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "http2_stream_half_closed_local" if args.len() == 1 => Some((
                "mako_native_http2_stream_half_closed_local",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http2_schedule_next" if args.is_empty() => Some((
                "mako_native_http2_schedule_next",
                &[],
                Some(Type::I64),
                false,
            )),
            "hot_reload_plugin_poll" if args.is_empty() => Some((
                "mako_native_hot_reload_plugin_poll",
                &[],
                Some(Type::I64),
                false,
            )),
            "hindex_put" if args.len() == 3 => Some((
                "mako_native_hindex_put",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "frame_reset" if args.len() == 1 => Some((
                "mako_native_frame_reset",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "fn_drop" if args.len() == 1 => Some((
                "mako_native_fn_drop",
                &[Type::FnPtr],
                Some(Type::I64),
                false,
            )),
            "tls_hs_is_app" if args.is_empty() => Some((
                "mako_native_tls_hs_is_app",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_server_new_mtls" if args.len() == 3 => Some((
                "mako_native_tls_server_new_mtls_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "tls_server_sni_update" if args.len() == 4 => Some((
                "mako_native_tls_server_sni_update_ptr",
                &[Type::Opaque, Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_isatty" if args.len() == 1 => Some((
                "mako_native_syscall_isatty",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "buf_reader_new" if args.len() == 1 => Some((
                "mako_native_buf_reader_new_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "bytes_buffer" if args.is_empty() => Some((
                "mako_native_bytes_buffer",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "det_rng_next" if args.len() == 1 => Some((
                "mako_native_det_rng_next",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "game_udp_bind" if args.len() == 1 => Some((
                "mako_native_game_udp_bind",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "cbor_decode_array_int" if args.len() == 1 => Some((
                "mako_native_cbor_decode_array_int_ptr",
                &[Type::Str],
                Some(Type::IntSlice),
                true,
            )),
            "gob_decode_map_ss" if args.len() == 1 => Some((
                "mako_native_gob_decode_map_ss_ptr",
                &[Type::Str],
                Some(Type::MapSS),
                true,
            )),
            "chan_select_value_str" if args.is_empty() => Some((
                "mako_native_chan_select_value_str",
                &[],
                Some(Type::Str),
                true,
            )),
            "hot_reload_status_json" if args.is_empty() => Some((
                "mako_native_hot_reload_status_json",
                &[],
                Some(Type::Str),
                true,
            )),
            "hot_reload_plugin_handle" if args.is_empty() => Some((
                "mako_native_hot_reload_plugin_handle",
                &[],
                Some(Type::I64),
                false,
            )),
            "buf_writer_new" if args.len() == 1 => Some((
                "mako_native_buf_writer_new_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "mmap_create" if args.len() == 2 => Some((
                "mako_native_mmap_create_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "page_alloc" if args.len() == 1 => Some((
                "mako_native_page_alloc",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "pcache_new" if args.is_empty() => Some((
                "mako_native_pcache_new",
                &[],
                Some(Type::Opaque),
                false,
            )),
            "hindex_new" if args.len() == 1 => Some((
                "mako_native_hindex_new",
                &[Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "pg_connect" if args.len() == 1 => Some((
                "mako_native_pg_connect_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "mysql_connect" if args.len() == 1 => Some((
                "mako_native_mysql_connect_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "smtp_send_starttls" if args.len() == 7 => Some((
                "mako_native_smtp_send_starttls_ptr",
                &[
                    Type::Str,
                    Type::I64,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                    Type::Str,
                ],
                Some(Type::I64),
                false,
            )),
            "evloop_shutdown" if args.len() == 1 => Some((
                "mako_native_evloop_shutdown",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "fn_has_env" if args.len() == 1 => Some((
                "mako_native_fn_has_env",
                &[Type::FnPtr],
                Some(Type::I64),
                false,
            )),
            "cbor_encode_array_int" if args.len() == 1 => Some((
                "mako_native_cbor_encode_array_int_ptr",
                &[Type::IntSlice],
                Some(Type::Str),
                true,
            )),
            "reflect_value_equal" if args.len() == 2 => Some((
                "mako_native_reflect_value_equal",
                &[Type::Opaque, Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "reflect_value_field_at" if args.len() == 2 => Some((
                "mako_native_reflect_value_field_at",
                &[Type::Opaque, Type::I64],
                Some(Type::Str),
                true,
            )),
            "debug_break_reset" if args.is_empty() => Some((
                "mako_native_debug_break_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "game_udp_fd" if args.len() == 1 => Some((
                "mako_native_game_udp_fd",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gob_encode_struct" if args.len() == 1 => Some((
                "mako_native_gob_encode_struct",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "hindex_get" if args.len() == 2 => Some((
                "mako_native_hindex_get",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "hot_reload_plugin_close" if args.is_empty() => Some((
                "mako_native_hot_reload_plugin_close",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_push_promise_frame" if args.len() == 4 => Some((
                "mako_native_http2_push_promise_frame_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mmap_write" if args.len() == 3 => Some((
                "mako_native_mmap_write_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "mysql_close" if args.len() == 1 => Some((
                "mako_native_mysql_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "nghttp2_post" if args.len() == 5 => Some((
                "mako_native_nghttp2_post_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "obj_pool_new" if args.len() == 1 => Some((
                "mako_native_obj_pool_new",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "page_write" if args.len() == 3 => Some((
                "mako_native_page_write_ptr",
                &[Type::Opaque, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "pg_exec" if args.len() == 2 => Some((
                "mako_native_pg_exec_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "predict_state" if args.len() == 1 => Some((
                "mako_native_predict_state",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "quic_ack_range_count" if args.len() == 1 => Some((
                "mako_native_quic_ack_range_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_crypto_offset" if args.len() == 1 => Some((
                "mako_native_quic_crypto_offset_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_txn_key" if args.len() == 2 => Some((
                "mako_native_sip_txn_key_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "slog_with3" if args.len() == 8 => Some((
                "mako_native_slog_with3_ptr",
                &[
                    Type::Str, Type::Str, Type::Str, Type::Str, Type::Str, Type::Str,
                    Type::Str, Type::Str,
                ],
                None,
                false,
            )),
            "sql_rows_next" if args.len() == 1 => Some((
                "mako_native_sql_rows_next",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_stmt_query_int" if args.len() == 2 => Some((
                "mako_native_sql_stmt_query_int_ptr",
                &[Type::I64, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "syscall_umask" if args.len() == 1 => Some((
                "mako_native_syscall_umask",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_trunc_hour" if args.len() == 1 => Some((
                "mako_native_time_trunc_hour",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_hs_advance" if args.len() == 1 => Some((
                "mako_native_tls_hs_advance_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_server_sni_remove" if args.len() == 2 => Some((
                "mako_native_tls_server_sni_remove_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "unicode_to_upper" if args.len() == 1 => Some((
                "mako_native_unicode_to_upper",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "uuid_string_upper" if args.len() == 1 => Some((
                "mako_native_uuid_string_upper_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "ws_send_binary" if args.len() == 2 => Some((
                "mako_native_ws_send_binary_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "det_rng_range" if args.len() == 2 => Some((
                "mako_native_det_rng_range",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "context_with_timeout" if args.len() == 1 => Some((
                "mako_native_context_with_timeout",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "uuid_urn" if args.len() == 1 => Some((
                "mako_native_uuid_urn_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sleep_ns" if args.len() == 1 => Some((
                "mako_native_sleep_ns",
                &[Type::I64],
                None,
                false,
            )),
            "mmap_open" if args.len() == 2 => Some((
                "mako_native_mmap_open_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Opaque),
                false,
            )),
            "sql_query_rows_str" if args.len() == 3 => Some((
                "mako_native_sql_query_rows_str_ptr",
                &[Type::Opaque, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sql_stmt_exec" if args.len() == 2 => Some((
                "mako_native_sql_stmt_exec_ptr",
                &[Type::I64, Type::IntSlice],
                Some(Type::I64),
                false,
            )),
            "pg_exec_row_count" if args.len() == 2 => Some((
                "mako_native_pg_exec_row_count_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "redis_connect" if args.len() == 1 => Some((
                "mako_native_redis_connect_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "gob_decode_struct" if args.len() == 1 => Some((
                "mako_native_gob_decode_struct_ptr",
                &[Type::Str],
                Some(Type::Opaque),
                false,
            )),
            "ws_send_pong" if args.len() == 2 => Some((
                "mako_native_ws_send_pong_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "uuid_bytes" if args.len() == 1 => Some((
                "mako_native_uuid_bytes_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_ok" if args.len() == 1 => Some((
                "mako_native_redis_ok",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pg_close" if args.len() == 1 => Some((
                "mako_native_pg_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "uuid_v7" if args.is_empty() => Some((
                "mako_native_uuid_v7",
                &[],
                Some(Type::Str),
                true,
            )),
            "redis_close" if args.len() == 1 => Some((
                "mako_native_redis_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "sql_query_col_str" if args.len() == 3 => Some((
                "mako_native_sql_query_col_str_ptr",
                &[Type::Opaque, Type::Str, Type::I64],
                Some(Type::StrSlice),
                true,
            )),
            "uuid_cmp" if args.len() == 2 => Some((
                "mako_native_uuid_cmp_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "duration_ms" if args.len() == 1 => Some((
                "mako_native_duration_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_trap_enabled" if args.is_empty() => Some((
                "mako_native_debug_trap_enabled",
                &[],
                Some(Type::I64),
                false,
            )),
            "context_remaining" if args.len() == 1 => Some((
                "mako_native_context_remaining",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "detached_join_all" if args.is_empty() => Some((
                "mako_native_detached_join_all",
                &[],
                None,
                false,
            )),
            "alloc_high_bytes" if args.is_empty() => Some((
                "mako_native_alloc_high_bytes",
                &[],
                Some(Type::I64),
                false,
            )),
            "alloc_live_bytes" if args.is_empty() => Some((
                "mako_native_alloc_live_bytes",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_last_opcode" if args.is_empty() => Some((
                "mako_native_ws_last_opcode",
                &[],
                Some(Type::I64),
                false,
            )),
            "uuid_from_bytes" if args.len() == 1 => Some((
                "mako_native_uuid_from_bytes_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_conn_ping" if args.len() == 1 => Some((
                "mako_native_redis_conn_ping",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "sql_query_col_int" if args.len() == 3 => Some((
                "mako_native_sql_query_col_int_ptr",
                &[Type::Opaque, Type::Str, Type::I64],
                Some(Type::IntSlice),
                true,
            )),
            "debug_bp" if args.len() == 1 => Some((
                "mako_native_debug_bp",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_bp_disable" if args.len() == 1 => Some((
                "mako_native_debug_bp_disable",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_bp_enable" if args.len() == 1 => Some((
                "mako_native_debug_bp_enable",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_break" if args.len() == 1 => Some((
                "mako_native_debug_break_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "debug_break_hits" if args.is_empty() => Some((
                "mako_native_debug_break_hits",
                &[],
                Some(Type::I64),
                false,
            )),
            "debug_current_task" if args.is_empty() => Some((
                "mako_native_debug_current_task",
                &[],
                Some(Type::I64),
                false,
            )),
            "debug_frame_depth" if args.is_empty() => Some((
                "mako_native_debug_frame_depth",
                &[],
                Some(Type::I64),
                false,
            )),
            "debug_frames_json" if args.is_empty() => Some((
                "mako_native_debug_frames_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "debug_get_int" if args.len() == 1 => Some((
                "mako_native_debug_get_int_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "debug_line_bp_clear" if args.len() == 1 => Some((
                "mako_native_debug_line_bp_clear",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_line_bp_hits" if args.len() == 1 => Some((
                "mako_native_debug_line_bp_hits",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_line_bp_set" if args.len() == 2 => Some((
                "mako_native_debug_line_bp_set_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_locals_json" if args.is_empty() => Some((
                "mako_native_debug_locals_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "debug_pop_frame" if args.is_empty() => Some((
                "mako_native_debug_pop_frame",
                &[],
                Some(Type::I64),
                false,
            )),
            "debug_push_frame" if args.len() == 3 => Some((
                "mako_native_debug_push_frame_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "debug_set_current_task" if args.len() == 1 => Some((
                "mako_native_debug_set_current_task",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_set_int" if args.len() == 2 => Some((
                "mako_native_debug_set_int_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "debug_snapshot_json" if args.is_empty() => Some((
                "mako_native_debug_snapshot_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "debug_trap_enable" if args.len() == 1 => Some((
                "mako_native_debug_trap_enable",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "duration_minutes" if args.len() == 1 => Some((
                "mako_native_duration_minutes",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "duration_seconds" if args.len() == 1 => Some((
                "mako_native_duration_seconds",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "duration_string" if args.len() == 1 => Some((
                "mako_native_duration_string_ptr",
                &[Type::I64],
                Some(Type::Str),
                true,
            )),
            "duration_to_hours" if args.len() == 1 => Some((
                "mako_native_duration_to_hours",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "duration_to_minutes" if args.len() == 1 => Some((
                "mako_native_duration_to_minutes",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "duration_to_seconds" if args.len() == 1 => Some((
                "mako_native_duration_to_seconds",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "env_get" if args.len() == 1 => Some((
                "mako_native_env_get_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "file_exists" if args.len() == 1 => Some((
                "mako_native_file_exists_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "game_udp_close" if args.len() == 1 => Some((
                "mako_native_game_udp_close",
                &[Type::Opaque],
                None,
                false,
            )),
            "game_udp_send_to" if args.len() == 4 => Some((
                "mako_native_game_udp_send_to_ptr",
                &[Type::Opaque, Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "game_udp_sender_addr" if args.len() == 1 => Some((
                "mako_native_game_udp_sender_addr_ptr",
                &[Type::Opaque],
                Some(Type::Str),
                true,
            )),
            "gfx_window_fill" if args.len() == 2 => Some((
                "mako_native_gfx_window_fill",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gfx_window_get_pixel" if args.len() == 3 => Some((
                "mako_native_gfx_window_get_pixel",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "gfx_window_pixels" if args.len() == 1 => Some((
                "mako_native_gfx_window_pixels",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "gfx_window_set_pixel" if args.len() == 4 => Some((
                "mako_native_gfx_window_set_pixel",
                &[Type::Opaque, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "h3_stream_read" if args.len() == 2 => Some((
                "mako_native_h3_stream_read_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "hindex_del" if args.len() == 2 => Some((
                "mako_native_hindex_del",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "hindex_free" if args.len() == 1 => Some((
                "mako_native_hindex_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "hindex_len" if args.len() == 1 => Some((
                "mako_native_hindex_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "http2_is_push_promise" if args.len() == 1 => Some((
                "mako_native_http2_is_push_promise_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_next_ready_stream" if args.is_empty() => Some((
                "mako_native_http2_next_ready_stream",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_push_promise_stream" if args.len() == 1 => Some((
                "mako_native_http2_push_promise_stream_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "http2_ready_streams" if args.is_empty() => Some((
                "mako_native_http2_ready_streams",
                &[],
                Some(Type::I64),
                false,
            )),
            "http2_stream_take" if args.len() == 1 => Some((
                "mako_native_http2_stream_take",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "http_decode_chunked" if args.len() == 1 => Some((
                "mako_native_http_decode_chunked_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "jpeg_has_sof0" if args.len() == 1 => Some((
                "mako_native_jpeg_has_sof0_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "log_debug" if args.len() == 1 => Some((
                "mako_native_log_debug_ptr",
                &[Type::Str],
                None,
                false,
            )),
            "mmap_close" if args.len() == 1 => Some((
                "mako_native_mmap_close",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mmap_read" if args.len() == 3 => Some((
                "mako_native_mmap_read_ptr",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "mmap_sync" if args.len() == 2 => Some((
                "mako_native_mmap_sync",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "mvcc_gc" if args.len() == 2 => Some((
                "mako_native_mvcc_gc",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "mvcc_live" if args.len() == 1 => Some((
                "mako_native_mvcc_live",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "mysql_connect_url" if args.len() == 1 => Some((
                "mako_native_mysql_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "mysql_driver_name" if args.len() == 1 => Some((
                "mako_native_mysql_driver_name_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "mysql_is_mariadb" if args.len() == 1 => Some((
                "mako_native_mysql_is_mariadb_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "nghttp2_get_two" if args.len() == 5 => Some((
                "mako_native_nghttp2_get_two_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "now_ms" if args.is_empty() => Some((
                "mako_native_now_ms",
                &[],
                Some(Type::I64),
                false,
            )),
            "obj_acquire" if args.len() == 1 => Some((
                "mako_native_obj_acquire",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "obj_available" if args.len() == 1 => Some((
                "mako_native_obj_available",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "obj_pool_cap" if args.len() == 1 => Some((
                "mako_native_obj_pool_cap",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "obj_release" if args.len() == 2 => Some((
                "mako_native_obj_release",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "page_free" if args.len() == 1 => Some((
                "mako_native_page_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "page_read" if args.len() == 3 => Some((
                "mako_native_page_read_ptr",
                &[Type::Opaque, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "path_join" if args.len() == 2 => Some((
                "mako_native_path_join_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "pcache_free" if args.len() == 1 => Some((
                "mako_native_pcache_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pcache_hits" if args.len() == 1 => Some((
                "mako_native_pcache_hits",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pcache_misses" if args.len() == 1 => Some((
                "mako_native_pcache_misses",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "pg_connect_url" if args.len() == 1 => Some((
                "mako_native_pg_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "predict_free" if args.len() == 1 => Some((
                "mako_native_predict_free",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "predict_input" if args.len() == 2 => Some((
                "mako_native_predict_input",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "predict_reconcile" if args.len() == 2 => Some((
                "mako_native_predict_reconcile",
                &[Type::Opaque, Type::I64],
                Some(Type::I64),
                false,
            )),
            "predict_tick" if args.len() == 1 => Some((
                "mako_native_predict_tick",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "quic_ack_first_range" if args.len() == 1 => Some((
                "mako_native_quic_ack_first_range_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_ack_smallest" if args.len() == 1 => Some((
                "mako_native_quic_ack_smallest_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_crypto_data" if args.len() == 1 => Some((
                "mako_native_quic_crypto_data_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_crypto_data_len" if args.len() == 1 => Some((
                "mako_native_quic_crypto_data_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_crypto_data_offset" if args.len() == 1 => Some((
                "mako_native_quic_crypto_data_offset_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_crypto_frame" if args.len() == 2 => Some((
                "mako_native_quic_crypto_frame_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_is_stream" if args.len() == 1 => Some((
                "mako_native_quic_is_stream_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_stream_data" if args.len() == 1 => Some((
                "mako_native_quic_stream_data_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "quic_stream_data_len" if args.len() == 1 => Some((
                "mako_native_quic_stream_data_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_stream_fin" if args.len() == 1 => Some((
                "mako_native_quic_stream_fin_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_stream_frame" if args.len() == 4 => Some((
                "mako_native_quic_stream_frame_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "quic_stream_id_of" if args.len() == 1 => Some((
                "mako_native_quic_stream_id_of_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "quic_stream_offset" if args.len() == 1 => Some((
                "mako_native_quic_stream_offset_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "read_file" if args.len() == 1 => Some((
                "mako_native_read_file_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "redis_connect_url" if args.len() == 1 => Some((
                "mako_native_redis_connect_url_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "remove_file" if args.len() == 1 => Some((
                "mako_native_remove_file_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "replay_append" if args.len() == 3 => Some((
                "mako_native_replay_append_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "replay_input" if args.len() == 2 => Some((
                "mako_native_replay_input_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "rtp_header_len" if args.is_empty() => Some((
                "mako_native_rtp_header_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "rtp_marker" if args.len() == 1 => Some((
                "mako_native_rtp_marker_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "rtp_parse_ok" if args.len() == 1 => Some((
                "mako_native_rtp_parse_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "rtp_payload" if args.len() == 1 => Some((
                "mako_native_rtp_payload_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "rtp_payload_type" if args.len() == 1 => Some((
                "mako_native_rtp_payload_type_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "rtp_timestamp" if args.len() == 1 => Some((
                "mako_native_rtp_timestamp_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "rtp_version" if args.len() == 1 => Some((
                "mako_native_rtp_version_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "rune_count" if args.len() == 1 => Some((
                "mako_native_rune_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sdp_append_line" if args.len() == 2 => Some((
                "mako_native_sdp_append_line_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_attr_candidate" if args.len() == 7 => Some((
                "mako_native_sdp_attr_candidate_ptr",
                &[Type::I64, Type::I64, Type::Str, Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_attr_fmtp" if args.len() == 2 => Some((
                "mako_native_sdp_attr_fmtp_ptr",
                &[Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_attr_rtpmap" if args.len() == 3 => Some((
                "mako_native_sdp_attr_rtpmap_ptr",
                &[Type::I64, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sdp_build_av" if args.len() == 7 => Some((
                "mako_native_sdp_build_av_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_connection" if args.len() == 1 => Some((
                "mako_native_sdp_connection_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_connection_is_ip6" if args.len() == 1 => Some((
                "mako_native_sdp_connection_is_ip6_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sdp_media_connection_addr" if args.len() == 2 => Some((
                "mako_native_sdp_media_connection_addr_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sdp_media_direction" if args.len() == 2 => Some((
                "mako_native_sdp_media_direction_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sdp_media_formats" if args.len() == 2 => Some((
                "mako_native_sdp_media_formats_ptr",
                &[Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sdp_origin_addr" if args.len() == 1 => Some((
                "mako_native_sdp_origin_addr_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_replace_connection_addr" if args.len() == 2 => Some((
                "mako_native_sdp_replace_connection_addr_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_replace_media_port" if args.len() == 3 => Some((
                "mako_native_sdp_replace_media_port_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sdp_set_media_direction" if args.len() == 3 => Some((
                "mako_native_sdp_set_media_direction_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sdp_timing" if args.len() == 1 => Some((
                "mako_native_sdp_timing_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "simd_dot_i64_4" if args.len() == 8 => Some((
                "mako_native_simd_dot_i64_4",
                &[Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "simd_sum_i64_4" if args.len() == 4 => Some((
                "mako_native_simd_sum_i64_4",
                &[Type::I64, Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_auth_param" if args.len() == 2 => Some((
                "mako_native_sip_auth_param_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_authorization_digest" if args.len() == 5 => Some((
                "mako_native_sip_authorization_digest_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_body_view" if args.len() == 1 => Some((
                "mako_native_sip_body_view_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_header_ci_eq" if args.len() == 3 => Some((
                "mako_native_sip_header_ci_eq_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_header_contains" if args.len() == 3 => Some((
                "mako_native_sip_header_contains_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_header_eq" if args.len() == 3 => Some((
                "mako_native_sip_header_eq_ptr",
                &[Type::Str, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_header_view" if args.len() == 2 => Some((
                "mako_native_sip_header_view_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_insert_via" if args.len() == 2 => Some((
                "mako_native_sip_insert_via_ptr",
                &[Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_method_eq" if args.len() == 2 => Some((
                "mako_native_sip_method_eq_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_method_view" if args.len() == 1 => Some((
                "mako_native_sip_method_view_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_msg_fix_top_via" if args.len() == 3 => Some((
                "mako_native_sip_msg_fix_top_via_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sip_msg_response_host" if args.len() == 1 => Some((
                "mako_native_sip_msg_response_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_msg_response_port" if args.len() == 1 => Some((
                "mako_native_sip_msg_response_port_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_record_route" if args.len() == 3 => Some((
                "mako_native_sip_record_route_ptr",
                &[Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_reply_with_to_tag" if args.len() == 6 => Some((
                "mako_native_sip_reply_with_to_tag_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str, Type::Str, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_udp_bind" if args.len() == 2 => Some((
                "mako_native_sip_udp_bind_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sip_udp_recv" if args.len() == 2 => Some((
                "mako_native_sip_udp_recv_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sip_udp_send" if args.len() == 4 => Some((
                "mako_native_sip_udp_send_ptr",
                &[Type::I64, Type::Str, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_via_fix_source" if args.len() == 3 => Some((
                "mako_native_sip_via_fix_source_ptr",
                &[Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sip_via_has_rport" if args.len() == 1 => Some((
                "mako_native_sip_via_has_rport_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_via_host" if args.len() == 1 => Some((
                "mako_native_sip_via_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_maddr" if args.len() == 1 => Some((
                "mako_native_sip_via_maddr_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_port" if args.len() == 1 => Some((
                "mako_native_sip_via_port_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_via_received" if args.len() == 1 => Some((
                "mako_native_sip_via_received_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_response_addr" if args.len() == 1 => Some((
                "mako_native_sip_via_response_addr_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_response_host" if args.len() == 1 => Some((
                "mako_native_sip_via_response_host_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_response_port" if args.len() == 1 => Some((
                "mako_native_sip_via_response_port_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_via_rport" if args.len() == 1 => Some((
                "mako_native_sip_via_rport_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_via_transport" if args.len() == 1 => Some((
                "mako_native_sip_via_transport_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_via_value_nat" if args.len() == 6 => Some((
                "mako_native_sip_via_value_nat_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::Str, Type::Str, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sip_via_value_rport" if args.len() == 4 => Some((
                "mako_native_sip_via_value_rport_ptr",
                &[Type::Str, Type::Str, Type::I64, Type::Str],
                Some(Type::Str),
                true,
            )),
            "sip_view_ci_eq" if args.len() == 1 => Some((
                "mako_native_sip_view_ci_eq_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_view_contains" if args.len() == 1 => Some((
                "mako_native_sip_view_contains_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_view_copy" if args.is_empty() => Some((
                "mako_native_sip_view_copy_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "sip_view_eq" if args.len() == 1 => Some((
                "mako_native_sip_view_eq_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "sip_view_len" if args.is_empty() => Some((
                "mako_native_sip_view_len",
                &[],
                Some(Type::I64),
                false,
            )),
            "sip_view_offset" if args.is_empty() => Some((
                "mako_native_sip_view_offset",
                &[],
                Some(Type::I64),
                false,
            )),
            "sleep_ms" if args.len() == 1 => Some((
                "mako_native_sleep_ms",
                &[Type::I64],
                None,
                false,
            )),
            "slog_flush" if args.is_empty() => Some((
                "mako_native_slog_flush",
                &[],
                None,
                false,
            )),
            "slog_set_output" if args.len() == 1 => Some((
                "mako_native_slog_set_output_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "slog_with_int" if args.len() == 4 => Some((
                "mako_native_slog_with_int_ptr",
                &[Type::Str, Type::Str, Type::Str, Type::I64],
                None,
                false,
            )),
            "snap_count" if args.len() == 1 => Some((
                "mako_native_snap_count_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "snap_encode2" if args.len() == 2 => Some((
                "mako_native_snap_encode2_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "snap_predict" if args.len() == 2 => Some((
                "mako_native_snap_predict",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "snap_reconcile" if args.len() == 2 => Some((
                "mako_native_snap_reconcile",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_rows_close" if args.len() == 1 => Some((
                "mako_native_sql_rows_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_rows_int" if args.len() == 2 => Some((
                "mako_native_sql_rows_int",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "sql_rows_str" if args.len() == 2 => Some((
                "mako_native_sql_rows_str_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "sql_stmt_close" if args.len() == 1 => Some((
                "mako_native_sql_stmt_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "store_len" if args.len() == 1 => Some((
                "mako_native_store_len",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "store_rollback" if args.len() == 1 => Some((
                "mako_native_store_rollback",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "str_eq" if args.len() == 2 => Some((
                "mako_native_str_eq_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "str_len" if args.len() == 1 => Some((
                "mako_native_str_len_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "syscall_dup" if args.len() == 1 => Some((
                "mako_native_syscall_dup",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "task_done" if args.len() == 1 => Some((
                "mako_native_task_done",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "task_id" if args.len() == 1 => Some((
                "mako_native_task_id",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "task_joined" if args.len() == 1 => Some((
                "mako_native_task_joined",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tasks_inspect_json" if args.is_empty() => Some((
                "mako_native_tasks_inspect_json_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "tcp_accept4" if args.len() == 1 => Some((
                "mako_native_tcp_accept4",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_connect_check" if args.len() == 1 => Some((
                "mako_native_tcp_connect_check",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_connect_nb" if args.len() == 2 => Some((
                "mako_native_tcp_connect_nb_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_connect_wait" if args.len() == 2 => Some((
                "mako_native_tcp_connect_wait",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_fd_copy" if args.len() == 3 => Some((
                "mako_native_tcp_fd_copy",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_listen_reuseport" if args.len() == 3 => Some((
                "mako_native_tcp_listen_reuseport_ptr",
                &[Type::Str, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_nodelay" if args.len() == 1 => Some((
                "mako_native_tcp_nodelay",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_reuseport" if args.len() == 1 => Some((
                "mako_native_tcp_reuseport",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_set_recv_buf" if args.len() == 2 => Some((
                "mako_native_tcp_set_recv_buf",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_set_send_buf" if args.len() == 2 => Some((
                "mako_native_tcp_set_send_buf",
                &[Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "tcp_splice" if args.len() == 3 => Some((
                "mako_native_tcp_splice",
                &[Type::I64, Type::I64, Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_local_offset_sec" if args.is_empty() => Some((
                "mako_native_time_local_offset_sec",
                &[],
                Some(Type::I64),
                false,
            )),
            "time_since_ms" if args.len() == 1 => Some((
                "mako_native_time_since_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "time_until_ms" if args.len() == 1 => Some((
                "mako_native_time_until_ms",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "tls_encrypted_extensions" if args.is_empty() => Some((
                "mako_native_tls_encrypted_extensions_ptr",
                &[],
                Some(Type::Str),
                true,
            )),
            "tls_finished" if args.len() == 1 => Some((
                "mako_native_tls_finished_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_handshake_step" if args.len() == 1 => Some((
                "mako_native_tls_handshake_step",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_certificate" if args.len() == 1 => Some((
                "mako_native_tls_hs_session_certificate_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_certificate_verify" if args.len() == 2 => Some((
                "mako_native_tls_hs_session_certificate_verify_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_client_hello" if args.len() == 1 => Some((
                "mako_native_tls_hs_session_client_hello_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_encrypted_extensions" if args.is_empty() => Some((
                "mako_native_tls_hs_session_encrypted_extensions",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_finished" if args.len() == 2 => Some((
                "mako_native_tls_hs_session_finished_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_finished_hex" if args.len() == 1 => Some((
                "mako_native_tls_hs_session_finished_hex_ptr",
                &[Type::Str],
                Some(Type::Str),
                true,
            )),
            "tls_hs_session_reset" if args.is_empty() => Some((
                "mako_native_tls_hs_session_reset",
                &[],
                Some(Type::I64),
                false,
            )),
            "tls_hs_session_server_hello" if args.len() == 1 => Some((
                "mako_native_tls_hs_session_server_hello_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "tls_is_init_finished" if args.len() == 1 => Some((
                "mako_native_tls_is_init_finished",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "tls_want_read" if args.len() == 1 => Some((
                "mako_native_tls_want_read",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "ulid_parse_ok" if args.len() == 1 => Some((
                "mako_native_ulid_parse_ok_ptr",
                &[Type::Str],
                Some(Type::I64),
                false,
            )),
            "unicode_is" if args.len() == 2 => Some((
                "mako_native_unicode_is_ptr",
                &[Type::Str, Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_simple_fold" if args.len() == 1 => Some((
                "mako_native_unicode_simple_fold",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "unicode_to_title" if args.len() == 1 => Some((
                "mako_native_unicode_to_title",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "wal_append" if args.len() == 2 => Some((
                "mako_native_wal_append_ptr",
                &[Type::Opaque, Type::Str],
                Some(Type::I64),
                false,
            )),
            "wal_next_off" if args.is_empty() => Some((
                "mako_native_wal_next_off",
                &[],
                Some(Type::I64),
                false,
            )),
            "wal_read_at" if args.len() == 2 => Some((
                "mako_native_wal_read_at_ptr",
                &[Type::Opaque, Type::I64],
                Some(Type::Str),
                true,
            )),
            "wal_size" if args.len() == 1 => Some((
                "mako_native_wal_size",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "wal_sync" if args.len() == 1 => Some((
                "mako_native_wal_sync",
                &[Type::Opaque],
                Some(Type::I64),
                false,
            )),
            "write_file" if args.len() == 2 => Some((
                "mako_native_write_file_ptr",
                &[Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_connect" if args.len() == 4 => Some((
                "mako_native_ws_client_connect_ptr",
                &[Type::Str, Type::I64, Type::Str, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_recv" if args.len() == 2 => Some((
                "mako_native_ws_client_recv_ptr",
                &[Type::I64, Type::I64],
                Some(Type::Str),
                true,
            )),
            "ws_client_send_binary" if args.len() == 2 => Some((
                "mako_native_ws_client_send_binary_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_send_close" if args.len() == 3 => Some((
                "mako_native_ws_client_send_close_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_send_ping" if args.len() == 2 => Some((
                "mako_native_ws_client_send_ping_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_client_send_text" if args.len() == 2 => Some((
                "mako_native_ws_client_send_text_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_close" if args.len() == 1 => Some((
                "mako_native_ws_close",
                &[Type::I64],
                Some(Type::I64),
                false,
            )),
            "ws_last_close_code" if args.is_empty() => Some((
                "mako_native_ws_last_close_code",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_last_fin" if args.is_empty() => Some((
                "mako_native_ws_last_fin",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_last_status" if args.is_empty() => Some((
                "mako_native_ws_last_status",
                &[],
                Some(Type::I64),
                false,
            )),
            "ws_send_close" if args.len() == 3 => Some((
                "mako_native_ws_send_close_ptr",
                &[Type::I64, Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            "ws_send_ping" if args.len() == 2 => Some((
                "mako_native_ws_send_ping_ptr",
                &[Type::I64, Type::Str],
                Some(Type::I64),
                false,
            )),
            _ => None,
        };
        let Some((cname, param_tys, ret_ty, ret_owned)) = spec else {
            return Ok(None);
        };
        if param_tys.len() != args.len() {
            return Err(IrError::new(format!(
                "native IR: {function} arity mismatch"
            )));
        }
        let mut lowered = Vec::with_capacity(args.len());
        let mut temps = Vec::new();
        for (arg, &expected) in args.iter().zip(param_tys.iter()) {
            let (v, actual, owned) = self.lower_expr(arg)?;
            // ChanI/ChanS/ChanP share the same runtime handle ABI (i64 pointer).
            let chan_alias = matches!(
                (expected, actual),
                (Type::ChanI, Type::ChanS)
                    | (Type::ChanS, Type::ChanI)
                    | (Type::ChanI, Type::ChanP(_))
                    | (Type::ChanP(_), Type::ChanI)
                    | (Type::ChanS, Type::ChanP(_))
                    | (Type::ChanP(_), Type::ChanS)
                    | (Type::Opaque, Type::I64)
                    | (Type::I64, Type::Opaque)
            )
                || matches!((expected, actual), (Type::I64, Type::FnPtr) | (Type::FnPtr, Type::I64))
                || (expected == Type::I64 && actual.is_ptr_sized() && actual != Type::F64)
                || (expected == Type::Opaque && actual.is_ptr_sized() && actual != Type::F64);
            if actual != expected
                && !(expected == Type::I64 && actual == Type::I1)
                && !chan_alias
            {
                return Err(IrError::new(format!(
                    "native IR: {function} argument type mismatch"
                )));
            }
            lowered.push(v);
            if owned {
                temps.push((v, actual));
            }
        }
        // APIs that free/take the first arg's heap header (or move it into the
        // result). Clear ownership of a bare local first arg so end-of-scope
        // does not double-free.
        let consumes_first = matches!(
            function,
            "buf_put"
                | "list_push_int"
                | "list_push_str"
                | "list_pop_int"
                | "list_pop_str"
                | "list_insert_int"
                | "list_insert_str"
                | "list_remove_int"
                | "list_remove_str"
                | "list_clear_int"
                | "list_clear_str"
                | "queue_pop_int"
                | "slices_reverse_ints"
                | "slices_reverse_strs"
                | "slices_unique_ints"
                | "slices_unique_strs"
                | "slices_reverse"
                | "slices_unique"
                | "queue_pop_str"
                | "strings_copy"
        );
        // Take-send moves the string (arg 1) into the channel or frees it on
        // failure — never drop it after the call, and clear local ownership.
        // map_si_set_take similarly consumes the key string (arg 1).
        let consumes_str_arg1 = matches!(
            function,
            "chan_str_send_take"
                | "chan_str_try_send_take"
                | "map_si_set_take"
                | "map_ss_set_take"
        );
        // map_ss_set_take also consumes the value string (arg 2).
        let consumes_str_arg2 = matches!(function, "map_ss_set_take");
        if consumes_first {
            if let Some(Expr::Ident(name)) = args.first() {
                self.heap_owned.insert(name.clone(), false);
            }
        }
        if consumes_str_arg1 {
            if let Some(Expr::Ident(name)) = args.get(1) {
                self.heap_owned.insert(name.clone(), false);
            }
        }
        if consumes_str_arg2 {
            if let Some(Expr::Ident(name)) = args.get(2) {
                self.heap_owned.insert(name.clone(), false);
            }
        }
        let out = if ret_ty.is_some() {
            Some(self.value())
        } else {
            None
        };
        self.emit(Inst::Call {
            out,
            function: cname.into(),
            args: lowered,
            ret: ret_ty,
        });
        for (v, t) in temps {
            // Shared pointer handles must not be post-call dropped: callees often
            // store an alias (domain_reg_put, free wrappers already released, …).
            // Identity "clone" means a drop here double-frees the same pointer.
            if t.is_shared_handle() {
                continue;
            }
            // Consuming APIs already took or freed the first-arg header.
            if consumes_first
                && matches!(
                    t,
                    Type::Str | Type::IntSlice | Type::StrSlice | Type::FloatSlice | Type::ByteSlice
                )
            {
                continue;
            }
            // Take-send: string payload is owned by the channel (or freed on fail).
            if consumes_str_arg1 && t == Type::Str {
                continue;
            }
            self.emit_drop(v, t);
        }
        let result = match (out, ret_ty) {
            (Some(o), Some(rt)) => {
                // Bridge bools come back as i64 0/1; coerce for `if`.
                if matches!(
                    function,
                    "regex_match"
                        | "str_has_prefix"
                        | "str_has_suffix"
                        | "json_has"
                        | "http2_detect"
                        | "uuid_parse_ok"
                        | "uuid_eq"
                        | "uuid_is_nil"
                        | "path_is_abs"
                        | "is_dir"
                        | "ints_contains"
                        | "strings_contains"
                        | "cmap_has"
                        | "yaml_get_bool"
                        | "yaml_has"
                        | "zip_deflate_available"
                        | "bytes_is_view"
                        | "http_route_match"
                        | "middleware_require_context"
                        | "middleware_allow_methods"
                        | "middleware_ran"
                        | "reqctx_has"
                ) && rt == Type::I64
                {
                    let b = self.value();
                    self.emit(Inst::Cast {
                        out: b,
                        value: o,
                        from: Type::I64,
                        to: Type::I1,
                    });
                    (b, Type::I1, false)
                } else {
                    (o, rt, ret_owned)
                }
            }
            _ => (self.const_int(0, Type::I64), Type::I64, false),
        };
        Ok(Some(result))
    }

    /// Box a concrete struct value into an opaque interface handle `{tag, data}`.
    fn box_iface(&mut self, value: Value, ty: Type) -> Result<Value, IrError> {
        let Type::Struct(id) = ty else {
            return Err(IrError::new("native IR: box_iface expects struct"));
        };
        let name = self.structs.layout_name(id);
        let tag = self
            .structs
            .concrete_tags
            .get(&name)
            .copied()
            .unwrap_or(0);
        let tag_v = self.const_int(tag, Type::I64);
        // Ensure we pass an owned pointer into the box (clone if needed is on caller).
        let out = self.value();
        self.emit(Inst::Call {
            out: Some(out),
            function: "mako_native_iface_box".into(),
            args: vec![tag_v, value],
            ret: Some(Type::Opaque),
        });
        Ok(out)
    }

    /// Dyn dispatch: unbox iface, call Adder_Concrete_method or Adder_method(self,…).
    fn lower_iface_method(
        &mut self,
        recv: Value,
        rty: Type,
        rowned: bool,
        method: &str,
        args: &[Expr],
    ) -> Result<Option<(Value, Type, bool)>, IrError> {
        // Prefer multi-concrete names Adder_Counter_add; also Adder_add(self: Counter).
        let suffix = format!("_{method}");
        let candidates: Vec<(String, Vec<Type>, Option<Type>)> = self
            .signatures
            .iter()
            .filter(|(name, (params, _))| {
                name.ends_with(&suffix)
                    && (params.len() == args.len() + 1 || params.len() == args.len())
            })
            .map(|(n, (p, r))| (n.clone(), p.clone(), *r))
            .collect();
        if candidates.is_empty() {
            return Ok(None);
        }
        // Prefer methods that take a struct self (params.len() == args+1).
        let with_self: Vec<_> = candidates
            .iter()
            .filter(|(_, p, _)| p.len() == args.len() + 1)
            .cloned()
            .collect();
        if with_self.is_empty() {
            // Unit methods handled by caller.
            return Ok(None);
        }
        // Single candidate: unbox data and call directly.
        if with_self.len() == 1 {
            let (fn_name, params, ret) = with_self.into_iter().next().unwrap();
            let data = self.value();
            self.emit(Inst::Call {
                out: Some(data),
                function: "mako_native_iface_data".into(),
                args: vec![recv],
                ret: Some(Type::Opaque),
            });
            // data is the struct pointer; cast conceptually to Struct type of first param.
            let mut call_args = vec![data];
            let mut temps = Vec::new();
            for (arg, expected) in args.iter().zip(params.iter().skip(1)) {
                let (mut v, t, o) = self.lower_expr(arg)?;
                if t != *expected {
                    return Err(IrError::new("native IR: iface method arg type"));
                }
                if t.is_heap() {
                    if !o {
                        v = self.emit_clone(v, t);
                    }
                    temps.push((v, t));
                } else if o {
                    temps.push((v, t));
                }
                call_args.push(v);
            }
            let out = ret.map(|_| self.value());
            self.emit(Inst::Call {
                out,
                function: fn_name,
                args: call_args,
                ret,
            });
            for (v, t) in temps {
                self.emit_drop(v, t);
            }
            if rowned {
                self.emit_drop(recv, rty);
            }
            return Ok(Some(match (out, ret) {
                (Some(o), Some(r)) => (o, r, r.is_heap()),
                _ => (self.const_int(0, Type::I64), Type::I64, false),
            }));
        }
        // Multi-concrete: branch on tag.
        let tag = self.value();
        self.emit(Inst::Call {
            out: Some(tag),
            function: "mako_native_iface_tag".into(),
            args: vec![recv],
            ret: Some(Type::I64),
        });
        let data = self.value();
        self.emit(Inst::Call {
            out: Some(data),
            function: "mako_native_iface_data".into(),
            args: vec![recv],
            ret: Some(Type::Opaque),
        });
        // Pre-lower args once.
        let mut arg_vals = Vec::new();
        let mut temps = Vec::new();
        // Use first candidate's param types after self for expected types.
        let expected_args = &with_self[0].1[1..];
        for (arg, expected) in args.iter().zip(expected_args.iter()) {
            let (mut v, t, o) = self.lower_expr(arg)?;
            if t != *expected {
                return Err(IrError::new("native IR: iface method arg type"));
            }
            if t.is_heap() {
                if !o {
                    v = self.emit_clone(v, t);
                }
                temps.push((v, t));
            } else if o {
                temps.push((v, t));
            }
            arg_vals.push(v);
        }
        let ret_ty = with_self[0].2;
        let result_slot = self.value();
        self.emit(Inst::Alloca {
            out: result_slot,
            ty: ret_ty.unwrap_or(Type::I64),
        });
        let merge = self.new_block();
        let mut test = self.current;
        for (i, (fn_name, params, ret)) in with_self.iter().enumerate() {
            let Type::Struct(sid) = params[0] else {
                continue;
            };
            let cname = self.structs.layout_name(sid);
            let ctag = self
                .structs
                .concrete_tags
                .get(&cname)
                .copied()
                .unwrap_or(0);
            self.current = test;
            let arm = self.new_block();
            let next = if i + 1 == with_self.len() {
                None
            } else {
                Some(self.new_block())
            };
            let expect = self.const_int(ctag, Type::I64);
            let cmp = self.value();
            self.emit(Inst::Binary {
                out: cmp,
                op: BinOp::Eq,
                left: tag,
                right: expect,
                ty: Type::I64,
            });
            if let Some(n) = next {
                self.terminate(Terminator::Branch {
                    condition: cmp,
                    then_block: arm,
                    else_block: n,
                })?;
                test = n;
            } else {
                self.terminate(Terminator::Jump(arm))?;
            }
            self.current = arm;
            let mut call_args = vec![data];
            call_args.extend(arg_vals.iter().copied());
            let out = ret.map(|_| self.value());
            self.emit(Inst::Call {
                out,
                function: fn_name.clone(),
                args: call_args,
                ret: *ret,
            });
            if let Some(o) = out {
                self.emit(Inst::Store {
                    ptr: result_slot,
                    value: o,
                });
            }
            self.terminate(Terminator::Jump(merge))?;
        }
        self.current = merge;
        for (v, t) in temps {
            self.emit_drop(v, t);
        }
        if rowned {
            self.emit_drop(recv, rty);
        }
        let loaded = self.value();
        let rty_out = ret_ty.unwrap_or(Type::I64);
        self.emit(Inst::Load {
            out: loaded,
            ptr: result_slot,
            ty: rty_out,
        });
        Ok(Some((loaded, rty_out, rty_out.is_heap())))
    }

    /// Emit the type-appropriate drop for an owned heap value.
    fn emit_drop(&mut self, value: Value, ty: Type) {
        match ty {
            Type::Str => self.emit(Inst::DropString { value }),
            Type::IntSlice => self.emit(Inst::DropSlice { value }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: None,
                function: "mako_native_float_slice_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::ByteSlice => self.emit(Inst::Call {
                out: None,
                function: "mako_native_byte_slice_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::BoolSlice => self.emit(Inst::Call {
                out: None,
                function: "mako_native_bool_slice_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::ChanI | Type::ChanS | Type::ChanF | Type::ChanP(_) => self.emit(Inst::Call {
                out: None,
                function: "mako_native_chan_drop".into(),
                args: vec![value],
                ret: None,
            }),
            Type::Arena => self.emit(Inst::Call {
                out: None,
                function: "mako_native_arena_free".into(),
                args: vec![value],
                ret: None,
            }),
            Type::Nursery => self.emit(Inst::Call {
                out: None,
                function: "mako_native_nursery_cancel_join".into(),
                args: vec![value],
                ret: None,
            }),
            Type::Task => self.emit(Inst::Call {
                out: None,
                function: "mako_native_task_drop".into(),
                args: vec![value],
                ret: None,
            }),
            Type::Opaque => {
                // Opaque handles: free if non-null (best-effort).
                self.emit(Inst::Call {
                    out: None,
                    function: "mako_native_opaque_drop".into(),
                    args: vec![value],
                    ret: None,
                });
            }
            Type::StructSlice(_) => self.emit(Inst::Call {
                out: None,
                // Free each calloc'd struct block, then the pointer array.
                function: "mako_native_ptr_slice_drop_free_elems".into(),
                args: vec![value],
                ret: None,
            }),
            Type::StrSlice => self.emit(Inst::DropStrSlice { value }),
            Type::MapII | Type::MapIPtr(_) => self.emit(Inst::Call {
                out: None,
                function: "mako_native_map_ii_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::MapSI | Type::MapSPtr(_) => self.emit(Inst::Call {
                out: None,
                function: "mako_native_map_si_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::PtrSlice(vk) => {
                // []Struct via PtrSlice still free's each calloc block.
                // Everything else is shell-only: channels/opaque are shared
                // handles (must not free), and nested slice/map headers need
                // typed element drops that the C free() loop cannot do.
                // Nested containers may leak element data until a full deep
                // walker lands; correctness > perfect reclaim for now.
                let free_elems = matches!(vk.to_type(), Type::Struct(_));
                self.emit(Inst::Call {
                    out: None,
                    function: if free_elems {
                        "mako_native_ptr_slice_drop_free_elems".into()
                    } else {
                        "mako_native_ptr_slice_drop".into()
                    },
                    args: vec![value],
                    ret: None,
                });
            },
            Type::MapSS => self.emit(Inst::Call {
                out: None,
                function: "mako_native_map_ss_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::MapIF => self.emit(Inst::Call {
                out: None,
                function: "mako_native_map_if_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::MapFI => self.emit(Inst::Call {
                out: None,
                function: "mako_native_map_fi_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::ShareInt => self.emit(Inst::Call {
                out: None,
                function: "mako_native_share_drop_ptr".into(),
                args: vec![value],
                ret: None,
            }),
            Type::Struct(id) => self.emit(Inst::DropStruct {
                value,
                struct_id: id,
            }),
            _ => {}
        }
    }

    /// Take ownership of a value stored into an aggregate field: move an owned
    /// temporary, clone a borrow, and reject an owned scalar (impossible).
    fn own_field_value(
        &mut self,
        value: Value,
        ty: Type,
        owned: bool,
    ) -> Result<Value, IrError> {
        if ty.is_heap() {
            Ok(if owned {
                value
            } else {
                self.emit_clone(value, ty)
            })
        } else if owned {
            Err(IrError::new("native IR: scalar field cannot be owned"))
        } else {
            Ok(value)
        }
    }

    /// Construct a named variant, selecting among Result/Option monomorphs by
    /// payload type when several share the same name (`Ok`, `Err`, `Some`).
    ///
    /// Always probe payload types and run `variant_matching` — even with a single
    /// registered monomorph — so `Some(Point{…})` / `Ok([1,2])` auto-intern the
    /// right layout instead of stuffing Struct/IntSlice into Option[int]'s I64 slot.
    fn build_variant_by_name(
        &mut self,
        name: &str,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        let candidates = self.structs.variants_named(name);
        if candidates.is_empty() {
            return Err(IrError::new(format!("native IR: unknown variant `{name}`")));
        }
        // Nullary unit tags (None, …): no payload to probe; sole monomorph is fine.
        if args.is_empty() {
            if candidates.len() == 1 {
                return self.build_variant(candidates[0], args);
            }
            let info = self.structs.variant_matching(name, &[]).ok_or_else(|| {
                IrError::new(format!("native IR: unknown nullary variant `{name}`"))
            })?;
            return self.build_variant(info, args);
        }
        // Probe payload types (may use wrong monomorphs for nested Some/None).
        let mut probe_tys = Vec::with_capacity(args.len());
        for arg in args {
            let (v, ty, owned) = self.lower_expr(arg)?;
            // Drop probe temps — real values re-lowered with slot expected types.
            if owned {
                self.emit_drop(v, ty);
            }
            probe_tys.push(ty);
        }
        let info = self.structs.variant_matching(name, &probe_tys).ok_or_else(|| {
            IrError::new(format!(
                "native IR: enum payload type mismatch for `{name}` with payload {probe_tys:?}"
            ))
        })?;
        // Rebuild with correct nested monomorphs for each payload slot.
        self.build_variant(info, args)
    }

    /// Construct an enum variant as a heap block `[tag, payload…, 0…]`.
    /// Payload args are lowered against each slot's expected type so nested
    /// `Some(Some(1))` / `Some(None)` pick the right monomorphs.
    fn build_variant(
        &mut self,
        info: VariantInfo,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        if args.len() != info.arity {
            return Err(IrError::new("native IR: enum variant arity mismatch"));
        }
        let mut payload = Vec::with_capacity(args.len());
        for (index, arg) in args.iter().enumerate() {
            let slot_ty = self.structs.field_type(info.enum_id, info.slot_base + index);
            let (value, ty, owned) = self.lower_expr_for_expected(arg, slot_ty)?;
            if ty != slot_ty {
                return Err(IrError::new(format!(
                    "native IR: enum payload type mismatch: got {ty:?}, want {slot_ty:?}"
                )));
            }
            // The enum owns its payload: move an owned temp in, clone a borrow.
            payload.push(self.own_field_value(value, slot_ty, owned)?);
        }
        let out = self.value();
        self.emit(Inst::EnumMake {
            out,
            enum_id: info.enum_id,
            tag: info.tag,
            slot_base: info.slot_base as u32,
            payload,
        });
        Ok((out, Type::Struct(info.enum_id), true))
    }

    /// Normalize a key for MapII/MapIPtr runtime tables (`int64` open-addressing).
    ///
    /// - `bool` / `float` → cast / bit-pattern
    /// - **enum** → tag at field 0 (value equality; unit variants compare equal)
    /// - **struct** → heap pointer (identity; same binding required)
    /// - `int` → as-is
    ///
    /// Owned enum/string temporaries used only for the tag/lookup are dropped
    /// here. Struct keys keep pointer identity and are not dropped (the map
    /// may retain the pointer as the key).
    /// `lib.fn(...)` package call, or `eng.Green(7)` / `eng.Color.Green(4)` variants.
    fn try_lower_pack_method(
        &mut self,
        receiver: &Expr,
        method: &str,
        args: &[Expr],
    ) -> Result<Option<(Value, Type, bool)>, IrError> {
        // eng.table_new() / eng.Green(7)
        // Only treat as pack when the alias looks like an import prefix (has
        // eng__* layouts or eng__fn signatures). Bare unit variants like
        // `Point.area()` must fall through to normal method dispatch.
        if let Expr::Ident(alias) = receiver {
            if !self.locals.contains_key(alias) {
                let mangled = format!("{alias}__{method}");
                if self.signatures.contains_key(&mangled) {
                    return Ok(Some(self.lower_pack_fn_call(&mangled, args)?));
                }
                let pack_prefix = format!("{alias}__");
                let is_pack_alias = self
                    .structs
                    .by_name
                    .keys()
                    .any(|n| n.starts_with(&pack_prefix))
                    || self.signatures.keys().any(|n| n.starts_with(&pack_prefix));
                if is_pack_alias {
                    // Pack enums under this alias (eng__Color, …).
                    let pack_enums: Vec<u32> = self
                        .structs
                        .by_name
                        .iter()
                        .filter(|(n, _)| n.starts_with(&pack_prefix))
                        .filter_map(|(_, &id)| {
                            if self.structs.is_enum(id) {
                                Some(id)
                            } else {
                                None
                            }
                        })
                        .collect();
                    for &eid in &pack_enums {
                        if let Some(info) = self.structs.variant_in_enum(method, eid) {
                            if info.arity == args.len() {
                                return Ok(Some(self.build_variant(info, args)?));
                            }
                        }
                    }
                    // Payload variant shared name: eng.Green(7)
                    let candidates = self.structs.variants_named(method);
                    if let Some(info) = candidates.into_iter().find(|i| i.arity == args.len()) {
                        return Ok(Some(self.build_variant(info, args)?));
                    }
                    return Err(IrError::new(format!(
                        "native IR: unknown pack call `{alias}.{method}`"
                    )));
                }
                // Not a pack alias: unit-variant receiver `Point.area()` continues.
            }
        }
        // eng.Color.Green(4) — Method with Field receiver.
        if let Expr::Field {
            base: inner,
            field: enum_short,
        } = receiver
        {
            if let Expr::Ident(alias) = inner.as_ref() {
                if !self.locals.contains_key(alias) {
                    let enum_name = format!("{alias}__{enum_short}");
                    if let Some(&eid) = self.structs.by_name.get(&enum_name) {
                        if let Some(info) = self.structs.variant_in_enum(method, eid) {
                            if info.arity == args.len() {
                                return Ok(Some(self.build_variant(info, args)?));
                            }
                        }
                        return Err(IrError::new(format!(
                            "native IR: unknown variant `{method}` on `{enum_name}`"
                        )));
                    }
                }
            }
        }
        Ok(None)
    }

    fn lower_pack_fn_call(
        &mut self,
        mangled: &str,
        args: &[Expr],
    ) -> Result<(Value, Type, bool), IrError> {
        let mut call_args = Vec::with_capacity(args.len());
        let mut temps = Vec::new();
        let (params, ret) = self.signatures[mangled].clone();
        if params.len() != args.len() {
            return Err(IrError::new("native IR: package method arity mismatch"));
        }
        for (arg, expected) in args.iter().zip(params.iter()) {
            let (mut v, t, o) = self.lower_expr(arg)?;
            // Allow pointer-sized / I64 / Struct handle aliases for pack types.
            let ok = t == *expected
                || (expected.is_ptr_sized() && t.is_ptr_sized() && t != Type::F64)
                || (*expected == Type::I64 && t.is_ptr_sized() && t != Type::F64);
            if !ok {
                return Err(IrError::new(format!(
                    "native IR: package method arg type mismatch (got {t:?}, want {expected:?})"
                )));
            }
            // Shared pointer handles (Opaque/chan/…) alias the caller's binding;
            // cloning is identity and post-call drop would free the live handle
            // (e.g. sync.rlock(m) must not opaque_drop m).
            if t.is_shared_handle() {
                // leave owned flag alone; no temp drop
            } else if t.is_heap() {
                if !o {
                    v = self.emit_clone(v, t);
                }
                temps.push((v, t));
            } else if o {
                temps.push((v, t));
            }
            call_args.push(v);
        }
        let out = ret.map(|_| self.value());
        self.emit(Inst::Call {
            out,
            function: mangled.into(),
            args: call_args,
            ret,
        });
        for (v, t) in temps {
            if t.is_shared_handle() {
                continue;
            }
            self.emit_drop(v, t);
        }
        Ok(match (out, ret) {
            (Some(o), Some(r)) => (o, r, r.is_heap()),
            _ => (self.const_int(0, Type::I64), Type::I64, false),
        })
    }

    /// Pack unit variants: `eng.Red` and `eng.Color.Red`.
    fn try_lower_pack_field(
        &mut self,
        base: &Expr,
        field: &str,
    ) -> Result<Option<(Value, Type, bool)>, IrError> {
        if let Expr::Ident(alias) = base {
            if !self.locals.contains_key(alias) {
                // Prefer variant in a pack enum eng__*.
                let pack_enums: Vec<u32> = self
                    .structs
                    .by_name
                    .iter()
                    .filter(|(n, _)| n.starts_with(&format!("{alias}__")))
                    .filter_map(|(_, &id)| {
                        if self.structs.is_enum(id) {
                            Some(id)
                        } else {
                            None
                        }
                    })
                    .collect();
                for eid in &pack_enums {
                    if let Some(info) = self.structs.variant_in_enum(field, *eid) {
                        if info.arity == 0 {
                            return Ok(Some(self.build_variant(info, &[])?));
                        }
                    }
                }
                // Bare nullary variant name (shared across packs).
                let nullary: Vec<_> = self
                    .structs
                    .variants_named(field)
                    .into_iter()
                    .filter(|i| i.arity == 0)
                    .collect();
                if let Some(info) = nullary.first().copied() {
                    return Ok(Some(self.build_variant(info, &[])?));
                }
                // eng.Color as a type path alone is not a value.
                if self
                    .structs
                    .by_name
                    .contains_key(&format!("{alias}__{field}"))
                {
                    return Err(IrError::new(format!(
                        "native IR: `{alias}.{field}` is a type, not a value"
                    )));
                }
                return Err(IrError::new(format!(
                    "native IR: unknown pack member `{alias}.{field}`"
                )));
            }
        }
        // eng.Color.Red
        if let Expr::Field {
            base: inner,
            field: enum_short,
        } = base
        {
            if let Expr::Ident(alias) = inner.as_ref() {
                if !self.locals.contains_key(alias) {
                    let enum_name = format!("{alias}__{enum_short}");
                    if let Some(&eid) = self.structs.by_name.get(&enum_name) {
                        if let Some(info) = self.structs.variant_in_enum(field, eid) {
                            if info.arity == 0 {
                                return Ok(Some(self.build_variant(info, &[])?));
                            }
                            return Err(IrError::new(format!(
                                "native IR: variant `{field}` needs {} field(s)",
                                info.arity
                            )));
                        }
                        return Err(IrError::new(format!(
                            "native IR: unknown variant `{field}` on `{enum_name}`"
                        )));
                    }
                }
            }
        }
        Ok(None)
    }

    /// Short-circuit boolean `&&` / `||` (also `and` / `or`).
    fn lower_short_circuit(
        &mut self,
        op: BinOp,
        left: &Expr,
        right: &Expr,
    ) -> Result<(Value, Type, bool), IrError> {
        let (mut l, lt, lo) = self.lower_expr(left)?;
        if lt != Type::I1 && lt != Type::I64 {
            return Err(IrError::new(
                "native IR: &&/|| operands must be bool (or int 0/1)",
            ));
        }
        if lt == Type::I64 {
            let b = self.value();
            self.emit(Inst::Cast {
                out: b,
                value: l,
                from: Type::I64,
                to: Type::I1,
            });
            l = b;
        }
        if lo {
            // Owned bool temps are impossible; ignore.
        }
        let then_b = self.new_block();
        let else_b = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::I1,
        });
        self.terminate(Terminator::Branch {
            condition: l,
            then_block: then_b,
            else_block: else_b,
        })?;
        match op {
            BinOp::And => {
                // true && r → r; false && r → false (skip r)
                self.current = then_b;
                {
                    let (mut r, rt, ro) = self.lower_expr(right)?;
                    if rt == Type::I64 {
                        let b = self.value();
                        self.emit(Inst::Cast {
                            out: b,
                            value: r,
                            from: Type::I64,
                            to: Type::I1,
                        });
                        r = b;
                    } else if rt != Type::I1 {
                        return Err(IrError::new(
                            "native IR: && right operand must be bool",
                        ));
                    }
                    let _ = ro;
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: r,
                    });
                }
                self.terminate(Terminator::Jump(merge))?;
                self.current = else_b;
                {
                    let f = self.const_int(0, Type::I1);
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: f,
                    });
                }
                self.terminate(Terminator::Jump(merge))?;
            }
            BinOp::Or => {
                // true || r → true (skip r); false || r → r
                self.current = then_b;
                {
                    let t = self.const_int(1, Type::I1);
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: t,
                    });
                }
                self.terminate(Terminator::Jump(merge))?;
                self.current = else_b;
                {
                    let (mut r, rt, ro) = self.lower_expr(right)?;
                    if rt == Type::I64 {
                        let b = self.value();
                        self.emit(Inst::Cast {
                            out: b,
                            value: r,
                            from: Type::I64,
                            to: Type::I1,
                        });
                        r = b;
                    } else if rt != Type::I1 {
                        return Err(IrError::new(
                            "native IR: || right operand must be bool",
                        ));
                    }
                    let _ = ro;
                    self.emit(Inst::Store {
                        ptr: slot,
                        value: r,
                    });
                }
                self.terminate(Terminator::Jump(merge))?;
            }
            _ => unreachable!(),
        }
        self.current = merge;
        let out = self.value();
        self.emit(Inst::Load {
            out,
            ptr: slot,
            ty: Type::I1,
        });
        Ok((out, Type::I1, false))
    }

    /// Meta for `mako_native_map_struct_key_*`:
    /// `(nfields, str_mask, nest_mask, nest_nf_pack, nest_sm_pack)`.
    /// Nested `Type::Struct` fields (Option/Result/user structs) are content-hashed
    /// one level deep so `map[WrapOpt{o: Some(1)}]` matches equal keys.
    fn struct_key_meta(&mut self, sid: u32) -> (Value, Value, Value, Value, Value) {
        let nfields = self.structs.field_count(sid) as i64;
        let mut str_mask: i64 = 0;
        let mut nest_mask: i64 = 0;
        let mut nest_nf_pack: i64 = 0;
        let mut nest_sm_pack: i64 = 0;
        let mut nest_i = 0i32;
        for i in 0..self.structs.field_count(sid) {
            match self.structs.field_type(sid, i) {
                Type::Str => str_mask |= 1i64 << i,
                Type::Struct(nid) => {
                    nest_mask |= 1i64 << i;
                    if nest_i < 4 {
                        let nnf = self.structs.field_count(nid) as i64;
                        let mut nsm: i64 = 0;
                        for j in 0..self.structs.field_count(nid) {
                            if self.structs.field_type(nid, j) == Type::Str {
                                nsm |= 1i64 << j;
                            }
                        }
                        nest_nf_pack |= (nnf & 0xffff) << (nest_i * 16);
                        nest_sm_pack |= (nsm & 0xffff) << (nest_i * 16);
                        nest_i += 1;
                    }
                }
                _ => {}
            }
        }
        (
            self.const_int(nfields, Type::I64),
            self.const_int(str_mask, Type::I64),
            self.const_int(nest_mask, Type::I64),
            self.const_int(nest_nf_pack, Type::I64),
            self.const_int(nest_sm_pack, Type::I64),
        )
    }

    fn map_i_key(&mut self, key: Value, kt: Type, ko: bool) -> Result<Value, IrError> {
        match kt {
            Type::I1 => {
                let c = self.value();
                self.emit(Inst::Cast {
                    out: c,
                    value: key,
                    from: Type::I1,
                    to: Type::I64,
                });
                Ok(c)
            }
            Type::F64 => {
                let bits = self.value();
                self.emit(Inst::Call {
                    out: Some(bits),
                    function: "mako_native_f64_to_bits".into(),
                    args: vec![key],
                    ret: Some(Type::I64),
                });
                Ok(bits)
            }
            Type::Struct(id) if self.structs.is_enum(id) => {
                let tag = self.value();
                self.emit(Inst::StructField {
                    out: tag,
                    base: key,
                    struct_id: id,
                    index: 0,
                    ty: Type::I64,
                });
                if ko {
                    self.emit_drop(key, Type::Struct(id));
                }
                Ok(tag)
            }
            Type::Struct(_) => {
                // Pointer to struct block; map_struct_key_* ops hash+compare content.
                Ok(key)
            }
            Type::I64 if !ko => Ok(key),
            _ => Err(IrError::new(
                "native IR: map key must be int/bool/float/struct/enum",
            )),
        }
    }

    /// Deep structural equality for a struct/enum layout (returns I1).
    fn lower_struct_eq(
        &mut self,
        left: Value,
        right: Value,
        sid: u32,
    ) -> Result<Value, IrError> {
        // Null pointer identity: both null → true; one null → false.
        let z = self.const_int(0, Type::I64);
        let ln = self.value();
        self.emit(Inst::Binary {
            out: ln,
            op: BinOp::Eq,
            left,
            right: z,
            ty: Type::I64,
        });
        let rn = self.value();
        self.emit(Inst::Binary {
            out: rn,
            op: BinOp::Eq,
            left: right,
            right: z,
            ty: Type::I64,
        });
        let both_null = self.value();
        self.emit(Inst::Binary {
            out: both_null,
            op: BinOp::And,
            left: ln,
            right: rn,
            ty: Type::I1,
        });
        let one_null = self.value();
        self.emit(Inst::Binary {
            out: one_null,
            op: BinOp::Or,
            left: ln,
            right: rn,
            ty: Type::I1,
        });
        // if both null → true; elif one null → false; else field compare
        let then_both = self.new_block();
        let else_one = self.new_block();
        let merge = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty: Type::I1,
        });
        self.terminate(Terminator::Branch {
            condition: both_null,
            then_block: then_both,
            else_block: else_one,
        })?;
        self.current = then_both;
        let t = self.const_int(1, Type::I1);
        self.emit(Inst::Store {
            ptr: slot,
            value: t,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = else_one;
        let mid = self.new_block();
        let fields_b = self.new_block();
        self.terminate(Terminator::Branch {
            condition: one_null,
            then_block: mid,
            else_block: fields_b,
        })?;
        self.current = mid;
        let f = self.const_int(0, Type::I1);
        self.emit(Inst::Store {
            ptr: slot,
            value: f,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = fields_b;
        // Enums: compare tag only, then active variant payloads (inactive slots are garbage).
        // Structs: compare every field.
        let fields_eq = if self.structs.is_enum(sid) {
            let tag_l = self.value();
            self.emit(Inst::StructField {
                out: tag_l,
                base: left,
                struct_id: sid,
                index: 0,
                ty: Type::I64,
            });
            let tag_r = self.value();
            self.emit(Inst::StructField {
                out: tag_r,
                base: right,
                struct_id: sid,
                index: 0,
                ty: Type::I64,
            });
            let tags_eq = self.value();
            self.emit(Inst::Binary {
                out: tags_eq,
                op: BinOp::Eq,
                left: tag_l,
                right: tag_r,
                ty: Type::I64,
            });
            // If tags differ → false; else compare payload slots for that tag.
            let tag_ok_b = self.new_block();
            let tag_bad_b = self.new_block();
            let tag_merge = self.new_block();
            let tag_slot = self.value();
            self.emit(Inst::Alloca {
                out: tag_slot,
                ty: Type::I1,
            });
            self.terminate(Terminator::Branch {
                condition: tags_eq,
                then_block: tag_ok_b,
                else_block: tag_bad_b,
            })?;
            self.current = tag_bad_b;
            let ff = self.const_int(0, Type::I1);
            self.emit(Inst::Store {
                ptr: tag_slot,
                value: ff,
            });
            self.terminate(Terminator::Jump(tag_merge))?;
            self.current = tag_ok_b;
            // Payload compare: walk all non-tag fields; only load Str when both non-null.
            let n = self.structs.field_count(sid);
            let mut acc: Option<Value> = Some(self.const_int(1, Type::I1));
            for i in 1..n {
                let fty = self.structs.field_type(sid, i);
                let lv = self.value();
                self.emit(Inst::StructField {
                    out: lv,
                    base: left,
                    struct_id: sid,
                    index: i as u32,
                    ty: fty,
                });
                let rv = self.value();
                self.emit(Inst::StructField {
                    out: rv,
                    base: right,
                    struct_id: sid,
                    index: i as u32,
                    ty: fty,
                });
                let feq = self.lower_field_eq(lv, rv, fty)?;
                let prev = acc.unwrap();
                let a = self.value();
                self.emit(Inst::Binary {
                    out: a,
                    op: BinOp::And,
                    left: prev,
                    right: feq,
                    ty: Type::I1,
                });
                acc = Some(a);
            }
            let peq = acc.unwrap();
            self.emit(Inst::Store {
                ptr: tag_slot,
                value: peq,
            });
            self.terminate(Terminator::Jump(tag_merge))?;
            self.current = tag_merge;
            let loaded = self.value();
            self.emit(Inst::Load {
                out: loaded,
                ptr: tag_slot,
                ty: Type::I1,
            });
            loaded
        } else {
            let n = self.structs.field_count(sid);
            let mut acc: Option<Value> = None;
            for i in 0..n {
                let fty = self.structs.field_type(sid, i);
                let lv = self.value();
                self.emit(Inst::StructField {
                    out: lv,
                    base: left,
                    struct_id: sid,
                    index: i as u32,
                    ty: fty,
                });
                let rv = self.value();
                self.emit(Inst::StructField {
                    out: rv,
                    base: right,
                    struct_id: sid,
                    index: i as u32,
                    ty: fty,
                });
                let feq = self.lower_field_eq(lv, rv, fty)?;
                acc = Some(match acc {
                    None => feq,
                    Some(prev) => {
                        let a = self.value();
                        self.emit(Inst::Binary {
                            out: a,
                            op: BinOp::And,
                            left: prev,
                            right: feq,
                            ty: Type::I1,
                        });
                        a
                    }
                });
            }
            acc.unwrap_or_else(|| self.const_int(1, Type::I1))
        };
        self.emit(Inst::Store {
            ptr: slot,
            value: fields_eq,
        });
        self.terminate(Terminator::Jump(merge))?;
        self.current = merge;
        let loaded = self.value();
        self.emit(Inst::Load {
            out: loaded,
            ptr: slot,
            ty: Type::I1,
        });
        Ok(loaded)
    }

    fn lower_field_eq(
        &mut self,
        lv: Value,
        rv: Value,
        fty: Type,
    ) -> Result<Value, IrError> {
        Ok(match fty {
            Type::Str => {
                // Null/null → true; one null → false; else string content eq.
                let z = self.const_int(0, Type::I64);
                let ln = self.value();
                self.emit(Inst::Binary {
                    out: ln,
                    op: BinOp::Eq,
                    left: lv,
                    right: z,
                    ty: Type::I64,
                });
                let rn = self.value();
                self.emit(Inst::Binary {
                    out: rn,
                    op: BinOp::Eq,
                    left: rv,
                    right: z,
                    ty: Type::I64,
                });
                let both = self.value();
                self.emit(Inst::Binary {
                    out: both,
                    op: BinOp::And,
                    left: ln,
                    right: rn,
                    ty: Type::I1,
                });
                let either = self.value();
                self.emit(Inst::Binary {
                    out: either,
                    op: BinOp::Or,
                    left: ln,
                    right: rn,
                    ty: Type::I1,
                });
                // if both null true; elif either null false; else StringEqual
                let b_both = self.new_block();
                let b_else = self.new_block();
                let b_merge = self.new_block();
                let s = self.value();
                self.emit(Inst::Alloca {
                    out: s,
                    ty: Type::I1,
                });
                self.terminate(Terminator::Branch {
                    condition: both,
                    then_block: b_both,
                    else_block: b_else,
                })?;
                self.current = b_both;
                let t = self.const_int(1, Type::I1);
                self.emit(Inst::Store {
                    ptr: s,
                    value: t,
                });
                self.terminate(Terminator::Jump(b_merge))?;
                self.current = b_else;
                let b_one = self.new_block();
                let b_cmp = self.new_block();
                self.terminate(Terminator::Branch {
                    condition: either,
                    then_block: b_one,
                    else_block: b_cmp,
                })?;
                self.current = b_one;
                let f = self.const_int(0, Type::I1);
                self.emit(Inst::Store {
                    ptr: s,
                    value: f,
                });
                self.terminate(Terminator::Jump(b_merge))?;
                self.current = b_cmp;
                let o = self.value();
                self.emit(Inst::StringEqual {
                    out: o,
                    left: lv,
                    right: rv,
                    negated: false,
                });
                self.emit(Inst::Store {
                    ptr: s,
                    value: o,
                });
                self.terminate(Terminator::Jump(b_merge))?;
                self.current = b_merge;
                let loaded = self.value();
                self.emit(Inst::Load {
                    out: loaded,
                    ptr: s,
                    ty: Type::I1,
                });
                loaded
            }
            Type::Struct(inner) => self.lower_struct_eq(lv, rv, inner)?,
            Type::I1 | Type::F64 => {
                let o = self.value();
                self.emit(Inst::Binary {
                    out: o,
                    op: BinOp::Eq,
                    left: lv,
                    right: rv,
                    ty: fty,
                });
                o
            }
            _ => {
                let o = self.value();
                self.emit(Inst::Binary {
                    out: o,
                    op: BinOp::Eq,
                    left: lv,
                    right: rv,
                    ty: Type::I64,
                });
                o
            }
        })
    }

    /// Zero-initialized owned struct / tuple (recursive for nested structs).
    /// Enums use a nullary variant when available (`None`, unit tags) so
    /// match arms stay valid for map missing-key defaults.
    fn make_zero_struct(&mut self, id: u32) -> Result<Value, IrError> {
        if self.structs.is_enum(id) {
            // Prefer `None` (Option), else any arity-0 variant for this enum.
            let pick = self
                .structs
                .variant_in_enum("None", id)
                .filter(|v| v.arity == 0)
                .or_else(|| {
                    self.structs
                        .variants
                        .borrow()
                        .values()
                        .flatten()
                        .find(|v| v.enum_id == id && v.arity == 0)
                        .copied()
                });
            if let Some(info) = pick {
                let out = self.value();
                self.emit(Inst::EnumMake {
                    out,
                    enum_id: info.enum_id,
                    tag: info.tag,
                    slot_base: info.slot_base as u32,
                    payload: vec![],
                });
                return Ok(out);
            }
            // Result has no nullary variant; missing-key default is Err(empty).
            if let Some(info) = self.structs.variant_in_enum("Err", id) {
                let mut payload = Vec::with_capacity(info.arity);
                for i in 0..info.arity {
                    let fty = self
                        .structs
                        .field_type(id, info.slot_base + i);
                    let z = match fty {
                        Type::Str => {
                            let lit = self.value();
                            self.emit(Inst::StringLiteral {
                                out: lit,
                                bytes: vec![],
                            });
                            self.emit_clone(lit, Type::Str)
                        }
                        Type::I64 | Type::I1 | Type::I32 => self.const_int(0, fty),
                        Type::F64 => {
                            let o = self.value();
                            self.emit(Inst::ConstFloat { out: o, value: 0.0 });
                            o
                        }
                        Type::Struct(sid) => self.make_zero_struct(sid)?,
                        other if other.is_heap() => {
                            let null_ptr = self.const_int(0, Type::I64);
                            self.null_to_empty_heap(null_ptr, other)?
                        }
                        other => self.const_int(0, other),
                    };
                    payload.push(z);
                }
                let out = self.value();
                self.emit(Inst::EnumMake {
                    out,
                    enum_id: info.enum_id,
                    tag: info.tag,
                    slot_base: info.slot_base as u32,
                    payload,
                });
                return Ok(out);
            }
        }
        let nfields = self.structs.field_count(id);
        let mut fields = Vec::with_capacity(nfields);
        for i in 0..nfields {
            let fty = self.structs.field_type(id, i);
            let z = match fty {
                Type::F64 => {
                    let o = self.value();
                    self.emit(Inst::ConstFloat { out: o, value: 0.0 });
                    o
                }
                Type::I1 => self.const_int(0, Type::I1),
                Type::Str => {
                    // Heap-own empty (static StringLiteral must not be dropped).
                    let lit = self.value();
                    self.emit(Inst::StringLiteral {
                        out: lit,
                        bytes: vec![],
                    });
                    self.emit_clone(lit, Type::Str)
                }
                Type::Struct(sid) => self.make_zero_struct(sid)?,
                t if t.is_heap() => {
                    // Slices / maps / etc.: empty via null_to_empty (or null for opaque).
                    let null = self.const_int(0, Type::I64);
                    self.null_to_empty_heap(null, t)?
                }
                t => self.const_int(0, t),
            };
            fields.push(z);
        }
        let o = self.value();
        self.emit(Inst::StructMake {
            out: o,
            struct_id: id,
            fields,
        });
        Ok(o)
    }

    /// If `value` is a null heap pointer, replace with an empty owned value of `ty`.
    fn null_to_empty_heap(&mut self, value: Value, ty: Type) -> Result<Value, IrError> {
        if !ty.is_heap() {
            return Ok(value);
        }
        let zero = self.const_int(0, Type::I64);
        let is_null = self.value();
        self.emit(Inst::Binary {
            out: is_null,
            op: BinOp::Eq,
            left: value,
            right: zero,
            ty: Type::I64,
        });
        let then_id = self.new_block();
        let else_id = self.new_block();
        let merge_id = self.new_block();
        let slot = self.value();
        self.emit(Inst::Alloca {
            out: slot,
            ty,
        });
        self.terminate(Terminator::Branch {
            condition: is_null,
            then_block: then_id,
            else_block: else_id,
        })?;
        self.current = then_id;
        let empty = match ty {
            Type::IntSlice => {
                let o = self.value();
                let z = self.const_int(0, Type::I64);
                self.emit(Inst::SliceMake {
                    out: o,
                    len: z,
                    cap: Some(z),
                });
                o
            }
            Type::StrSlice => {
                let o = self.value();
                let z = self.const_int(0, Type::I64);
                self.emit(Inst::StrSliceMake {
                    out: o,
                    len: z,
                    cap: Some(z),
                });
                o
            }
            Type::FloatSlice | Type::ByteSlice | Type::BoolSlice | Type::StructSlice(_) | Type::PtrSlice(_) => {
                let o = self.value();
                let z = self.const_int(0, Type::I64);
                let fname = match ty {
                    Type::FloatSlice => "mako_native_float_slice_make_ptr",
                    Type::ByteSlice => "mako_native_byte_slice_make_ptr",
                    Type::BoolSlice => "mako_native_bool_slice_make_ptr",
                    _ => "mako_native_ptr_slice_make",
                };
                self.emit(Inst::Call {
                    out: Some(o),
                    function: fname.into(),
                    args: vec![z, z],
                    ret: Some(ty),
                });
                o
            }
            Type::Str => {
                // Heap-own empty string (static StringLiteral must not be dropped).
                let lit = self.value();
                self.emit(Inst::StringLiteral {
                    out: lit,
                    bytes: vec![],
                });
                self.emit_clone(lit, Type::Str)
            }
            Type::Struct(id) => self.make_zero_struct(id)?,
            other => {
                let o = self.value();
                self.emit(Inst::NullHeap {
                    out: o,
                    ty: other,
                });
                o
            }
        };
        self.emit(Inst::Store {
            ptr: slot,
            value: empty,
        });
        self.terminate(Terminator::Jump(merge_id))?;
        self.current = else_id;
        self.emit(Inst::Store {
            ptr: slot,
            value,
        });
        self.terminate(Terminator::Jump(merge_id))?;
        self.current = merge_id;
        let loaded = self.value();
        self.emit(Inst::Load {
            out: loaded,
            ptr: slot,
            ty,
        });
        Ok(loaded)
    }

    /// Emit the type-appropriate clone for a borrowed heap value, returning the
    /// fresh owned value.
    fn emit_clone(&mut self, value: Value, ty: Type) -> Value {
        let out = self.value();
        match ty {
            Type::Str => self.emit(Inst::StringClone { out, value }),
            Type::IntSlice => self.emit(Inst::SliceClone { out, slice: value }),
            Type::FloatSlice => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_float_slice_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::FloatSlice),
            }),
            Type::ByteSlice => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_byte_slice_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::ByteSlice),
            }),
            Type::BoolSlice => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_bool_slice_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::BoolSlice),
            }),
            Type::ChanI
            | Type::ChanS
            | Type::ChanF
            | Type::ChanP(_)
            | Type::Arena
            | Type::Nursery
            | Type::Opaque
            | Type::Task
            | Type::FnPtr => {
                // Handles are not deep-cloned; share the pointer (caller owns).
                return value;
            }
            Type::StructSlice(sid) => {
                // Deep clone: shallow header clone would share element pointers
                // that DropStruct frees — Result[Ok([]Point)] was UAF.
                let nbytes = (self.structs.field_count(sid) * 8) as i64;
                let nb = self.const_int(nbytes, Type::I64);
                self.emit(Inst::Call {
                    out: Some(out),
                    function: "mako_native_ptr_slice_clone_deep".into(),
                    args: vec![value, nb],
                    ret: Some(ty),
                });
            }
            Type::StrSlice => self.emit(Inst::StrSliceClone { out, slice: value }),
            Type::MapII | Type::MapIPtr(_) => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_map_ii_clone_ptr".into(),
                args: vec![value],
                ret: Some(ty),
            }),
            Type::MapSI | Type::MapSPtr(_) => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_map_si_clone_ptr".into(),
                args: vec![value],
                ret: Some(ty),
            }),
            Type::PtrSlice(_) => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_ptr_slice_clone".into(),
                args: vec![value],
                ret: Some(ty),
            }),
            Type::MapSS => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_map_ss_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::MapSS),
            }),
            Type::MapIF => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_map_if_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::MapIF),
            }),
            Type::MapFI => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_map_fi_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::MapFI),
            }),
            Type::ShareInt => self.emit(Inst::Call {
                out: Some(out),
                function: "mako_native_share_clone_ptr".into(),
                args: vec![value],
                ret: Some(Type::ShareInt),
            }),
            Type::Struct(id) => self.emit(Inst::StructClone {
                out,
                base: value,
                struct_id: id,
            }),
            _ => return value,
        }
        out
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lexer::Lexer;
    use crate::parser::Parser;

    #[test]
    fn lowers_scalar_control_flow() {
        let source = r#"
            fn twice(n: int) -> int { return n + n }
            fn main() {
                let mut i = 0
                while i < 3 { i = i + 1 }
                if i == 3 { print_int(twice(i)) }
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        assert_eq!(module.functions.len(), 2);
        assert!(module.functions.iter().all(|function| {
            function
                .blocks
                .iter()
                .all(|block| block.terminator.is_some())
        }));
    }

    #[test]
    fn makes_string_ownership_explicit() {
        let source = r#"
            fn main() {
                let greeting = "hello, " + "world"
                let moved = greeting
                print(moved)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let instructions = module.functions[0]
            .blocks
            .iter()
            .flat_map(|block| &block.instructions)
            .collect::<Vec<_>>();
        assert!(instructions
            .iter()
            .any(|instruction| matches!(instruction, Inst::StringConcat { .. })));
        assert_eq!(
            instructions
                .iter()
                .filter(|instruction| matches!(instruction, Inst::DropString { .. }))
                .count(),
            1
        );
    }

    #[test]
    fn lowers_scalar_structs_with_copy_semantics() {
        let source = r#"
            struct Point { x: int, y: int }
            fn add(a: Point, b: Point) -> Point {
                return Point { x: a.x + b.x, y: a.y + b.y }
            }
            fn main() {
                let p = Point { x: 3, y: 4 }
                var q = p
                q.x = q.x + 1
                let r = add(p, q)
                print_int(r.x)
                let s = Point { x: 100, ..r }
                print_int(s.y)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        // Point plus synthetic Result/Option monomorph layouts.
        assert!(module.structs.iter().any(|s| s.name == "Point"));
        let main = module
            .functions
            .iter()
            .find(|f| f.name == "main")
            .expect("main");
        let instructions = main
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .collect::<Vec<_>>();
        // `var q = p` clones (copy semantics); every owned struct is dropped.
        let makes = instructions
            .iter()
            .filter(|i| matches!(i, Inst::StructMake { .. }))
            .count();
        let clones = instructions
            .iter()
            .filter(|i| matches!(i, Inst::StructClone { .. }))
            .count();
        let drops = instructions
            .iter()
            .filter(|i| matches!(i, Inst::DropStruct { .. }))
            .count();
        assert!(makes >= 1, "expected struct literal lowering");
        assert!(clones >= 1, "expected a copy-on-bind clone for `var q = p`");
        assert!(drops >= 1, "expected owned struct drops");
    }

    #[test]
    fn lowers_scalar_tuples_and_destructuring() {
        let source = r#"
            fn swap(a: int, b: int) -> (int, int) { return (b, a) }
            fn main() {
                let t = (3, 4)
                let a, b = t
                let s = swap(a, b)
                let c, d = s
                print_int(c)
                print_int(d)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        // The `(int,int)` shape is interned exactly once and reused.
        let tuple_layouts = module
            .structs
            .iter()
            .filter(|layout| layout.fields.len() == 2 && layout.fields[0].0 == "0")
            .count();
        assert_eq!(tuple_layouts, 1, "the (int,int) shape must be interned once");
        // Every owned tuple is dropped exactly once (no leaks, no double-free).
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let instructions = main
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .collect::<Vec<_>>();
        let makes = instructions
            .iter()
            .filter(|i| matches!(i, Inst::StructMake { .. }))
            .count();
        let drops = instructions
            .iter()
            .filter(|i| matches!(i, Inst::DropStruct { .. }))
            .count();
        // `t` and `s` are constructed and both dropped after destructuring.
        assert_eq!(makes, 1, "one tuple literal `(3,4)` built in main");
        assert_eq!(drops, 2, "both owned tuples (`t`, `s`) dropped once each");
    }

    #[test]
    fn return_none_moves_without_clone_or_leak() {
        // Returning an owned enum temp must move, not clone-and-leak the source.
        let source = r#"
            fn f() -> Option[int] { return None }
            fn main() {
                let o = f()
                print_int(1)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let f = module.functions.iter().find(|fn_| fn_.name == "f").unwrap();
        let insts: Vec<_> = f.blocks.iter().flat_map(|b| &b.instructions).collect();
        let enums = insts
            .iter()
            .filter(|i| matches!(i, Inst::EnumMake { .. }))
            .count();
        let clones = insts
            .iter()
            .filter(|i| matches!(i, Inst::StructClone { .. }))
            .count();
        assert_eq!(enums, 1, "None constructs one enum block");
        assert_eq!(
            clones, 0,
            "returning owned Option temp must not StructClone (would leak the source)"
        );
        let main = module.functions.iter().find(|fn_| fn_.name == "main").unwrap();
        let main_drops = main
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .filter(|i| matches!(i, Inst::DropStruct { .. }))
            .count();
        assert!(
            main_drops >= 1,
            "main must drop the owned Option local"
        );
    }

    #[test]
    fn lowers_enum_match_with_single_scrutinee_drop() {
        let source = r#"
            enum Shape { Circle(int), Rect(int, int), Point }
            fn describe(s: Shape) -> int {
                return match s {
                    Circle(r) => r * r,
                    Rect(w, h) => w * h,
                    Point => 0,
                }
            }
            fn main() {
                let owned = Circle(5)
                print_int(describe(owned))
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        // Shape is laid out as [tag, p0, p1] (max payload arity 2).
        let shape = module
            .structs
            .iter()
            .find(|l| l.name == "Shape")
            .expect("Shape layout");
        // Non-overlapping slots: [tag, Circle_0, Rect_0, Rect_1].
        assert_eq!(shape.fields.len(), 4);
        assert_eq!(shape.fields[0].0, "tag");
        // `describe`'s borrowed param must not be dropped inside the match (the
        // caller owns the copy); its scrutinee is a borrow, so zero drops here.
        let describe = module.functions.iter().find(|f| f.name == "describe").unwrap();
        let describe_drops = describe
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .filter(|i| matches!(i, Inst::DropStruct { .. }))
            .count();
        assert_eq!(describe_drops, 0, "borrowed enum param is not dropped by callee");
        // The match dispatches on the tag with `Eq` compares and Branches.
        let branches = describe
            .blocks
            .iter()
            .filter(|b| matches!(b.terminator, Some(Terminator::Branch { .. })))
            .count();
        assert!(branches >= 2, "expected a tag-dispatch decision chain");
    }

    #[test]
    fn clones_and_drops_string_struct_fields() {
        let source = r#"
            struct User { name: string, age: int }
            fn main() {
                let u = User { name: "Ada", age: 36 }
                var v = u
                v.name = "Grace"
                print(u.name)
                print(v.name)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let user = module.structs.iter().find(|l| l.name == "User").unwrap();
        assert_eq!(user.fields[0].1, Type::Str);
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let insts = main.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        // `var v = u` deep-copies the struct (StructClone); `v.name = "Grace"`
        // clones the literal and drops the old field; both structs drop at exit.
        assert!(insts.iter().any(|i| matches!(i, Inst::StructClone { .. })));
        // Old-field drop on reassignment + two struct drops at scope exit.
        let drop_strings = insts.iter().filter(|i| matches!(i, Inst::DropString { .. })).count();
        assert!(drop_strings >= 1, "field reassignment must drop the old string");
        let drop_structs = insts.iter().filter(|i| matches!(i, Inst::DropStruct { .. })).count();
        assert_eq!(drop_structs, 2, "both owned Users dropped once at scope exit");
    }

    #[test]
    fn clones_and_drops_int_slice_struct_fields() {
        let source = r#"
            struct Bag { items: []int, tag: int }
            fn main() {
                let b = Bag { items: [1, 2, 3], tag: 9 }
                var c = b
                print_int(c.items[0])
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let bag = module.structs.iter().find(|l| l.name == "Bag").unwrap();
        assert_eq!(bag.fields[0].1, Type::IntSlice);
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let insts = main.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        // `var c = b` deep-copies (StructClone); both bags drop at scope exit,
        // and the backend recursively clones/drops the []int field.
        assert!(insts.iter().any(|i| matches!(i, Inst::StructClone { .. })));
        assert_eq!(
            insts.iter().filter(|i| matches!(i, Inst::DropStruct { .. })).count(),
            2,
            "both owned Bags dropped once at scope exit"
        );
    }

    #[test]
    fn destructures_owned_tuple_with_per_binding_clone() {
        let source = r#"
            fn split(s: string) -> (string, string) {
                return ("Hi, " + s, "Bye, " + s)
            }
            fn main() {
                let a, b = split("Ada")
                print(a)
                print(b)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let insts = main.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        // Each owned tuple element is cloned into its binding, and the owned
        // tuple temp is dropped once (recursively freeing its two strings).
        let clones = insts.iter().filter(|i| matches!(i, Inst::StringClone { .. })).count();
        assert_eq!(clones, 2, "both owned string bindings are cloned out");
        assert_eq!(
            insts.iter().filter(|i| matches!(i, Inst::DropStruct { .. })).count(),
            1,
            "the owned tuple temp is dropped exactly once"
        );
    }

    #[test]
    fn enum_string_payloads_use_non_overlapping_slots() {
        let source = r#"
            enum Msg { Text(string), Code(int), Quit }
            fn main() {
                let a = Text("hi")
                var b = a
                print_int(0)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let msg = module.structs.iter().find(|l| l.name == "Msg").unwrap();
        // [tag, Text_0:string, Code_0:int]; Quit has no payload slots.
        assert_eq!(msg.fields.len(), 3);
        assert_eq!(msg.fields[1].1, Type::Str);
        assert_eq!(msg.fields[2].1, Type::I64);
        // `var b = a` deep-copies the enum (StructClone recurses over the string
        // slot null-safely); both enums drop once at scope exit.
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let insts = main.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        assert!(insts.iter().any(|i| matches!(i, Inst::EnumMake { .. })));
        assert!(insts.iter().any(|i| matches!(i, Inst::StructClone { .. })));
    }

    #[test]
    fn makes_slice_ownership_explicit() {
        let source = r#"fn main() { let xs = [1, 2]; let n = len(xs); print_int(xs[0]); }"#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let instructions = module.functions[0]
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .collect::<Vec<_>>();
        assert!(instructions
            .iter()
            .any(|i| matches!(i, Inst::SliceLiteral { .. })));
        assert!(instructions
            .iter()
            .any(|i| matches!(i, Inst::SliceLen { .. })));
        assert!(instructions
            .iter()
            .any(|i| matches!(i, Inst::SliceIndex { .. })));
        assert!(instructions
            .iter()
            .any(|i| matches!(i, Inst::DropSlice { .. })));
    }

    #[test]
    fn lowers_nested_struct_fields() {
        let source = r#"
            struct Addr { city: string, zip: int }
            struct Person { name: string, addr: Addr }
            fn main() {
                let p = Person { name: "Ada", addr: Addr { city: "Paris", zip: 1 } }
                print(p.addr.city)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let person = module.structs.iter().find(|l| l.name == "Person").unwrap();
        assert!(matches!(person.fields[1].1, Type::Struct(_)));
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        let insts = main.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        assert!(insts.iter().any(|i| matches!(i, Inst::StructMake { .. })));
        assert!(insts.iter().any(|i| matches!(i, Inst::DropStruct { .. })));
    }

    #[test]
    fn owned_match_results_clone_payload_before_scrutinee_drop() {
        let source = r#"
            enum Msg { Text(string), Quit }
            fn payload(m: Msg) -> string {
                return match m {
                    Text(s) => s,
                    Quit => "x",
                }
            }
            fn main() { print(payload(Text("hi"))) }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let payload = module.functions.iter().find(|f| f.name == "payload").unwrap();
        let insts = payload.blocks.iter().flat_map(|b| &b.instructions).collect::<Vec<_>>();
        // Payload arm clones the borrowed string before the match result is stored.
        assert!(
            insts.iter().any(|i| matches!(i, Inst::StringClone { .. })),
            "owned match result must clone a borrowed payload"
        );
    }

    #[test]
    fn lowers_for_loops_with_latch() {
        let source = r#"
            fn main() {
                var s = 0
                for i in 3 { s = s + i }
                print_int(s)
            }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let main = module.functions.iter().find(|f| f.name == "main").unwrap();
        assert!(main.blocks.len() >= 4, "header/body/latch/done");
        assert!(main
            .blocks
            .iter()
            .any(|b| matches!(b.terminator, Some(Terminator::Branch { .. }))));
    }

    #[test]
    fn lowers_match_guards() {
        let source = r#"
            enum Msg { Text(string), Code(int), Quit }
            fn classify(m: Msg) -> int {
                return match m {
                    Text(s) if len(s) > 3 => 1,
                    Text(s) => 2,
                    Code(n) if n > 10 => 3,
                    Code(n) => 4,
                    Quit => 0,
                }
            }
            fn main() { print_int(classify(Text("hi"))) }
        "#;
        let tokens = Lexer::new(source).tokenize().unwrap();
        let program = Parser::new(tokens).parse().unwrap();
        let module = lower(&program).unwrap();
        let classify = module.functions.iter().find(|f| f.name == "classify").unwrap();
        let branches = classify
            .blocks
            .iter()
            .filter(|b| matches!(b.terminator, Some(Terminator::Branch { .. })))
            .count();
        assert!(branches >= 3, "tag dispatch + at least one guard branch");
        assert!(classify
            .blocks
            .iter()
            .flat_map(|b| &b.instructions)
            .any(|i| matches!(i, Inst::StringLen { .. })));
    }
}

 
 
 

// gap-close 1784647512




