// Typed HttpRequest — parse rawv HTTP/1.1 (same shape used under TLS after decrypt).
fn main() {
    let rawv = "GET /hello HTTP/1.1\r\nHost: localhost\r\n\r\n"
    let req = http_request_parse(rawv)
    print(http_request_method(req))
    print(http_request_path(req))
}
