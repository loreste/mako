// float channels via int-ring bitcast.

fn TestChanFloatRoundtrip() {
    let ch = chan_open[float](2)
    let _ = ch.send(3.5)
    let _ = ch.send(-1.25)
    let a = ch.recv()
    let b = ch.recv()
    assert(a > 3.4 && a < 3.6)
    assert(b > -1.3 && b < -1.2)
    ch.close()
}
