fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 3 {
        i = i + 2
        if total > 4 {
            continue
        }
        total = total + 3
    }
    return total
}
