fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 4 {
        let step = i * 2
        if total > 10 {
            break
        }
        total = total + step
        i = i + 1
    }
    return total
}
