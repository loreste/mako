fn compute(value: int) -> int {
    let mut a = 1
    let mut b = 2
    while a < value * 3 {
        a = a + 2
    }
    while b < value * 4 {
        b = b + 1
    }
    while a < value * 5 {
        a = a + 1
        b = b + 1
    }
    return a * 100 + b
}
