fn main() -> int {
    let memory = unsafe_alloc(4096)
    unsafe_ptr_store8(memory, 65)
    unsafe_ptr_store8(memory + 1, 66)
    let written = unsafe_write(1, memory, 2)
    unsafe_free(memory, 4096)
    return written
}
