fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 6 {
        total = total + 2
        if i > 5 {
            break
        }
        i = i + 3
    }
    return total
}
