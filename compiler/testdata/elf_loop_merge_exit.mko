fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 4 {
        if i > 1 {
            total = total + i
        } else {
            total = total + 10
        }
        i = i + 1
    }
    return total
}
