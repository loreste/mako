fn compute(value: int) -> int {
    let mut total = 0
    if value > 1 {
        total = 5
    } else {
        total = 10
    }
    let mut i = 0
    while i < value * 3 {
        total = total + i
        i = i + 1
    }
    return total
}
