fn double(v: int) -> int {
    return v * 2
}

fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 4 {
        let step = double(i)
        total = total + step
        i = i + 1
    }
    return total
}
