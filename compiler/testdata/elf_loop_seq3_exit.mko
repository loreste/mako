fn compute(value: int) -> int {
    let mut total = 0
    let mut i = 0
    while i < value * 2 {
        total = total + i
        i = i + 1
    }
    while i < value * 4 {
        total = total + 2
        i = i + 1
    }
    return total
}
